<!--
STATUS REPORT — close-out snapshot for roadmap #7b (envelope↔envelope reallocation), which
completes #7 / ADR-0004. Spine = the Definition of Done (00 §5 / ENGINEERING_STANDARDS §2).
-->

# Status Report — 2026-06-14 (#07)

| Field  | Value                                          |
| ------ | ---------------------------------------------- |
| Status | Snapshot                                       |
| Date   | 2026-06-14                                     |
| Author | Wesley Cutting + agent                         |
| Scope  | Delta since #06 — roadmap **`#7b`** (envelope↔envelope reallocation); completes **`#7`** / ADR-0004 |

**Resume here:** Budgeteer now has **both** ADR-0004 money-movement primitives. `#7b` adds
**envelope↔envelope reallocation**: re-budget money between two envelopes (e.g. Groceries →
Vacation) with **no** account movement, via a dedicated `envelope_transfers` row. Envelope
balance is now a **two-source derivation** — `Σ allocations + Σ incoming − Σ outgoing` (the
`v_envelope_balances` view rebuilt). Moving **into** an archived envelope is blocked (`400`),
**draining from** an archived one is allowed, and envelopes may go **negative** (owner decision,
consistent with over-spending). UI: a **Move money between envelopes** form on the Dashboard.
This closes `#7`/ADR-0004 — the two axes are orthogonal and the split model (`#3`–`#6`) is
untouched. **Next: refunds `#8`** (negative allocation within a split), recurring `#9`,
reconcile `#10`, then the **analysis** area (`#11`–`#14`).

## Definition of Done — `#7b` (the spine)

| # | DoD check | State | Evidence |
| - | --------- | ----- | -------- |
| 1 | **Vertical & usable** (data→API→UI) | ✅ | `envelope_transfers` table + two-source `v_envelope_balances` → `POST /envelope-transfers` → Dashboard "Move money" form; HTTP smoke: $150 Groceries→Vacation ⇒ 45000/55000, account untouched (100000) |
| 2 | **Gate green** — types · format · unit+integration · build | ✅ | `tsc` clean · **82 tests** (was 73) · `vite build` ok · prettier clean |
| 2a | **Lint (ESLint)** · **Browser E2E** | ⚠ deferred | unchanged (tsc-strict carries lint; Playwright in CI) |
| 3 | **Acceptance criteria met & tested** | ✅ | API: move+conserve+account-untouched; same-envelope/zero → 400, missing → 404; into-archived → 400, drain-from-archived → 201; overdraw → negative. Web: Dashboard reallocation updates both balances; same-envelope guard surfaces inline error |
| 4 | **Accessibility** on new UI | ✅ (baseline) | labeled `form` "Move money between envelopes" + labeled selects/inputs; errors via `role="alert"`; conveyed by text, not color |
| 5 | **Security** — boundary validation; no secrets/real data | ✅ | household-scoped; both envelopes validated server-side (default-deny: archived destination blocked, missing → 404); positive magnitude + distinct + `YYYY-MM-DD` at the boundary; synthetic fixtures |
| 6 | **Docs updated in same change**; statuses promoted | ✅ | FEAT-007 `Implemented` (#7a+#7b) · UX `Accepted` (+§11) · `04_DOMAIN_MODEL` (EnvelopeTransfer entity + two-source balance) · `05_DATA_MODEL` (table + view) · `06_API_CONTRACT` · roadmap |

## Test totals

| Surface | #06 | #07 | Δ |
| ------- | --- | --- | - |
| Unit (domain) | 19 | 22 | +3 (validateEnvelopeTransfer accept/reject · envelopeBalanceWithTransfers incl. negative) |
| Integration (API/PGlite) | 32 | 36 | +4 (move+conserve+account-untouched · same/zero/missing · into-archived 400 + drain ok · overdraw→negative) |
| Component/unit (web/jsdom) | 22 | 24 | +2 (Dashboard reallocation updates both balances · inline guard error) |
| **Total** | 73 | **82** | **+9** |

## Manual carries / deferred

| Item | Why | When |
| ---- | --- | ---- |
| Browser E2E, ESLint, domain-share-with-web, prod-PG, percentage templates | Carried | as noted #01–#06 |
| **Edit / delete a transfer or reallocation** | Immutable for now | FEAT-007 open-Q (later) |
| **Archived envelope as a reallocation *source* in the UI** | API allows drain-from-archived; the picker offers active envelopes only | later (pairs with "move remaining on archive", #6 open-Q) |
| **Warn (not block) on overdrawing an envelope/account** | Negative balances allowed in V1 | later |
| Discard `spikes/04-transfer-modeling/` (throwaway; findings absorbed) | Kit hygiene | optional |

## Outstanding & next steps

- **`#8` Refunds** (negative allocation within a split) — resolves the "negative allocation
  rows?" open question.
- Then **`#9` recurring**, **`#10` reconcile-to-bank**, and the **analysis** area (`#11`–`#14`).

## Commands & gotchas (cold-start)

Same as #01–#06. New: `POST /envelope-transfers` `{ fromEnvelopeId, toEnvelopeId, amount,
occurredOn?, memo? }` → `201 { envelopeTransfer }`. **Envelope balance is now two-source** —
`v_envelope_balances` = `Σ allocations + Σ incoming − Σ outgoing` envelope transfers; an envelope
transfer creates **no** transaction (accounts untouched). Reallocation into an **archived**
destination is rejected; draining **from** archived is allowed; balances may go **negative**.

**Suggested commit:**
`feat: #7b — envelope↔envelope reallocation (ADR-0004 B; two-source envelope balances)`
