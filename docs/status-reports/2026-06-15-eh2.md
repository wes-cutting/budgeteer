<!--
STATUS REPORT — close-out snapshot for engineering-health item EH2 (extract API service plumbing).
Spine = the Definition of Done (00 §5 / ENGINEERING_STANDARDS §2). See docs/00_WAYS_OF_WORKING.md §9.
-->

# Status Report — 2026-06-15 (EH2)

| Field  | Value                                                            |
| ------ | --------------------------------------------------------------- |
| Status | Snapshot                                                        |
| Date   | 2026-06-15                                                      |
| Author | Wesley Cutting + agent                                          |
| Scope  | Engineering-health **`EH2`** — extract repeated API service plumbing (dates · `groupBy` · household constant) |

**Resume here:** The 8 API services no longer each re-derive the same plumbing. Three extractions,
all pure refactors with no behavior change:

1. **`apps/api/src/util/dates.ts`** — `todayStr` / `toISO` / `toDateStr`. Replaced 5 hand-copied
   `toDateStr` converters (transfer, envelope-transfer, reconcile, recurring, transaction), 2
   `toISO` copies (account, envelope), and the `todayStr`/`today` copies in account + recurring +
   `server.ts`. A **dead `todayStr`** in the transaction service was removed (defined, never used).
2. **`apps/api/src/util/groupBy.ts`** — a generic, insertion-order-preserving `groupBy(items,
   keyOf, valueOf)`. Replaced the "fetch children → bucket into a `Map` by parent id → attach"
   loop in the transaction service (×2: allocations + transfer-leg counterparts), the template
   service, and the recurring service.
3. **`apps/api/src/constants.ts`** — `DEFAULT_HOUSEHOLD_ID` moved out of `db/migrate.ts`. All 8
   services **and** the migrator now import it from `../constants`, breaking the coupling of the
   household constant to the migration module (the review's finding).

**Next: `EH3`** — map the DB unique-violation to a 409 (`DuplicateNameError`) and adopt typed route
params (remove the `req.params as {…}` casts).

## Definition of Done — `EH2` (the spine)

| # | DoD check | State | Evidence |
| - | --------- | ----- | -------- |
| 1 | **Vertical & usable** (pure refactor — no behavior change) | ✅ | API surface unchanged; the 52 API integration tests (real Fastify `inject` against PGlite) exercise every refactored path — list/create/attach for accounts, envelopes, transactions, templates, recurring, transfers, reconcile |
| 2 | **Gate green** — types · format · unit+integration · build | ✅ | `tsc` clean (domain · api · web) · **121 tests** (unchanged) · prettier clean · web untouched (no build delta) |
| 2a | **Lint (ESLint)** · **Browser E2E** | ⚠ deferred | unchanged — ESLint is **`EH4`**, browser e2e is **`EH5`** |
| 3 | **Acceptance criteria met & tested** | ✅ | No service imports the constant from `db/migrate` (`grep` → none); no local date-helper definitions remain in `services/`/`http/` (`grep` → none); `groupBy` callers preserve `order by position` (recurring/template line ordering still asserted green) |
| 4 | **Accessibility** on new UI | ✅ (n/a) | No UI change |
| 5 | **Security** — boundary validation; no secrets/real data | ✅ | Household scoping unchanged (every query still filters `household_id`); no input-validation contract touched; synthetic fixtures only |
| 6 | **Docs updated in same change** | ✅ | Roadmap: EH2 → ✅ Done, current-focus + re-sequencing log updated, EH3 promoted to next; this report |

## Test totals

| Surface | EH1 | EH2 | Δ |
| ------- | --- | --- | - |
| Unit (domain) | 41 | 41 | 0 |
| Integration (API/PGlite) | 52 | 52 | 0 |
| Component/unit (web/jsdom) | 28 | 28 | 0 |
| **Total** | 121 | **121** | **0** (behavior-preserving refactor — existing integration tests are the proof) |

## Design notes / decisions

- **`util/` directory (new).** `dates` and `groupBy` are framework-free pure utilities used by both
  the service layer and `http/server.ts`. Putting them under `apps/api/src/util/` (rather than in
  `services/`) avoids an `http → services` dependency for a generic helper and matches the
  pure-library spirit of ARCHITECTURE §3.
- **`constants.ts` (not folded into `config.ts`).** `config.ts` is the validated *env* config
  (zod); `DEFAULT_HOUSEHOLD_ID` is a fixed compile-time constant, so a separate module is clearer
  and keeps the env schema focused. When multi-household (#19) lands, the household id becomes a
  request-derived value and this constant goes away.
- **`groupBy` signature.** `(items, keyOf, valueOf) => Map<string, V[]>` fit all four call sites
  cleanly (key is always a string id; value is the per-row view object). Insertion order is
  preserved so an upstream `order by position` survives — important for template/recurring lines.
- **Out of scope (left intentionally):** `server.ts` still has its own `DATE_RE` and a
  `parseMoney`-in-try/catch (`parsePositiveMagnitude`) that EH1's `tryParseMoney` could simplify —
  not part of EH2's "service plumbing" remit; candidate for a later tidy.

## Files touched

- **new:** `apps/api/src/constants.ts`, `apps/api/src/util/dates.ts`, `apps/api/src/util/groupBy.ts`.
- **edited:** `apps/api/src/db/migrate.ts` (import the constant); `services/{account,envelope,
  transfer,envelopeTransfer,reconcile,transaction,template,recurring}Service.ts` (import dates /
  `groupBy` / constant; drop local copies); `http/server.ts` (import `todayStr`).

## Commands & gotchas (cold-start)

Same gate as before: `npm run typecheck`, `npm test`, `npm run build --workspace @budgeteer/web`,
`npm run format`. New shared modules live at `apps/api/src/util/` (`dates`, `groupBy`) and
`apps/api/src/constants.ts`; services import them via `../util/…` and `../constants`. The household
constant is no longer exported from `db/migrate.ts` — import `DEFAULT_HOUSEHOLD_ID` from
`../constants`.

**Suggested commit:**
`refactor: EH2 — extract API service plumbing (dates util, generic groupBy, household constant)`
