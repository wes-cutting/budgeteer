<!--
STATUS REPORT — point-in-time snapshot for clean hand-offs. Optimized for a cold reader.
-->

# Status Report — 2026-06-13 (#02)

| Field  | Value                                              |
| ------ | -------------------------------------------------- |
| Status | Snapshot                                           |
| Date   | 2026-06-13                                         |
| Author | Wesley Cutting + agent                             |
| Scope  | Delta since #01 — **Slice 1 (core enter→allocate loop)** |

**Resume here:** On top of the Foundation (accounts/envelopes), **Slice 1 is Done and
gate-green**: you can enter a deposit/withdrawal into an account and **allocate** it —
**Single** (whole amount to one envelope) or **Split** (rows with a live `Allocated /
Remaining` tally and a per-row "use remaining"), **partial allowed** ("save now, split
later"), with anything unallocated (including opening balances) surfaced in a
**Needs-allocation** list you can finish from. Account/envelope balances are derived and
update live; over-allocation is rejected client- and server-side. This is the **validated
heart** of the product and it exercises the SPIKE-01 felt-friction caveat in the running
app. **Next: Slice 2 — the accelerators (templates first), then edit-a-past-split.**

## 1. What landed since #01

| Item | Notes | Source |
| ---- | ----- | ------ |
| Transactions + allocation (FEAT-003) | create txn (+allocations), account register, allocate-later, needs-allocation | [feature](../features/transactions.md) · [UX](../ux/transactions.md) |
| API endpoints | `POST/GET /accounts/:id/transactions`, `PUT /transactions/:id/allocations`, `GET /transactions/needs-allocation` | [06_API_CONTRACT](../06_API_CONTRACT.md) |
| Allocation editor (web) | reusable Single/Split editor; live remainder; use-remaining; over-allocation disables Save | `apps/web/src/AllocationEditor.tsx` |
| Simple view navigation | Dashboard → Account register → Needs-allocation | `apps/web/src/App.tsx` |

## 2. Definition of Done — Slice 1

| Check | State | Evidence |
| ----- | ----- | -------- |
| Acceptance criteria met & tested | ✅ | FEAT-003 criteria covered by API + web tests |
| Gate green (types/format/tests/build) | ✅ | `tsc` clean · 44 tests · web `vite build` · prettier clean |
| Usable end-to-end (data→API→UI) | ✅ | HTTP smoke: partial deposit → needs-allocation → allocate-later → balances; over-allocation 400 |
| Docs updated in same change | ✅ | feature/UX/API-contract/roadmap promoted |
| Lint (ESLint) · Browser E2E | ⚠ deferred | unchanged from #01 (tsc-strict carries lint; Playwright to add in CI) |

## 3. Test totals

| Surface | #01 | #02 | Δ |
| ------- | --- | --- | - |
| Unit (domain) | 15 | 15 | — |
| Integration (API/PGlite) | 12 | 19 | +7 |
| Component (web/jsdom) | 4 | 10 | +6 |
| **Total** | 31 | **44** | **+13** |

## 4. Manual carries / deferred

| Item | Why | When |
| ---- | --- | ---- |
| Browser E2E, ESLint, domain-share-with-web, prod-PG | Carried from #01 | as noted in #01 |
| Editing a *fully* allocated split (dedicated UX) | Scoped to slice #5 | later |
| Register pagination | Not needed at V1 volumes | later |

## 5. Outstanding & next steps

- **Slice 2 — accelerators** (roadmap #4): allocation **templates/presets** (primary;
  the paycheck slog-killer), keyboard-first row entry, distribute-remainder. Reuse the
  `AllocationEditor`.
- Then **edit-a-past-split** (#5), **archive an envelope** (#6).

## 6. Commands & gotchas (cold-start)

Same as #01 (`npm install` · `npm run typecheck` · `npm test` · `npm run -w @budgeteer/api dev` ·
`npm run -w @budgeteer/web dev`). New: amounts are entered as **positive magnitudes**; the
API signs them by transaction direction. `AllocationEditor` is reused for both create and
allocate-later (`initial` prefills rows).
