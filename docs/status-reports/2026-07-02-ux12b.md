# Status Report — 2026-07-02 (UX12 · thread 2 of 4)

| Field  | Value                                                                 |
| ------ | --------------------------------------------------------------------- |
| Status | Snapshot                                                              |
| Date   | 2026-07-02                                                            |
| Author | Wesley Cutting + agent                                                |
| Scope  | `UX12` (thread 2 of 4) — **skeleton loaders** ([FEAT-UX12b](../features/skeleton-loaders.md)); the **second slice of the Phase-4 Polish track** (`UX12`–`UX15`) |

**Resume here:** The **second UX12 thread** is done. UX12 bundles **four independent polish threads**
(skeletons · toasts · destructive-action confirms · inline validation); thread 1 (destructive confirms,
UX12a) shipped last session. This slice ships **thread 2 — skeleton loaders**: the **16** bare
`<p>Loading…</p>` / `<p role="status">Loading…</p>` strings across the eight lists/management views
and the eight Insights views now render the **existing UX4 `Skeleton` primitive** (exactly as the home
`Cockpit` already does — default 3 decorative rows, `aria-hidden` bars + an sr-only `role="status"`
"Loading…" announce). This is a **net accessibility improvement**: the ~9 sites that used a bare `<p>`
(no live region) announced **nothing** to AT and now announce politely. It is **reduced-motion-safe by
construction** (the primitive has no entrance animation) and light/dark fall out of the token sheet.
**No ADR/spike, no new dependency, no new code** — a pure presentation swap reusing a validated
primitive (§11 fast-path). Gate green: **343 Vitest passing** (+3; 1 pre-existing unrelated carry —
see §4) **+ 83 e2e** (unchanged), build **111.44 KB gz** (net **−0.04 KB** as literal strings drop
out; < 120 KB). **Next:** the remaining `UX12` threads — success toasts (`UX12c`, needs a new Radix
Toast primitive) · inline validation (`UX12d`) — then `UX13`–`UX15`.

## 1. What landed since the last report

| Item | Notes | Source |
| ---- | ----- | ------ |
| 16 loading strings → `Skeleton` | The `data === null`/`=== undefined` loading branch of each view now renders `<Skeleton />` (default 3 rows) instead of a bare paragraph. Sites: `EnvelopesList`, `AccountsList`, `ManageView` (net-worth summary), `TemplatesView`, `NeedsAllocation`, `EnvelopeLedger`, `EnvelopeLedgerRoute`, `RecurringView`, `BudgetVsActualView`, `AnalysisView`, `SpendingBreakdownView`, `SpendingTrendsView`, `NetWorthView`, `BudgetBurndownView`, `PayoffView`, `CreditView`. Five of these gained a `Skeleton` import; the rest already imported from `./ui`. | FEAT-UX12b §3 |
| Unit tests (+3) | Loading-state assertions at three representative sites — `EnvelopesList` + `AccountsList` (early-return list pattern) + `BudgetVsActualView` (Insights `role="status"` pattern): each asserts the initial render shows the accessible Skeleton (`role="status"` "Loading…") before the async read resolves. | TESTING |
| Docs | New [FEAT-UX12b](../features/skeleton-loaders.md) sibling note; [FEAT-UX12a](../features/destructive-confirms.md) §2 scope table (skeletons → ✅); [roadmap](../03_ROADMAP.md) (UX12 narrative + table row + changelog); [07_NFR](../07_NFR.md) bundle (111.44 KB); this report | — |

## 2. Definition of Done

| Check | State | Evidence |
| ----- | ----- | -------- |
| Vertical & usable (data → API → UI) | ✅ | Every view's loading branch now paints a skeleton placeholder end-to-end in the running app; no new server surface (presentation-only). |
| Gate green (typecheck · lint · format · unit · e2e · build) | ✅ | typecheck ✓ · lint ✓ (`--max-warnings 0`) · format ✓ · **343 Vitest passing** ✓ (1 pre-existing carry, §4) · **83 e2e** ✓ · build **111.44 KB gz** ✓ |
| Acceptance criteria met & tested | ✅ | FEAT-UX12b §5 all ✅; grep confirms no bare `Loading…` `<p>` remains; unit covers the accessible loading state at 3 sites |
| UX states (loading) | ✅ | The loading state is now the shared `Skeleton` everywhere (consistency); empty/error/ready branches unchanged |
| Accessibility (WCAG 2.2 AA, light + dark) | ✅ | Net a11y win — ~9 bare `<p>` sites now announce via `role="status"`; primitive is axe-validated (`ui.test.tsx`) + exercised in the light+dark e2e axe suite via `Cockpit`; no motion added (reduced-motion-safe) |
| Input validation & secrets | ✅ | No input boundary touched; synthetic fixtures only; no secrets |
| Docs updated in the same change | ✅ | FEAT-UX12b (new); FEAT-UX12a scope table; roadmap; 07_NFR; this report |
| Bundle budget respected | ✅ | **111.44 KB gz** (net **−0.04 KB** — reuses the already-bundled `Skeleton`, drops literal strings) vs the **120 KB** ceiling (~8.6 KB headroom) |

## 3. Test totals

| Surface | Prev (UX12a) | Now | Δ |
| ------- | ---- | --- | - |
| Unit + integration (Vitest) | 340 passing | 343 passing (344 total incl. the 1 carry) | +3 — loading-state tests in `EnvelopesList` · `AccountsList` · `BudgetVsActualView` |
| E2E (Playwright) | 83 | 83 | 0 — skeletons are transient (a live scan would be racy, §11); the primitive's a11y is covered by `ui.test.tsx` + the `Cockpit` axe scan |

## 4. Notes / decisions

| Item | Note |
| ---- | ---- |
| **Why this thread next** | Lowest-risk remaining UX12 thread — reuses an **already-validated** UX4 primitive (no new component, **no new dependency, no bundle growth**) and touches **no data/API/domain/chart**. A pure presentation swap. |
| **No ADR / no spike — explicitly** | Additive presentation on a validated primitive; §11 fast-path — the FEAT-UX12b note is the whole paperweight. |
| **Row count** | Default `<Skeleton />` (3 rows) everywhere, matching `Cockpit`. A skeleton is a generic placeholder, not a per-screen layout mock; consistency is the goal, so no per-site tuning. |
| **E2E scope (deliberately none added)** | The skeleton only paints while a read is in flight, so catching it live in Playwright means artificially delaying a route — racy and low-value given the primitive is already axe-clean. Per §11 we rely on the primitive's existing tests + the `Cockpit` axe scan and cover the state via unit tests instead. |
| **Pre-existing carry (recurring, unrelated) — RE-CONFIRMED on main** | `apps/api/test/recurring.test.ts` › *"post-due generates every due occurrence…"* fails (expects 4 posted, gets 1). **Reproduced on clean `main` this session via `git stash` + isolated re-run** (1 failed | 4 passed). A relative-date fixture assumption vs. the sandbox clock — **not** this slice's regression (this slice is web-only). Not touched. |
| **Pre-existing carry (forecast e2e, flaky, unrelated)** | The cash-flow forecast e2e's `getByLabel("Account")` strict-mode collision (documented UX10/UX11 §4). **Did not trip** this run (83/83). Not touched. |
| **Verification** | The primitive is rendered-and-axe-scanned in real Chromium via the `Cockpit` panels in the e2e a11y suite (light + dark); the per-site swaps are unit-covered (accessible loading state) + the full e2e suite drives the resolved states. Interactive preview not exercised (no browser-observable regression risk beyond the Playwright + axe suites — same rationale as UX8–UX12a). |

## 5. Outstanding & next steps

- **`UX12` remaining threads** (tracked, not dropped): success toasts — needs a **new Radix Toast
  primitive** (`UX12c`); inline validation surfaces (`UX12d`). Then `UX13` (money & budget-health
  visual encoding), `UX14` (empty states & onboarding), `UX15` (responsive pass).
- The two pre-existing carries (`recurring.test.ts`, the flaky forecast e2e selector) remain
  candidates for a small unrelated cleanup slice — neither blocks the next `UX12` thread.

## 6. Commands & gotchas (cold-start)

```sh
# Full gate:
npm run typecheck && npm run lint && npm run format
npm test                 # 343 Vitest passing (recurring.test.ts's post-due case is a pre-existing,
                          # unrelated carry — see §4; perf.test.ts may trip under load)
npm run test:e2e         # 83 Playwright
npm run build --workspace @budgeteer/web   # 111.44 KB gz JS, 3.23 KB gz CSS
```

- **`Skeleton` is the loading placeholder** (`apps/web/src/ui/Feedback.tsx`): `<Skeleton rows={n} />`
  (default 3). It owns its a11y — decorative `aria-hidden` bars + an sr-only `role="status"`
  "Loading…". **Don't** hand-roll a bare `<p>Loading…</p>` for a new loading branch; reach for this.
- **Bundle:** 120 KB gz ceiling, 111.44 KB now (~8.6 KB headroom). This slice added **no dependency
  and no new code** — it net-*dropped* 0.04 KB by removing literal strings.
- **Known carries (not this slice's regressions):** `recurring.test.ts`'s post-due occurrence-count
  case, and a flaky `getByLabel("Account")` collision in the forecast e2e — both reproduce on `main`,
  see §4.

## 7. Next-session kickoff prompt

```text
You are resuming Budgeteer (built from the baseline starter kit) in a fresh context window.
Get your bearings first:
- Read CLAUDE.md and docs/00_WAYS_OF_WORKING.md.
- Read the NEWEST file in docs/status-reports/ (2026-07-02-ux12b.md) — its "Resume here" has state.
- Read docs/03_ROADMAP.md (UX Uplift section, UX12–UX15 "Polish" rows) and
  docs/reviews/2026-06-25-ux-uplift-initiative.md (the initiative brief, §5 Phase 4).

TWO of UX12's four Polish threads are done: UX12a — destructive-action confirms (ConfirmDialog on the
validated Radix Dialog; see docs/features/destructive-confirms.md); and UX12b — skeleton loaders (the
16 bare Loading… strings now render the existing UX4 Skeleton primitive; see
docs/features/skeleton-loaders.md). TWO threads remain: success toasts (UX12c) and inline validation
(UX12d).

Next milestone: pick up the next UX12 thread. Recommend a decision up front with the owner:
- UX12d (inline validation) is the lower-risk vertical — it reuses existing primitives (Field/Input/
  Alert) with NO new dependency; surface field-level validation messages on the forms that currently
  only fail at submit. No ADR expected.
- UX12c (success toasts) gives feedback-on-mutation but needs a NEW Radix Toast primitive (+bundle,
  ~8.6 KB headroom). Treat it as its own vertical; spike the primitive ONLY if a11y looks uncertain
  (reduced-motion, no text-opacity animation, SR announce via role="status"), and if you add it,
  prove it axe-clean LIGHT AND DARK with the toast visible before wiring call sites.

Before building, CHALLENGE THE PLAN (ways-of-working): state in your own words what the next thread
is, why it's next, and its riskiest assumption. These are small, low-risk slices — right-size and
COMPRESS (§11), but keep the non-negotiables: vertical slices, gate-green, a11y light+dark, secrets
out of the repo, docs-in-the-same-change. Decide ADR-or-not explicitly (inline validation: no;
toasts: probably no, but if you add a Radix Toast confirm it's axe-clean light+dark first). No
chart-primitive work is expected; if any, honour the ADR-0007 contract. Re-check the bundle
(111.44 KB gz now, ceiling 120 KB — ~8.6 KB headroom).

Keep the slice vertical and gate-green. Add/extend tests (unit + e2e) and keep the axe suite green
LIGHT AND DARK. Update docs in the same change (new sibling FEAT note or extend, roadmap, this
report's successor). Two pre-existing, UNRELATED test carries exist (see this report §4) — do NOT fix
them unless asked: apps/api/test/recurring.test.ts's post-due occurrence-count case (fails on main
too), and a flaky getByLabel("Account") collision in the forecast e2e test. Confirm they're still
present-but-unrelated (git stash + isolated re-run) rather than assuming.

Gate: npm run typecheck && npm run lint && npm run format && npm test && npm run test:e2e && npm run
build --workspace @budgeteer/web.

Confirm, in your own words, where things stand and the plan (and its risks) before building. At the
end, leave the project handoff-ready with the next-session kickoff prompt, and provide a single-line
short commit message; I will review and commit when ready.
```
