---
type: roadmap
status: Append-only
---
<!--
ROADMAP HISTORY v2 — the append-only narrative split out of 03_ROADMAP-v2.md (K31): the
re-sequencing log + the shipped ledger. Migrated verbatim from 03_ROADMAP.md §5–§6 with only
standalone roadmap ids re-keyed to BUD-*. The living plan is 03_ROADMAP-v2.md.
-->

# Roadmap history — Budgeteer (v2)

| Field   | Value          |
| ------- | -------------- |
| Status  | Append-only (narrative history) |
| Owner   | Wesley Cutting |
| Plan    | [`03_ROADMAP-v2.md`](03_ROADMAP-v2.md) (the living plan of record) |

> Migrated verbatim from `03_ROADMAP.md` §5–§6. Only standalone roadmap ids were re-keyed to
> `BUD-*`; `FEAT-*`, `features/*`, `status-reports/*`, and `ADR-*` references are unchanged.
> Rows are in their original (chronological / status) order.

## 1. Re-sequencing log

| Date | Change | Trigger | Effect on the plan |
| ---- | ------ | ------- | ------------------ |
| 2026-06-13 | History migration (→ SPIKE-03/BUD-S79) and bank integration (→ BUD-S80) **deferred to post-V1** | Discovery forks A & B: **fresh start + manual** | Retired the two heaviest risks; V1 starts at the value/UX bet instead of a data-extraction slog |
| 2026-06-13 | **Templates** elevated to the primary accelerator (BUD-S3); **partial allocation** folded into the core loop (BUD-S2); **transfers/refunds/recurring/edit-split/archive** added as slices (BUD-S4–BUD-S9, mapped to A1.7/A1.5/A5.3 of the sheet) | [SPIKE-01](spikes/01-split-allocation-ux.md) confirmed the model + surfaced these | Shaped the domain-slice backlog; flagged transfers (double-entry) & edit-split (invariant) as the careful ones |
| 2026-06-13 | Added **SPIKE-02** (feasibility) ahead of the foundation | Stack still unchosen; SPIKE-01 was a *paper* test, didn't probe the build | `ADR-0001`/`0002` now gate the foundation slice |
| 2026-06-13 | **SPIKE-02 Done**; stack chosen (TS · React+Vite · Fastify · Postgres); `ADR-0001` `Validated`, `ADR-0002` `Proposed`, `ADR-0003` `Accepted` | [SPIKE-02](spikes/02-stack-feasibility.md) confirmed integer-money + split invariant exact (8/8 tests) | **Foundation slice → `Ready`**; no further pre-build spike needed |
| 2026-06-13 | **Foundation slice Done** (gate-green); `ADR-0002` → `Validated`; domain/data model → `Accepted`; FEAT-001/002 → `Implemented`; `06_API_CONTRACT` written; `ADR-0001` → `Accepted` | Built data→API→UI with 31 passing tests + HTTP smoke | **Slice 1** (core enter→allocate loop) is now unblocked & next |
| 2026-06-13 | **Slice 1 Done** (gate-green); FEAT-003 `Implemented`; UX `Accepted`; transaction/allocation endpoints added to `06_API_CONTRACT` | Built enter→split-allocate across domain→API→UI (44 tests + HTTP smoke); SPIKE-01 felt-friction caveat exercised in-app | **Next: Slice 2** (accelerators — templates) |
| 2026-06-13 | **Slice 2 Done** (gate-green); FEAT-004 `Implemented`; templates tables in `05_DATA_MODEL`; template endpoints in `06_API_CONTRACT`; template lines = **fixed amounts** (PRD open Q resolved) | Built templates + distribute/keyboard accelerators (55 tests + HTTP smoke); smoke caught & fixed a content-type/error-status bug | **Next: edit-a-past-split (BUD-S4), archive (BUD-S5)** |
| 2026-06-13 | **Slice BUD-S4 Done** (gate-green); FEAT-005 `Implemented`; **no new API** (reused `PUT /transactions/:id/allocations`) | Made register transactions editable via the AllocationEditor (57 tests + HTTP smoke) | **Next: archive an envelope (BUD-S5)** |
| 2026-06-13 | **`BUD-S5` Done** (gate-green); FEAT-006 `Implemented`; archive/unarchive endpoints in `06_API_CONTRACT` (**no schema change** — `archived_at` existed since the Foundation) | Archive/unarchive + picker filtering + Dashboard Archived section (62 tests + HTTP smoke) | **Next: transfers `BUD-S6/S7`** (consider a modeling spike), then analysis |
| 2026-06-14 | **SPIKE-04 Done** (modeling); confirmed an **additive two-primitive** transfer model (account-transfer legs + `envelope_transfers` table); owner confirmed transfers must cover **envelope↔envelope** too | [SPIKE-04](spikes/04-transfer-modeling.md): both movements proven exact + orthogonal, split model untouched (8/8 tests) | **`BUD-S6/S7` Ready** → recommend split into **`BUD-S6` account-transfer** then **`BUD-S7` envelope-reallocation**; produces `ADR-0004`. *Pending owner nod on the split + negative-envelope-balance Q before building.* |
| 2026-06-14 | Owner confirmed: **split `BUD-S6/S7` into `BUD-S6`/`BUD-S7`**; **negative envelope balances allowed**. **`BUD-S6` Done** (gate-green); `ADR-0004` `Accepted`; FEAT-007 `Implemented`; `transfers` table + `transactions.transfer_id`/kind-check in `05_DATA_MODEL`; `POST /transfers` in `06_API_CONTRACT` | Built account-transfer across domain→API→UI (73 tests + HTTP smoke) | **Next: `BUD-S7`** (envelope reallocation — the second ADR-0004 primitive), then refunds `BUD-S8` |
| 2026-06-14 | **`BUD-S7` Done** (gate-green) — completes `BUD-S6/S7`/ADR-0004; `envelope_transfers` table + **two-source** `v_envelope_balances` in `05_DATA_MODEL`; `POST /envelope-transfers` in `06_API_CONTRACT`; EnvelopeTransfer entity in `04_DOMAIN_MODEL` | Built envelope-reallocation across domain→API→UI (82 tests + HTTP smoke); orthogonal to accounts, split model untouched | **Next: refunds `BUD-S8`**, recurring `BUD-S9`, reconcile `BUD-S10`, then analysis (`BUD-S15`–`BUD-S18/S19`) |
| 2026-06-14 | Owner confirmed BUD-S8 scope: **mixed-sign rows within a split** via a per-row **Refund** toggle. **`BUD-S8` Done** (gate-green); FEAT-008 `Implemented`; **no schema change** (split invariant already governs the signed total); allocation inputs gain `refund` in `06_API_CONTRACT`; PRD §9 negative-rows Q **resolved** | Found the heart invariant already admits mixed signs — only the boundary blocked it; opened it + added the editor toggle (93 tests + HTTP smoke) | **Next: recurring `BUD-S9`**, reconcile `BUD-S10`, then analysis (`BUD-S15`–`BUD-S18/S19`) |
| 2026-06-14 | Owner confirmed BUD-S9 scope: **weekly/biweekly/monthly**, explicit **Post due**, rule **carries its own split**. **`BUD-S9` Done** (gate-green); FEAT-009 `Implemented`; `recurring_transactions` + `recurring_lines` tables + `transactions.recurring_id` in `05_DATA_MODEL`; recurring endpoints in `06_API_CONTRACT`; RecurringTransaction entity in `04_DOMAIN_MODEL` | Built rule + idempotent generator across domain→API→UI (106 tests + HTTP smoke); pure schedule core with monthly anchor-day clamp | **Next: reconcile `BUD-S10`**, then analysis (`BUD-S15`–`BUD-S18/S19`) |
| 2026-06-15 | Owner confirmed BUD-S10 scope: **plain compare + recorded history** (not a cleared/statement workflow); on mismatch **show the difference** (no auto-adjustment). **`BUD-S10` Done** (gate-green); FEAT-010 `Implemented`; `reconciliations` table in `05_DATA_MODEL`; reconcile endpoints in `06_API_CONTRACT`; Reconciliation entity in `04_DOMAIN_MODEL`; PRD §9 reconcile Q **resolved** | Built record+compare across domain→API→UI (120 tests + HTTP smoke); a recorded compare, no ledger/balance effect | **Next: the analysis area (`BUD-S15`–`BUD-S18/S19`)** |
| 2026-06-15 | **CORS + .env fix** (not a roadmap slice): the browser app couldn't reach the API (no CORS headers → "Failed to fetch"); added `@fastify/cors` allowlist (`CORS_ORIGINS`) + repo-root `.env` auto-load (dotenv / Vite `envDir`) | Found via running the app at `localhost:5173`; the browser→API path was never exercised by curl/inject smokes | Hardening follow-up: add **browser e2e** (would have caught this) |
| 2026-06-15 | **Repo-wide review** captured ([reviews/2026-06-15-repo-review.md](reviews/2026-06-15-repo-review.md)); added **Engineering-health items `BUD-S22`–`BUD-S27`**, sequenced **before the analysis area** | No live bugs; themes = browser-path coverage gap + duplication drift | New order: **BUD-S22→BUD-S27 → analysis `BUD-S15`–`BUD-S18/S19` → hardening `BUD-S76/S77`–`BUD-S78`** (full e2e/a11y); no-auth folds into `BUD-E13` |
| 2026-06-15 | **`BUD-S22` Done** (gate-green); web now imports money/date from `@budgeteer/domain`. Added `tryParseMoney` (single `DECIMAL_RE` home) + moved `splitEvenly` to domain; widened `formatMoney(value: number)`; `apps/web` declares the dep; `fakeApi` uses `dueOccurrences`/`anchorDayOf`. Refined the review's "delete format.ts": kept `formatCents` (the `$`/locale **display** formatter) as a presentation concern | The duplicated penny-exact parse regex (`MONEY_RE ≡ DECIMAL_RE`) + `splitEvenly` + the date mirror were a second source of truth; analysis (`BUD-S15`+) would build more atop it | **Next: `BUD-S23`** (extract API service plumbing). 121 tests (+1 net) |
| 2026-06-15 | **`BUD-S23` Done** (gate-green); API service plumbing extracted. New `util/dates.ts` (`todayStr`/`toISO`/`toDateStr`), `util/groupBy.ts` (generic, order-preserving), and top-level `constants.ts` for `DEFAULT_HOUSEHOLD_ID` (moved out of `db/migrate.ts`). Deduped the date converters (5→1, removed a dead `todayStr`), the bucket-by-parent loop (×4), and the household-constant↔migration coupling (9 importers) | Repeated service plumbing flagged by the review; analysis (`BUD-S15`+) adds more read services that would re-copy it | **Next: `BUD-S24`** (map DB unique→409 + typed route params). 121 tests (no behavior change) |
| 2026-06-15 | **`BUD-S24` Done** (gate-green); DB unique-violation now → 409 (was 500). New `services/dbErrors.ts` (`isUniqueViolation`/`asDuplicateName`) maps SQLSTATE `23505` → `DuplicateNameError` at the **service** layer (boundary rule: HTTP stays datastore-agnostic); wraps account/envelope/template create + rename/update. All 12 `req.params as {…}` casts replaced with Fastify route generics (`IdParams`/`AccountIdParams`) | The DB index is the real name guard (in-app check intercepts every *constructible* dup, so the index only fires under concurrency — and its failure was unhandled); casts were the last typed-assertion escape hatch | **Next: `BUD-S25`** (ESLint in the gate). 125 tests (+4; backstop proven against real PGlite) |
| 2026-06-15 | **`BUD-S25` Done** (gate-green); ESLint 9 flat config added (`@typescript-eslint` recommended + `react-hooks`) with `npm run lint` as a **zero-warning gate**; README gate list + commands updated. Caught & fixed a real rules-of-hooks error (non-hook helper `useRemaining` → `fillRemaining`); made the mount-load `exhaustive-deps` suppressions consistent across all 5 sites | No linter ran before, so the react-hooks rules were unenforced and the existing `eslint-disable` comments were inert | **Next: `BUD-S26`** (minimal browser e2e — Playwright). 125 tests (no behavior change; one rename) |
| 2026-06-15 | **`BUD-S26` Done** (gate-green); first **real browser→API** test layer. Added Playwright (Chromium-only): `playwright.config.ts` boots the real API + web (web on `:5173` to match the CORS allowlist) and Chromium drives one spec — dashboard-loads-vs-real-API + the account→envelope→deposit-allocated→derived-balance journey. New `npm run test:e2e` step (kept out of `npm test`); e2e TS folded into `typecheck`; ESLint/`.gitignore`/`.prettierignore` updated for the new surface | The CORS bug shipped because the three existing layers (domain unit · API `inject` · jsdom fake-API) never exercise a real browser→API round-trip ([2026-06-15 review](reviews/2026-06-15-repo-review.md) BUD-S26 · [KIT_FEEDBACK](KIT_FEEDBACK.md) K3) | **Next: `BUD-S27`** (repo hygiene — discard the absorbed transfer-modeling spike; record accepted notes). **125 Vitest + 1 e2e** |
| 2026-06-15 | **`BUD-S15` Done** (gate-green) — **first analysis slice**; FEAT-011 `Implemented`. Owner settled the core modeling Qs: **net signed allocation flow** per cell, **exclude** envelope↔envelope reallocations, **monthly grid + annual rollup**; archived envelopes **included**. Built data→API→UI: read-only `makeAnalysisService` aggregate (**no schema change**) + `GET /analysis/envelope-spend?grain=month\|year` + thin `AnalysisView` grid; `06_API_CONTRACT` + `05_DATA_MODEL` (read-path note) updated | No spike needed (derives from existing `allocations`/`transactions`, low risk); the spend definition was a *definition* decision, captured in the feature spec (not an ADR) | **Next: `BUD-S16`** budget-vs-actual (needs per-envelope monthly targets — open Q). **135 Vitest + 1 e2e** (+10) |
| 2026-06-16 | **`BUD-S16` Done** (gate-green) — second analysis slice; FEAT-012 `Implemented`. Owner settled the targets model: **one recurring monthly target per envelope** (a definition, **no ADR** — not effective-dated) and **actual = outflow spend** (funding excluded, refunds netted). Added `envelope_targets` table (idempotent migration) in `05_DATA_MODEL` + EnvelopeTarget in `04_DOMAIN_MODEL`; `targetService` (set/clear) + sibling `analysisService.budgetVsActual(month)`; `GET /analysis/budget-vs-actual` + `PUT`/`DELETE /envelopes/:id/target` in `06_API_CONTRACT`; resolved PRD §9 target Q. **Fixed a latent CORS bug** (preflight `Access-Control-Allow-Methods` defaulted to `GET,HEAD,POST`, blocking all browser `PUT`/`PATCH`/`DELETE`) — explicit methods allowlist + an e2e write-verb guard | The "actual" data already existed (FEAT-011); the gap was the **budget** store + the outflow definition — both settled with the owner (no spike: derives from existing data + a tiny store). The CORS gap surfaced because targets are the **first** browser write verbs the POST-only e2e exercised | **Next: `BUD-S17`** cash-flow forecast, then `BUD-S18/S19` debt/credit, then hardening `BUD-S76/S77`–`BUD-S78`. **151 Vitest + 1 e2e** (+16) |
| 2026-06-16 | **SPIKE-05 Done** then **`BUD-S17` Done** (gate-green) — third analysis slice, the area's only forward-looking one; FEAT-013 `Implemented`. Owner chose the **richer** model (per-account cash; **scheduled + expected-spend toggle**, default on; event-stepped 90-day default), which reintroduced the fuzzy fork → a spike was warranted. [SPIKE-05](spikes/05-cashflow-forecast.md) (8/8) proved the recurring engine projects the future **unchanged** and that netting expected spend against scheduled/actuals **kills the double-count** (naive raw-targets over-counted $1,620/mo vs. the correct $550 discretionary on the fixture). Built data → API → UI: **pure domain** `cashFlowForecast` + `analysisService.cashFlowForecast` read + `GET /analysis/cash-flow-forecast` + thin `ForecastView`; `06_API_CONTRACT` + `04_DOMAIN_MODEL` updated; **no schema change** (a pure projection). | The forecast *projects* (unlike BUD-S15/BUD-S16 which aggregate), so it carried the area's one genuine modeling unknown; the spike retired it, and "+ targets" was settled by the netting/spreading definition (no ADR) | **Next: `BUD-S18/S19`** debt & credit trends (the analysis area's last slice), then hardening `BUD-S76/S77`–`BUD-S78`. **168 Vitest + 1 e2e** (+16) |
| 2026-06-16 | **`BUD-S19` Done** (gate-green) — **completes the split `BUD-S18/S19` and the analysis area (`BUD-S15`–`BUD-S18/S19`)**; FEAT-014b `Implemented`. Owner settled: **add a `kind='loan'` account type** (vs. a kind-agnostic store — symmetric with `BUD-S18`: credit→limit→utilization, loan→original→payoff); **single original principal** (a definition, **no ADR**). Evolved the accounts kind check idempotently (+`'loan'` in schema/domain/web); added `loan_principals` (idempotent migration) in `05_DATA_MODEL` + LoanPrincipal in `04_DOMAIN_MODEL`; pure-domain `debtPayoff` (owed = −balance; payoff = 1 − owed/original in bps, truthful/unclamped; cumulative monthly trend; roll-up); `loanPrincipalService` (set/clear, loan-kind only) + `analysisService.debtPayoff`; `GET /analysis/debt-payoff` + `PUT`/`DELETE /accounts/:id/original-principal` in `06_API_CONTRACT` | No spike: aggregates existing balances + one stored number (like `BUD-S15`/`BUD-S16`/`BUD-S18`); the cumulative-trend technique was already proven by `BUD-S18`. The forks (loan modeling + effective-dating) were settled with the owner | **Analysis area complete. Next: hardening — `BUD-S76/S77` backup/export, then `BUD-S78`. 214 Vitest + 1 e2e** (+22) |
| 2026-06-16 | **`BUD-S18/S19` split → `BUD-S18` + `BUD-S19`; `BUD-S18` Done** (gate-green) — fourth analysis slice; FEAT-014a `Implemented`. Owner split the roadmap's "debt & credit trends" (as `BUD-S6/S7` was split): **credit utilization now (`BUD-S18`)**, installment-debt **payoff-% later (`BUD-S19`)**. Owner settled: **utilization, not payoff**; **per-account + portfolio roll-up**; a **separate `credit_limits` config table** (mirrors `envelope_targets`; a reversible definition, **no ADR**). Added `credit_limits` (idempotent migration) in `05_DATA_MODEL` + CreditLimit in `04_DOMAIN_MODEL`; pure-domain `creditUtilization` (owed = −balance; owed/limit in bps; cumulative monthly trend; roll-up); `creditLimitService` (set/clear, credit-kind only) + `analysisService.creditUtilization`; `GET /analysis/credit-utilization` + `PUT`/`DELETE /accounts/:id/credit-limit` in `06_API_CONTRACT`; resolved the `04_DOMAIN_MODEL` credit-balance **sign** open Q (owed = −balance at the analysis layer, ledger sign unchanged) | No spike: aggregates existing balances + one stored number (like `BUD-S15`/`BUD-S16`); the only new logic (cumulative balance per period vs. `BUD-S15`'s per-period net flow) is unit-proven, not a spike-level unknown. The gap was the **limit** store + the owed/utilization definitions — all settled with the owner | **Next: `BUD-S19`** debt payoff %, then hardening `BUD-S76/S77`–`BUD-S78`. **192 Vitest + 1 e2e** (+24) |
| 2026-06-15 | **`BUD-S27` Done** (gate-green) — **completes the engineering-health track `BUD-S22`–`BUD-S27`**. Discarded the absorbed throwaway spike code `spikes/04-transfer-modeling/` (verified inert: no importer, not a workspace, `vitest.workspace.ts`/`eslint` already exclude `spikes/`); ticked the spike report's own discard follow-up. Recorded two accepted notes (no behavior change): `bigint`→`Number()` exact to ~`2^53` cents (≈$90T) on the `Cents` type + `05_DATA_MODEL`; `normalizeName` ⊃ DB `lower(btrim())` in `naming.ts` + `05_DATA_MODEL` | Kit rule: spike code is throwaway once its findings are absorbed (here into `ADR-0004` + `BUD-S6`/`BUD-S7` + the kept report); the two notes were "accepted as-is" in the [2026-06-15 review](reviews/2026-06-15-repo-review.md) (BUD-S27) and needed a durable home | **Next: the analysis area (`BUD-S15`–`BUD-S18/S19`)**, starting with `BUD-S15` spend-by-envelope-over-time. **125 Vitest + 1 e2e** (no test-count change) |
| 2026-06-17 | **e2e strategy expanded.** BUD-S47 promoted from P3 DX/"fold into `BUD-S78`" to a standalone P1 hardening item sequenced **before `BUD-S76/S77`**: scope expanded to (1) split `journey.spec.ts` into per-area files, (2) write full journey coverage for all shipped features (BUD-S2–BUD-S19), and (3) establish the per-slice e2e convention (each BUD-S11–BUD-S20 slice lands its own spec). `BUD-S78` updated: removed BUD-S47 reference, added **e2e-in-CI** (automated `test:e2e` trigger in `gate.yml`) as the CI gate companion to ESLint-in-CI. BUD-S26 note updated to point to BUD-S47 instead of `BUD-S78` for full journeys. BUD-S11–BUD-S20 rows gated on BUD-S47 and each note their target spec file. | Owner decision: incorporate more e2e tests throughout | New order: BUD-S47 → BUD-S76/S77 → BUD-S11–BUD-S20 (each with own spec) → BUD-S78 (CI gates) |
| 2026-06-17 | **Improvement review captured; `BUD-S39`–`BUD-S47` added to this roadmap.** Post-`BUD-S19` review surfaced: 3 zero-API-work quick wins (BUD-S39–BUD-S41), 2 UX improvements using existing data (BUD-S21–BUD-S42), 4 new feature slices (BUD-S11–BUD-S20), and 5 DX items (BUD-S43–BUD-S47). `docs/03b_IMPROVEMENTS.md` (the working draft) deleted — this roadmap is the single plan of record. *(BUD-S47 scope and sequencing subsequently revised — see next entry.)* | Post-analysis improvement review | Added "UX polish" (BUD-S39–BUD-S42) and "Developer experience" (BUD-S43–BUD-S47) sections; BUD-S11–BUD-S20 appended to domain-slices table; current focus updated |
| 2026-06-17 | **BUD-S47 Done** (gate-green). `e2e/journey.spec.ts` deleted; replaced by 8 per-area spec files (`accounts`, `envelopes`, `transactions`, `templates`, `transfers`, `recurring`, `reconcile`, `analysis`) + shared `e2e/setup.ts` helper (`createAccount`, `createEnvelope`, `goToDashboard`). 20 Playwright tests (was 1), covering all features BUD-S2–BUD-S19: single/split/partial allocation · edit-split · refunds · archive/unarchive · templates (create, save-as, apply, rename) · account transfer · envelope reallocation · recurring + post-due · reconcile (match + mismatch) · all 5 analysis views. Convention established: every new slice lands with its own `e2e/*.spec.ts`. | BUD-S47 scoped before `BUD-S76/S77` so the per-area harness exists first | **Next: `BUD-S76/S77` backup/export** (see [BUD-S47 status report](status-reports/2026-06-17-r14.md) kickoff prompt) |
| 2026-06-17 | **`BUD-S77` deferred.** Owner decided to continue forward rather than tackle restore/import now. `BUD-S77` remains in the hardening table for later but is not next. **New order: `BUD-S78` (a11y + CI gates) is the next hardening item; `BUD-S39`/`BUD-S43` are quick wins alongside; `BUD-S11` (transaction delete) is the first new feature slice.** | Owner preference | Removed `BUD-S77` from "next"; `BUD-S78` is now the hardening focus |
| 2026-06-21 | **`BUD-S14` added — envelope ledger.** Surfaced by the question "can I view individual transactions in an envelope?". No schema change (data already in `allocations ⋈ transactions ⋈ accounts`). Gated on BUD-S2 · BUD-S47. Scope: `GET /envelopes/:id/ledger` (newest-first allocation list) + `EnvelopeLedger.tsx` panel; envelope-transfer reallocations out of scope for V1 (balance is still explained via the balance header). | Owner discovery during dev session | Added to domain-slices table as `BUD-S14`; FEAT-015 + UX spec written at `Proposed` |
| 2026-06-21 | **`BUD-S14` Done** (gate-green) — envelope ledger. `GET /envelopes/:id/ledger` returns all allocations for an envelope, newest-first. Envelope names on the Dashboard are now buttons that open `EnvelopeLedger.tsx`. No schema change. | Low risk, clean spec, no spike needed | **Next: `BUD-S78` (a11y + CI gates), `BUD-S39` (account rename), `BUD-S43` (API watch mode), `BUD-S11` (transaction delete). 227 Vitest + 23 e2e** (+9/+1) |
| 2026-06-21 | **`BUD-S39` Done** (gate-green) — account rename in UI. `renameAccount` wired in `api.ts` + `fakeApi.ts`; `AccountList` gains inline Rename button + input + Save; Dashboard `refreshAccounts` handler. BUD-S43 (API watch mode) was already in `apps/api/package.json`. | Zero API work; UI-only | **`exact: true` convention established** for `getByRole("button", { name: entityName })` where entityName appears in a sibling button's aria-label (applied across all 8 e2e specs). +1 e2e. **Next: `BUD-S78`, `BUD-S11`. 227 Vitest + 24 e2e.** |
| 2026-06-21 | **`BUD-S78` Done** (gate-green) — a11y pass + perf/NFR budgets + CI wiring. **A11y:** installed `@axe-core/playwright`; wrote `e2e/a11y.spec.ts` (12 tests, one per view); discovered 2 serious violations and fixed them: (1) `target-size` (WCAG 2.5.8 AA) — browser-default button/input/select height < 24 px → added `apps/web/src/index.css` with `min-height: 24px` on those elements; (2) `dlitem` (WCAG 1.3.1 A) — `<dl role="status">` in CreditView, PayoffView, ForecastView broke `<dt>`/`<dd>` containment → removed the role override. **Perf:** `apps/api/test/perf.test.ts` seeds synthetic data and measures key endpoints; all 3 budgets met: `GET /accounts` 0.9 ms p95, `GET /analysis/envelope-spend` 3.4 ms p95, `GET /export` 11.5 ms p95. **CI gate:** `gate.yml` wired with all 7 steps (typecheck · lint · format · unit+integration · e2e incl. a11y · build · SCA `npm audit --omit=dev --audit-level=critical`); `workflow_dispatch` kept intentionally. `07_NFR.md` updated to Active; 4 operational-readiness items ticked. | Challenge-first: axe scan before assuming clean; fixed 2 violations; measured before writing numbers. | **Next: `BUD-S11` (transaction delete). 230 Vitest + 36 e2e.** |
| 2026-06-21 | **`BUD-S11` Done** (gate-green) — transaction delete. `ConflictError` (→ 409) added; `transactionService.remove` + `transferService.remove`; `DELETE /transactions/:id` + `DELETE /transfers/:id` routes; `deleteTransaction`/`deleteTransfer` in `api.ts` + `fakeApi.ts`; Delete buttons on every register row. Allocations cascade via DB FK; transfer pair teardown cascades both legs. 5 new integration tests + 2 new e2e tests. | Hard delete; DB cascades handle allocations automatically — no application-layer cleanup needed | **Next: `BUD-S12` (account archive/close). 235 Vitest + 38 e2e.** |
| 2026-06-21 | **`BUD-S12` Done** (gate-green) — account archive/close. `accountService.setArchived(id, archived)` + `POST /accounts/:id/archive` / `/unarchive` (mirrors envelope `BUD-S5`); `archiveAccount`/`unarchiveAccount` in `api.ts` + `fakeApi.ts`; `AccountList` splits active/archived with a "Show archived" toggle and per-account `aria-label`'d Archive/Unarchive buttons. Challenge resolved: archived accounts stay **fully viewable** (register, transactions, delete all reachable via the archived list) — only **new** transfers reject them, via the **pre-existing** transfer-create guard (no new block needed). No schema change (`archived_at` existed since Foundation). 1 integration test + 1 e2e test. | Pure mirror of the envelope-archive pattern; the transfer-create archived-account guard already covered the only money-affecting path, so no domain/guard change was needed | **Next: `BUD-S13` (transaction search/filter). 236 Vitest + 39 e2e.** |
| 2026-06-22 | **`BUD-S13` Done** (gate-green) — transaction search/filter. `GET /accounts/:id/transactions` takes an inclusive `?from&to` window (defaults to the current calendar month via `currentMonthRange()`); `transactionService.listByAccount(accountId, { from, to })` filters by `occurred_on` but **always includes `opening` rows**. `listTransactions(accountId, range?)` threads the window through `api.ts` + `fakeApi.ts`; `AccountRegister` gained From/To date inputs (re-fetch on change) + a client-side payee/memo search box. Aligned `fakeApi`'s default txn date to today (the server already defaults to `todayStr()`), removing a latent date-fixture time-bomb. 3 integration tests + 1 web test + 1 e2e. | **Challenge resolved:** defaulting to the current month *would* hide the opening row (dated account-creation day) for older accounts → opening rows are special-cased to always show (the register's balance anchor); needs-allocation surfacing is a separate household-wide endpoint, so it is unaffected by the window. No schema change | **Next: `BUD-S20` (net worth over time). 240 Vitest + 40 e2e.** |
| 2026-06-22 | **`BUD-S20` Done** (gate-green) — net worth over time. Pure-domain `netWorthOverTime` + `isLiabilityKind` (new in `account.ts`); `analysisService.netWorth(grain)` gathers per-(period, kind) net flows over **all** transactions; `GET /analysis/net-worth?grain=month\|year` (reuses the `SpendQuery` generic); `getNetWorth` in `api.ts` + `fakeApi.ts`; thin `NetWorthView` (current totals + a cumulative Assets / Liabilities / Net trend) wired into `App`/`Dashboard` ("Net worth" header button). 7 domain + 7 integration + 4 web tests; `e2e/analysis.spec.ts` +1, `e2e/a11y.spec.ts` +1. | **Challenge resolved:** net worth = `Σ` of all **signed** account balances — liability kinds (`credit`/`loan`) carry debt negative (the FEAT-014a/b `owed = −balance` convention), so `net = assets + liabilities` falls out and transfer legs net to zero. Chose a **by-kind** split (stable over time) over a sign-based one (jagged: accounts flip columns as the balance crosses 0). The household-wide aggregate has no per-entity row, so the e2e asserts the **invariant** (net = assets + liabilities), immune to the shared DB. No schema change. | **Analysis area extended; next: the remaining UX-polish (`BUD-S40`–`BUD-S42`) / DX (`BUD-S44`–`BUD-S46`) backlog. 258 Vitest + 42 e2e.** |
| 2026-06-22 | **`BUD-S21` Done** (gate-green) — Dashboard net worth summary. A `NetWorthSummary` table below the account list (Total assets / Total liabilities / Net worth), computed **client-side** in `Dashboard.tsx` from the already-loaded account list — reuses the shared `isLiabilityKind`, sums **all** accounts (active + archived). `Dashboard.test.tsx` +2 (exact kind-based sums vs. a clean fake; hidden until an account exists); `e2e/accounts.spec.ts` +1 (invariant). No new endpoint, no schema/API change. | **Challenge resolved (rejected the kickoff's "call the endpoint" recommendation):** checked the SQL — `v_account_balances` and `BUD-S20`'s net-worth query sum the **same** transaction set, so a client-side `Σ` grouped by `isLiabilityKind` is **provably equal** to `BUD-S20`'s endpoint totals (no drift). Computing from the loaded list avoids a 3rd dashboard fetch and keeps the snapshot a faithful `Σ` of the rows shown right above it. The only real decision client-side forces — archived accounts — is settled by matching `BUD-S20` (include them). | **Next: the remaining UX-polish (`BUD-S40`, `BUD-S41`, `BUD-S42`) / DX (`BUD-S44`–`BUD-S46`) backlog. 260 Vitest + 43 e2e.** |
| 2026-06-22 | **`BUD-S40` Done** (gate-green) — needs-allocation count badge. The Dashboard fetches `api.listNeedsAllocation()` on mount (independently of the core account/envelope `Promise.all`, so a badge fetch failure leaves the badge absent rather than blanking the page) and renders the count as a red pill (`#991b1b`/white) on the "Needs allocation" header button, with the count in the button's `aria-label`. `Dashboard.tsx` only (state + independent fetch + `BADGE` style + button); `Dashboard.test.tsx` +2; `e2e/transactions.spec.ts` +1. No new endpoint, no schema/API/domain change. | **Challenge resolved (confirmed the kickoff's lean):** front-end fetch of the existing endpoint over a new count aggregate — zero API work, and it's the *same* single round-trip the `NeedsAllocation` view already makes (no N+1). Freshness needs no cache wiring: `App` conditionally renders the Dashboard keyed on `view.name`, so it remounts (re-fetches) on back-navigation after an allocation is completed. Accessibility is name-based, not color-based; the pill's ~8.3:1 contrast is validated by the existing Dashboard axe scan. | **Next: the remaining UX-polish (`BUD-S41`, `BUD-S42`) / DX (`BUD-S44`–`BUD-S46`) backlog. 262 Vitest + 44 e2e.** |
| 2026-06-22 | **`BUD-S41` Done** (gate-green) — grouped navigation, unified Analysis section. New thin `AnalysisSection` container groups all **6** analysis views (Spend, Budget, Forecast, Credit, Payoff, Net worth) behind one Dashboard "Analysis" entry: a `<nav aria-label="Analysis views">` sub-nav of buttons (`aria-current="page"` active) that **mounts only the active view** — so each view keeps its own `<h1>` + data fetch (no eager fetch-all-six) + its own `← Dashboard` exit. `App.tsx` collapses six `view` states + six `onOpen*` props into one `analysis`/`onOpenAnalysis`; `Dashboard.tsx` drops five header buttons. The six view components are untouched. `AnalysisSection.test.tsx` +3; `e2e/setup.ts` gains `openAnalysis(page, tab)`; `analysis.spec.ts`/`a11y.spec.ts` re-pointed to navigate via Analysis → tab (forecast test switches tab-to-tab in-section, exercising the new capability). | **Challenge resolved:** semantically-correct nav buttons over a `role="tablist"` widget (each view is a full `<main>`/`<h1>` page, not one swappable `tabpanel`); grouped **Net worth** too (BUD-S20 added it after the BUD-S41 line said "5"); no transition → `prefers-reduced-motion` N/A. Pure React/navigation refactor, no schema/API/domain change | **Next: the remaining UX-polish (`BUD-S42`) / DX (`BUD-S44`–`BUD-S46`) backlog. 265 Vitest + 44 e2e.** |
| 2026-06-22 | **`BUD-S42` Done** (gate-green) — **completes the UX-polish track (`BUD-S39`–`BUD-S42`)**; envelope targets visible inline. The Dashboard fetches `getBudgetVsActual(currentMonth)` on mount **independently** of the core account/envelope `Promise.all` (a `Map<envelopeId, BudgetVsActualRow>`), and each **active** envelope row renders an `EnvelopeBudgetInline` showing **target · spent · remaining** when a target is set. `Dashboard.tsx` only (state + independent fetch + `currentMonth` helper + `EnvelopeBudgetInline` + a threaded `budgetByEnvelope` prop on `EnvelopeList`); `Dashboard.test.tsx` +2 (triplet vs. a clean fake; un-budgeted row shows nothing extra); `e2e/envelopes.spec.ts` +1 (set a target in the Budget view → shows inline on the Dashboard row). No new endpoint, no schema/API/domain change. | **Challenge resolved (confirmed the kickoff's lean):** independent fetch of the existing budget endpoint mirrors the `BUD-S40` badge — a target-fetch failure leaves the inline figures absent, never blanks the Dashboard. Rendered **only** when a target is set (no faked `$0`); archived list + the BUD-S14 envelope-name click target + existing row controls all untouched; money via `formatCents`; figures are plain labelled text (not colour-only). Freshness needs no cache wiring — `App` remounts the Dashboard on back-navigation, so a target set in the Budget view shows on return. | **Next: the DX (`BUD-S44`–`BUD-S46`) backlog. 267 Vitest + 45 e2e.** |
| 2026-06-22 | **`BUD-S44` Done** (gate-green) — route modularization. The 682-line `http/server.ts` (43 routes inline) split into **9 per-domain Fastify plugin modules** (`http/routes/{accounts,envelopes,transactions,reconcile,transfers,recurring,analysis,templates,backup}.ts`) + `routes/shared.ts` (error envelope `fail`, `parsePositiveMagnitude`/`parseTemplateLines`, `DATE_RE`/`MONTH_RE`, `IdParams`/`AccountIdParams`, a `Services` container + `RoutePlugin` alias). `buildServer` → **104 lines**: keeps CORS + content-type parser + the single error handler + `/health`, builds the one `Services` object, and `app.register`s each plugin with `{ services }`. | **Challenge resolved:** the riskiest assumption — that encapsulated child plugins preserve the global error envelope / CORS / JSON body-parser — held, because all three are set on the **root** (inherited by children) and the route plugins add no hooks/decorators of their own. **No Fastify `prefix`** (domains share crossing URL roots — credit-limit/target setters live under `/accounts`/`/envelopes` but belong to analysis), so **every literal path is unchanged**; services constructed once and shared by reference. Pure DX refactor; the 125 API integration tests (incl. the `cors.test.ts` preflight + the 400/404/409 envelope) + 45 e2e are the regression net — green, unchanged. No schema/API/domain change. | **Next: `BUD-S45` (React error boundaries), then `BUD-S46` (structured API logging). 267 Vitest + 45 e2e** (unchanged). |
| 2026-06-23 | **`BUD-S45` Done** (gate-green) — React error boundaries. One reusable `ErrorBoundary` (`apps/web/src/ErrorBoundary.tsx`) placed twice: top-level wrapping `<App>` in `main.tsx` (recovery = reload) and per active analysis view in `AnalysisSection` (`key={tab}` → fresh boundary per tab; recovery = `← Dashboard`). Fallback = `<main>` + `<h1>` + `role="alert"` + a real button, a high-contrast card (`#1a1a1a`/`#fff`, dark-red border, `colorScheme: light`) readable in any color scheme. e2e-reachable via a dev-only `?boom` hook in `main.tsx` (`import.meta.env.DEV`-gated → tree-shaken from the prod bundle, verified 0 occurrences). `ErrorBoundary.test.tsx` +3, `e2e/error-boundary.spec.ts` +1, `e2e/a11y.spec.ts` +1. Zero API/schema/domain change. | **Challenge resolved:** error boundaries catch **render/lifecycle** throws only — *not* event handlers or async/fetch rejections (those already degrade gracefully via each view's `loadError` state). So the boundary's real job is the one currently-unprotected failure mode: a render-time throw, which `main.tsx`'s direct `<App/>` render would otherwise unmount to a blank white screen. Chose top-level **+** per-analysis-view (the analysis views do the most derived-data render work and sit behind a persistent sub-nav) over per-route-everywhere (over-engineering). Live dark-mode preview caught that the default-color fallback was black-on-dark (unreadable) — fixed by pinning the card's own high-contrast colors. | **Next: `BUD-S46` (structured API logging) — the last DX backlog item. 270 Vitest + 47 e2e.** |
| 2026-06-23 | **`BUD-S46` Done** (gate-green) — **completes the DX backlog (`BUD-S43`–`BUD-S46`)**; structured API logging. `buildServer`'s `logger` option widened `boolean`→`FastifyServerOptions["logger"]`; `index.ts` passes `{ logger: { level: config.LOG_LEVEL } }` → Fastify bundled-pino request/response/error lines at the configured level (a `reqId` correlation id per line). `LOG_LEVEL` (already in `config.ts`) tightened from `z.string()` to a **closed enum** of pino levels — validated at the boundary so a typo fails loudly at startup (spine §8). Tests omit the option → `logger: false` → quiet + deterministic (unchanged). `logging.test.ts` +2, `config.test.ts` +3. | **Challenge resolved:** logging is a cross-cutting effect, not a route; the risk was leaking financial request bodies/headers. Verified Fastify's **default** serializers emit only `method`/`url`/`statusCode`/`responseTime` (+ error type/message/stack) — **never** bodies or headers — so we keep them and add **no** body serializer (transaction memo/payee/amount + the `/export` snapshot never reach the logs; SECURITY.md §1/§5). Regression net asserts **both** halves via a capture stream: the access line is present *and* a money-route body + `authorization` header are absent; a real-startup smoke confirmed structured JSON to stdout, no leak. No API-contract/schema/domain change. | **DX + UX backlog clear; remaining work is the Hardening track + new feature slices. 275 Vitest + 47 e2e.** [status report](status-reports/2026-06-23-r13.md) |
| 2026-06-17 | **`BUD-S76/S77` split → `BUD-S76` + `BUD-S77`; `BUD-S76` Done** (gate-green) — first hardening slice. Owner confirmed: **export-only now** (`BUD-S76`); restore/import deferred to `BUD-S77` (riskier half — FK order, ID collisions, partial failure, validation — warrants a spike). Owner confirmed: **JSON snapshot** (engine-neutral, integer cents, round-trippable) over CSV (lossy) or SQL dump (Postgres-specific). Owner confirmed: **server endpoint** (`GET /export`) over CLI script, so the web app delivers a "Download backup" button (usable vertical slice). No spike for export — serializes existing rows, no unknown assumptions. `07_NFR.md` created (hardening track home; perf/a11y budgets are `BUD-S78` placeholders). Built: `backupService` (15-table parallel read, cents as `Number()`) + `GET /export` route (Content-Disposition: attachment, body never logged) + "Download backup" `<a>` in Dashboard header + `exportUrl` exported from `api.ts`. `e2e/backup.spec.ts` added (2 tests: link visible + API shape). | Owner settled all three forks (format / scope / delivery) before building; no spike warranted for export | **`BUD-S76` done. Next: `BUD-S77` (restore — spike first) or `BUD-S78` (a11y + CI). 218 Vitest + 22 e2e** (+4/+2) |
| 2026-06-25 | **UX Uplift initiative scoped; `SPIKE-06`–`BUD-S60` added.** An owner design session set the framing — **foundation-first** (design system + client routing/app-shell, then migrate screens as vertical slices) and **desktop-first** — and three priorities: a **router-based** page framework, a **budget + future-planning landing cockpit**, and **new ways to visualize spending**. Captured thoroughly in [reviews/2026-06-25-ux-uplift-initiative.md](reviews/2026-06-25-ux-uplift-initiative.md); tracked here as the new **"UX Uplift"** section. The initiative **reverses the repo's deliberate "no design system" stance**, so it is **ADR-gated + spike-first** (`SPIKE-06`→`ADR-0005`/`ADR-0006`; `SPIKE-07`→`ADR-0007`), mirroring how `SPIKE-02` produced the stack ADRs. | Owner design session 2026-06-25 | New "UX Uplift" track added after the DX section; **next is `SPIKE-06`** (design-system + routing spike on one real screen). No code/spec/ADR written yet — items are `Planned`, not `Ready`. |
| 2026-06-25 | **`SPIKE-06` scoped as [SPIKE-06](spikes/06-design-system-routing.md).** Owner chose the **Account Register** as the probe screen and a **bake-off** (compare alternatives head-to-head) over single-hypothesis validation. Spike plan written: one falsifiable question, three decision axes — routing (React Router vs TanStack) × styling (CSS tokens+Modules vs Tailwind) × a11y primitives (React Aria vs Radix) — with a11y as a hard disqualifying gate, a measured bundle budget, and explicit pass/kill criteria. Blocks `ADR-0005` (styling) + `ADR-0006` (routing) → on green, `BUD-S49`/`BUD-S48` become `Ready`. | Owner scoping decisions (probe screen + bake-off) | `SPIKE-06` → `Ready`; **next is to execute SPIKE-06** (throwaway harness at `spikes/06-design-system-routing/`). |
| 2026-06-25 | **`SPIKE-06` Done — [SPIKE-06](spikes/06-design-system-routing.md) executed.** A throwaway Vite harness rebuilt the Account Register and baked off routing × styling × a11y primitives on **real rendered output**: a real-Chromium axe scan (WCAG 2.2 AA) and isolated per-library gzip bundle measurements. **Result: a viable axe-green stack exists** (lead screen + both dialogs = **0 violations**); chose **React Router + design tokens/CSS-Modules + Radix Primitives**. **The bake-off flipped the pre-spike lean** — both React Aria and Radix pass a11y, but Radix's dialog is **13.9 KB gz vs React Aria's 29.7 KB**, so the primitive choice moved React Aria → **Radix**; TanStack Router was found *slightly smaller* than React Router yet React Router wins on conventionality. `ADR-0005` (design system) + `ADR-0006` (routing) written → **`Validated`**. | Owner: execute SPIKE-06 | `SPIKE-06` → Done; **`BUD-S49` (app shell) + `BUD-S48` (component library) → `Ready`** on the chosen stack. Follow-ups: set the web bundle budget in `07_NFR` from the `BUD-S49` shell baseline; discard the spike harness once absorbed. |
| 2026-06-25 | **`BUD-S48` scoped → [FEAT-UX4](features/design-system.md).** Owner chose **seed-and-grow** (token sheet + only the starter primitives the first screen needs — Button/Field/Input/Select/Table/Badge/Dialog/Alert/EmptyState/Skeleton — then grow per slice) over a full-library-upfront build, and to **prove them by restyling the Account Register in place** (current nav unchanged). **Flagged + resolved a horizontal-layer risk:** a whole component library no screen uses violates *vertical, not horizontal* — so BUD-S48 is now a vertical slice (tokens → primitives → one real styled screen, gate-green incl. a dark-mode axe pass). **Re-sequenced BUD-S48 before BUD-S49** (in-place restyle needs no routing; BUD-S49 swaps in React Router after). No data/API/domain change. | Owner scoping decisions (breadth + proof screen) | `BUD-S48` → `Ready (next)`; `BUD-S49` follows. FEAT-UX4 spec at `Proposed`. |
| 2026-06-25 | **`BUD-S48` Done (gate-green)** — design-system foundation, first UX-Uplift build slice. `apps/web/src/ui/`: token sheet (`tokens.css`, dark mode via `prefers-color-scheme`) + base layer (`base.css`, element styling; **absorbs + deletes `index.css`**) + seed primitives (Button/Badge/Card/Alert/EmptyState/Skeleton/Field/Input/Select). The **Account Register** restyled **in place** onto them (no routing/flow change; `<li>` rows + all aria/roles/text preserved), axe-clean (WCAG 2.2 AA) in **light AND dark** (a dark-scheme axe pass added to `e2e/a11y.spec.ts`). **`Dialog` + `Table` deferred** (seed-and-grow — the editor is a shared inline expand, not a modal; modal-izing it is a flow change). No data/API/domain change. | Owner: build BUD-S48 | `BUD-S48` → Done; **`BUD-S49` (routing/app shell) is next**. FEAT-UX4 → `Implemented`. Follow-up: set the web bundle budget in `07_NFR` at the `BUD-S49` shell build; discard the SPIKE-06 harness. **283 Vitest + 49 e2e.** |
| 2026-06-27 | **`BUD-S49` Done (gate-green)** — routing + persistent app shell. `App.tsx`'s hand-rolled `view` state machine retired for **React Router 7 (data router)** behind a persistent `AppShell` (banner + primary `NavLink` nav + keyed route `<Outlet>`); new `api-context.tsx` (provide the `Api` above `RouterProvider`) + `routes.tsx` (the route map + thin adapters) + `EnvelopeLedgerRoute.tsx` (resolves `/envelopes/:id` against the list → refresh-safe). Every screen is URL-addressable / deep-linkable / refresh-safe with working back/forward; the per-screen "← Dashboard" + register "Needs allocation" buttons are gone (nav + the BUD-S40 count badge moved to the shell); `AnalysisSection` is now route-driven at `/insights/:view`. A reduced-motion-aware route transition (a **transform** slide, not an opacity fade). No data/API/domain change. `ADR-0006` → **`Accepted`**; FEAT-UX3 spec added; web bundle budget set in `07_NFR` (91.4 KB gz, < 120 KB budget); the **SPIKE-06 throwaway harness discarded** (findings persist in the spike report + ADR). | Owner: build BUD-S49 | **Challenge resolved / flag:** `/manage` + the `/accounts`·`/envelopes` **list** routes were **deferred to `BUD-S51`** (and `/transactions/new` to `BUD-S52`) per the brief — building them empty now is the *Build-Without-Use* anti-pattern; BUD-S49 routes only the screens that exist today. Caught a real a11y regression in review: an opacity route-fade dropped muted-text contrast below 4.5:1 during the axe scan → switched to a transform slide. **Next: `BUD-S50` (the cockpit).** **283 Vitest + 52 e2e.** [status report](status-reports/2026-06-27-ux3.md) |
| 2026-06-27 | **`BUD-S50` Done (gate-green)** — home budget + future-planning cockpit. New `Cockpit.tsx` (+`.module.css`, +`Cockpit.test.tsx`) rendered at the top of the home's `<main>` (`Dashboard.tsx`): five panels (this-month budget · needs-allocation · upcoming recurring · cash-flow forecast · net worth) that **compose existing reads** — `getBudgetVsActual` · `listNeedsAllocation` · `listRecurring` · `listAccounts` (one read feeds both the net-worth snapshot and the forecast account pick) · `getCashFlowForecast` — each panel loading/erroring independently (BUD-S40/BUD-S42). Each panel deep-links to its detail route; figures reconcile to the ledger (asserted). On the BUD-S48 design system (`Card`/`Badge`/`EmptyState`/`Skeleton`) + BUD-S49 router `<Link>`s. New `e2e/cockpit.spec.ts` (+2) + a dark-mode populated-home axe scan (+1). Fixed two stale `2026-06-25-ux4.md` → `2026-06-26-ux4.md` links (the BUD-S48 report was renamed) in this roadmap + `features/design-system.md`. No new endpoint, no schema/API/domain change. | Owner: build BUD-S50; the brief's open Q (home-summary endpoint vs. fan-out) | **Open Q resolved → fan-out** (no aggregate; BUD-S21/BUD-S42 precedent). **Challenge resolved / flag:** account/envelope management **stays on the home below the cockpit** — demoting it to `/manage` is `BUD-S51`'s explicit acceptance, and removing the only setup path now would break *usable-at-every-step* (Download-backup already moved to the shell in BUD-S49). **Visual check caught a real bug:** the budget panel first showed non-reconciling totals (the report's `totalSpent` counts untargeted spend while `totalRemaining` is targeted-only) → now sums over targeted envelopes so budgeted − spent = remaining; strengthened the unit test to cover untargeted spend. **Next: `BUD-S51` (demote management to `/manage`).** **296 Vitest + 55 e2e.** [status report](status-reports/2026-06-27-ux5.md) |
| 2026-06-28 | **`BUD-S51` Done (gate-green)** — demote management off the home. New route components `Home.tsx` (cockpit-only home), `AccountsList.tsx`/`EnvelopesList.tsx` (the `/accounts`·`/envelopes` **LIST routes** deferred from BUD-S49 — progressive "Add" affordance, each name now a `<Link>` to its detail, inline rename/archive + BUD-S42 budget inline), and `ManageView.tsx` (the `/manage` **hub**: the BUD-S21 net-worth summary + Move-money + links to the two list pages); `Dashboard.tsx` retired. `routes.tsx` gains `/accounts`·`/envelopes`·`/manage`; the shell nav gains **Accounts · Envelopes · Manage**. Figures reconcile to the ledger; fan-out over existing reads (no new endpoint, no schema/API/domain change). The **bulk was the e2e re-point** (like BUD-S49): `setup.ts` helpers (`createAccount`/`createEnvelope` drive `/accounts`·`/envelopes` + the progressive Add; new `goToAccounts`/`goToEnvelopes`/`goToManage`/`openAccount`/`openEnvelope`) + 9 specs moved off the home; `Dashboard.test.tsx` split into `Home`/`AccountsList`/`EnvelopesList`/`ManageView` tests; a11y suite scans the three new surfaces light + dark. | Owner: build BUD-S51; **surface-split fork** (everything-on-`/manage` vs. list-routes-own-CRUD) | **Fork resolved → list routes own CRUD; `/manage` is the cross-cutting hub** (net-worth spans all accounts, Move-money spans envelopes) — reads the brief's "and" as a clean, non-duplicated distribution. **Caught in the axe sweep:** the `/manage` hub's links-in-prose tripped WCAG 1.4.1 `link-in-text-block` (link not distinguished from surrounding text) → moved the prose to a standalone intro `<p>` so each hub link stands alone. Home `/` is now the **cockpit only**; this also retires the BUD-S50-flagged redundant net-worth (home + BUD-S21) and the within-mount freshness carry. **Next: `BUD-S52` (global quick-add transaction).** **304 Vitest + 61 e2e.** [status report](status-reports/2026-06-28-ux6.md) |
| 2026-06-28 | **`BUD-S52` Done (gate-green)** — global quick-add transaction. New `QuickAddTransaction.tsx` mounted as a **modal route** (`/transactions/new`, a child of the shell); an always-available **Add transaction** action in the shell nav opens it. It **reuses the register's editor** — `AddTransactionForm` gains an optional `accounts` picker mode, `AllocationEditor` gains an optional `disabled` gate (both default to the unchanged register behavior) — and **fans out to the existing `createTransaction`** (no new endpoint, no schema/API/domain change); a **partial** allocation's remainder still surfaces in needs-allocation. The modal is the new **`Dialog` primitive** (`apps/web/src/ui/Dialog.tsx`, `@radix-ui/react-dialog`) — the first genuine modal, so the BUD-S48 seed-and-grow `Dialog` deferral lands here. History-safe close (`navigate(-1)`; deep-link → `/`). | Owner: build BUD-S52; **Dialog mechanism fork** (Radix vs hand-rolled; child-route vs background-overlay) | **Forks resolved → Radix `Dialog` + child-route overlay.** ADR-0005 (Validated) reserves **Radix for the hard a11y widgets (Dialog named)** and SPIKE-06 validated the Radix dialog axe-clean, so Radix is the doc-aligned, lowest-a11y-risk path (focus trap / ESC / overlay-close / focus-restore / `role="dialog"` for free); +~12 KB gz → 105.4 KB (< 120 KB). With a data router a true background-location overlay is awkward, so a **child route + portal'd Dialog + history-safe back** gives the modal contract cleanly. **Entry = shell nav only** (the brief's "and/or") — return-focus-solid (the trigger stays mounted) and it avoids breaking the cockpit NeedsPanel's "all-clear → no link" test. Axe-green **light AND dark** on the open modal. **Next: `BUD-S53` (Analysis → Insights), gated by the `SPIKE-07` charting spike.** **311 Vitest + 66 e2e.** [status report](status-reports/2026-06-28-ux7.md) |
| 2026-06-28 | **`SPIKE-07` Done — [SPIKE-07](spikes/07-accessible-charting.md) executed (the charting gate before the Insights phase).** A throwaway Vite harness built **one real chart** (net-worth-over-time, the `getNetWorth` shape) **hand-rolled in SVG** with the production design tokens, then ran a **real-Chromium axe scan** (same WCAG 2.2 AA tag set + serious/critical filter as `e2e/a11y.spec.ts`) and **React-externalized gzip bundle measurements** (the SPIKE-06 technique). **Result: an accessible chart is achievable hand-rolled** — **0 axe violations** across **light/dark × table-shown/hidden**, keyboard- and SR-reachable via a real `<table>` fallback, **color never the sole signal** (line color + dash + marker shape + direct label). **The decider was bundle:** hand-rolled adds **1.94 KB gz** (≈13% of the ~14.6 KB headroom) vs **Recharts 129 KB gz** — *larger than the entire 120 KB budget* — so a charting library is **disqualified**. `ADR-0007` (hand-rolled SVG + img-summary/table-fallback contract + new `--chart-*` tokens + a shared `Chart` primitive) written → **`Validated`**. App gate untouched (spike-only). | Owner: execute the SPIKE-07 charting spike; **fork** (charting library vs hand-rolled SVG) | **Fork resolved → hand-rolled SVG** (the budget forces it *and* it gives total a11y control). **Surprise:** Recharts v2's tree-shaken cost (~129 KB gz) far exceeds its "modular" framing; even the lightest credible lib (uPlot ~40 KB, visx modular) would eat most/all of the headroom for *one* of six charts. The fallback table doubles as the existing analysis content, so **BUD-S53 is a low-risk additive migration** ("keep the table, add an SVG above it"). **Residual (low):** validated on a line/time-series; bars (spend/budget) + gauges (credit/payoff) reuse the shape-agnostic contract and re-run the axe gate per shape — no follow-up spike. **`BUD-S53` → `Ready`.** Follow-up: discard the spike harness once `BUD-S53` absorbs it. **App gate still 311 Vitest + 66 e2e, 105.4 KB gz.** [status report](status-reports/2026-06-28-ux2.md) |
| 2026-06-28 | **`BUD-S53` Done (gate-green)** — Analysis → Insights migration, the first build slice of the Insights phase. The six analysis views are **renamed** ("Insights — …"), **restyled** onto the BUD-S48 design system (`Insights.module.css` + `Field`/`Input`/`Button`), and each gains a **hand-rolled accessible chart** above its existing table on the ADR-0007 contract. New shared **`apps/web/src/ui/Chart.tsx`** (`ChartFigure` scaffold owning the a11y contract + `LineChart`/`BarChart`/`Gauge`) + new **`--chart-1/2/3`/`--chart-grid`** tokens (light+dark, ≥ 3:1). Net worth + cash-flow = **line**, spend + budget (grouped target-vs-spent, 2nd series hatched) = **bar**, credit + payoff (overall ratio + threshold marker) = **gauge**. Every chart `role="img"` + one-line summary, `aria-hidden` innards, the **existing table as the data-table fallback** (toggle default-shown so inline editors stay reachable), **colour never the sole signal**. `e2e/a11y.spec.ts` now **seeds data per shape** and axe-scans every chart **LIGHT AND DARK** (the previous insights a11y tests scanned empty states only). **Presentation-only — no new endpoint/schema/API/domain change.** | Owner: build BUD-S53; chart-shape choices (gauge vs. extra line for credit/payoff) | **Shape calls:** credit/payoff get **one aggregate gauge** (the per-account + month trends stay in their tables as detail/fallback), keeping the slice tight; spend charts **per-envelope net totals** as diverging bars (direction encodes sign). **Integration call:** the `Chart` primitive takes the view's **existing table as a slot** (one table, editors preserved) rather than generating a second — faithful to the ADR's "add an SVG above the table". `ADR-0007` → **`Accepted`**; SPIKE-07 harness discarded (findings persist in spike report + ADR). Bundle **108.33 KB gz** (+2.9 KB for six charts + primitive; < 120 KB). **Verification note:** charts are rendered-and-axe-scanned in real Chromium by the e2e suite (interactive preview was blocked by a port clash with another running dev server). **Next: `BUD-S54` (new viz: spending breakdown).** **315 Vitest + 72 e2e.** [status report](status-reports/2026-06-28-ux8.md) |
| 2026-07-01 | **`BUD-S55` Done (gate-green)** — new viz: spending trends over time, the third build slice of the Insights phase. New `SpendingTrendsView.tsx` at `/insights/trends` ("Trends" tab, next to Breakdown): a multi-series **line chart** — household **total outflow** + **top-2 envelopes by outflow** — over a selectable **3/6/12-month trailing window** ending at a chosen month, **reusing the existing `LineChart` shape** (no new chart shape). **Read decision:** composes the **existing `getBudgetVsActual(month)` read, once per month in the window** (`Promise.all`) — **pure outflow**, not `getEnvelopeSpend`'s net flow, for the same reason BUD-S54 chose it (funding-then-spend in one month would otherwise understate the trend); no new endpoint/schema/API/domain change, extending the BUD-S21/BUD-S42/BUD-S54 fan-out precedent to several calls. Chart design: 3 series max, **one `--chart-*` token each** (no colour reuse) — Total solid/circle, the two ranked envelopes dashed/square and dashed/triangle — each also carrying a direct end label, so colour is never the sole signal; envelopes with zero outflow across the whole window are excluded from ranking, a ranked envelope's zero-spend month is **0-filled** (not omitted) to keep the axis aligned. `e2e/a11y.spec.ts` seeds outflow across two real backdated months and is **axe-green LIGHT AND DARK**. | Owner: build BUD-S55; the read decision (net flow vs. pure outflow; one read vs. several) and how a multi-envelope trend stays legible without colour | **Both resolved as anticipated in the BUD-S54 kickoff:** pure outflow via several `getBudgetVsActual` calls (not `getEnvelopeSpend`'s single-call net flow) — semantically correct over cheap, matching BUD-S54's own precedent; legibility via **Total + top-2** (not all envelopes, not per-envelope small multiples) — one combined chart answers both "is spending trending up?" and "what's driving it?" without a new shape. **Next: `BUD-S56` (budget burn-down).** **323 Vitest + 78 e2e.** Bundle **109.90 KB gz** (< 120 KB). [status report](status-reports/2026-07-01-ux10.md) |
| 2026-07-01 | **`BUD-S56` Done (gate-green)** — new viz: budget burn-down, the **fourth and final** new-visualisation slice of the Insights phase. New `BudgetBurndownView.tsx` at `/insights/burndown` ("Burn-down" tab, next to Budget): a **`Gauge`** of **within-month budget-consumed** (spent ÷ target) with the gauge's **threshold marker repurposed as the elapsed-time pace** ("pace today"), for a chosen month + scope (all budgeted envelopes, or one) — so a user sees "am I on pace?" **before** month-end (budget-vs-actual only shows it after). **Read decision:** composes the **existing `getBudgetVsActual(month)` read — ONE call, no fan-out, no new endpoint/schema/API change** (the scope picker selects the household aggregate or one budgeted envelope from that single read); the only new code is **one pure, unit-tested domain function** (`assessBurndown` in `packages/domain/src/burndown.ts` — elapsed-day-fraction linear-burn pace, `today` passed in per the pure-core/impure-shell convention, reusing `daysInMonth`). **Chart-shape decision:** reuse `Gauge` **as-is** (no new shape/variant) — the burn-down is one ratio + one reference marker, exactly the gauge's job. `status ∈ over-budget | over-pace | on-track`, carried by a **verdict sentence + marker position + a table Pace column** — never colour alone; `role="img"` + one-line summary, `aria-hidden` innards, an exact-figures table (target · spent · % · pace + aggregate footer). `e2e/a11y.spec.ts` seeds a budgeted envelope with mid-month outflow and is **axe-green LIGHT AND DARK**. | Owner: build BUD-S56; the pace/read decision (elapsed-time proxy vs. something else; one read vs. several; pure fn or not) and the chart-shape decision (reuse Gauge vs. new shape) | **Both resolved as the kickoff anticipated:** elapsed-time pace as a documented linear-burn **proxy** in a **pure domain function** (non-trivial date math ⇒ pure-core, mirroring `forecast.ts`); **ONE read** (lighter than BUD-S55's fan-out — a burn-down is a single month, and that call already returns every row + the totals, so the scope picker needs no extra requests); **reuse `Gauge` as-is** (the threshold marker fits "pace today" with no variant). A day-by-day burn-down **curve** + an end-of-month **projection** are deferred (both need per-day spend, i.e. a new read). **Next: `BUD-S57` (UX Uplift Polish track).** **335 Vitest + 81 e2e.** Bundle **111.06 KB gz** (< 120 KB). [status report](status-reports/2026-07-01-ux11.md) |
| 2026-07-02 | **`BUD-S57` in progress — thread 1 of 4 Done (gate-green)** — the first slice of the Phase-4 **Polish** track: **confirmation on destructive actions**. BUD-S57 bundles four independent polish threads (skeletons · toasts · destructive-action confirms · inline validation); per *vertical, not horizontal* (§5) + right-size/compress (§11), this slice ships **one** and leaves the rest tracked. New **`ConfirmDialog`** primitive (`apps/web/src/ui/ConfirmDialog.tsx`) — a **controlled/transient** modal on the **same SPIKE-06-validated Radix `Dialog`** the quick-add uses (reuses `Dialog.module.css` + a `.actions` row; **no new dependency**) — now gates the three previously one-click destructive actions: **Archive envelope, Archive account, Delete template**. State is carried by the **action-word text + dialog title, never colour** (the `danger` button variant is emphasis only); ESC/overlay = cancel, the mutation runs only on the explicit confirm; Cancel is first in DOM so it's the safe default focus. `e2e/a11y.spec.ts` scans the confirm dialog **open, LIGHT AND DARK**. New `FEAT-UX12` note (this note is the UX spec — §11 compressed). No ADR/spike (reuses a validated primitive, no dep, no data/API/domain/chart change). | Owner: which of the four threads to ship first (chose **destructive confirms**); whether reversible **Archive** gets a blocking confirm too (chose **all three, consistent**) | Both owner-decided up front. **No ADR/spike** — additive UI on a validated primitive, compressed ceremony (§11). Confirmed the two pre-existing carries still reproduce on clean `main` (recurring post-due; flaky forecast selector — the latter didn't trip this run). **Deferred, tracked:** skeletons (`BUD-T2`) · success toasts (`BUD-T3`) · inline validation (`BUD-T4`). **Next: finish the `BUD-S57` threads, then `BUD-S58`–`BUD-S60`.** **340 Vitest + 83 e2e.** Bundle **111.48 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12.md) |
| 2026-07-02 | **`BUD-S57` thread 2 of 4 Done (gate-green)** — the second Polish-track slice: **skeleton loaders**. The **16** bare `<p>Loading…</p>` / `<p role="status">Loading…</p>` strings across the lists (`EnvelopesList`, `AccountsList`, `ManageView`, `TemplatesView`, `NeedsAllocation`, `EnvelopeLedger`, `EnvelopeLedgerRoute`, `RecurringView`) and the **eight Insights views** now render the existing BUD-S48 **`Skeleton`** primitive (as the home `Cockpit` already does — default 3 rows, no per-site tuning). **Net a11y win:** the ~9 sites that used a bare `<p>` (no live region) announced **nothing** to AT; they now announce politely via the primitive's sr-only `role="status"` "Loading…". Reduced-motion-safe (the primitive has no entrance animation); light/dark from the token sheet. **No ADR/spike, no new dependency, no new code** — a pure presentation swap reusing a validated primitive. New [FEAT-UX12b](features/skeleton-loaders.md) note. New unit tests assert the accessible loading state at 3 representative sites (early-return list + Insights `role="status"`); skeleton a11y otherwise covered by the primitive's existing tests + the `Cockpit` axe scan (the state is transient → a live Playwright scan would be racy — §11). | Owner: which thread next (chose **skeletons** — lowest risk) | **No ADR/spike** — additive presentation on a BUD-S48 primitive, compressed ceremony (§11). Re-confirmed the two pre-existing carries still reproduce on clean `main` via `git stash` + isolated re-run (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Deferred, tracked:** success toasts (`BUD-T3` — needs a new Radix Toast primitive) · inline validation (`BUD-T4`). **Next: `BUD-T3`/`BUD-T4`, then `BUD-S58`–`BUD-S60`.** **343 Vitest + 83 e2e.** Bundle **111.44 KB gz** (net −0.04 KB; < 120 KB). [status report](status-reports/2026-07-02-ux12b.md) |
| 2026-07-02 | **`BUD-S57` thread 3 of 4 Done (gate-green)** — the third Polish-track slice: **inline validation**. Money-amount inputs that failed *silently* (the quick-add / register amount just greys out `AllocationEditor`'s Save when `magnitudeCents` drops to 0) or only at the **server** round-trip (transfer · move-money · add-account starting-balance) now surface a **field-level** message the instant an amount can't parse. New pure **`amountFieldError`** (`apps/web/src/validation.ts`, reusing the single penny-exact parser `tryParseMoney` so the inline check can't drift from the boundary one) drives a new **`FieldError`** primitive (`apps/web/src/ui/Feedback.tsx`) — a `<span>` the input points `aria-describedby` at (a **description, not a live region**) paired with `aria-invalid`; the message **text** carries the state (`--color-danger` ~6.5:1 light+dark, **never colour alone**). Wired at 4 call sites (transaction · transfer · move-money · add-account); the three submit-driven forms also **guard the mutation** client-side (no round-trip on a known-bad amount), shown on blur (touched) then live so it clears as the user corrects. Every existing `aria-label`/`<label>` association preserved (the suites select by label text). `e2e/a11y.spec.ts` scans the add-account form with an amount error **visible** LIGHT AND DARK. New [FEAT-UX12d](features/inline-validation.md) note. | Owner: which thread next (chose **inline validation** over success toasts — lower-risk, no new dep) | **No ADR/spike** — additive UI reusing an existing pure parser + design-system primitives; no dependency, no data/API/domain/chart change (§11). **Scope call:** narrowed to the one *silent* failure (un-parseable money); required-field inline messaging + a `Field`/`Input` form migration are tracked follow-ons; semantic rules (positive amount, funds, distinct envelopes) stay **server-authoritative**. Re-confirmed the two pre-existing carries on clean `main` (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Deferred, tracked:** success toasts (`BUD-T3` — needs a new Radix Toast primitive). **Next: `BUD-T3`, then `BUD-S58`–`BUD-S60`.** **349 Vitest + 86 e2e.** Bundle **111.68 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12d.md) |
| 2026-07-02 | **`BUD-S57` thread 4 of 4 Done — `BUD-S57` COMPLETE (gate-green)** — the final Polish-track slice: **success toasts**, and the only BUD-S57 thread needing a **new dependency**. Every successful mutation was **silent** (the global quick-add even navigates away, confirming nothing); a transient, auto-dismissing toast now confirms it. New **`ToastProvider`/`useToast()`** (`apps/web/src/ui/Toast.tsx`) on **`@radix-ui/react-toast`** — the last of the "hard interactive a11y widgets" ADR-0005 reserved for Radix — mounted at the **app root** (`App.tsx`, wrapping `RouterProvider` so a toast **survives** the quick-add's navigate-away). Wired to the successful-mutation set: create **account/envelope/transaction** (register + quick-add) · **transfer** · **move money** · **archive** account/envelope · **delete** template — each fires from the existing success path (**no data/API/domain/chart change**). A11y: Radix mirrors each toast into a **`role="status"` + `aria-live="polite"`** live region (`type="background"`, **never** `role="alert"`); entrance is **transform-only** (no text-opacity fade — the BUD-S49 route-fade trap); reduced-motion-safe (global reset); an explicit **Dismiss** button; state carried by **text**, not the decorative `--color-success` accent. `useToast()` **no-ops without a provider** (a toast is an auxiliary affordance — mirrors the needs-badge degrade pattern), so direct-render unit tests need no churn. Proven **axe-clean light AND dark with the toast visible** before any wiring. New [FEAT-UX12c](features/success-toasts.md) note; new `Toast.test.tsx` (announce contract + dismiss + no-op) + `e2e/toasts.spec.ts` + a11y scans (light+dark). | Owner: which thread last (**toasts** — the one needing a dep); ADR-or-not (chose **no ADR** — ADR-0005 covers Radix Toast) | **No ADR** — ADR-0005 already reserves Radix for toast; the a11y risk (auto-dismissing animated live region) was retired **before wiring** by an axe-clean-light+dark scan with the toast visible. **Bundle:** **116.31 KB gz** (+4.63 KB — the Radix Toast is the real add; headroom now ~3.7 KB, was ~8.3 — `BUD-S58`–`BUD-S60` must watch it). **Consequence handled in-change:** the new app-wide `role="status"` live region made 3 pre-existing **bare** `getByRole("status")` e2e selectors (reconcile ×2, recurring) ambiguous → scoped to their view's notice. Added a jsdom pointer-capture polyfill (Radix swipe) to the web test setup. Re-confirmed the two pre-existing carries on clean `main` (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Next: `BUD-S58` (money & budget-health visual encoding), then `BUD-S59`–`BUD-S60`.** **354 Vitest + 89 e2e.** Bundle **116.31 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12c.md) |
| 2026-07-02 | **`BUD-S58` Done (gate-green)** — the Phase-4 Polish slice **money & budget-health visual encoding**. Budget health was **text-only** (target/spent/remaining as bare numbers; over-budget/negative money signalled by a **minus sign alone** — the V1 encoding). New **`ProgressBar`** primitive (`apps/web/src/ui/ProgressBar.tsx` + `.module.css`) — the last BUD-S48 seed-and-grow primitive, **hand-rolled on the tokens + CSS with no charting dependency** (the SVG `Gauge`/`BarChart` from BUD-S53 stay for the Insights charts; utilization/payoff already have a `Gauge`, so the new bar surface is budget **target-vs-spent**). Wired at two budget-health surfaces: a per-row **Spent-of-target** column in `BudgetVsActualView`'s table + a **summary bar** in the cockpit `BudgetPanel`; and negative money (over-budget remaining · liabilities · negative net worth) now gets **weight + `--color-danger` tone** in the budget table + cockpit figures, with the **minus sign kept as the non-colour signal** (supersedes "minus sign only"). A11y: the bar is **decorative** (`aria-hidden`, no `progressbar` role — no new AT surface, the figure is always adjacent text); the `over` tone lays a diagonal **hatch SHAPE** over the danger fill (a non-colour "past 100%" signal, mirroring the `Gauge` over-cap); the track carries a `border-strong` outline and each fill clears **3:1** vs the track (1.4.11) light+dark; **no animation** (reduced-motion-safe by construction). New `ProgressBar.test.tsx` (decorative + clamp + over-shape + tone) + wiring assertions in `BudgetVsActualView.test.tsx`/`Cockpit.test.tsx`; a new **over-budget** `e2e/a11y.spec.ts` scan (light AND dark) with the danger encoding **visible** (the existing `scanBudget` already covers the under-budget state). New [FEAT-UX13](features/budget-health-encoding.md) note. | Scope: right-sized (§11) to budget-health + a minimal net-worth money treatment — no app-wide money sweep, no `Amount` primitive (a possible later consolidation) | **No ADR/spike/dep** — additive presentation on the BUD-S48 tokens; no data/API/domain/chart change (§11 fast-path). One **same-change** test consequence: the new Progress column added a second em-dash to an untargeted Budget row → updated that existing assertion. Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (91/91 e2e). **Next: `BUD-S59` (empty states & first-run onboarding), then `BUD-S60` (responsive pass).** **360 Vitest + 91 e2e.** Bundle **116.87 KB gz** (+0.56 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux13.md) |
| 2026-07-02 | **`BUD-S59` Done (gate-green)** — the Phase-4 Polish slice **empty states & first-run onboarding**. A brand-new user opening a **truly empty** app met the cockpit's **five disconnected per-panel empty states** ("No monthly targets yet", "Everything is allocated", "No forecast yet"…) — a fragmented, un-guided first impression. The home now **DERIVES** whether the app is completely empty (**no accounts AND no envelopes**, from `listAccounts`+`listEnvelopes` — **never a stored "has onboarded" flag**; derive-don't-store) and, when it is, renders one guided next step: a new **`FirstRunOnboarding`** component (`apps/web/src/FirstRunOnboarding.tsx` + `.module.css`) that **composes the existing BUD-S48 `EmptyState`** with a welcome + two CTA `<Link>`s ("Add an account" → `/accounts`, "Add envelopes" → `/envelopes`), tied to the PRD's week-one success metric. The moment **anything** exists, the cockpit takes over (its per-panel empty states then guide the remaining setup — progressive disclosure). New `FirstRunOnboarding.test.tsx` (region + two ordered steps + CTA hrefs) + updated `Home.test.tsx` (empty → onboarding; seeded → cockpit); `e2e/a11y.spec.ts` scans the onboarding surface **visible LIGHT AND DARK** — the empty-store scans are placed **first**, before any test seeds the run-scoped (never-reset) store. New [FEAT-UX14](features/first-run-onboarding.md) note. | ADR-or-not / spike-or-not (chose **neither** — additive presentation on a validated primitive); replace-vs-prepend the cockpit (chose **replace** on the truly-empty app — a guided screen, not a dashboard of empty panels) | **No ADR/spike/dep** — composes the BUD-S48 `EmptyState` + routing; no data/API/domain change (§11 fast-path). Derive-don't-store honoured (no new server "onboarded" state). One **same-change** test consequence: `Home.test.tsx`'s empty-app case previously expected the Overview region → now expects onboarding (updated + a seeded case added). Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (92/92 e2e). **Next: `BUD-S60` (responsive pass) — the last Phase-4 Polish item.** **364 Vitest + 92 e2e.** Bundle **117.20 KB gz** (+0.33 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux14.md) |
| 2026-07-02 | **`BUD-S60` Done — the LAST UX-Uplift item; the initiative is COMPLETE (gate-green)** — the Phase-4 Polish slice **responsive pass**: desktop-first layouts that degrade gracefully to phone width. Much was **already responsive** by design (cockpit grid + Insights figure lists are `auto-fit minmax(...)` → stack; shell nav + register rows `flex-wrap`; ADR-0007 chart SVGs `width:100%` → scale), so this is a small **CSS/token-only** pass. (1) **Reflow (WCAG 1.4.10):** wide data tables previously overflowed the page at ~320px → now scroll within their own **keyboard-focusable** region via a global **`.table-scroll`** utility (`ui/base.css`, beside `.sr-only`) applied once in **`ChartFigure`** (every Insights table flows through it as the chart's data-table fallback — one edit covers all nine) + the standalone envelope-ledger table; each wrapper is `tabindex="0"` + named `role="group"` (axe `scrollable-region-focusable`). `display:block` on `<table>` was rejected (strips AT table semantics). (2) **Shell header** (`AppShell.module.css`, ≤ 640px) stacks brand over the wrapping nav with comfortable tap targets; the **Insights sub-nav** (`Insights.module.css` `.subnav`, wired in `AnalysisSection`) — previously raw adjacent `<a>`s that touch when wrapped — becomes a wrapping tab row with a ≥ 24px tap target per tab. (3) **Page padding** tightens ≤ 640px. New `.table-scroll` unit assertions (`Chart.test.tsx`, `EnvelopeLedger.test.tsx`) + a new **"responsive reflow at phone width"** e2e describe that seeds the widest table at 320px and asserts **no horizontal page scroll** + the focusable region, axe-clean **LIGHT AND DARK**. New [FEAT-UX15](features/responsive-pass.md) note. | Scope: "nav collapses" — hamburger vs. graceful degradation (chose **graceful degradation / no hamburger** — a hamburger is a new interactive a11y surface, against the slice's low-risk + desktop-first-not-mobile-first appetite; flagged as an available follow-on) | **No ADR/spike/dep** — media queries + one global utility on the existing BUD-S49/BUD-S48/BUD-S53 modules (§11 fast-path). The narrow axe scan **caught** the Insights sub-nav's target-size gap at phone width (styled + fixed in-change) — reality before paper. Added the `DOM` lib to `tsconfig.e2e.json` (scoped to e2e) so the `page.evaluate` reflow probe type-checks. Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (94/94 e2e). **Next: the UX Uplift (`SPIKE-06`–`BUD-S60`) is complete — no remaining Uplift items; see the status report for post-Uplift candidates.** **366 Vitest + 94 e2e.** Bundle **117.27 KB gz** (+0.07 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux15.md) |
| 2026-07-02 | **`BUD-S28` Done (gate FULLY green)** — the injected clock. `buildServer` accepts `clock?: () => Date` (default = real clock), threaded to `accountService`/`recurringService`/`analysisService` + every route plugin (`RouteOptions.clock`) for the 5 route-level date defaults; `todayStr`/`currentMonthRange` now **require** the clock so new call sites can't regress. **Root cause of the ~6-week recurring carry finally diagnosed:** post-due always posted correctly — the *register endpoint's* server-derived current-month default window (BUD-S13) clipped the −21/−14/−7 relative-date fixtures out of view on any calendar day ≤ the 21st (the test could only pass on days 22–31). Fixed properly: `createTestApp({ today })` pins the clock; absolute fixture dates; `isoOffset` deleted. The flaky forecast-e2e `getByLabel("Account")` collision also fixed (`exact: true` — the chart's aria-label can contain the substring; BUD-S39/K16 convention). No API-contract/schema/domain/web-app change. | Owner: proceed with the next work; BUD-S28 was the only item urgent on the project's own rules (a failing test blocks completion) | **The gate is fully green for the first time since ~June 22: 367/367 Vitest + 94/94 e2e**, build 117.29 KB gz. Scope note: `archived_at`/`updated_at` event timestamps deliberately not threaded (instants, not calendar derivations). BUD-S29's timezone decision now has its seam ready (one conversion, one place). **Next: BUD-S29–BUD-S35 / BUD-S61/BUD-S62 — owner's call** (review suggests BUD-S32, the one-line HOST default). [status report](status-reports/2026-07-02-eh7.md) |
| 2026-07-02 | **`BUD-S61`/`BUD-S62` added to the plan** — SPIKE-08's two candidates promoted from the report's §8 into a new §4 **"Sheet-parity features"** section (`BUD-S61` pay-period planning · `BUD-S62` still-owed-this-month), both `Proposed` | Owner decision: add them to the roadmap | BUD-S61 = the last capability keeping the owner's spreadsheet alive; recommended **after `BUD-S28`** (injected clock → deterministic pay-date tests) and **derived-only first** (an assignment store is a §11 scale-up, decided at spec time). BUD-S62 is a §11-compressed warm-up. Relative order vs. `BUD-S28`–`BUD-S35` still the owner's call |
| 2026-07-02 | **SPIKE-08 captured** ([spikes/08-budgethome-sheet-analysis.md](spikes/08-budgethome-sheet-analysis.md)) — feature/formula/format analysis of the owner's working `BudgetHome.xlsx` planner (gitignored; amounts + creditors redacted in the report), mapped feature-by-feature to Budgeteer | Owner requested the sheet be analyzed and mapped to the existing system | Most of the sheet is already covered (recurring rules, biweekly income, cash-flow forecast, targets, budget health, debt payoff). **One genuine gap: BUD-S61 pay-period planning** (bills pre-assigned to specific paychecks ~a month ahead, with a committed-obligations headroom line) + a small **BUD-S62 "still owed this month"** derived read — both candidate items, **owner's call, not scheduled**. The 12-yr history work (`SPIKE-03`/`BUD-S79`, SPIKE-03) is unchanged — this is the *current* planner, not the history file |
| 2026-07-02 | **`BUD-S32` Done (gate-green)** — bind localhost by default. `HOST` added to the validated config (trimmed non-empty string, default `127.0.0.1`; blank fails loudly at startup) and `index.ts` listens on `config.HOST` instead of the hardcoded `"0.0.0.0"`. Docs in the same change: `.env.example` (commented `HOST` with the exposure warning), README config table, and SECURITY.md §3 gains the "reachable surface as small as the auth story" bullet — opting into `0.0.0.0` is the documented trigger to pull `BUD-E13` forward. §11-compressed (a fix: one validated config line + tests + docs; no spec/ADR). | Owner picked `BUD-S32` as the next item (the review's suggested order) | The LAN-writable exposure is closed by default; multi-device use is now a deliberate, documented opt-in. No API-contract/schema/domain/web change; 3 new config boundary tests (370/370 Vitest). **Next: BUD-S29–BUD-S35 remainder / BUD-S61/BUD-S62 — owner's call** (review suggests BUD-S29, the timezone policy, whose BUD-S28 seam is ready). [status report](status-reports/2026-07-02-eh11.md) |
| 2026-07-02 | **Architecture review captured** ([reviews/2026-07-02-architecture-review.md](reviews/2026-07-02-architecture-review.md)); added **Engineering-health items `BUD-S28`–`BUD-S35`** to §4 (all `Proposed` — sequencing is the owner's call) | Owner requested a repo-wide architect's review at UX-Uplift completion | No live bugs; themes = clock-as-I/O (BUD-S28 explains the carried red recurring test) · ops readiness (restore `BUD-S31`, versioned migrations `BUD-S30`, localhost binding `BUD-S32`) · convention-only boundaries (`BUD-S33`/`BUD-S34`). Suggested order in the review: **BUD-S28 → BUD-S32 → BUD-S29 → BUD-S31 → BUD-S30/BUD-S35 → BUD-S33/BUD-S34**; BUD-S28 is the only item urgent on the project's own rules (a failing test blocks completion) |
| 2026-07-03 | **`BUD-S29` Done (gate-green)** — the timezone policy, decided and enforced: **calendar dates are user-local** (`04_DOMAIN_MODEL` §6). The client derives "today"/"this month" from the local clock (new `apps/web/src/dates.ts`; the 8 UTC-deriving components + the fakeApi switched off `toISOString()`); the server **requires** every user-facing date/month at the boundary (`occurredOn`, `reconciledOn`, new `openedOn`, register `from`/`to`, budget-vs-actual `month`, and `today` on recurring list/create/post-due + the forecast — all `400` when missing) and derives none (`currentMonthRange` deleted; `todayStr` survives only for the backup filename; the recurring/account/analysis services no longer take a clock). `06_API_CONTRACT` §1 + every touched endpoint updated in the same change. §11-compressed: a correctness fix on an existing surface — policy bullet + contract doc, no spec/ADR (not contested). | Owner picked `BUD-S29` as the next item (the review's suggested order) | The evening-west-of-UTC bug class is closed: quick-add defaults, the register window, "this month" reads, burndown pace, recurring due-ness, and the forecast's day zero all follow the user's calendar. **Breaking API change** (documented; single bundled client). +6 Vitest (376/376). **Next: BUD-S31 / BUD-S30+BUD-S35 / BUD-S33/BUD-S34 / BUD-S61/BUD-S62 — owner's call** (review suggests BUD-S31, the restore proof — check BUD-S30's migrator-first constraint when scoping). [status report](status-reports/2026-07-03-eh8.md) |
| 2026-07-03 | **`BUD-S30` + `BUD-S35` Done (gate-green)** — the versioned migrator and the structural post-due idempotency, bundled as planned. `migrate.ts` is now a Kysely `Migrator` wrapper over an **in-code registry** (`db/migrations/`): `0001-baseline` = the old idempotent migration function **frozen verbatim** (its idempotence is the adoption path — re-running it on a pre-migrator store is a no-op that records the baseline as executed) · `0002-recurring-occurrence-idempotency` = BUD-S35's partial unique index `(recurring_id, occurred_on) where recurring_id is not null`. Migrations are **forward-only and frozen once committed**; a failing batch rolls back atomically (transactional DDL) and startup fails loudly. The default-household seed stays a startup ensure in `migrateToLatest` (not a run-once migration) so `db:reset`'s Postgres truncate path keeps healing. `postDue` treats the index violation as **already-posted** (0 posted, no error — the concurrent winner advanced the cursor). §11 scale-up honored: data-shape change → `05_DATA_MODEL` §2/§4 rewritten in the same change. | Owner picked `BUD-S30`+`BUD-S35` (the migrator-before-`BUD-S77` constraint pulls BUD-S30 ahead of the review's suggested BUD-S31; BUD-S35 rides as its first migration) | **Unblocks BUD-S31/`BUD-S77`** (a restore now has a schema version to validate against). +7 Vitest (383/383): 5 migrator (fresh-store order · re-run no-op · pre-migrator adoption · truncate-heal contract · fail-loud-naming-the-migration on pre-existing duplicates) + 2 recurring (stale-cursor re-post → already-posted, ledger undoubled · manual txns unconstrained by the partial index). No API-contract/domain/web change. **Next: BUD-S31 / BUD-S33/BUD-S34 / BUD-S61/BUD-S62 — owner's call** (review suggests BUD-S31, now unblocked). [status report](status-reports/2026-07-03-eh9-eh14.md) |
| 2026-07-03 | **`BUD-S31` Done (gate-green)** — restore is proven; the export is finally a backup, not a hope. [SPIKE-09](spikes/09-restore-roundtrip.md) (§11 scale-up honored: spike report before building) answered the three open questions: **FK order** — the file's key order is FK-unsafe (`transactions` precedes `recurring_transactions`); an explicit topological order lives in `restoreService`. **ID collisions** — exactly one by construction: the seeded default-household row every migrated store carries; resolved by upsert so the exported values win, all other UUIDs preserved. **Versioning** — the export now stamps `schema: { migrations }` from `kysely_migration` (additive; `version` stays 1); restore refuses a file from a newer schema naming the missing migration, and pre-stamping files restore with a warning (refusing them would orphan the owner's real accumulated backups). The slice: `restoreService` (zod-validated envelope; PG constraints validate rows; one transaction — partial failure restores nothing; **non-destructive** — refuses any store with user data, flow is `db:reset` → restore) + `npm run db:restore -- <file>` CLI (counts only, never contents). Deliberately CLI-only: an unauthenticated HTTP restore would be a remote wipe-and-replace primitive (`BUD-E13`). Docs in the same change: `06_API_CONTRACT` (schema stamp + restore section), `07_NFR` ("Restore proven" ✅, FK-order question closed), README, K22. | Owner picked `BUD-S31` (the review's suggested order, unblocked by BUD-S30) | `07_NFR`'s recovery-path claim is now tested, not asserted: the **`export → restore → export` equivalence gate test** (store populated via the real HTTP API exercising all 15 tables, restored into a fresh store, re-exported deep-equal) locks the round-trip into every future gate run. +6 Vitest (389/389): equivalence · occupied-store refusal · newer-schema refusal · legacy-file warning path · foreign-household refusal · malformed-file fail-loud (plus schema-stamp assertions inside the existing backup contract test). **Verified live:** the owner's real 1,019-row store exported → CLI-restored into a scratch `PGLITE_DIR` → re-exported byte-identical. No web change (the export shape change is additive; bundle untouched). **Next: BUD-S33/BUD-S34 / BUD-S61/BUD-S62 — owner's call** (BUD-S61 is the last spreadsheet-parity gap; BUD-S33/BUD-S34 are small P3s). [status report](status-reports/2026-07-03-eh10.md) |
| 2026-07-03 | **`BUD-S33` + `BUD-S34` Done (gate-green) — the 2026-07-02 architecture review is CLOSED OUT** (BUD-S28–BUD-S35 all ✅). **BUD-S33:** new types-only `apps/api/src/contract.ts` exposed as **`@budgeteer/api/contract`** (one `exports` line — the same raw-TS-source sharing `@budgeteer/domain` has always used; the api.ts header's "once the domain package ships a build" premise was never needed) re-exports the server's own view types + the domain-defined report types; web `api.ts` swaps its ~25 hand-copied interfaces for `import type` + `export type *` re-exports (components untouched; web's `NetWorthReport` was api's `NetWorthRollup` — renamed at its one use). Client-side **input** shapes (dollar-string amounts, adapter-filled dates) deliberately stay web-local: the wire inputs are the routes' zod schemas. **Stance decided: the client trusts the typed contract on reads** — recorded in `06_API_CONTRACT` §4 (single first-party client on a loopback-bound API; drift is now a compile error; a client-side schema mirror = BUD-S22's two-sources-of-truth one layer up + bundle spend re-checking what the server guarantees; revisit at `/v1`). **BUD-S34:** per-zone `no-restricted-imports` in `eslint.config.js` — domain bans fastify/kysely/pg/react/`node:*`; web bans kysely/pg/fastify **and any runtime import of `@budgeteer/api`** (`allowTypeImports` — the rule that keeps BUD-S33 honest); `http/routes/**` bans `**/db/**`. §11-compressed (refactor + tooling, no behavior change; no spec/ADR). | Owner picked `BUD-S33`+`BUD-S34` bundled (close the engineering-health track before starting BUD-S61's spec) | **Zero type drift found** when the hand-copies were swapped for the real definitions, and **zero pre-existing boundary violations** — the conventions had held, but only by manual discipline; erosion now fails the gate. Every ban verified to fire (probe files, then deleted). Bundle **byte-identical** (117.32 KB gz — `import type` is erased). Tests unchanged (389 Vitest + 94 e2e; the lint IS the enforcement). Docs: `06_API_CONTRACT` §4 · `ARCHITECTURE` §2 · K23. One first-run Vitest flake (389→1 failed, 3 clean re-runs — consistent with the `perf.test.ts` watch item). **Next: BUD-S61 (spec-first) / BUD-S62 — owner's call; engineering health is empty.** [status report](status-reports/2026-07-03-eh12-eh13.md) |
| 2026-07-03 | **`BUD-S62` Done (gate-green) + `BUD-S61` specs written — the sheet-parity session.** **BUD-S62:** pure **`stillOwedCents`** in `@budgeteer/domain` (Σ over **withdrawal** rules of amount × unposted occurrences through local month-end, from each rule's `nextOccurrenceOn` cursor — **past-due unposted count**, posting is what clears the figure; deposits contribute 0) + a labelled `<dl>` **"Still owed this month"** figure on the cockpit's Upcoming panel, **client-derived** per the cockpit's own BUD-S21/BUD-S42 compose-existing-reads precedent (month-end via `dates.ts`, BUD-S29) — **no API/schema/contract change**. §11-compressed as planned ([FEAT-S9](features/still-owed.md) note). Verified live against the real store (the figure reconciled to the one unposted July occurrence). **BUD-S61 (spec-first, no code):** [FEAT-S7](features/pay-periods.md) + [UX spec](ux/pay-periods.md), both **Proposed** — derived-only V1 (no schema change; the bill↔paycheck assignment store stays a recorded scale-up with an explicit trigger); assignment policy = **latest expected paycheck ≥ `leadDays` (default 7) before the bill's due date**, bucket zero = from current balance; planned spending = SPIKE-05-netted monthly residual split across that month's checks (three-check months handled by construction, the sheet's ×2 bug closed); **S8 headroom folds in at commitment time** (payday, not due date); colour-only blue/red join replaced by structure + text (ADR-0007/BUD-S58). | Owner picked **BUD-S62 warm-up then BUD-S61 spec** (BUD-S62 first validates the unposted-occurrence derivation BUD-S61's plan math leans on) | BUD-S62's known sheet-divergence: none (the D-column semantics carried over exactly). BUD-S61's known sheet-divergence, **flagged in the spec**: the sheet parks a whole 7th–30th cluster on one check; the derived policy spreads bills across preceding checks — **FEAT-S7 §9 names the ~1 h validation against the live rules that decides** (acceptable → build; structural mismatch → tune `leadDays` or pull the §8 store forward). **+4 Vitest (393), 94 e2e** (the cockpit journey gains the BUD-S62 figure assertion). **Next: FEAT-S7 §9 validation, then the BUD-S61 build slice — owner's call.** [status report](status-reports/2026-07-03-s9-s7-spec.md) |
| 2026-07-03 | **`BUD-S61` Done (gate-green) — the sheet's last parity gap is closed.** **§9 validation first ([SPIKE-10](spikes/10-payperiod-policy-validation.md), ~1 h honored):** the dev store turned out to hold **no real bill cadence** (one demo paycheck + `E2E ` fixtures), so the validation ran against `BudgetHome.xlsx` itself (read-only, SPIKE-08 stance) — and **invalidated the specced date-only policy**: bills cluster at the month boundary, so "latest check ≥ 7 days before due" parks ~149% of a check on one payday while its neighbor idles at ~47%; no `leadDays` constant fixes it (7/14/21 swept) and reservation-style earliest-fit is horizon-unstable. **§5 revised to balanced latest-fit** (capacity-aware: largest bills first into the latest feasible check with room; overflow surfaced as `overCommitted`; unfundable → bucket zero) — on the real data every bucket lands at 95–100% of its check, exactly the owner's hand balance, min 7-day float, mean ~19 days. **Then the derived-only V1 slice:** pure `payPeriodPlan` (`packages/domain/src/payperiod.ts`, reusing `dueOccurrences` via `scheduledEvents` + the SPIKE-05 netting via `expectedSpendEvents`) → `analysisService.payPeriodPlan` over the same gather as the forecast (extracted `gatherProjectionInputs`, shared) → **`GET /analysis/pay-periods?accountId&today`** (`today` required, BUD-S29; horizon fixed 90 d; typed via `contract.ts`) → Insights **Pay periods** tab (`/insights/pay-periods`): bucket sections with structural join, coverage sentence, `.table-scroll` bill tables, planned-spending row, `<dl>` figures, text badges (Covered · Plan breaks here · Short · Over-committed). No schema change. | Kickoff plan anticipated the mismatch fork; the third option (capacity-aware policy) was validated against the sheet before building — spec revised in place (`Proposed`, §4 ladder permits one slice) and promoted with evidence | **Residual divergence for owner ratification** (FEAT-S7 §5 / SPIKE-10 §3.6): the sheet pre-reserves whole clusters (~22–27 d float on the big 1st-of-month cluster); the policy guarantees ≥ 7 d (8–13 d there) and mirror-balances membership. Pinning a bill to a check = §8's store (trigger sharpened). **+22 Vitest (415) · +5 e2e (99)** (journey + light/dark a11y + light/dark 320 px reflow) · build **118.67 KB gz** (+0.97; headroom to 120 KB now **~1.3 KB** — next UI-bearing slice likely forces the budget conversation). Verified live against the dev store (honest output: the leaked fixture targets over-commit the demo paycheck — see K24 cleanup). **Next: owner ratifies the §5 divergence + decides the K24 fixture cleanup — then SPIKE-03/BUD-S79 (history import) or hardening, owner's call.** [status report](status-reports/2026-07-03-s7-slice.md) |
| 2026-07-04 | **Roadmap refocused: the header “Current focus” consolidated** from a ~170-line running changelog (BUD-S49–BUD-S60 narrated in full) to a concise current-state block: all tracks Done · gate 415 Vitest + 99 e2e · the two open owner decisions (FEAT-S7 §5 ratification · K24 fixture cleanup) · next fronts (`SPIKE-03`/`BUD-S79` or deferred). No information lost — every removed paragraph exists in its item row (§4), this log, or its status report. Also fixed the stale `BUD-S49` table status (was `Ready`, shipped 2026-06-27), trimmed the Sources row to the primary docs, and refreshed the watch-items bundle headroom (~1.3 KB after BUD-S61) | The block had become a second copy of §4/§5 and buried the entry point it was meant to be | The roadmap header is again a fast “where are we / what's next” orientation; per-item history stays in §4/§5/status reports |
| 2026-07-06 | **Security review captured** ([reviews/2026-07-06-security-review.md](reviews/2026-07-06-security-review.md)); added **Security-review items `BUD-S36`–`BUD-S38`** to §4 (`BUD-S36` Proposed · `BUD-S37` Info · `BUD-S38` folds into `BUD-E13`) | Owner requested a whole-repo security review against the exploited-in-practice vuln classes | **No live bugs:** clean on every injection class (SQL/command/XSS/path-traversal), no secret in the tree, no unsafe deserialization, no log leakage, every query household-scoped. Findings are posture/supply-chain only. Net-new action = **BUD-S36** (retire vulnerable-but-unreached `kysely@0.27.6`, a *direct runtime* dep the gate comment wrongly calls "not used at runtime"; not exploitable here — Postgres dialect · enum-fed `sql.lit` · no JSON-path/`Kysely<any>`). `BUD-S38` reaffirms the accepted no-auth stance + sharpens `/export`'s one-request-total-exfil blast radius; `HOST=0.0.0.0` stays the documented trigger to pull `BUD-E13` forward. No code changed — review + tracking only |
| 2026-07-06 | **UX Redesign initiative opened; `BUD-S63` added** ([reviews/2026-07-06-ux-redesign-initiative.md](reviews/2026-07-06-ux-redesign-initiative.md)) — owner design session working from a reference dashboard screenshot: align the app's layout with its chrome (grouped left sidebar + top bar + content canvas; structure borrowed, not its SaaS content). First item captured: **`BUD-S63` sidebar app shell** — [UX spec](ux/app-shell-sidebar.md) `Draft`, with the open design calls (nav grouping · Add-transaction placement · `<h1>` ownership · icon approach · split option) flagged in its §11 for owner reaction. **Capture in progress:** the owner's per-page ideas land as `BUD-S64`+ in the same session | Owner design session 2026-07-06 (reference screenshot; save to `docs/ux/assets/`) | New **"UX Redesign"** track added after the sheet-parity section. Tensions named up front (brief §3): the **~1.3 KB bundle headroom** — a first icon set + collapse/drawer JS makes this the slice the BUD-S61 entry predicted would force the 120 KB budget conversation (owner decision at `Ready`: re-baseline vs. trim) · the top-bar page title vs. the one-`<h1>`-per-page axe invariant (touches every view + suites if the shell takes the title) · the narrow-screen drawer **deliberately supersedes BUD-S60's "no hamburger" scope call** (its recorded follow-on). Items `Planned`, not `Ready` — docs only, no code |
| 2026-07-06 | **Bundle budget re-baselined 120 → 140 KB gz** (`07_NFR` §1, footnote ³ holds the record) — the BUD-S61 entry's predicted "budget conversation," held during the UX-Redesign capture session instead of waiting for `BUD-S63`'s `Ready` gate. Owner first floated 200; rejected together as governance-neutering (~80 KB of slack lets whole dependency classes land without another conversation) — **140 funds the entire redesign initiative** (first icon set + sidebar chrome + per-page slices, est. +7–14 KB) while keeping dependency-class additions (icon lib · another Radix suite · any chart lib, ~129 KB) a deliberate decision. Discipline carries: per-slice costs stay logged in `07_NFR` ³ · revisit-if-past-140 · route-level code-splitting is the recorded lever for the *next* conversation. Re-measured 2026-07-06 via fresh `vite build`: **118.67 KB gz** (+3.85 KB CSS) — the NFR's stale BUD-S60-era measured cell (117.27) updated in the same change | Owner decision (2026-07-06 design session): "re-baseline… push the limit up to 140" | Brief §3.1's BUD-S63 tension **resolved**; BUD-S63's remaining `Ready` gates are `<h1>` ownership + the e2e re-point. Docs only — no code change |
| 2026-07-06 | **BUD-S63 `<h1>` ownership resolved: the shell owns the page `<h1>`** ([UX spec §11 Q3 → §8](ux/app-shell-sidebar.md)) — the top-bar title *is* the page's single `<h1>`. Mechanism recorded with the decision: static routes title via route `handle`; **dynamic routes (account register · envelope ledger) publish their resolved name through a shell title context** with a kind-label fallback until their data arrives (the views fetch — no route loaders — so the router can't know the name); `document.title` syncs ("\<Page\> — Budgeteer"); the quick-add modal never retitles the page (its `Dialog` names itself). Views drop their `<h1>`s; content headings start at `<h2>` (Insights keeps "Insights" as `<h1>`, the active tab becomes `<h2>`) | Owner decision (2026-07-06 design session): covering the session's called-out items in order — the `<h1>` call recorded as the spec's lean, option (a) | Blast radius accepted into BUD-S63's scope: every view + its heading assertions (unit + e2e) re-pointed **once, in the slice**; axe light+dark green throughout. Brief §3.2 **resolved** — BUD-S63's remaining `Ready` work is the e2e nav re-point (mechanical) + the smaller §11 Qs (grouping · Add-transaction placement · icon authoring · split option). Docs only — no code change |
| 2026-07-06 | **BUD-S63 nav grouping resolved (§11 Q1)** — the sidebar's cut is **Budget** (Home · Insights) · **Ledgers** (Accounts · Envelopes · Needs allocation w/ badge) · **Planning** (Templates · Recurring · Pay periods) · **Administration** (Manage · Download backup). Owner amended the proposal three ways: **Money renamed Ledgers** · the **needs-allocation queue filed with the ledgers** (bookkeeping, not the daily-loop group) · **Download backup moved from the sidebar footer into Administration** — leaving **no sidebar footer in V1** (the reference's changelog/help footer has no analog; the slot stays available) | Owner decision (2026-07-06 design session): "rename Money to Ledgers… move Needs Allocation to Ledgers… moving Download Backup to Admin" | [UX spec](ux/app-shell-sidebar.md) §2/§5/§7 re-cut to the resolved groups; BUD-S63's open §11 Qs shrink to **Q2** (Add-transaction placement) · **Q4** (icon authoring) · **Q5** (split option). Docs only — no code change |
| 2026-07-06 | **BUD-S63 + BUD-S64: every remaining design question resolved; both UX specs promoted `Draft` → `Proposed`.** **BUD-S63 Q2 (additive):** the always-visible **Add transaction** moves to the **sidebar footer** (the slot Q1 emptied; full button expanded/in-drawer, icon-only in the rail) + a compact top-bar **+ Add** at ≤ 640px (the sidebar is off-canvas there — preserves BUD-S52's from-anywhere acceptance bar) + a page-local button atop the Accounts page (rides the Accounts-page item, `BUD-S65`+ when captured). **Q4:** icons via the middle path — **~10 SVG paths copied from lucide (ISC) into repo-owned components**: professional drawings, zero dependency, attribution kept. **Q5:** **BUD-S63 ships whole** (owner accepts the larger slice; no UXR1a/1b split). **BUD-S64 Q1:** Balance = projected payday balance (forecast math); **Reserve = running Σ of per-check headroom** — owner confirmed the sheet's *Funds* meant accumulating leftovers. **Q2:** figures are **API-provided** (additive `GET /analysis/pay-periods` fields; `06_API_CONTRACT` in the same change). **Q3:** left-to-pay in **both scopes** — month-scoped column with month subtotal rows (the sheet's Total semantics) + a 90-day pane figure. **Q4:** the cockpit **Next-paycheck** line ships in BUD-S64 | Owner answers (2026-07-06 design session) to the batched open questions | BUD-S63's `Ready` work is now purely FEAT-UXR1 + the e2e re-point; BUD-S64 waits on BUD-S63. Also this session: `docs/ux/assets/` created (README + the sheet screenshot **gitignored by name** — real creditors/amounts; verified the rule fires) and the empty `examples/dashboard-rework.ts` removed. Docs only — no code change |
| 2026-07-07 | **Per-page batch captured: `BUD-S65`–`BUD-S70` added** ([brief §4](reviews/2026-07-06-ux-redesign-initiative.md) holds the depth) — the owner dictated the page changes; itemized along two shared patterns + two standalones: **BUD-S65 Ledgers tables** (Accounts/Envelopes/Needs-allocation lists → tables, one treatment; + the queued Accounts-page Add button) · **BUD-S66 Templates** (list → table + the editor re-organized — **establishes the form-layout pattern**) · **BUD-S67 Recurring** (forms onto the pattern + a readable rules list) · **BUD-S68 Insights IA** (sub-nav → top tab bar; lean = five category tabs w/ segmented sub-views, URLs preserved) · **BUD-S69 Manage** (Move-money form onto the pattern; §11-compressed) · **BUD-S70 demo-grade synthetic seed** (the "insights masked behind limited data" callout is a tooling gap — rich synthetic dataset; the durable fix stays `SPIKE-03`/`BUD-S79`). **Numbering = capture order; recommended build order** (brief §5): BUD-S70 anytime → BUD-S63 → BUD-S64 → BUD-S65 ∥ BUD-S66→BUD-S67/BUD-S69 ∥ BUD-S68 | Owner dictation (2026-07-07 design session): per-page changes, "split these up how you see fit… itemize in sensible slices" | Six items `Planned`; per-item UX specs at `Ready` (the specs-as-gating-work pattern — BUD-S63/2 got theirs early only because they were designed in-session). Capture-complete **pending owner confirmation**. Docs only — no code change |
| 2026-07-07 | **UX-Redesign capture CLOSED — `BUD-S63`–`BUD-S70` is the full initiative; Insights IA direction endorsed.** Owner confirmed capture complete and endorsed the BUD-S68 lean: **five category tabs** (Spending · Budget · Cash flow · Debt · Net worth) with segmented sub-views, every `/insights/:view` URL preserved; the flat-tab alternative passed over. Brief promoted → **`Proposed`** (capture complete, owner-aligned) | Owner: "I like your suggestion for Insights. Beyond that capture is complete" | The initiative is fully scoped: **8 items** (2 with `Proposed` UX specs, every design question resolved), recommended build order in brief §5. **Next actionable: FEAT-UXR1** (shell → `Ready`) and/or **BUD-S70** (no gate). Docs only — no code change |
| 2026-07-07 | **Initiative documentation COMPLETE — all eight UXR items specced (`Proposed`), zero code.** Feature specs: [FEAT-UXR1](features/app-shell-sidebar.md) (shell mechanism — route-handle titles + title context · localStorage rail · `Dialog`-machinery drawer · lucide-path icon set · blast-radius table) · [FEAT-UXR2](features/pay-periods-planner.md) (additive `projectedBalanceCents`/`reserveCents` on the pay-periods read from the shared gather; both countdown scopes client-derived; cockpit line in-slice; reserve runs down through over-committed buckets, no clamp). UX specs: [BUD-S65 Ledgers tables](ux/ledgers-tables.md) · [BUD-S66 Templates](ux/templates-page.md) (**its §3 defines the form-layout pattern** BUD-S67/BUD-S69 reuse) · [BUD-S67 Recurring](ux/recurring-page.md) (split → per-row disclosure; **Payee surfaced as a column** — captured today but never rendered; **one flagged behavior addition: rule delete gains the BUD-S57 `ConfirmDialog` — owner veto point**) · [BUD-S68 Insights IA](ux/insights-ia.md) (five category tabs; label renames Spend → **By envelope**, Budget → **vs Actual**; URLs preserved). FEAT notes: [BUD-S69 Manage form](features/manage-move-money.md) · [BUD-S70 demo seed](features/demo-seed.md) (**additive `seed:demo`, baseline seed untouched** — e2e/K24 depend on its determinism; synthetic-only, the sheet's real creditor names explicitly banned) | Owner: "write the remaining documentation… all BUD-S63-8 slices" — docs only, no code | Specs verified against the real components before writing (`TemplatesView` · `RecurringView` · `MoveMoneyForm` · `AnalysisSection`). Every item now needs only an owner nod to flip `Ready`. Review points for the owner: the BUD-S67 delete-confirm addition · the BUD-S68 label renames · the BUD-S67 Payee column. Docs only — no code change |
| 2026-07-07 | **The three flagged UXR calls ratified** (owner, no veto): the BUD-S67 rule-delete `ConfirmDialog` · the BUD-S67 Payee column · the BUD-S68 label renames (Spend → By envelope · Budget → vs Actual) — ratification stamps added in the specs + item rows | Owner: "I am not going to veto any of those three judgement calls. I like them" | Nothing pending on `BUD-S63`–`BUD-S70` beyond the per-item `Ready` nods. Docs only — no code change |
| 2026-07-07 | **BUD-S70 built → `Done` (first UX-Redesign code).** New standalone **`npm run seed:demo`** ([`seedDemo.ts`](../apps/api/src/db/seedDemo.ts)): a deterministic, strictly-synthetic ~6-month dataset (4 accounts · 22 envelopes · 216 txns · 8 recurring incl. a biweekly paycheck + a 1/2/7/15/29 bill cluster · 3 templates · targets · credit/loan surfaces). Resolved the FEAT note's internal tension in the owner's favour — **standalone into a fresh store** (refuses an occupied store, BUD-S31), *not* layered on the baseline `seed` (which is byte-identical, no diff). Verified: two runs identical; occupied-store refusal; a 7-bucket pay-period plan against the demo store. | Owner kickoff: "build BUD-S70 first (additive `seed:demo`, baseline seed untouched, strictly synthetic)"; standalone-vs-layered resolved via owner Q | First code of the redesign; baseline `seed`/e2e untouched so the gate is unaffected. [Status report](status-reports/2026-07-07-uxr8-demo-seed.md) |
| 2026-07-07 | **BUD-S63 built → `Done` — the sidebar app shell (first user-facing redesign slice).** The BUD-S49 top-banner chrome replaced by the reference layout: grouped left sidebar (**Budget · Ledgers · Planning · Administration**, footer = the global **Add transaction**) · top bar (collapse toggle · the page's single `<h1>` · compact **+ Add** at ≤ 640px) · route as canvas. Desktop **collapse-to-rail** persisted (`localStorage budgeteer.sidebar`); ≤ 640px off-canvas **drawer on the Radix `Dialog`** (focus trap / Esc / scrim / restore — **no new dependency**, reuses BUD-S52's). **Shell owns the single `<h1>`** (Q3): static route-`handle` titles + a `PageTitleContext` for the dynamic account/envelope names, `document.title` synced — **every routed view dropped its `<h1>`** (Insights → `<h2>`; `ErrorBoundary` keeps its top-level `<h1>`). Icons (Q4): **15 lucide (ISC) paths copied into repo-owned `ui/icons.tsx`**, zero dependency. Blast radius realised once: unit + e2e heading assertions re-pointed (`"Budgeteer"`→`"Home"`; Insights level 1→2), `e2e/setup.ts` nav helpers re-pointed (Insights sub-nav scoping), new `e2e/app-shell.spec.ts`, `a11y.spec.ts` rail + drawer scans (light AND dark). **Bundle +3.84 KB gz → 122.51** (CSS +0.70 → 4.55). | Owner-nodded BUD-S63 → `Ready`; every §11 design Q resolved 2026-07-06 | **NO route/data/API/domain change** (ADR-0006 unchanged); FEAT-UX3 chrome **superseded**. Gate green: **421 Vitest + 109 e2e**. [Status report](status-reports/2026-07-07-uxr1-sidebar-shell.md) |
| 2026-07-07 | **BUD-S64 built → `Done` — the pay-period planner (first UXR item to touch data/API).** The BUD-S61 Insights tab promoted to a first-class **`/pay-periods`** route (sidebar Planning item retargets + lights `aria-current`; `/insights/pay-periods` → `<Navigate replace>`; the Insights sub-nav drops the tab — retiring the BUD-S63 dual-highlight) and re-laid as **two side-by-side ledgers** (Bills: bill · due · amount · month-scoped left-to-pay + subtotal rows + a 90-day pane figure · Covered-by ‖ Paychecks: payday · income · committed · per-check headroom · projected balance · reserve · status), stacked ≤ 640px (Paychecks first). Selection is `aria-pressed` payday toggles highlighting covered bills — **additive** over the permanent "Covered by" text join, announced politely. **Additive API fields** (BUD-S33, no version bump) on `GET /analysis/pay-periods`: `projectedBalanceCents` (forecast cash-flow math as-of each payday — reconciles with the forecast endpoint, asserted) + `reserveCents` (running Σ of per-check headroom, **≡ `headroomAfterCents` by construction** — owner chose to surface it anyway); both countdowns derive client-side. Cockpit Upcoming panel gains a **Next paycheck** deep-link line. **Bundle +1.29 KB gz → 123.80** (CSS +0.30 → 4.85). | Owner-nodded BUD-S64 → build; every §11 design Q resolved 2026-07-06; the reserve-redundancy call raised + owner chose "add the field" | **Assignment POLICY untouched** (FEAT-S7 §5 ratify/veto stays open); no schema change. Gate green: **426 Vitest + 110 e2e**. [Status report](status-reports/2026-07-07-uxr2-pay-period-planner.md) |
| 2026-07-07 | **BUD-S65 built → `Done` — the Ledgers tables (presentation-only slice).** The three Ledgers-group lists — **Accounts** (Name · Kind · Balance · Actions) · **Envelopes** (+ BUD-S42 Target · Spent · Remaining, **"—" when untargeted**) · **Needs allocation** (Date · Payee/memo · Account · Amount, with the list's "needs $X" kept as a muted sub-line + the Allocate editor in-row) — re-laid as real design-system tables over **one shared [`Ledgers.module.css`](../apps/web/src/Ledgers.module.css)** (kept separate from BUD-S64's `Insights.module.css`; both token-derived). `<th scope="col">` headers · name cell = `<th scope="row">` `<Link>` (Accounts/Envelopes) · money right-aligned/tabular/signed · archived rows a separate named table · each table in the global `.table-scroll` focusable region (320px reflow verified). **Needs gains a Date column** (`occurredOn`, already in the read — no API change). **Accounts page-local Add transaction** `<Link>` → the BUD-S52 `/transactions/new` modal (BUD-S63 §11 Q2's additive half; `default` variant so Add account stays the accent primary). Blast radius realised once: the 3 unit specs + e2e (`setup.ts` helpers · accounts/envelopes/needs/routing/transactions/transfers/quick-add) re-pointed `role="list"`→`role="table"` (+ `getByRole("row")`; **`exact:true`** to keep "Accounts"/"Envelopes" from matching "Archived …"). **Bundle +0.29 KB gz → 124.09** (CSS +0.08 → 4.93). | Owner-nodded BUD-S65 → build; spec of record `docs/ux/ledgers-tables.md` | **NO data/API/domain change** (a table treatment over the existing BUD-S51/BUD-S42 reads; every flow carries). Gate green: **427 Vitest + 110 e2e**. [Status report](status-reports/2026-07-07-uxr3-ledgers-tables.md) |
| 2026-07-07 | **BUD-S66 built → `Done` — the Templates page (presentation-only; establishes the form-layout pattern).** The saved-templates list → a real table (**Name** `<th scope="row">` · **Lines** · **Total** · **Actions**) reusing the **BUD-S65 [`Ledgers.module.css`](../apps/web/src/Ledgers.module.css) treatment verbatim** — no third duplicate; per-row action names ("Rename {name}" / "Delete {name}"), inline rename swaps the name cell, the BUD-S57 delete `ConfirmDialog` carried. The template editor re-organized onto the **new form-layout pattern in [`FormLayout.module.css`](../apps/web/src/FormLayout.module.css)** (the genuinely new, reusable part — BUD-S67/BUD-S69 restyle by importing it, no new pattern code): `<fieldset>`+visible `<legend>`, the `Field`/`Input`/`Select` primitives (stacked label→control), the envelope/amount **line mini-grid** (shared grid template on header + rows so columns align; amount right-aligned/monospaced; per-row `aria-label`s kept — "Template envelope N" · "Template amount N" · "Remove line N"; "+ Add line" beneath), right-aligned **action row** (Save template the accent primary), form capped ~44rem. **Behavior byte-for-byte** (blank/zero lines filtered on save · form resets after create · no new field validation — form-level failure stays `role="alert"`). **In passing:** fixed a latent `.numeric` specificity bug (`.table td`/`th` had silently beaten `.numeric` — all BUD-S65 money columns were rendering *left*-aligned despite the CSS comment); scoped `.table .numeric` (0,0,2,0) so money now right-aligns across **all four** ledger/Templates tables as intended. Blast radius: `TemplatesView.test` re-pointed (`role="list"`→`role="table"`; Name label; "Delete {name}"), `templates.spec` + `a11y.spec` re-pointed (**+1 dark Templates axe scan**, and the table seeded so it's covered; `exact:true` on "Name" so it doesn't substring-match "Re**name**"). **Bundle +0.38 KB gz → 124.47** (CSS +0.14 → 5.07). | Owner-nodded BUD-S66 → build; spec of record `docs/ux/templates-page.md` (its §3 = the pattern) | **NO data/API/domain change** (presentation over the existing template reads/flows). The `.numeric` fix touches the 3 Done BUD-S65 tables' rendered alignment — a documented-intent correction, disclosed. Gate green: **427 Vitest + 111 e2e**. [Status report](status-reports/2026-07-07-uxr4-templates-page.md) |
| 2026-07-07 | **BUD-S67 built → `Done` — the Recurring page (presentation + 2 owner-ratified additions; first proof the form pattern reuses).** The rule form re-laid on the BUD-S66 pattern by **importing [`FormLayout.module.css`](../apps/web/src/FormLayout.module.css)** — fieldset/legend, the `Field`/`Input`/`Select` primitives, the carried Deposit/Withdrawal radiogroup unchanged, natural pairs **gridded** (Amount+Payee · Frequency+First date) stacking ≤ 640px; the shared **AllocationEditor** (the split, with its own submit) unchanged, only its framing adopts the pattern. **The one pattern-completing addition** (reconciles the spec's "zero pattern code"): §3.1 always specified gridded pairs, but BUD-S66's Templates form had none, so the module never realized a pair-row — BUD-S67, the first consumer that needs it, lands a single `.fieldRow` class (the pattern realizing its own §3.1; BUD-S69 reuses it). The rules list → a real table on the **BUD-S65 [`Ledgers.module.css`](../apps/web/src/Ledgers.module.css) treatment verbatim** (Payee `<th scope="row">` · Account · Amount signed/right-aligned · Frequency · Next date · Status · Split · Actions): **Payee surfaced** (data the old list never rendered); the **split moved out of the row** into a per-row disclosure (`<button aria-expanded aria-controls>`, "Show N lines for {payee}") revealing an indented `colSpan` detail region (small page-local [`RecurringView.module.css`](../apps/web/src/RecurringView.module.css)). **Delete gains the BUD-S57 `ConfirmDialog`** ("Delete this rule?", §4 copy). Both additions owner-ratified 2026-07-07. **Behavior byte-for-byte** otherwise (create/reset/post-due unchanged). Blast radius: `RecurringView.test` re-pointed (`role="list"`→`role="table"`; "Up to date"; delete-confirm step), `recurring.spec` + `cockpit.spec` + `setup.ts` re-pointed (list→table, assert Payee, expand disclosure for the split), `a11y.spec` `scanRecurring` seeds+expands a rule (light AND dark). **In passing:** hardened a pre-existing pay-periods a11y selection race (`openPayPeriodsFor` selected before the default plan settled — a controlled-`<select>` race this slice's larger seed widened into 4 deterministic failures; fixed in the helper, no app logic). **Bundle +0.49 KB gz → 124.96** (CSS +0.08 → 5.15). | Owner-nodded BUD-S67 → build; spec of record `docs/ux/recurring-page.md`; owner queued **BUD-S68 next** | **NO data/API/domain change** (presentation over the existing recurring reads/flows + the 2 ratified additions). Gate green: **427 Vitest + 112 e2e**. [Status report](status-reports/2026-07-07-uxr5-recurring-page.md) |
| 2026-07-07 | **BUD-S68 built → `Done` — the Insights IA (presentation-only IA restructure).** The flat nine-link Insights sub-nav (`<nav aria-label="Insights views">`) re-laid as a **two-row category IA** in [`AnalysisSection.tsx`](../apps/web/src/AnalysisSection.tsx): a `CATEGORIES` map (**Spending** · **Budget** · **Cash flow** · **Debt** · **Net worth**) drives a **primary** `<nav aria-label="Insights categories">` of five category `<Link>`s (each `to` its default sub-view; marked `aria-current="page"` for **any** view in its section — weight + a bottom-edge bar, so a plain `<Link>` with computed current, not a `NavLink`) and a **secondary** `<nav aria-label="{Category} views">` of the active category's sub-views as `NavLink`s (`end`), rendered **only when the category has > 1 view** (Cash flow · Net worth get no row). **Deliberately links, not ARIA tabs** — these are routes (ADR-0006/BUD-S49): back/forward, deep links, and refresh keep working. **Every `/insights/:view` URL is preserved** — the `/insights` index redirect, unknown → `spend`, and `/insights/pay-periods` → `/pay-periods` all carry; a new **`routing.spec` sweep deep-links all nine views** and asserts each renders + marks its category current. Two labels read better under their category — `spend` → **By envelope**, `budget` → **vs Actual** — **nav-label only**: routes and each view's own `<h2>` ("Insights — …") are untouched, so every a11y/cockpit `level: 2` heading assertion and every cockpit `/insights/*` href is unaffected (Cockpit tests needed no change). CSS: BUD-S60 `.subnav` superseded by `.categoryNav` + `.segmentNav` (both wrap, ≥ 24px targets, no 320px page scroll). Blast radius: `AnalysisSection.test` rewritten (five category tabs · segment-row-only-when->1 · renamed labels · category current on a non-default sub-view), e2e `openAnalysis` walks category → segment (a label→category map; the two renamed call-site labels re-pointed), analysis.spec's cross-category "→ Forecast" switch re-pointed to the Cash flow category tab; axe light AND dark + 320px reflow over the new chrome green. **Bundle +0.19 KB gz → 125.15** (CSS +0.06 → 5.21). | Owner-queued BUD-S68 → build; spec of record `docs/ux/insights-ia.md` (owner-endorsed; label renames ratified 2026-07-07) | **NO data/API/domain change; NO view/chart change** (Insights *chrome* only — no pattern/CSS dependency on BUD-S67). Gate green: **431 Vitest + 121 e2e**. [Status report](status-reports/2026-07-07-uxr6-insights-ia.md) |
| 2026-07-07 | **BUD-S69 built → `Done` — the Manage Move-money form (presentation-only; SECOND proof the form pattern reuses; CLOSES the `BUD-S63`–`BUD-S70` track).** [`MoveMoneyForm`](../apps/web/src/MoveMoneyForm.tsx) re-laid on the BUD-S66 pattern by **importing [`FormLayout.module.css`](../apps/web/src/FormLayout.module.css)** — a grouped `<fieldset>`+visible `<legend>`, every control via the BUD-S48 `Field`/`Input`/`Select` primitives, natural pairs **gridded** as `.fieldRow` (From+To envelope · Amount+Memo) stacking ≤ 640px, form capped ~44rem, right-aligned **action row** (Move money). **No new pattern code** (BUD-S67 already landed `.fieldRow`; this slice only imports the module — the reuse-evidence the FEAT note promised). **Behavior byte-for-byte:** the BUD-T4 inline amount validation (`FieldError` moved inside the Amount `Field`, `aria-invalid`/`aria-describedby` carried), the both-envelopes/different-envelopes guards, reset-on-success, the hide-until-two-active-envelopes rule, and the `createEnvelopeTransfer` API call are all unchanged. **The shell owns the `<h1>` (BUD-S63)**; [`ManageView`](../apps/web/src/ManageView.tsx) keeps its `<h2>Move money</h2>` (untouched), and the form gains a matching `<legend>Move money</legend>`. **Blast radius: zero test re-points** — `MoveMoneyForm.test`, `ManageView.test`, and the e2e (`transfers.spec`, `a11y.spec`) all select by label text ("From envelope"/"To envelope"/"Amount"), the form aria-label, and the button name, every one preserved by the primitives — the whole suite passed **unchanged**. Verified live via preview: renders on the pattern, `.fieldRow` 2-col desktop / 1-col at 320px with no page overflow, no console errors, labels correctly associated. **Bundle +0.06 KB gz → 125.21** (CSS unchanged at 5.21 — **no new CSS**, import only). | Owner-nodded BUD-S69 → build (the last UX Redesign build); spec of record `docs/features/manage-move-money.md` (FEAT note, §11-compressed) | **NO data/API/domain change** (presentation over the existing Move-money reads/flows). **Closes `BUD-S63`–`BUD-S70`** — the whole UX Redesign is `Done`. Gate green: **431 Vitest + 121 e2e**. [Status report](status-reports/2026-07-07-uxr7-manage-form.md) |
| 2026-07-07 | **Post-track polish batch opened (`BUD-S71`–`BUD-S74`); `BUD-S71` built → `Done`.** A second owner-directed batch after the `BUD-S63`–`BUD-S70` track closed — polish/IA the owner surfaced from living with the redesign; captured as four presentation-only slices continuing the UXR numbering (`BUD-S72`–`BUD-S74` `Planned`). **BUD-S71 (Dashboard IA):** the home + pay-period planner become two sub-tabs of one **Dashboard** — a new pathless [`DashboardLayout`](../apps/web/src/DashboardLayout.tsx) wraps **Overview** (`/`, the cockpit) + **Pay periods** (`/pay-periods`, the FEAT-UXR2 planner) behind a centered `.categoryNav` sub-tab nav; the sidebar "Home" item is renamed **Dashboard** and the Planning group's "Pay periods" is removed (absorbed — its URL preserved, so deep links + the cockpit Next-paycheck link + the `/insights/pay-periods` redirect all carry). The site-wide sub-tab navs (Dashboard **and** Insights) are **centered** (`justify-content: center` on `.categoryNav`/`.segmentNav`; `.categoryNav` active now also matches NavLink `aria-current="page"`), and the planner `main` is **widened to 1200px**. Two owner decisions taken up front: **absorb** `/pay-periods` (not duplicate) · keep Bills/Paychecks **together** on one tab (preserve the payday→bills highlight) rather than a three-tab split. **Behavior byte-for-byte** (cockpit + planner untouched — no data/API/domain change). Blast radius: `AppShell.test` re-pointed (Home→Dashboard; no "Pay periods" sidebar link); e2e `setup.goToDashboard` + new `dashboardViews` helper re-point the nav (Pay periods reached via the sub-tab); the `"Home"` `level:1` assertions across specs → `"Dashboard"`; `analysis.spec` pay-periods active-state moved to the Dashboard sub-tab nav. Verified live via preview (centered nav, wide planner, Bills+Paychecks side-by-side). **Bundle +0.06 KB gz → 125.27** (CSS +0.02 → 5.23). | Owner: "mostly polish and redesign… continue the UXR numbering… document these into additional slices"; two design forks resolved via owner Q (absorb `/pay-periods`; keep the panes together) | **NO data/API/domain change.** Gate green: **431 Vitest + 121 e2e**. [UX spec](ux/dashboard-ia.md) · [Status report](status-reports/2026-07-07-uxr9-dashboard-ia.md) |
| 2026-07-07 | **`BUD-S75` added to the post-track polish batch (capture-only, no code).** The shared **`AllocationEditor`** ([`AllocationEditor.tsx`](../apps/web/src/AllocationEditor.tsx)) still renders raw `<label>`/`<select>`/`<input>`/`<button>` inside its `<fieldset><legend>Allocate</legend>` — so on the **Recurring** rule form (owner's callout) the Allocate section clashes with the `FormLayout`-patterned fields around it. `BUD-S75` = re-lay it on the `Field`/`Input`/`Select` primitives + the split line mini-grid (BUD-S66 pattern). Flagged that it's **shared** (also the add-transaction modal (`BUD-S73`), the account register, and needs-allocation), so it should coordinate with `BUD-S73`; behaviour stays byte-for-byte. Batch is now `BUD-S71`–`BUD-S75` (`BUD-S71` Done; `BUD-S72`–`BUD-S75` Planned). | Owner: "on the Recurring page the Allocate Form section needs to be formatted to match the existing component. Document this in the roadmap" | Capture-only — roadmap row + focus/next-fronts/log updated; no code, no gate impact. |
| 2026-07-07 | **BUD-S72 built → `Done` — Insights chart X-axis readability (presentation-only; shared `ui/Chart.tsx`).** The categorical/line X-axis labels (long envelope names) previously rendered horizontal + thinned to ~8 max, so most were dropped and the survivors still collided. Both `LineChart` and `BarChart` now **slant their x labels to -35°** (end-anchored, each carrying its own `rotate(-35 x y)` transform) and **de-thin** them via one shared `xLabelStep(n) = ceil(n/24)` helper (keep every label up to 24, then thin — slanted labels pack ~3× tighter). To hold the slanted long names without clipping, the shared viewBox grew **320→360** (`PAD.bottom` **40→84**, a new `AXIS_LABEL_Y` anchor). Only the shared primitive changed — **every Insights chart** (spend-by-envelope, budget-vs-actual, net-worth/cash-flow/trends lines) benefits at once; BreakdownBars/Gauge (own viewBoxes) untouched. Verified live via preview: `/insights/spend` now shows **all 22 envelope names** slanted without collision (was ~8), `/insights/trends` line labels slanted, no console errors. **Follow-on owner tweak (same slice):** removed the `.svg` `max-width: 720px` cap so each chart **fills its figure = as wide as the data table below it** (both bounded by the 960px reading measure); verified live the SVG widens 720→912px at 1440px to match the table. +2 Vitest (slant transform + density; existing 6 Chart tests unchanged). **Bundle +0.05 KB gz → 125.32** (CSS unchanged at 5.23 — geometry lives in the SVG). | Owner picked BUD-S72 next (batch ordering is the owner's call); owner then asked to widen charts to match the tables | **NO data/API/domain change** (a11y contract intact — labels are still inside the `aria-hidden` decorative group; the data table remains the SR source of truth). Gate green: **433 Vitest + 121 e2e**. [Status report](status-reports/2026-07-07-uxr10-chart-xaxis.md) |
| 2026-07-08 | **BUD-S73 built → `Done` — add-transaction cleanup (presentation-only).** Removed the redundant page-local **Add transaction** `<Link>` on `/accounts` ([`AccountsList.tsx`](../apps/web/src/AccountsList.tsx)) — the persistent [`AppShell`](../apps/web/src/AppShell.tsx) sidebar-footer action is the single, always-available entry to the `/transactions/new` quick-add modal, so a second per-page control was redundant (dropped the now-unused `Button.module.css` import). Re-laid [`AddTransactionForm`](../apps/web/src/AddTransactionForm.tsx) — the **quick-add modal** (account-picker mode) **and** the **account-register embed** (fixed-account mode), one shared component — on the BUD-S66 pattern by **importing [`FormLayout.module.css`](../apps/web/src/FormLayout.module.css)**: a grouped `New transaction` `<fieldset>`+visible `<legend>`, every field via the BUD-S48 `Field`/`Input`/`Select` primitives, Amount+Date **gridded** as `.fieldRow` (stacking ≤ 640px), full-width Payee. The carried Deposit/Withdrawal radiogroup and the embedded shared **`AllocationEditor`** (still raw — restyled next in `BUD-S75`) are unchanged; the visible Allocate-section clash is exactly `BUD-S75`'s scope. **Behaviour byte-for-byte:** the amount field keeps `aria-label="Transaction amount"` (distinct from its visible "Amount" label) so every test/e2e selector resolves; the picker Save-gate, BUD-T4 inline amount validation, reset-on-success, and the `createTransaction` fan-out are untouched. Verified live via preview: the modal renders on the pattern with all accessible names preserved (form "Add transaction", combobox "Account", textbox "Transaction amount"/"Date"/"Payee"), the `/accounts` `main` shows only "Add account", no console errors, `.fieldRow` collapses to one column at 375px. **Tests −1/+1** (the `/accounts` Add-transaction-link assertion → a "no page-local control" assertion), net **433 Vitest**; the register/quick-add specs passed unchanged (selectors preserved). **Bundle +0.01 KB gz → 125.33** (CSS unchanged at 5.23 — import only, no new CSS). | Owner picked BUD-S73 next (batch ordering is the owner's call) | **NO data/API/domain change** (presentation over the existing add-transaction reads/flows; coordinated with `BUD-S75`, which restyles the embedded `AllocationEditor`). Gate green: **433 Vitest + 121 e2e**. [Status report](status-reports/2026-07-08-uxr11-add-transaction.md) |
| 2026-07-08 | **BUD-S75 built → `Done` — Allocate form on the pattern (presentation-only).** Re-laid the shared [`AllocationEditor`](../apps/web/src/AllocationEditor.tsx) — until now raw `<label>`/`<select>`/`<input>`/`<button>` inside its `<fieldset><legend>Allocate</legend>` — on the BUD-S66 form-layout pattern: the grouped fieldset shell + the `Field`/`Input`/`Select`/`Button` primitives + `.fieldRow`/`.amount`/`.removeLine`/`.actionRow` from [`FormLayout.module.css`](../apps/web/src/FormLayout.module.css), with the **allocation-specific rows** (the mode radiogroup, the single/split lines with their extra **refund** + **use-remaining** controls, and the live summary) in a **new sibling** [`AllocationEditor.module.css`](../apps/web/src/AllocationEditor.module.css). The split row is genuinely richer than TemplatesView's 3-col `.lineRow` (envelope · amount · refund · use-remaining · remove = 5 columns), so its grid lives in the component module rather than polluting the shared pattern — **FormLayout untouched, so TemplatesView is unaffected**. The `Apply template` / `Save as new template` accelerators become `Field`s in a `.fieldRow`; the split rows reflow to one column ≤ 640px. **Behaviour byte-for-byte:** every accessible name (`Allocate`, `Allocation mode`, Single/Split, `Apply template`, `New template name`, `Envelope`, `Envelope/Amount/Refund/Remove for row N`, `use remaining`, `Add row`, `distribute remaining`, the save label) plus every handler and the save guard are unchanged — so all **four embeds** (quick-add modal, account register, needs-allocation, Recurring rule form) and every unit/e2e selector resolve. This closes the clash `BUD-S73` deliberately left (the raw Allocate section next to patterned fields). Verified live via preview: the Recurring Allocate section and the quick-add modal now match the fieldsets around them, the split-row grid aligns (envelope grows, amount right-aligned, refund/use-remaining/remove trailing), the rows reflow at 375px, no console errors. **Tests unchanged** — 433 Vitest (AllocationEditor + register/quick-add/templates/recurring/transfers specs all pass unchanged, selectors preserved) + 121 e2e. **Bundle +0.15 KB gz → 125.48** (CSS +0.14 → 5.37 — the new component module). | Owner picked BUD-S75 next (batch ordering is the owner's call) | **NO data/API/domain change** (presentation over the existing allocation flows; coordinates with `BUD-S73`, which shares the add-transaction modal). Gate green: **433 Vitest + 121 e2e**. [Status report](status-reports/2026-07-08-uxr13-allocate-form.md) |
| 2026-07-08 | **BUD-S74 built → `Done` — Manage page formatting (presentation-only); closes the `BUD-S71`–`BUD-S75` batch.** Re-laid [`ManageView`](../apps/web/src/ManageView.tsx) on the design-system treatment. The **net-worth summary** — previously a raw `<table>` with an inline `text-align:right` style on each `<td>` and a visible caption redundant with the section `<h2>` — now uses the shared BUD-S65 [`Ledgers.module.css`](../apps/web/src/Ledgers.module.css) `.table` treatment (bordered rows, `.numeric` right-aligned tabular money), wrapped in the global `.table-scroll` group, with the caption made `className="sr-only"` (the `<h2>Net worth` already labels it — the ledger convention). The two **management links** (`Accounts` · `Envelopes`) become a row of button-like `<Link>`s — a new [`ManageView.module.css`](../apps/web/src/ManageView.module.css) mirrors `ui/Button`'s default variant (border + surface + radius, hover to surface-2) — in place of the bare bullet list. **Behaviour byte-for-byte:** the `Management` nav keeps its `aria-label` + both links' names/hrefs; the net-worth table keeps its accessible name (`Net worth summary`) and figures; Move-money (already BUD-S69-patterned) is untouched — so all four `ManageView.test.tsx` specs pass unchanged. Verified live via preview: `/manage` renders the bordered net-worth table (right-aligned tabular figures) and the two pill links, matching the rest of the app; no console errors. **Tests unchanged** — 433 Vitest + 121 e2e. **Bundle +0.04 KB gz → 125.52** (CSS +0.04 → 5.41 — the new component module). | Owner picked BUD-S74 last (the remaining batch item) | **NO data/API/domain change** (presentation over the existing `/manage` reads). Gate green: **433 Vitest + 121 e2e**. This is the final `BUD-S71`–`BUD-S75` slice — the polish batch is closed. [Status report](status-reports/2026-07-08-uxr12-manage-formatting.md) |
| 2026-07-10 | **`SPIKE-03` + `BUD-S79` Done — the 12-year history is in the app.** The owner ran the `Budget.xlsx` ETL **out-of-repo** (two sessions, 2026-06-23: a repo-derived `SCHEMA_MAP.yaml` "Rosetta Stone" → a Rev-2 extraction roadmap with owner-locked decisions D1–D7 → a deterministic `extract.py`) and transferred the results into `/budget-extraction/` (tooling + redaction-safe docs committed; workbook, import JSON, statements, and full-fidelity working docs **gitignored**). This session: **(1) reviewed** the drop ([LOAD_READINESS_ANALYSIS.md](../budget-extraction/LOAD_READINESS_ANALYSIS.md)) and flagged the two working docs' real amounts → owner gitignored them (commit hygiene before commit); **(2) independently re-verified** `budgeteer_import.json` from scratch — Σ all transaction cents = 0, **0** split-invariant violations, **0** FK dangles, all integer cents/ISO dates, 27 unique envelopes (5 archived), exactly one Reset transaction (10 allocations), row columns match `schema.ts`; **(3) wrapped** it in the `BudgeteerBackup` envelope (`wrap_backup.py`, ~40 lines — the `tables` payload needed zero changes) and **(4) restored 26,640 rows** via the BUD-S31 `db:restore` CLI into a **dedicated gitignored store** `data/budgeteer-ledger` (dev store untouched), first try, no warnings; **(5) verified live** — register renders real weeks incl. the 2025-10-03 Reset, balances $0 (correct post-reset), archived section right, Insights computes the full 2014→2025 range. **SPIKE-03's question is answered yes** — findings promoted redacted to [spikes/03-history-extraction.md](spikes/03-history-extraction.md) (SPIKE-08 stance). **No app code changed** — the BUD-S31 restore path ingested the data as designed. Real data revealed one latent display bug: fractional-cent y-axis ticks render malformed in `ui/Chart.tsx` (e.g. `-$895.84.75`) — filed as a follow-up task, not fixed in this change. | Owner transferred the ETL results and directed "C + A" (commit hygiene → load & verify) after this session's review suggested paths | **The last V1-era track is closed; the BUD-S70 demo-seed callout has its durable fix** (real data in a real store). Next fronts (owner's call): the **statement ETL** (fills 2025-10 → now; sharpens `BUD-S80`; decision points in SPIKE-03 §6) · FEAT-S7 §5 ratification · `BUD-E13`. [Status report](status-reports/2026-07-10-history-import.md) |
| 2026-07-10 | **SPIKE-11 (`SPIKE-11`) Done — statement profiling answered yes; `BUD-S80` promoted to `Ready` with every decision locked.** A throwaway pdfplumber parser (session scratchpad; preserved with its full-fidelity JSON output in the new gitignored `/budget-extraction/statement-spike/`) parsed all 20 statement PDFs: **30/30 account-statements reconcile to the penny** (BofA: per-section sums vs the statements' own stated totals + beginning+Σ=ending; Capital One: the running-balance chain verified on every row) into **1,009 rows** across **three real accounts** — the Capital One PDFs each carry a checking *and* a savings account — with cross-statement chains unbroken 2025-08-15 → 2026-06-30 (no missing statements). The evidence retired the SPIKE-03 §6 unknowns: **row-level overlap dedup is structurally impossible** (only 59/165 overlap rows match on amount within the week — the workbook's weekly consolidated budget-view grain ≠ bank settlement grain), and **reality mirrors the reset** (all three accounts drain to exactly $0 in early Oct 2025 — the same life event as the workbook zero-out — so cut-date balances are penny-exact, two of three $0.00). Owner confirmed **D1–D4**: era cut at 2025-10-03 (import the 844 post-cut rows; workbook authoritative before it) · three new real accounts + computed opening anchors, synthetic `Budget` unchanged · merchant→envelope rules for the high-frequency head (top ~25 merchants = 69% of spend rows) + the rest unallocated into the domain's needs-allocation flow · 23/23 intra-Capital-One pairs → ADR-0004 transfers, external-source deposits stay plain deposits. Load-path constraint found: BUD-S31 restore refuses non-empty stores → the `BUD-S80` slice is a **full-store rebuild** (extract → merge with the workbook backup → `db:reset` + `db:restore` `data/budgeteer-ledger`), zero app code expected, repeatable when each new month's statements drop. Docs-only change (+ one `.gitignore` line for the spike artifacts). Also: removed a stale merged `.claude` worktree that was failing repo-wide lint (→ K28), and the chart-tick fix landed separately (`0113e96`, +1 Vitest). Gate green **434 Vitest + 121 e2e**, build 125.53 KB gz. | Owner picked the statement-ETL front (the strongest continuation per the `SPIKE-03`/`BUD-S79` handoff); SPIKE-03 §6 had named the decision points | `BUD-S80` is `Ready`; next session builds the `BUD-S80` slice (extractor + owner-reviewed rules file + merge + rebuild + live verify). [Status report](status-reports/2026-07-10-spike-11-statement-profiling.md) |
| 2026-07-10 | **`BUD-S80` Done — statement import built; the post-reset gap is filled.** Three committed, redaction-safe Python tools (`extract_statements.py` + `build_merchant_rules.py` + `merge_statements.py`, on the `extract.py` pattern) parse the 20 gitignored PDFs into **845** statement-era rows (D1 cut 2025-10-03 → 844 post-cut + 1 BofA opening anchor), merge with the workbook backup into one **27,523-row** `BudgeteerBackup`, and rebuild `data/budgeteer-ledger` (`db:reset`→`db:restore`). Verified live: three new real accounts at their statements' stated closings (BofA $0.00 · CO Checking $819.53 · CO Savings $21.27), **70** ADR-0004 transfer legs Σ=0 (excluded from spend), needs-allocation **775**, net worth $840.80 spanning into 2026-06. **Zero app code changed.** Building it surfaced two reality corrections (D1–D4 unchanged): (a) intra-CO transfers are **bidirectional → 35 pairs**, not F5's one-directional 23 — else 12 internal moves would read as spend/income; (b) Capital One **wraps long merchant descriptions across physical lines**, silently dropped by the spike parser (259 blank payees) — the extractor reassembles them (0 blank). Attribution is owner-reviewed & opt-in: default load imports all spend unallocated; the gitignored `merchant_rules.json` draft is applied via a one-command idempotent re-run (`--rules`). Gate green **434 Vitest + 121 e2e**, build 125.53 KB gz. | Owner picked the `BUD-S80` build (the last data track) off the SPIKE-11 handoff | The data tracks are complete; next is owner's-call (apply merchant rules · FEAT-S7 §5 · `BUD-E13`). [Status report](status-reports/2026-07-10-statement-import.md) |


## 2. Done / shipped (generated)

Every shipped story (plan status **Done**), newest first — **generated from the plan** by
`npm run docs:crosswalk`, so it can't drift; do not hand-edit. Shipped date = the item's
newest linked status report. Full detail lives in the plan (`03_ROADMAP-v2.md` §3) and the
§1 log above.

| Shipped | ID | Was | Item | Report |
| --- | --- | --- | --- | --- |
| 2026-08-02 | `BUD-S92` | `—` | First-run setup UI (`/setup` — the first admin from the browser) | [report](../status-reports/2026-08-02-docs-onboarding-gap.md) |
| 2026-08-02 | `BUD-S91` | `—` | Accessibility scans for the auth surfaces | [report](../status-reports/2026-08-02-bud-s91-auth-a11y.md) |
| 2026-08-02 | `BUD-S84` | `—` | CI image publishing to GHCR (ARM64) | [report](../status-reports/2026-08-02-bud-s84-ghcr-publish.md) |
| 2026-08-01 | `BUD-S90` | `—` | A rebuilt store has a way in (reset/seed/demo auth gap) | [report](../status-reports/2026-08-01-bud-s90-rebuilt-store-access.md) |
| 2026-08-01 | `BUD-S83` | `—` | Production config profile + deploy contract | [report](../status-reports/2026-08-01-bud-s81-s85-hub-deploy.md) |
| 2026-08-01 | `BUD-S82` | `—` | Production Postgres validation + `/health` DB readiness | [report](../status-reports/2026-08-01-bud-s81-s85-hub-deploy.md) |
| 2026-08-01 | `BUD-S81` | `—` | Containerization — multi-stage ARM64 image | [report](../status-reports/2026-08-01-bud-s81-s85-hub-deploy.md) |
| 2026-07-31 | `BUD-S89` | `—` | Login hardening + threat-model tests | [report](../status-reports/2026-07-31-bud-s89-login-hardening.md) |
| 2026-07-31 | `BUD-S88` | `—` | Roles + user management | [report](../status-reports/2026-07-31-bud-s88-roles-user-management.md) |
| 2026-07-29 | `BUD-S87` | `—` | Auth core + login | [report](../status-reports/2026-07-29-bud-s87-auth-core.md) |
| 2026-07-29 | `BUD-S86` | `—` | Principal seam refactor | [report](../status-reports/2026-07-29-bud-s86-principal-seam.md) |
| 2026-07-10 | `BUD-S80` | `#20` | Statement import | [report](../status-reports/2026-07-10-statement-import.md) |
| 2026-07-10 | `BUD-S79` | `#18` | Historical import | [report](../status-reports/2026-07-10-history-import.md) |
| 2026-07-08 | `BUD-S75` | `UXR13` | Allocate form on the pattern | [report](../status-reports/2026-07-08-uxr13-allocate-form.md) |
| 2026-07-08 | `BUD-S74` | `UXR12` | Manage page formatting | [report](../status-reports/2026-07-08-uxr12-manage-formatting.md) |
| 2026-07-08 | `BUD-S73` | `UXR11` | Add-transaction cleanup | [report](../status-reports/2026-07-08-uxr11-add-transaction.md) |
| 2026-07-07 | `BUD-S72` | `UXR10` | Insights chart X-axis readability | [report](../status-reports/2026-07-07-uxr10-chart-xaxis.md) |
| 2026-07-07 | `BUD-S71` | `UXR9` | Dashboard IA | [report](../status-reports/2026-07-07-uxr9-dashboard-ia.md) |
| 2026-07-07 | `BUD-S70` | `UXR8` | Demo-grade synthetic seed | [report](../status-reports/2026-07-07-uxr8-demo-seed.md) |
| 2026-07-07 | `BUD-S69` | `UXR7` | Manage page | [report](../status-reports/2026-07-07-uxr7-manage-form.md) |
| 2026-07-07 | `BUD-S68` | `UXR6` | Insights IA | [report](../status-reports/2026-07-07-uxr6-insights-ia.md) |
| 2026-07-07 | `BUD-S67` | `UXR5` | Recurring page | [report](../status-reports/2026-07-07-uxr5-recurring-page.md) |
| 2026-07-07 | `BUD-S66` | `UXR4` | Templates page | [report](../status-reports/2026-07-07-uxr4-templates-page.md) |
| 2026-07-07 | `BUD-S65` | `UXR3` | Ledgers tables | [report](../status-reports/2026-07-07-uxr3-ledgers-tables.md) |
| 2026-07-07 | `BUD-S64` | `UXR2` | Pay-period planner | [report](../status-reports/2026-07-07-uxr2-pay-period-planner.md) |
| 2026-07-07 | `BUD-S63` | `UXR1` | Sidebar app shell | [report](../status-reports/2026-07-07-uxr1-sidebar-shell.md) |
| 2026-07-03 | `BUD-S62` | `S9` | "Still owed this month" | [report](../status-reports/2026-07-03-s9-s7-spec.md) |
| 2026-07-03 | `BUD-S61` | `S7` | Pay-period planning | [report](../status-reports/2026-07-03-s7-slice.md) |
| 2026-07-03 | `BUD-S35` | `EH14` | Make post-due idempotency structural | [report](../status-reports/2026-07-03-eh9-eh14.md) |
| 2026-07-03 | `BUD-S34` | `EH13` | Lint the layer boundaries | [report](../status-reports/2026-07-03-eh12-eh13.md) |
| 2026-07-03 | `BUD-S33` | `EH12` | Share the DTO types; decide the client-boundary stance | [report](../status-reports/2026-07-03-eh12-eh13.md) |
| 2026-07-03 | `BUD-S31` | `EH10` | Prove restore | [report](../status-reports/2026-07-03-eh10.md) |
| 2026-07-03 | `BUD-S30` | `EH9` | Versioned migrator | [report](../status-reports/2026-07-03-eh9-eh14.md) |
| 2026-07-03 | `BUD-S29` | `EH8` | Decide + document the timezone policy | [report](../status-reports/2026-07-03-eh8.md) |
| 2026-07-02 | `BUD-S60` | `UX15` | Responsive pass | [report](../status-reports/2026-07-02-ux15.md) |
| 2026-07-02 | `BUD-S59` | `UX14` | Empty states & first-run onboarding | [report](../status-reports/2026-07-02-ux14.md) |
| 2026-07-02 | `BUD-S58` | `UX13` | Money & budget-health visual encoding | [report](../status-reports/2026-07-02-ux13.md) |
| 2026-07-02 | `BUD-S57` | `UX12` | Feedback & states | [report](../status-reports/2026-07-02-ux12d.md) |
| 2026-07-02 | `BUD-S32` | `EH11` | Bind localhost by default | [report](../status-reports/2026-07-02-eh11.md) |
| 2026-07-02 | `BUD-S28` | `EH7` | Inject the clock into services | [report](../status-reports/2026-07-02-eh7.md) |
| 2026-07-01 | `BUD-S56` | `UX11` | New viz: budget burn-down | [report](../status-reports/2026-07-01-ux11.md) |
| 2026-07-01 | `BUD-S55` | `UX10` | New viz: spending trends over time | [report](../status-reports/2026-07-01-ux10.md) |
| 2026-06-28 | `BUD-S54` | `UX9` | New viz: spending breakdown | [report](../status-reports/2026-06-28-ux9.md) |
| 2026-06-28 | `BUD-S53` | `UX8` | rename + restyle the 6 views, replace number grids with accessible cha… | [report](../status-reports/2026-06-28-ux8.md) |
| 2026-06-28 | `BUD-S52` | `UX7` | Global quick-add transaction | [report](../status-reports/2026-06-28-ux7.md) |
| 2026-06-28 | `BUD-S51` | `UX6` | Demote management to a dedicated surface | [report](../status-reports/2026-06-28-ux6.md) |
| 2026-06-27 | `BUD-S50` | `UX5` | Home: budget + future-planning cockpit | [report](../status-reports/2026-06-27-ux5.md) |
| 2026-06-27 | `BUD-S49` | `UX3` | Routing + persistent app shell | [report](../status-reports/2026-06-27-ux3.md) |
| 2026-06-26 | `BUD-S48` | `UX4` | Design-system foundation | [report](../status-reports/2026-06-26-ux4.md) |
| 2026-06-23 | `BUD-S46` | `R13` | Structured API logging | [report](../status-reports/2026-06-23-r13.md) |
| 2026-06-23 | `BUD-S45` | `R12` | React error boundaries | [report](../status-reports/2026-06-23-r12.md) |
| 2026-06-22 | `BUD-S44` | `R11` | Route modularization | [report](../status-reports/2026-06-22-r11.md) |
| 2026-06-22 | `BUD-S42` | `R5` | Envelope targets visible inline | [report](../status-reports/2026-06-22-r5.md) |
| 2026-06-22 | `BUD-S41` | `R3` | Grouped navigation — unified Analysis section | [report](../status-reports/2026-06-22-r3.md) |
| 2026-06-22 | `BUD-S40` | `R2` | Needs-allocation count badge | [report](../status-reports/2026-06-22-r2.md) |
| 2026-06-22 | `BUD-S21` | `R4` | Dashboard net worth summary | [report](../status-reports/2026-06-22-r4.md) |
| 2026-06-22 | `BUD-S20` | `R9` | Net worth over time analysis | [report](../status-reports/2026-06-22-r9.md) |
| 2026-06-22 | `BUD-S13` | `R8` | Transaction search / filter | [report](../status-reports/2026-06-22-r8.md) |
| 2026-06-21 | `BUD-S78` | `#16` | a11y pass | [report](../status-reports/2026-06-21-16.md) |
| 2026-06-21 | `BUD-S39` | `R1` | Account rename in UI | [report](../status-reports/2026-06-21-r1.md) |
| 2026-06-21 | `BUD-S14` | `R15` | Envelope ledger | [report](../status-reports/2026-06-21-r15.md) |
| 2026-06-21 | `BUD-S12` | `R7` | Account archive / close | [report](../status-reports/2026-06-21-r7.md) |
| 2026-06-21 | `BUD-S11` | `R6` | Transaction delete | [report](../status-reports/2026-06-21-r6.md) |
| 2026-06-17 | `BUD-S76` | `#15a` | Backup / export | [report](../status-reports/2026-06-17-slice-15a.md) |
| 2026-06-17 | `BUD-S47` | `R14` | e2e expansion | [report](../status-reports/2026-06-17-r14.md) |
| 2026-06-16 | `BUD-S19` | `#14b` | debt payoff % | [report](../status-reports/2026-06-16-slice-14b.md) |
| 2026-06-16 | `BUD-S18` | `#14a` | credit utilization | [report](../status-reports/2026-06-16-slice-14a.md) |
| 2026-06-16 | `BUD-S17` | `#13` | cash-flow forecast | [report](../status-reports/2026-06-16-slice-13.md) |
| 2026-06-16 | `BUD-S16` | `#12` | budget vs. actual | [report](../status-reports/2026-06-16-slice-12.md) |
| 2026-06-15 | `BUD-S27` | `EH6` | Repo hygiene | [report](../status-reports/2026-06-15-eh6.md) |
| 2026-06-15 | `BUD-S26` | `EH5` | Browser e2e | [report](../status-reports/2026-06-15-eh5.md) |
| 2026-06-15 | `BUD-S25` | `EH4` | Add ESLint to the gate | [report](../status-reports/2026-06-15-eh4.md) |
| 2026-06-15 | `BUD-S24` | `EH3` | Map DB unique-violation → 409 | [report](../status-reports/2026-06-15-eh3.md) |
| 2026-06-15 | `BUD-S23` | `EH2` | Extract API service plumbing | [report](../status-reports/2026-06-15-eh2.md) |
| 2026-06-15 | `BUD-S22` | `EH1` | Share the domain in the web | [report](../status-reports/2026-06-15-eh1.md) |
| 2026-06-15 | `BUD-S15` | `#11` | spend by envelope over time | [report](../status-reports/2026-06-15-slice-11.md) |
| 2026-06-15 | `BUD-S10` | `#10` | Reconcile to bank | [report](../status-reports/2026-06-15-slice-10.md) |
| 2026-06-14 | `BUD-S9` | `#9` | Recurring transactions | [report](../status-reports/2026-06-14-slice-9.md) |
| 2026-06-14 | `BUD-S8` | `#8` | Refunds | [report](../status-reports/2026-06-14-slice-8.md) |
| 2026-06-14 | `BUD-S7` | `#7b` | Reallocation | [report](../status-reports/2026-06-14-slice-7b.md) |
| 2026-06-14 | `BUD-S6` | `#7a` | Transfer | [report](../status-reports/2026-06-14-slice-7a.md) |
| 2026-06-13 | `BUD-S5` | `#6` | Archive an envelope | [report](../status-reports/2026-06-13-slice-6.md) |
| 2026-06-13 | `BUD-S4` | `#5` | Edit a past split | [report](../status-reports/2026-06-13-slice-5.md) |
| 2026-06-13 | `BUD-S3` | `#4` | accelerators — templates/presets | [report](../status-reports/2026-06-13-slice-2.md) |
| 2026-06-13 | `BUD-S2` | `#3` | core enter→allocate loop — enter deposit/withdrawal → allocate in Sing… | [report](../status-reports/2026-06-13-slice-1.md) |
| 2026-06-13 | `BUD-S1` | `#1` | Foundation | [report](../status-reports/2026-06-13-foundation-slice.md) |
| — | `BUD-S43` | `R10` | API watch mode | — |
