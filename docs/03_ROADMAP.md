<!--
ROADMAP — copy of templates/ROADMAP-TEMPLATE.md, filled for Budgeteer. The living plan of
record: spikes + vertical slices ordered by uncertainty × value (NOT by layer). Update
statuses as work lands; re-sequence (and log it, §5) when a spike changes what we know.
Sequencing model: docs/00_WAYS_OF_WORKING.md §7.
-->

# Roadmap — Budgeteer

| Field         | Value          |
| ------------- | -------------- |
| Status        | Living         |
| Owner         | Wesley Cutting |
| Last updated  | 2026-07-02     |
| Sources       | [`01_INTAKE.md`](01_INTAKE.md) · [`02_PRD.md`](02_PRD.md) · [`spikes/01-split-allocation-ux.md`](spikes/01-split-allocation-ux.md) · [`spikes/04-transfer-modeling.md`](spikes/04-transfer-modeling.md) · [`reviews/2026-06-15-repo-review.md`](reviews/2026-06-15-repo-review.md) · [`status-reports/2026-06-15-eh1.md`](status-reports/2026-06-15-eh1.md) · [`status-reports/2026-06-15-eh2.md`](status-reports/2026-06-15-eh2.md) · [`status-reports/2026-06-15-eh3.md`](status-reports/2026-06-15-eh3.md) · [`status-reports/2026-06-15-eh4.md`](status-reports/2026-06-15-eh4.md) · [`status-reports/2026-06-15-eh5.md`](status-reports/2026-06-15-eh5.md) · [`status-reports/2026-06-15-eh6.md`](status-reports/2026-06-15-eh6.md) · [`features/analysis-envelope-spend.md`](features/analysis-envelope-spend.md) · [`ux/analysis-envelope-spend.md`](ux/analysis-envelope-spend.md) · [`status-reports/2026-06-15-slice-11.md`](status-reports/2026-06-15-slice-11.md) · [`features/budget-vs-actual.md`](features/budget-vs-actual.md) · [`ux/budget-vs-actual.md`](ux/budget-vs-actual.md) · [`status-reports/2026-06-16-slice-12.md`](status-reports/2026-06-16-slice-12.md) · [`spikes/05-cashflow-forecast.md`](spikes/05-cashflow-forecast.md) · [`features/cash-flow-forecast.md`](features/cash-flow-forecast.md) · [`ux/cash-flow-forecast.md`](ux/cash-flow-forecast.md) · [`status-reports/2026-06-16-slice-13.md`](status-reports/2026-06-16-slice-13.md) · [`features/credit-utilization.md`](features/credit-utilization.md) · [`ux/credit-utilization.md`](ux/credit-utilization.md) · [`status-reports/2026-06-16-slice-14a.md`](status-reports/2026-06-16-slice-14a.md) · [`features/debt-payoff.md`](features/debt-payoff.md) · [`ux/debt-payoff.md`](ux/debt-payoff.md) · [`status-reports/2026-06-16-slice-14b.md`](status-reports/2026-06-16-slice-14b.md) · [2026-06-17 improvement review](reviews/2026-06-17-improvement-review.md) · [`status-reports/2026-06-17-r14.md`](status-reports/2026-06-17-r14.md) · [`status-reports/2026-06-17-slice-15a.md`](status-reports/2026-06-17-slice-15a.md) · [2026-06-25 UX uplift initiative](reviews/2026-06-25-ux-uplift-initiative.md) · [2026-07-02 architecture review](reviews/2026-07-02-architecture-review.md) · [`spikes/08-budgethome-sheet-analysis.md`](spikes/08-budgethome-sheet-analysis.md) |

**Current focus:** **V1 is functionally complete and gate-green (275 Vitest + 47 e2e)** — the domain core, the full
enter→allocate loop, transfers/refunds/recurring/reconcile, the six-view analysis area, and the DX (`R10`–`R13`),
UX-polish (`R1`–`R5`), and hardening tracks are all done. The next initiative is a **UX Uplift**: a
**foundation-first, desktop-first** overhaul of the experience — a design system + client routing/app-shell, a
**budget-and-future-planning landing cockpit**, and **new ways to visualize spending** — captured thoroughly in the
[2026-06-25 UX uplift initiative](reviews/2026-06-25-ux-uplift-initiative.md) and tracked below as the **UX Uplift**
section (`UX1`–`UX15`). It **reverses the repo's deliberate "no design system" stance**, so it is **ADR-gated and
spike-first**: nothing migrates until the foundation spikes validate the approach on one real screen, exactly as
`SPIKE-02` produced the stack ADRs. **`UX1` ✅ done** — [SPIKE-06](spikes/06-design-system-routing.md) baked off the
options on the Account Register and chose **React Router + design tokens/CSS-Modules + Radix Primitives** (both
primitive libs axe-clean in real Chromium; Radix < half React Aria's bundle, so the lean flipped); `ADR-0005`
(design system) + `ADR-0006` (routing) are **`Validated`**. **`UX4` is now scoped →
[FEAT-UX4](features/design-system.md)** — **seed-and-grow** (token sheet + only the primitives the first screen
needs), **proven by restyling the Account Register in place** (no routing yet), so `UX4` ran **before** `UX3`.
**`UX4` ✅ done** ([status](status-reports/2026-06-26-ux4.md), 283 Vitest + 49 e2e, gate-green) — the token sheet +
base layer + seed primitives (`apps/web/src/ui/`) are in and the Account Register is restyled in place, axe-clean in
**light and dark** (`Dialog`/`Table` deferred per seed-and-grow). **`UX3` ✅ done**
([FEAT-UX3](features/app-shell.md) · [status](status-reports/2026-06-27-ux3.md), 283 Vitest + 52 e2e, gate-green) —
the hand-rolled `view` state machine is retired for **React Router 7 (data router)** behind a **persistent app
shell** (`AppShell`); every screen is URL-addressable, deep-linkable, refresh-safe, with working back/forward and a
reduced-motion-aware route transition; the per-screen "← Dashboard" buttons are gone (nav lives in the shell);
`ADR-0006` → **`Accepted`** and the SPIKE-06 throwaway harness was discarded (findings persist in the spike report +
ADR). `/manage` + the `/accounts`·`/envelopes` list routes are deferred to `UX6` per the brief (not built empty).
**`UX5` ✅ done** ([FEAT-UX5](features/cockpit.md) · [status](status-reports/2026-06-27-ux5.md), 296 Vitest + 55 e2e,
gate-green) — the console home at `/` now opens on a **budget + future-planning cockpit**: five panels (this-month
budget health · needs-allocation · upcoming recurring · cash-flow snapshot · net worth) that **compose existing reads**
(fan-out, **no new endpoint** — the brief's open Q resolved to fan-out, R4/R5 precedent), each deep-linking to its
detail route; figures reconcile to the ledger (asserted). **Deliberate scope call (flagged):** account/envelope
management **stays on the home below the cockpit** — demoting it to `/manage` is `UX6`'s explicit job, so removing it
now (with nowhere to go) would break usable-at-every-step.
**`UX6` ✅ done** ([FEAT-UX6](features/manage.md) · [status](status-reports/2026-06-28-ux6.md), 304 Vitest + 61 e2e,
gate-green) — management is **demoted off the home**: the `/accounts` · `/envelopes` **LIST routes** (deferred from
UX3, built now) own per-entity CRUD — a **progressive "Add" affordance**, each name a `<Link>` to its detail (UX3 left
these as buttons), inline rename/archive — while the new `/manage` hub owns the **cross-entity** tools (the R4
net-worth summary that spans all accounts + Move-money between envelopes) and links to the two list pages. The home
(`/`) now renders the **UX5 cockpit only**; the shell nav gains **Accounts · Envelopes · Manage**. Figures still
reconcile to the ledger; fan-out over existing reads (no new endpoint).
**`UX7` ✅ done** ([FEAT-UX7](features/quick-add-transaction.md) · [status](status-reports/2026-06-28-ux7.md), 311
Vitest + 66 e2e, gate-green) — **PRD journey #1 is no longer buried**: an always-available **Add transaction** action
in the shell nav opens a **modal route** (`/transactions/new`) that lets you **pick the account**, enter amount/payee,
and allocate in Single or Split, then **returns you where you were**. It **reuses the register's editor**
(`AddTransactionForm` → `AllocationEditor`, + an account picker) and **fans out to the existing
`createTransaction`/`setAllocations`** — no new endpoint, no schema/API/domain change; a partial allocation's remainder
still surfaces in needs-allocation. The modal is the new **`Dialog` primitive** (`@radix-ui/react-dialog` per ADR-0005;
the deferred UX4 Dialog landed here) — focus trap / ESC / overlay-close / focus-restore, **axe-clean light AND dark**;
bundle 105.4 KB gz (< 120 KB).
**`UX8` ✅ done** ([FEAT-UX8](features/insights-charts.md) · [UX](ux/insights-charts.md) · [status](status-reports/2026-06-28-ux8.md),
315 Vitest + 72 e2e, gate-green) — the **Analysis → Insights migration**, the first build slice of the Insights
phase. The six analysis views are **renamed** ("Insights — …"), **restyled** onto the UX4 design system, and each
gains a **hand-rolled accessible chart** above its existing table on the [ADR-0007](adr/ADR-0007-accessible-charting.md)
contract: a shared **`ui/Chart` primitive** (`ChartFigure` + `LineChart`/`BarChart`/`Gauge`) + new
**`--chart-1/2/3`/`--chart-grid`** tokens. Net worth + cash-flow = **line**, spend + budget = **bar**, credit +
payoff = **gauge** — every chart `role="img"` with a one-line summary, `aria-hidden` innards, the **existing table
as the data-table fallback**, and **colour never the sole signal** (dash / marker / hatch / bar-direction / direct
labels). `e2e/a11y.spec.ts` seeds data per shape and is **axe-green LIGHT AND DARK with each chart rendered**.
**Presentation-only — no new endpoint/schema/API/domain change** (the six reads were done). Bundle **108.33 KB gz**
(< 120 KB); `ADR-0007` → **`Accepted`**; the SPIKE-07 throwaway harness was discarded (findings persist in the spike
report + ADR).
**`UX9` ✅ done** ([FEAT-UX9](features/spending-breakdown.md) · [UX](ux/spending-breakdown.md) · [status](status-reports/2026-06-28-ux9.md),
320 Vitest + 75 e2e, gate-green) — the **second build slice of the Insights phase**, a **new
visualisation**: `/insights/breakdown` ("Breakdown" tab, next to Spend) answers "**where did the money
go?**" — each envelope's **share of a chosen month's OUTFLOW** as **ranked horizontal bars** on the
[ADR-0007](adr/ADR-0007-accessible-charting.md) contract, via a new **`BreakdownBars`** shape on the
shared **`ui/Chart`** primitive. It **composes the existing `getBudgetVsActual(month)` read** (whose
`spentCents` is pure outflow — funding excluded, refunds netted — the correct breakdown numerator,
unlike spend-by-envelope's net flow) — **no new endpoint/schema/API/domain change** (R4/R5/UX5 fan-out
precedent). The hardest a11y case (many categories, no colour-only) is solved by making colour carry
**no categorical meaning at all**: one bar colour + a **direct text label** per bar (rank · name ·
outflow · share) + **rank order** + bar length. `role="img"` + one-line summary, `aria-hidden` innards,
the ranked **table** (envelope · outflow · % share) as the SR/keyboard source of truth; shares sum to
100%. `e2e/a11y.spec.ts` seeds real outflow and is **axe-green LIGHT AND DARK**. Bundle **108.93 KB gz**
(< 120 KB).
**`UX10` ✅ done** ([FEAT-UX10](features/spending-trends.md) · [UX](ux/spending-trends.md) · [status](status-reports/2026-07-01-ux10.md),
323 Vitest + 78 e2e, gate-green) — the **third build slice of the Insights phase**, a **new
visualisation**: `/insights/trends` ("Trends" tab, next to Breakdown) answers "**is my spending going
up or down?**" — a **month-over-month OUTFLOW trend** (household total + top-2 envelopes by outflow)
as a **multi-series line chart**, reusing the **existing `LineChart` shape** on the
[ADR-0007](adr/ADR-0007-accessible-charting.md) contract — no new shape. It **composes the existing
`getBudgetVsActual(month)` read, once per month in a selectable 3/6/12-month window** (`Promise.all`
— still no new endpoint/schema/API/domain change, extending the R4/R5/UX9 fan-out precedent to
several calls because that read is per-month only); pure outflow (not `getEnvelopeSpend`'s net flow)
for the same reason UX9 chose it — funding-then-spend in one month would otherwise understate the
trend. Three series (Total + top-2 envelopes), **one `--chart-*` token each** (no colour reuse), each
also carrying its own dash + marker + direct label. `role="img"` + one-line summary (direction + top
spenders), `aria-hidden` innards, an exact-figures **table** (month · total · each charted envelope)
mirroring the chart. `e2e/a11y.spec.ts` seeds outflow across two real months and is **axe-green LIGHT
AND DARK**. Bundle **109.90 KB gz** (< 120 KB).
**`UX11` ✅ done** ([FEAT-UX11](features/budget-burndown.md) · [UX](ux/budget-burndown.md) · [status](status-reports/2026-07-01-ux11.md),
335 Vitest + 81 e2e, gate-green) — the **fourth and final new-visualisation slice of the Insights
phase**: `/insights/burndown` ("Burn-down" tab, next to Budget) answers "**am I on pace this
month?**" — **within-month** budget-consumed (spent ÷ target) vs. the **elapsed-time pace**, as a
**`Gauge`** reused **as-is** on the [ADR-0007](adr/ADR-0007-accessible-charting.md) contract (no new
shape — the gauge's threshold marker is repurposed as "pace today"). It **composes the existing
`getBudgetVsActual(month)` read — ONE call, no fan-out, no new endpoint/schema/API change** (a scope
picker selects the household aggregate or one budgeted envelope from that single read); the only new
code is one **pure, unit-tested domain function** (`assessBurndown`, elapsed-day-fraction linear-burn
pace) per the pure-core/impure-shell convention. `status ∈ over-budget | over-pace | on-track`,
carried by **verdict text + marker position + a table Pace column** — never colour alone. `role="img"`
+ one-line summary, `aria-hidden` innards, an exact-figures **table** (target · spent · % · pace +
aggregate). `e2e/a11y.spec.ts` seeds a budgeted envelope with mid-month outflow and is **axe-green
LIGHT AND DARK**. Bundle **111.06 KB gz** (< 120 KB).
**`UX12` ✅ Done — all 4 threads shipped** ([FEAT-UX12a](features/destructive-confirms.md) ·
[FEAT-UX12b](features/skeleton-loaders.md) · [FEAT-UX12c](features/success-toasts.md) ·
[FEAT-UX12d](features/inline-validation.md) ·
[status](status-reports/2026-07-02-ux12c.md), 354 Vitest + 89 e2e, gate-green) — the **Polish track**.
UX12 bundles four polish threads; per *vertical, not horizontal* each ships on its own. **Thread 1 —
confirmation on destructive actions:** a new **`ConfirmDialog`** primitive (controlled/transient, on
the SPIKE-06-validated Radix `Dialog` — **no new dependency**, +0.42 KB gz) gates the three previously
one-click destructive actions — **Archive envelope, Archive account, Delete template** — with a
consistent blocking confirm (owner call: all three, not just the irreversible Delete); state via
action-word text + dialog title, **never colour**; ESC/overlay = cancel; axe-green **light AND dark**
with the dialog open. **Thread 2 — skeleton loaders:** the 16 bare `Loading…` strings across the lists
+ Insights views now render the existing UX4 **`Skeleton`** primitive (as the home `Cockpit` already
does) — a **net a11y win** (the ~9 sites with a bare `<p>` now announce politely via `role="status"`),
reduced-motion-safe, **no new dep, no new code, net −0.04 KB gz**. **Thread 3 — inline validation:**
money-amount inputs that failed *silently* (a greyed-out Save) or only at the server round-trip now
surface a **field-level** message the moment an amount can't parse — a new pure `amountFieldError`
(reusing `tryParseMoney`) drives a new **`FieldError`** primitive (`aria-invalid` + `aria-describedby`,
message text carries the state — **never colour**) across the transaction / transfer / move-money /
add-account forms; submit is guarded client-side, semantic rules stay server-authoritative. **No new
dep, no ADR**; axe-clean **light AND dark** with the error visible. **Thread 4 — success toasts:** the
last thread and the only one needing a **new dependency** — `@radix-ui/react-toast` (the last of the
hard a11y widgets ADR-0005 reserved for Radix). A new **`ToastProvider`/`useToast()`** (`ui/Toast.tsx`)
at the app root fires a transient, auto-dismissing success toast on the successful-mutation set
(create account/envelope/transaction · transfer · move · archive · delete); it announces via
**`role="status"` polite (never `role="alert"`)**, entrance is **transform-only** (no opacity fade),
reduced-motion-safe, state carried by text. Proven **axe-clean light AND dark with the toast visible**
before wiring; **no ADR** (ADR-0005 covers it). Bundle **116.31 KB gz** (+4.63 KB — the Radix Toast;
< 120 KB, ~3.7 KB headroom).

**`UX13` ✅ Done — money & budget-health visual encoding** ([FEAT-UX13](features/budget-health-encoding.md)
· [status](status-reports/2026-07-02-ux13.md), 360 Vitest + 91 e2e, gate-green). A new **`ProgressBar`**
primitive — the last UX4 seed-and-grow primitive, hand-rolled on the tokens + CSS with **no charting
dependency** (the SVG `Gauge`/`BarChart` stay for the Insights charts; the new bar surface is budget
target-vs-spent) — drives a per-row **spent-of-target** bar in the Budget table and a **summary bar** in
the cockpit budget panel; over-budget/negative money (remaining · liabilities · net worth) now carries
**weight + colour** with the minus sign kept as the non-colour signal, **superseding the V1 "minus sign
only" encoding**. The bar is **decorative** (`aria-hidden` — no new AT surface), the `over` tone lays a
diagonal **hatch SHAPE** over the danger fill, track+fills clear 3:1, and there is **no animation**
(reduced-motion-safe by construction); axe-green **light AND dark** with both the under- and
**over**-budget encoding visible. **No ADR/spike/dep**, no data/API/domain/chart change. Bundle **116.87
KB gz** (+0.56 KB, no dep; < 120 KB).
**`UX14` ✅ done** ([FEAT-UX14](features/first-run-onboarding.md) ·
[status](status-reports/2026-07-02-ux14.md), 364 Vitest + 92 e2e, gate-green) — **first-run onboarding**:
a brand-new user opening a truly empty app previously met the cockpit's **five disconnected per-panel
empty states**; now the home **derives** whether the app is completely empty (no accounts AND no
envelopes — from the ledger reads, **never a stored "has onboarded" flag**; derive-don't-store) and, when
it is, shows one guided next step — a small **`FirstRunOnboarding`** component that **composes the
existing UX4 `EmptyState`** with two CTA `<Link>`s ("Add an account" → `/accounts`, "Add envelopes" →
`/envelopes`), tied to the PRD's week-one success metric. The moment anything exists the cockpit takes
over (its per-panel empty states guide the rest). **No ADR/spike/dep**, no data/API/domain change; proven
**axe-green LIGHT AND DARK with the onboarding surface visible** (the empty-store scan runs first, before
any e2e seeding). Bundle **117.20 KB gz** (+0.33 KB, no dep; < 120 KB, ~2.8 KB headroom).
**`UX15` ✅ done** ([FEAT-UX15](features/responsive-pass.md) ·
[status](status-reports/2026-07-02-ux15.md), 366 Vitest + 94 e2e, gate-green) — the **responsive
pass**, and the **LAST UX-Uplift item**. The uplift was built desktop-first with graceful degradation
already partly baked in (the cockpit grid + Insights figure lists are `auto-fit minmax(...)` and
**already stack**; the shell nav + register rows already `flex-wrap`; the ADR-0007 chart SVGs are
`width:100%` and **already scale**), so this slice is a small **CSS/token-only** pass closing the
phone-width gaps: (1) **reflow (WCAG 1.4.10)** — wide data tables no longer overflow the page but
scroll within their own **keyboard-focusable** region (a global `.table-scroll` utility applied once
in **`ChartFigure`**, which every Insights table flows through, + the standalone envelope-ledger
table); (2) the **shell header** degrades to a narrow column (brand over the wrapping nav, comfortable
tap targets) and the **Insights sub-nav** becomes a real wrapping tab row with valid **target-size**
(WCAG 2.5.8) at any width; (3) **page padding** tightens at ≤ 640px. **Scope call (flagged):** "nav
collapses" is met by graceful degradation (the nav already wraps) — **no hamburger** (a new
interactive a11y surface, against the slice's low-risk/desktop-first appetite); a hamburger is an
available follow-up. Proven **axe-clean at 320px LIGHT AND DARK** with a wide table + the reflow
layout visible (new "responsive reflow at phone width" e2e describe asserts no horizontal page scroll
+ the focusable scroll region). **No ADR/spike/dep**, no data/API/domain/chart change. Bundle **117.27
KB gz** (+0.07 KB, no dep; < 120 KB, ~2.7 KB headroom). **This completes the Phase-4 Polish track and
the entire UX Uplift (`UX1`–`UX15`).**

---

## 1. How to use this roadmap

The plan of record, kept live. **Top = next.** Budgeteer is a **focused product/app** (the
full-kit row in [§11](00_WAYS_OF_WORKING.md)), so it runs the full chain — but every *build*
item below is a **vertical, usable slice** that passes the gate before it's `Done`; there
are no "layer" items. Item statuses track *delivery*; the `Proposed/Validated/Accepted`
ladder tracks *documents* — don't conflate them.

**Item status vocabulary:** `Planned` · `Ready` · `In progress` · `Done` · `Deferred` · `Dropped`.

## 2. Sequencing model

Adapted from [§7](00_WAYS_OF_WORKING.md), justified by what discovery + SPIKE-01 already
retired:

1. **Decisions & feasibility first.** The value/UX bet is de-risked (SPIKE-01); the
   *unretired* risk is the **unchosen stack** + unproven technical feasibility. So a tight
   **feasibility spike (SPIKE-02)** runs before the foundation, producing the stack ADRs.
2. **Foundation slice** — a usable shell (accounts + envelopes you can create) to build the
   core loop into. Security guardrails are set here, day-zero (see note).
3. **Domain slices, validated heart first** — the core enter→allocate loop (closes the
   SPIKE-01 felt-friction caveat), then the accelerators that make the paycheck painless,
   then the further capabilities.
4. **Analysis**, once the core loop produces real data to analyze.
5. **Hardening**, once there's real data/usage to measure against.

**Specs & ADRs produced as gating work** (written *with* the slice that needs them, per the
[doc map](README.md)):

| Artifact | When | Note |
| -------- | ---- | ---- |
| [`ADR-0003`](adr/ADR-0003-money-integer-minor-units.md) — money = **integer minor units** | ✅ done | **`Accepted`** (2026-06-13); demonstrated exact by SPIKE-02. |
| [`ADR-0004`](adr/ADR-0004-transfer-modeling.md) — **transfer modeling** (account legs + envelope-transfer rows) | ✅ done | **`Accepted`** (2026-06-14); **`Validated`** by [SPIKE-04](spikes/04-transfer-modeling.md); (A) realized in `#7a`, (B) in `#7b`. |
| [`ADR-0001`](adr/ADR-0001-stack.md) — stack (TS · React+Vite · Fastify) | ✅ done | **`Validated`** by [SPIKE-02](spikes/02-stack-feasibility.md); awaiting owner nod → `Accepted`. |
| [`ADR-0002`](adr/ADR-0002-datastore.md) — datastore (Postgres · Kysely) | ✅ done | **`Validated`** — schema + queries run on PGlite (Postgres-in-WASM) with 12 passing API tests; prod node-postgres path wired, confirm at deploy. |
| [`04_DOMAIN_MODEL`](04_DOMAIN_MODEL.md) · [`05_DATA_MODEL`](05_DATA_MODEL.md) | ✅ realized | **`Accepted`** — implemented + tested in the foundation slice. |
| [accounts](features/accounts.md) · [envelopes](features/envelopes.md) specs · [foundation UX](ux/foundation.md) · [`06_API_CONTRACT`](06_API_CONTRACT.md) | ✅ built | FEAT-001/002 **`Implemented`**; UX `Accepted`; API contract documented. |

> **Security, day-zero (not deferred):** the Foundation slice sets the `.gitignore` to
> exclude confidential financial data **before** any such data can exist, and all tests use
> **synthetic fixtures** ([SECURITY.md](SECURITY.md), spine §8). This is a guardrail, not a
> hardening item.

## 4. The plan

Ordered by **Risk × Value**, top = next. `Gated by` names what must land first.

> **Numbering:** the `#` column is each item's **stable id**, counting from the top of the
> plan — Foundation = `#1`, SPIKE-02 = `#2`, so the **domain slices run `#3`+**. The PRD's
> *Slice 1* / *Slice 2* are `#3` / `#4`; from `#5` on, items are referred to by their `#`.
> (So "archive an envelope" = `#6` = the 4th domain slice built.) Status-report files are
> numbered separately by report sequence (`#01`, `#02`, …).

### Foundation

| # | Item | Kind | Value | Risk | Gated by | Status | Links (will produce) |
| - | ---- | ---- | ----- | ---- | -------- | ------ | -------------------- |
| 1 | **Foundation** — app shell + Postgres store + domain core + **account CRUD** (open with a starting balance, held *unallocated*) + **envelope CRUD** (create/rename). *Usable: set up your real accounts & envelopes and see balances.* Sets the **day-zero `.gitignore` + synthetic fixtures** guardrail. | slice | High | Med | ✅ SPIKE-02 · ADR-0001/0002/0003 | **✅ Done** | Built & gate-green (31 tests + HTTP smoke); [status report](status-reports/2026-06-13-foundation-slice.md) |

### Spikes (risk retirement)

| # | Item | Kind | Value | Risk | Answers (the question) | Status | Spike report |
| - | ---- | ---- | ----- | ---- | ---------------------- | ------ | ------------ |
| 0 | Split-allocation UX | spike | High | High | Is enter-once-then-split lower-friction than the sheet, esp. the many-way splits? | **Done** | [SPIKE-01](spikes/01-split-allocation-ux.md) — *Confirmed* |
| 2 | **Technical feasibility / stack** | spike | High | High | Can the TS stack deliver **integer-minor-unit money** + the **exact split invariant** (the prior float failure)? | **Done** | [SPIKE-02](spikes/02-stack-feasibility.md) — *Confirmed* (strict typecheck + 8/8 tests); produced `ADR-0001`/`ADR-0002` |
| 4 | **Transfer modeling** (gates `#7`) | spike | High | High | How to model **account↔account** *and* **envelope↔envelope** moves without breaking the split invariant? | **Done** | [SPIKE-04](spikes/04-transfer-modeling.md) — *Confirmed* (strict typecheck + 8/8 tests); recommends additive two-primitive model → `ADR-0004` |
| 5 | **Cash-flow forecast model** (gates `#13`) | spike | Med | Med | Can we project per-account cash, and fold monthly targets in as expected spend **without double-counting**? | **Done** | [SPIKE-05](spikes/05-cashflow-forecast.md) — *Confirmed* (strict typecheck + 8/8 tests); engine projects the future unchanged; netting kills the double-count → FEAT-013 |

### Domain slices

| # | Item | Kind | Value | Risk | Gated by | Status | Notes |
| - | ---- | ---- | ----- | ---- | -------- | ------ | ----- |
| 3 | **Slice 1 — core enter→allocate loop** — enter deposit/withdrawal → allocate in **Single** (one-tap) or **Split** (multi-row, live `Allocated/Remaining`, use-remaining) + **partial allocation** (save now, "needs allocation" surface) → balances reconcile, invariant holds | slice | High | Med | #1 | **✅ Done** | Built & gate-green (44 tests + HTTP smoke); [feature](features/transactions.md)·[UX](ux/transactions.md); felt-friction caveat now exercised in-app |
| 4 | **Slice 2 — accelerators** — **templates/presets** (primary, **fixed-amount** lines) · keyboard-first row entry · distribute-remainder | slice | High | Med | #3 | **✅ Done** | Built & gate-green (55 tests + HTTP smoke); [feature](features/templates.md)·[UX](ux/templates.md) |
| 5 | **Edit a past split** (preserve the sum invariant) | slice | High | Med | #3 | **✅ Done** | Built & gate-green (57 tests + HTTP smoke); reuses editor + `PUT allocations`; [feature](features/edit-split.md)·[UX](ux/edit-split.md) |
| 6 | **Archive an envelope** (soft-delete; history preserved) | slice | Med | Low | #1 | **✅ Done** | Built & gate-green (62 tests + HTTP smoke); archive/unarchive + picker filtering; [feature](features/archive-envelope.md)·[UX](ux/archive-envelope.md) |
| 7a | **Transfer** (account↔account, **double-entry**) | slice | Med | High | #3 · ✅ SPIKE-04 | **✅ Done** | Built & gate-green (73 tests + HTTP smoke); `transfers` parent + `kind:'transfer'` legs, exempt from needs-allocation; [ADR-0004](adr/ADR-0004-transfer-modeling.md)·[feature](features/transfers.md)·[UX](ux/transfers.md) |
| 7b | **Reallocation** (envelope↔envelope) | slice | Med | Med | #3 · ✅ SPIKE-04 · 7a | **✅ Done** | Built & gate-green (82 tests + HTTP smoke); `envelope_transfers` table + two-source `v_envelope_balances`; into-archived blocked, drain/negative allowed; [ADR-0004](adr/ADR-0004-transfer-modeling.md)·[feature](features/transfers.md)·[UX](ux/transfers.md) |
| 8 | **Refunds** (negative allocation within a split) | slice | Med | Med | #3 | **✅ Done** | Built & gate-green (93 tests + HTTP smoke); per-row **Refund** toggle, signed-total invariant (no schema change); [feature](features/refunds.md)·[UX](ux/refunds.md). Resolved the "negative rows?" open Q |
| 9 | **Recurring transactions** | slice | Med | Low | #3 | **✅ Done** | Built & gate-green (106 tests + HTTP smoke); rule + split + schedule, idempotent **Post due** generator; [feature](features/recurring.md)·[UX](ux/recurring.md) |
| 10 | **Reconcile to bank** (manual balance compare) | slice | Med | Low | #1 | **✅ Done** | Built & gate-green (120 tests + HTTP smoke); plain compare + recorded history; resolved the cleared-vs-compare open Q (plain compare); [feature](features/reconcile.md)·[UX](ux/reconcile.md) |
| 11 | **Analysis — spend by envelope over time** (monthly/annual rollups) | slice | Med | Low | real data from #3 | **✅ Done** | Built & gate-green (135 tests + 1 e2e); generated net-flow grid, reallocations excluded, archived included; **no schema change** (read-only aggregate query); [feature](features/analysis-envelope-spend.md)·[UX](ux/analysis-envelope-spend.md) |
| 12 | **Analysis — budget vs. actual** | slice | Med | Med | envelope **monthly targets** capability | **✅ Done** | Built & gate-green (151 tests + 1 e2e); new `envelope_targets` store (one recurring monthly target, set/cleared inline) + sibling `budgetVsActual(month)` read (actual = **outflow** spend, funding excluded, refunds netted); resolved PRD §9 target Q (single recurring, no ADR); shipped a latent CORS allow-methods fix; [feature](features/budget-vs-actual.md)·[UX](ux/budget-vs-actual.md) |
| 13 | **Analysis — cash-flow forecast** (pay-period projection) | slice | Med | Med | #3 · #9 · ✅ SPIKE-05 | **✅ Done** | Built & gate-green (168 tests + 1 e2e); per-account event-stepped cash projection from `v_account_balances` + scheduled recurring + netted expected-spend toggle; pure domain `cashFlowForecast`, **no schema change**; de-risked by [SPIKE-05](spikes/05-cashflow-forecast.md); [feature](features/cash-flow-forecast.md)·[UX](ux/cash-flow-forecast.md) |
| 14a | **Analysis — credit utilization** (owed vs. limit · trend · roll-up) | slice | Med | Med | `kind='credit'` accounts + a credit-limit store | **✅ Done** | Built & gate-green (192 tests + 1 e2e); new `credit_limits` store (one limit per credit account, set/cleared inline) + pure-domain `creditUtilization` (owed = −balance; owed/limit in bps; cumulative monthly trend; portfolio roll-up); resolved the credit-balance **sign** open Q (owed = −balance at the analysis layer); [feature](features/credit-utilization.md)·[UX](ux/credit-utilization.md) |
| 14b | **Analysis — debt payoff %** (installment loans: 1 − owed/original) | slice | Med | Med | a `kind='loan'` account + an original-principal store | **✅ Done** | Built & gate-green (214 tests + 1 e2e); new **`kind='loan'`** account type (idempotent kind-check evolution) + `loan_principals` store (one original per loan, set/cleared inline) + pure-domain `debtPayoff` (owed = −balance; payoff = 1 − owed/original in bps, truthful; cumulative monthly trend; portfolio roll-up); completes the analysis area; [feature](features/debt-payoff.md)·[UX](ux/debt-payoff.md) |
| R6 | **Transaction delete** — no way to remove a mis-entered `normal` transaction or a transfer pair; edit-split (`#5`) handles corrections-in-place but a duplicate or completely wrong entry has no corrective path | slice | High | Med | #3 · R14 | **✅ Done** | Hard delete. `DELETE /transactions/:id` (normal/opening only; 409 for transfer legs) + `DELETE /transfers/:id` (cascades both legs + allocations via DB FK). Delete buttons on every row in `AccountRegister`. **235 Vitest + 38 e2e.** [status report](status-reports/2026-06-21-r6.md) |
| R7 | **Account archive / close** — `archived_at` exists on accounts in the schema and `AccountView.archivedAt` is already in `api.ts`, but no service method or UI exposes it | slice | Med | Low | #1 · R14 | **✅ Done** | Mirrors the envelope-archive pattern (`#6`): `accountService.setArchived` + `POST /accounts/:id/archive` / `/unarchive`; Dashboard `AccountList` splits active/archived with a "Show archived" toggle; archived accounts stay viewable (register + transaction delete work) — only **new** transfers reject them (existing guard). No schema change. **236 Vitest + 39 e2e.** [status report](status-reports/2026-06-21-r7.md) |
| R8 | **Transaction search / filter** — the account register loads all transactions with no server-side filter; as data grows the list becomes unbounded | slice | Med | Low | #3 · R14 | **✅ Done** | Date-range query params (`?from=YYYY-MM-DD&to=YYYY-MM-DD`, default = current month) on `GET /accounts/:id/transactions` + From/To inputs and a client-side payee/memo search box on the register. **Challenge resolved:** `opening` rows are always included (the register's balance anchor), and needs-allocation surfacing is a separate household-wide endpoint so it is unaffected by the window. No schema change; `occurredOn` already stored. **240 Vitest + 40 e2e.** [status report](status-reports/2026-06-22-r8.md) |
| R9 | **Net worth over time analysis** — analysis covers envelopes and credit/loan trends but has no account-level trend; the "how am I doing overall?" picture is missing | slice | Med | Low | real data from #3 · R14 | **✅ Done** | Monthly/annual `Σ account balances` derived from existing transaction data (same aggregate-query pattern as `#11`). New `GET /analysis/net-worth?grain=month\|year` + thin `NetWorthView` (Assets / Liabilities / Net trend). **Challenge resolved:** net worth = `Σ` of all **signed** balances — liabilities (`kind ∈ {credit, loan}`) sum in negative (the FEAT-014a/b `owed = −balance` convention), so `net = assets + liabilities` and transfer legs cancel; decomposed **by kind** (stable over time), not by sign. Pure-domain `netWorthOverTime`; **no schema change** (read-only aggregate). Extended `e2e/analysis.spec.ts` (+1) + `e2e/a11y.spec.ts` (+1). **258 Vitest + 42 e2e.** [status report](status-reports/2026-06-22-r9.md) |
| R15 | **Envelope ledger** — the Dashboard shows each envelope's derived balance but gives no way to drill into what's in it; you can't answer "what transactions touched Groceries this month?" without going through the Spend-by-Envelope analysis view | slice | Med | Low | #3 · R14 | **✅ Done** | Read-only per-envelope allocation list: `GET /envelopes/:id/ledger` (allocations ⋈ transactions ⋈ accounts, newest-first, household-scoped) + `EnvelopeLedger.tsx` panel opened by clicking an envelope name on the Dashboard. No schema change. Opening- and transfer-kind rows included; envelope-transfer reallocations out of scope for V1. **e2e/envelopes.spec.ts extended (+1 test).** [feature](features/envelope-ledger.md)·[UX](ux/envelope-ledger.md)·[status report](status-reports/2026-06-21-r15.md) |

### Engineering health (from the [2026-06-15 review](reviews/2026-06-15-repo-review.md))

Cross-cutting cleanups, **sequenced before the analysis area** (`#11`): analysis adds more web
money-formatting + read services, so the dedup/extraction is cheapest now. Do **EH1 → EH6 in
order**, then `#11`+. No live bugs — these are coverage + drift-prevention.

| # | Item | Kind | Pri | Status | Notes |
| - | ---- | ---- | --- | ------ | ----- |
| EH1 | **Share the domain in the web** — import money/format/date from `@budgeteer/domain`; delete the `format.ts` reimpl + `fakeApi` date mirror | refactor | P1 | **✅ Done** | Added `tryParseMoney` (single `DECIMAL_RE` home) + moved `splitEvenly` to domain; widened `formatMoney`; web declares the dep + imports it; kept only the `$`-display `formatCents` (presentation). 121 tests; Vite dev+build consume the workspace TS. [status report](status-reports/2026-06-15-eh1.md) |
| EH2 | **Extract API service plumbing** — shared `dates` util + generic `groupBy`; move `DEFAULT_HOUSEHOLD_ID` out of `db/migrate.ts` into a constants/config module | refactor | P2 | **✅ Done** | New `util/dates.ts` + `util/groupBy.ts` + top-level `constants.ts`; date converters deduped (5→1, dropped a dead `todayStr`), Map-by-parent loop replaced (×4 incl. transfer legs), household const decoupled from the migrator (9 importers). No behavior change; 121 tests. [status report](status-reports/2026-06-15-eh2.md) |
| EH3 | **Map DB unique-violation → 409** + typed route params | fix | P2 | **✅ Done** | New `services/dbErrors.ts` (`isUniqueViolation`/`asDuplicateName`) maps SQLSTATE `23505` → `DuplicateNameError` (→409) at the service layer; wraps the 6 name-writing methods. All 12 `req.params as {…}` casts → Fastify route generics. 125 tests (+4, incl. real-PGlite violation). [status report](status-reports/2026-06-15-eh3.md) |
| EH4 | **Add ESLint to the gate** (`@typescript-eslint` + `react-hooks`); remove now-dead `eslint-disable` comments | tooling | P3 | **✅ Done** | ESLint 9 flat config + `npm run lint` (zero-warning gate); caught & fixed a real rules-of-hooks error (`useRemaining`→`fillRemaining`); disables now meaningful + made consistent. 125 tests. [status report](status-reports/2026-06-15-eh4.md) |
| EH5 | **Browser e2e (Playwright)** — minimal now (dashboard loads vs. real API + 1 journey); **full journeys and per-area split land in `R14`** | hardening | P1 | **✅ Done** | Playwright (Chromium-only) drives the real web app vs. the real API: dashboard loads (the CORS-class check) + one journey (account → envelope → deposit allocated → derived envelope balance). New `test:e2e` gate step, kept out of `npm test`. 125 Vitest + **1 e2e**. [status report](status-reports/2026-06-15-eh5.md) |
| EH6 | **Repo hygiene** — discard the absorbed `spikes/04-transfer-modeling/`; record accepted notes (bigint→Number; normalize-vs-btrim) | chore | P3 | **✅ Done** | Removed the throwaway spike code (no importer; not a workspace; tooling already excluded `spikes/`); findings persist in `ADR-0004` + `#7a`/`#7b` + the [spike report](spikes/04-transfer-modeling.md). Two notes recorded as accepted (no behavior change): bigint→`Number()` exact to ~$90T in `money.ts`/`05_DATA_MODEL`; `normalizeName` ⊃ DB `lower(btrim())` in `naming.ts`/`05_DATA_MODEL`. No test-count change (125 + 1 e2e). [status report](status-reports/2026-06-15-eh6.md) |

### Engineering health (from the [2026-07-02 architecture review](reviews/2026-07-02-architecture-review.md))

Taken at UX-Uplift completion. No live money/invariant bugs; the themes are **time obtained
impurely** (EH7 is the root cause of the carried red recurring test), **operational readiness
lagging feature completeness** (restore, migrations, network exposure), and **boundary rules held
by convention only**. Full findings + rationale in the review; suggested order there
(EH7 → EH11 → EH8 → EH10 → EH9/EH14 → EH12/EH13) — **sequencing is the owner's call**.

| # | Item | Kind | Pri | Status | Notes |
| - | ---- | ---- | --- | ------ | ----- |
| EH7 | **Inject the clock into services** — `todayStr()`/`currentMonthRange()` reach for `new Date()` inside `recurringService` + the register's default window; the carried **failing** `recurring.test.ts` post-due case is the symptom. Thread `now?: () => Date` through `buildServer`/service factories; make the recurring fixtures absolute; fold in the flaky forecast-e2e selector fix. Retires both carries properly and restores a fully green gate. | fix | P1 | **✅ Done** | `buildServer({ clock })` → 3 services + every route plugin; `todayStr`/`currentMonthRange` **require** the clock; `createTestApp({ today })` pins it; both carries retired (root cause: the register's current-month default window clipped relative-date fixtures on days ≤ 21; the e2e flake: non-exact `getByLabel` matched the chart aria-label → `exact: true`). **Gate fully green: 367/367 Vitest + 94/94 e2e.** [status report](status-reports/2026-07-02-eh7.md) |
| EH8 | **Decide + document the timezone policy** — "today"/"this month" are UTC-derived on server (`util/dates.ts`) and client (8 components); every default shifts a day/month early for users west of UTC from ~evening on. Client derives local calendar dates; server stops deriving "today" for user-facing defaults (rides EH7's seam). Record in `04_DOMAIN_MODEL`. | fix | P2 | Proposed | User-visible correctness for a budgeting app's calendar dates |
| EH9 | **Versioned migrator** — `migrate.ts`'s single idempotent migration has outgrown its own "replace me when the schema evolves" premise (drop/re-add kind checks, defensive `add column if not exists`). Adopt Kysely `Migrator`; freeze today's file as `0001-baseline`. **Must land before real-Postgres data or `#15b`.** | refactor | P2 | Proposed | `05_DATA_MODEL` updated in the same change |
| EH10 | **Prove restore (`#15b` re-raise)** — the export has never round-tripped; `07_NFR` names it the only recovery path while "Restore proven" is unchecked and real data accumulates in `PGLITE_DIR`. Spike (FK order, ID collisions, versioning) → minimal CLI restore + a gate test proving `export → restore → export` equivalence. UI can wait; the proof can't. | hardening | P2 | Proposed | Re-raise of the owner's 2026-06-17 deferral, now that the yielding roadmap is complete |
| EH11 | **Bind localhost by default** — the API hardcodes `host: "0.0.0.0"` with no auth: the household ledger is writable by any device on the LAN (CORS doesn't stop `curl`). Add validated `HOST` config, default `127.0.0.1`; opting into `0.0.0.0` is the documented trigger to pull `#19` forward. | fix | P2 | Proposed | SECURITY.md default-deny; narrows the 2026-06-15 "anyone reaching :3001" acceptance |
| EH12 | **Share the DTO types; decide the client-boundary stance** — web `api.ts` hand-duplicates the service view interfaces (`AccountView` et al.) and trusts responses via one `as T` cast. Export view types from the api workspace (types-only) and import them in web; then either add a light runtime check on reads (measure vs. the ~2.7 KB bundle headroom) or record "client trusts the typed contract" in `06_API_CONTRACT`. | refactor | P3 | Proposed | EH1's two-sources-of-truth lesson, one layer up |
| EH13 | **Lint the layer boundaries** — ARCHITECTURE §2 prefers tooling-enforced boundaries; ESLint (EH4) is syntactic only. Per-zone `no-restricted-imports`: domain bans fastify/kysely/pg/react/node:*; web bans kysely/pg; `http/routes/**` bans direct `db/*`. ~20 lines of config; erosion fails the gate instead of the next review. | tooling | P3 | Proposed | — |
| EH14 | **Make post-due idempotency structural** — concurrent `postDue` calls can double-post (both read the cursor pre-commit). Add `unique (recurring_id, occurred_on) where recurring_id is not null`; treat the violation as already-posted via the EH3 `dbErrors` shim. Bundle with EH9's first migration file. | hardening | P3 | Proposed | Load-bearing once posting is scheduled or multi-device (`#19`) |

Watch items (no action, keep visible — see the review): Insights read fan-out (up to 12 calls);
`perf.test.ts` flaking under machine load inside the unit gate; ~2.7 KB bundle headroom;
`@budgeteer/domain` ships raw TS (fine while consumers are in-repo).

### UX polish (from the 2026-06-17 improvement review)

Quick wins and small UI improvements. Items R1–R3 require **zero API work** (endpoints already exist
and are tested); R4–R5 are thin derived-data additions with no schema change. Do these opportunistically
around the hardening track — they are not gated on each other.

| # | Item | Kind | Pri | Gated by | Status | Notes |
| - | ---- | ---- | --- | -------- | ------ | ----- |
| R1 | **Account rename in UI** — `PATCH /accounts/:id` is implemented and tested; `api.ts` has no `renameAccount` method and the Dashboard has no rename control. Envelopes already expose inline rename. | polish | P1 | — (API done) | **✅ Done** | `renameAccount` in `api.ts` + `fakeApi.ts`; `AccountList` inline rename (Rename button + input + Save); `refreshAccounts` in Dashboard. `exact: true` convention applied across all e2e specs. +1 e2e. **227 Vitest + 24 e2e.** [status report](status-reports/2026-06-21-r1.md) |
| R2 | **Needs-allocation count badge** — the "Needs allocation" header button has no count, so its urgency is invisible until clicked | polish | P1 | — (API done) | **✅ Done** | The Dashboard fetches `api.listNeedsAllocation()` once on mount (**independently** of the core account/envelope load, so a badge failure degrades to "no badge" rather than blanking the page) and renders the count as a red pill on the "Needs allocation" button. **Challenge resolved:** front-end fetch of the existing endpoint over a new count aggregate (zero API work; same single round-trip the `NeedsAllocation` view already makes; no N+1). Freshness is automatic — `App` remounts the Dashboard on back-navigation, so completing an allocation and returning refreshes the count. Accessible: the count is in the button's `aria-label` ("Needs allocation (3)"), and the pill is white-on-`#991b1b` (~8.3:1, passes AA — validated by the existing Dashboard axe scan). `Dashboard.test.tsx` +2 (exact count vs. a clean fake; hidden when caught up); `e2e/transactions.spec.ts` +1 (count renders accessibly + links to the right list). **262 Vitest + 44 e2e.** [status report](status-reports/2026-06-22-r2.md) |
| R3 | **Grouped navigation — unified Analysis section** — the header had grown a long flat row of analysis-view buttons (Spend, Budget, Forecast, Credit, Payoff, **Net worth**) that required full back-navigation between them | polish | P2 | — | **✅ Done** | New thin `AnalysisSection` container groups all **6** analysis views behind one Dashboard "Analysis" entry: a `<nav aria-label="Analysis views">` sub-nav of buttons (`aria-current="page"` on the active one) that **mounts only the active view**, so each view keeps its own `<h1>` + its own data fetch (no eager fetch-all-six) and its own `← Dashboard` exit. **Challenge resolved:** chose semantically-correct nav buttons over a `role="tablist"` widget (each view is a full `<main>`/`<h1>` page, not one swappable panel); grouped Net worth too (R9 added it after R3 was written). The 6 views are untouched — only `App.tsx`/`Dashboard.tsx` changed. No transition added → nothing for `prefers-reduced-motion` to gate. `AnalysisSection.test.tsx` +3; `e2e/analysis.spec.ts` + `e2e/a11y.spec.ts` re-pointed to navigate via Analysis → tab (forecast test now switches tab-to-tab in-section). No schema/API/domain change. **265 Vitest + 44 e2e.** [status report](status-reports/2026-06-22-r3.md) |
| R4 | **Dashboard net worth summary** — accounts are listed individually but never summed; no aggregate gives the "how am I doing?" snapshot | polish | P2 | — | **✅ Done** | The current-snapshot sibling of `R9`. A `NetWorthSummary` table below the account list shows Total assets / Total liabilities / Net worth, computed **client-side** from the already-loaded account list — no new endpoint. **Challenge resolved:** the sketched **sign-based** split (`Σ balance>0`/`<0`) was rejected for `R9`'s **kind-based** split — it reuses the shared `isLiabilityKind` (credit/loan = liabilities, carried negative) so the snapshot agrees with `NetWorthView`. Sums **all** accounts (active + archived) to match `R9`'s endpoint, and is equal to it by construction (same transactions via `v_account_balances`, same classifier). Extended `e2e/accounts.spec.ts` (+1) + `Dashboard.test.tsx` (+2). **260 Vitest + 43 e2e.** [status report](status-reports/2026-06-22-r4.md) |
| R5 | **Envelope targets visible inline** — monthly targets exist (`#12`) but are only visible in the Budget view; reaching them from the daily-use Dashboard requires a navigation hop | polish | P3 | #12 (done) | **✅ Done** | Each **active** envelope's **target · spent · remaining** shows inline in its Dashboard envelope-list row. The Dashboard fetches `getBudgetVsActual(currentMonth)` on mount **independently** of the core account/envelope load (mirrors the `R2` badge — a target-fetch failure degrades to "no inline figures", never blanks the page), then joins to active rows by `envelopeId`. **Challenge resolved:** rendered **only** when a target is set (no faked `$0`); archived list + R15 envelope-name click target + existing row controls untouched; money via `formatCents`; figures are plain labelled text (not colour-only). Display-layer addition only — no schema/API/domain change. `Dashboard.test.tsx` +2, `e2e/envelopes.spec.ts` +1. **267 Vitest + 45 e2e.** [status report](status-reports/2026-06-22-r5.md) |

### Developer experience (from the 2026-06-17 improvement review)

Cross-cutting developer-loop improvements. R10 is a one-liner. **R14 is sequenced before
`#15`** — it is the e2e foundation all new slices (R6–R9) build into. R11–R13 are best
tackled alongside a new feature. No behavior change for any item.

| # | Item | Kind | Pri | Status | Notes |
| - | ---- | ---- | --- | ------ | ----- |
| R10 | **API watch mode** — the API dev server (`npm run start`) has no file-watching; every API change requires a manual restart during development | DX | P1 | **✅ Done** | `"dev": "tsx watch src/index.ts"` already in `apps/api/package.json` from a prior session — no work needed. |
| R11 | **Route modularization** — `apps/api/src/http/server.ts` is 682 lines with all 40+ routes inline; adding a new route to any area requires reading the whole file | DX | P2 | **✅ Done** | Split into **9 per-domain Fastify plugin modules** (`http/routes/{accounts,envelopes,transactions,reconcile,transfers,recurring,analysis,templates,backup}.ts`) + a `routes/shared.ts` (error envelope `fail`, money/date helpers, `IdParams`/`AccountIdParams` generics, `Services` container type), each `app.register`'d from `buildServer` with `{ services }`. `buildServer` 682→**104 lines** (keeps CORS + body parser + the one error handler + `/health`). **Challenge resolved:** no Fastify `prefix` (domains share crossing URL roots) → **every literal path unchanged**; the global error/CORS/parser are root-level (inherited by encapsulated children); services constructed once. Pure DX refactor, no behavior/schema/API change. **267 Vitest + 45 e2e** (unchanged — the regression net held). [status report](status-reports/2026-06-22-r11.md) |
| R12 | **React error boundaries** — no `ErrorBoundary` exists; a render error in any view crashes the entire app to a blank screen | DX | P2 | **✅ Done** | One reusable `ErrorBoundary` (`apps/web/src/ErrorBoundary.tsx`) placed twice: top-level wrapping `<App>` in `main.tsx` (recovery = reload) **and** per active analysis view in `AnalysisSection` (`key={tab}`; recovery = `← Dashboard`). Accessible fallback — `<main>` + `<h1>` + `role="alert"` + a real button, a high-contrast card (`#1a1a1a`/`#fff`, `colorScheme: light`) readable in dark mode. **Challenge resolved:** catches **render/lifecycle** throws only (async/fetch already degrade via `loadError`), so it protects the one unprotected failure — a render crash that `main.tsx`'s direct `<App/>` render would blank. e2e-reachable via a **dev-only** `?boom` hook (`import.meta.env.DEV`-gated → tree-shaken from the prod build; verified). `ErrorBoundary.test.tsx` +3, `e2e/error-boundary.spec.ts` +1, `e2e/a11y.spec.ts` +1. Zero API/schema/domain change. [status report](status-reports/2026-06-23-r12.md). **270 Vitest + 47 e2e.** |
| R13 | **Structured API logging** — `buildServer` defaulted to `logger: false`; the API emitted no request/response logging. Fastify bundles `pino` at zero cost. | DX | P3 | **✅ Done** | `buildServer`'s `logger` widened `boolean`→`FastifyServerOptions["logger"]`; `index.ts` passes `{ logger: { level: config.LOG_LEVEL } }` → Fastify bundled-pino request/response/error lines at the configured level. `LOG_LEVEL` tightened to a **closed enum** of pino levels, validated at the config boundary (fail-loud, spine §8). Tests omit the option → `logger: false` → quiet + deterministic. **Challenge resolved:** keep Fastify's **default** serializers (log `method`/`url`/`statusCode`/`responseTime` + error type/message/stack — never bodies/headers) and add **no** body serializer, so financial bodies (transaction memo/payee/amount, the `/export` snapshot) never reach the logs (SECURITY.md §1/§5). Regression net = a capture-stream test asserting the access line is present *and* a money-route body/`authorization` header never leak, + a `config.test.ts` fail-loud check; real-startup smoke confirmed structured JSON to stdout with a `reqId` correlation id. No API-contract/schema/domain change. `logging.test.ts` +2, `config.test.ts` +3. **275 Vitest + 47 e2e.** [status report](status-reports/2026-06-23-r13.md) |
| R14 | **e2e expansion** — `e2e/journey.spec.ts` is a single growing file chaining 8+ steps; no full journey coverage exists for features shipped before `#11` (transactions, templates, transfers, refunds, recurring, reconcile); failures are slow to isolate; the monolith grows with every new slice | hardening | P1 | **✅ Done** | `journey.spec.ts` deleted; replaced by 8 per-area files + `e2e/setup.ts` helper. 20 Playwright tests (was 1): accounts (2), envelopes (1), transactions (5), templates (2), transfers (2), recurring (1), reconcile (2), analysis (5). Full coverage for all features #3–#14b. Convention established: every new slice lands with its own spec. [status report](status-reports/2026-06-17-r14.md) |

### UX Uplift (from the [2026-06-25 UX uplift initiative](reviews/2026-06-25-ux-uplift-initiative.md))

A **foundation-first, desktop-first** overhaul of the experience — a design system + client
routing/app-shell, a **budget-and-future-planning landing cockpit**, and **new ways to visualize
spending**. It **reverses the repo's deliberate "no design system" stance**, so it is
**spike-first / ADR-gated**: `UX1` (design-system + routing → `ADR-0005`/`ADR-0006`) and `UX2`
(accessible charting → `ADR-0007`) must validate the approach on **one real screen** before any
Phase-1 build slice begins. Phase-4 polish (`UX12`–`UX15`) is woven into each migrating slice but
tracked so none is dropped. The full decomposition (per-item scope, riskiest assumption, and
acceptance) lives in the [initiative brief](reviews/2026-06-25-ux-uplift-initiative.md); every item
produces its UX/feature spec (and the spikes their ADRs) as gating work.

| # | Item | Phase | Kind | Value | Risk | Gated by | Status |
| - | ---- | ----- | ---- | ----- | ---- | -------- | ------ |
| UX1 | **Spike: design-system + routing foundation** — bake-off on the Account Register → **chose React Router + design tokens/CSS-Modules + Radix Primitives** (both primitive libs axe-clean; Radix < half the bundle, so the lean flipped from React Aria). `ADR-0005` + `ADR-0006` **`Validated`** | 0 Foundation | spike | High | High | — | **✅ Done** — [SPIKE-06](spikes/06-design-system-routing.md) · [ADR-0005](adr/ADR-0005-frontend-design-system.md) · [ADR-0006](adr/ADR-0006-client-routing.md) |
| UX2 | **Spike: accessible charting / viz a11y** — built one real chart (net-worth-over-time) → **0 axe violations** (WCAG 2.2 AA, light+dark × table-shown/hidden), keyboard + SR via a data-table fallback; **hand-rolled SVG 1.94 KB gz vs Recharts 129 KB gz** → a library blows the budget. `ADR-0007` **`Validated`** | 0 Foundation | spike | High | High | — | **✅ Done** — [SPIKE-07](spikes/07-accessible-charting.md) · [ADR-0007](adr/ADR-0007-accessible-charting.md) |
| UX4 | **Design-system foundation** (tokens + base layer + primitives · ADR-0005) — **seed-and-grow**: token sheet + dark mode + primitives (Button/Field/Input/Select/Badge/Card/Alert/EmptyState/Skeleton), **proven by restyling the Account Register in place**; Dialog/Table deferred to the first modal/table slice. [FEAT-UX4](features/design-system.md) | 1 Shell | slice | High | Med | UX1 ✅ | **✅ Done** — [status](status-reports/2026-06-26-ux4.md) |
| UX3 | **Routing + persistent app shell** (React Router · ADR-0006) — retire `App.tsx`'s `view` state machine; URL-addressable, back/forward, refresh-safe, deep-linkable; reduced-motion-aware | 1 Shell | slice | High | Med | UX1 ✅ · after UX4 | **Ready** |
| UX5 | **Home: budget + future-planning cockpit** — this-month envelope health + upcoming recurring + cash-flow snapshot + needs-allocation + net-worth, composed from existing reads ([FEAT-UX5](features/cockpit.md)·[UX](ux/cockpit.md)) | 2 Cockpit | slice | High | Med | UX3 · UX4 | **✅ Done** — [status](status-reports/2026-06-27-ux5.md) |
| UX6 | **Demote management to a dedicated surface** — account/envelope CRUD onto the `/accounts`·`/envelopes` LIST routes (progressive add, name-as-Link) + the cross-cutting `/manage` hub (net-worth summary + Move-money); home becomes cockpit-only ([FEAT-UX6](features/manage.md)·[UX](ux/manage.md)) | 2 Cockpit | slice | Med | Low | UX3 · UX4 · UX5 | **✅ Done** — [status](status-reports/2026-06-28-ux6.md) |
| UX7 | **Global quick-add transaction** — always-available entry (modal route) for PRD journey #1 (the buried common case); reuses the register editor + an account picker, fans out to existing endpoints; new `Dialog` primitive (Radix) ([FEAT-UX7](features/quick-add-transaction.md)·[UX](ux/quick-add-transaction.md)) | 2 Cockpit | slice | High | Med | UX3 · UX4 · UX5 | **✅ Done** — [status](status-reports/2026-06-28-ux7.md) |
| UX8 | **Analysis → Insights migration** — rename + restyle the 6 views, replace number grids with accessible charts (chart + table fallback, per ADR-0007: hand-rolled SVG + a shared `Chart` primitive) | 3 Insights | slice | High | Med | UX2 ✅ · UX3 ✅ · UX4 ✅ | **✅ Done** — [FEAT-UX8](features/insights-charts.md)·[UX](ux/insights-charts.md)·[status](status-reports/2026-06-28-ux8.md); shared `ui/Chart` (line/bar/gauge) on the ADR-0007 contract; axe-green light+dark with each chart rendered; **315 Vitest + 72 e2e**, 108.33 KB gz; ADR-0007 → `Accepted` |
| UX9 | **New viz: spending breakdown** — "where did the money go" composition for a period (per-envelope share of outflow) | 3 Insights | slice | High | Med | UX2 · UX8 | **✅ Done** — [FEAT-UX9](features/spending-breakdown.md)·[UX](ux/spending-breakdown.md)·[status](status-reports/2026-06-28-ux9.md); new `/insights/breakdown` ranked-bars view on the `ui/Chart` primitive (new `BreakdownBars` shape); **composes the existing `getBudgetVsActual` read** (no new endpoint); colour carries no categorical meaning (one bar colour + direct labels + rank); axe-green light+dark; **320 Vitest + 75 e2e**, 108.93 KB gz |
| UX10 | **New viz: spending trends over time** — per-envelope + total trend lines, month-over-month comparison | 3 Insights | slice | High | Med | UX2 · UX8 | **✅ Done** — [FEAT-UX10](features/spending-trends.md)·[UX](ux/spending-trends.md)·[status](status-reports/2026-07-01-ux10.md); new `/insights/trends` line-chart view (reuses `LineChart`, no new shape) — total + top-2 envelopes by outflow over a 3/6/12-month window; **composes the existing `getBudgetVsActual` read, several times** (no new endpoint); 3 series, one chart token each, colour never sole signal; axe-green light+dark; **323 Vitest + 78 e2e**, 109.90 KB gz |
| UX11 | **New viz: budget burn-down** — within-month pace vs. target (on-track projection) | 3 Insights | slice | Med | Med | UX2 · UX5 | **✅ Done** — [FEAT-UX11](features/budget-burndown.md)·[UX](ux/budget-burndown.md)·[status](status-reports/2026-07-01-ux11.md); new `/insights/burndown` **gauge** view (reuses `Gauge`, no new shape) — within-month budget-consumed (spent ÷ target) vs. the **elapsed-time pace** (threshold marker = "pace today"); **composes the existing `getBudgetVsActual` read, ONE call** (scope picker: household aggregate or one envelope) + one new **pure domain fn** (`assessBurndown`); `over-budget`/`over-pace`/`on-track` via text + position, never colour; axe-green light+dark; **335 Vitest + 81 e2e**, 111.06 KB gz |
| UX12 | **Feedback & states** — skeletons, success toasts, **confirm on destructive actions** (Archive/Delete), inline validation | 4 Polish | slice | Med | Low | UX4 | **✅ Done** — all 4 threads shipped. **UX12a** [FEAT-UX12a](features/destructive-confirms.md)·[status](status-reports/2026-07-02-ux12.md): new **`ConfirmDialog`** primitive (controlled/transient, on the validated Radix `Dialog` — no new dep) gates the three one-click destructive actions (**Archive envelope · Archive account · Delete template**); consistent blocking confirm, state via action-word text + title (never colour), axe-green light+dark with the dialog open. **UX12b** [FEAT-UX12b](features/skeleton-loaders.md)·[status](status-reports/2026-07-02-ux12b.md): the 16 bare `Loading…` strings → the existing UX4 **`Skeleton`** primitive (net a11y win — ~9 sites now announce via `role="status"`; no new dep/code, net −0.04 KB gz). **UX12d** [FEAT-UX12d](features/inline-validation.md)·[status](status-reports/2026-07-02-ux12d.md): money-amount inputs that failed silently/at-submit now surface a **field-level** error (new pure `amountFieldError` reusing `tryParseMoney` → new **`FieldError`** primitive: `aria-invalid` + `aria-describedby`, text carries the state never colour) across the transaction/transfer/move/add-account forms; submit guarded client-side, semantic rules stay server-authoritative; no new dep; axe-green light+dark with the error visible. **UX12c** [FEAT-UX12c](features/success-toasts.md)·[status](status-reports/2026-07-02-ux12c.md): new **`ToastProvider`/`useToast()`** on **`@radix-ui/react-toast`** (the last hard a11y widget ADR-0005 reserved for Radix — the only thread needing a new dep) fires a transient success toast on the successful-mutation set (create/transfer/move/archive/delete); announces **`role="status"` polite (never alert)**, transform-only entrance, reduced-motion-safe, text carries the state; proven axe-green light+dark with the toast visible. **354 Vitest + 89 e2e**, 116.31 KB gz (+4.63 KB Radix Toast). |
| UX13 | **Money & budget-health visual encoding** — progress bars + weight/color (never color-only) for over-budget / utilization / payoff / net worth | 4 Polish | slice | Med | Low | UX4 | **✅ Done** — [FEAT-UX13](features/budget-health-encoding.md)·[status](status-reports/2026-07-02-ux13.md); new **`ProgressBar`** primitive (the last UX4 seed-and-grow primitive — hand-rolled on tokens + CSS, **no charting dep**; the SVG `Gauge`/`BarChart` stay for the Insights charts) drives a per-row **spent-of-target** bar in the Budget table + a **summary bar** in the cockpit budget panel; over-budget/negative money (remaining · liabilities · net worth) gets **weight + colour** with the minus sign kept as the non-colour signal (supersedes the V1 "minus sign only" encoding). Bar is **decorative** (`aria-hidden` — no new AT surface), the `over` tone adds a diagonal **hatch SHAPE**, track+fills clear 3:1, **no animation**; axe-green light+dark with both the under- and **over**-budget encoding visible. No ADR/spike/dep, no data/API/domain/chart change. **360 Vitest + 91 e2e**, 116.87 KB gz (+0.56 KB, no dep). |
| UX14 | **Empty states & first-run onboarding** — guided "add an account, add envelopes" (ties to the PRD week-one metric) | 4 Polish | slice | Med | Low | UX3 · UX4 | **✅ Done** — [FEAT-UX14](features/first-run-onboarding.md)·[status](status-reports/2026-07-02-ux14.md); new **`FirstRunOnboarding`** component (**composes the existing UX4 `EmptyState`** — no new dep) shown by the home when the app is completely empty, a state **DERIVED** from `listAccounts`+`listEnvelopes` (no stored "has onboarded" flag; derive-don't-store); replaces the cockpit's five per-panel empty states with one guided next step (two CTA `<Link>`s → `/accounts` · `/envelopes`), tied to the PRD week-one metric; the moment anything exists the cockpit takes over. Axe-green light+dark with the onboarding surface visible (empty-store scan runs first). No ADR/spike/dep, no data/API/domain change. **364 Vitest + 92 e2e**, 117.20 KB gz (+0.33 KB, no dep). |
| UX15 | **Responsive pass** — desktop-first layouts that degrade gracefully to phone width | 4 Polish | slice | Med | Low | UX3 · UX4 | **✅ Done** — [FEAT-UX15](features/responsive-pass.md)·[status](status-reports/2026-07-02-ux15.md); **CSS/token-only** (no dep). Much was already responsive (cockpit + figure grids `auto-fit`, nav + rows `flex-wrap`, chart SVGs `width:100%`); this slice closes the phone-width gaps — **reflow (WCAG 1.4.10)**: wide data tables scroll within their own **keyboard-focusable** region (one global `.table-scroll` wrapper in **`ChartFigure`** covers all nine Insights tables, + the envelope ledger), never the page; the **shell header** degrades to a narrow column + the **Insights sub-nav** becomes a wrapping tab row with valid **target-size** (WCAG 2.5.8); page padding tightens ≤ 640px. **Scope call:** "nav collapses" = graceful degradation (already wraps), **no hamburger** (follow-on). Axe-clean **at 320px light+dark** (new reflow e2e describe asserts no horizontal page scroll). No ADR/spike/dep, no data/API/domain/chart change. **366 Vitest + 94 e2e**, 117.27 KB gz (+0.07 KB, no dep). **Completes the UX Uplift (`UX1`–`UX15`).** |

### Sheet-parity features (from [SPIKE-08](spikes/08-budgethome-sheet-analysis.md))

The two capabilities of the owner's working `BudgetHome.xlsx` planner that Budgeteer does not
yet cover (everything else the sheet does is already shipped — the mapping is SPIKE-08 §6).
Shipping S7 retires the owner's last reason to keep the spreadsheet. **Sequencing note:** S7
touches the recurring engine, so it benefits from landing **after `EH7`** (injected clock) —
its pay-date math gets deterministic tests from day one; S9 is a §11-compressed warm-up that
can ride alongside or fold into S7.

| # | Item | Kind | Value | Risk | Gated by | Status |
| - | ---- | ---- | ----- | ---- | -------- | ------ |
| S7 | **Pay-period planning ("which paycheck covers what")** — assign recurring rules + per-period envelope funding to **expected income occurrences**; show each paycheck's bucket total and a **committed-obligations headroom** line (SPIKE-08 S8 folds in); carry the sheet's ~month-ahead reservation buffer. The sheet's colour-only blue/red bucket join is expressed as **text + structure, never colour alone** (ADR-0007/UX13 conventions). Start **derived-only** (buckets computed from recurring rules + targets, no schema change); an explicit bill↔paycheck **assignment store** is a §11 ceremony scale-up (spec + data-model change, possibly an ADR) — decide at spec time. | slice | High | Med | Feature spec + UX spec; recommended after `EH7` | Proposed |
| S9 | **"Still owed this month"** — a small derived read (unposted recurring occurrences through month-end, summed) surfaced on the cockpit's upcoming-recurring panel: the sheet's D-column countdown, computed instead of hand-chained. No schema change; §11 compressed (FEAT note, no spike). | polish | Med | Low | — | Proposed |

### Hardening

| # | Item | Kind | Value | Risk | Trigger (when) | Status | Links |
| - | ---- | ---- | ----- | ---- | -------------- | ------ | ----- |
| 15a | **Backup / export** — `GET /export` → `budgeteer-backup-YYYY-MM-DD.json` (JSON snapshot of all 15 tables); "Download backup" link on the Dashboard. Cents as numbers, timestamps as ISO strings, no views included. | hardening | Med | Med | real daily data exists | **✅ Done** | [`07_NFR`](07_NFR.md) · [`status-reports/2026-06-17-slice-15a.md`](status-reports/2026-06-17-slice-15a.md) |
| 15b | **Restore / import** — round-trip a JSON backup file back into the local store. Gated on a spike: FK insert order, ID collision handling, partial failure, schema evolution. | hardening | Med | High | `#15a` done | Deferred | Owner tabled restore for now — continuing forward with `#16` + new feature slices. Spike question (when revisited): does a JSON snapshot round-trip cleanly through the schema, IDs and all? |
| 16 | **a11y pass** (WCAG 2.2 AA) + perf/NFR budgets + **ESLint in CI** ([EH4](reviews/2026-06-15-repo-review.md)) + **e2e in CI** (automated `test:e2e` trigger in `gate.yml`, companion to ESLint-in-CI) | hardening | Med | Low | UI surfaces exist · R14 done | **✅ Done** | [`07_NFR`](07_NFR.md) · [status report](status-reports/2026-06-21-16.md) — axe-core scan (12 tests, 2 violations fixed); perf budgets measured + verified; gate.yml wired; SCA added |

### Deferred — post-V1

| # | Item | Kind | Why deferred | Status |
| - | ---- | ---- | ------------ | ------ |
| 17 | **SPIKE-03 — data-profiling:** does the 12-yr `Budget.xlsx` reconcile/extract cleanly from its in-cell formula strings? | spike | History migration is out of V1; this is the proper spike for the extraction that "got too messy" | Deferred |
| 18 | **Historical import** | slice | Gated by SPIKE-03 | Deferred |
| 19 | **Multi-user / household scoping** (auth · owner/household isolation) | epic | Future direction; a §11 scale-up trigger → full ceremony (tenancy ADR + threat model + property tests). **Absorbs the review's "no authentication" finding** ([2026-06-15 review](reviews/2026-06-15-repo-review.md)) | Deferred |
| 20 | **CSV/statement import**, later **live bank API** | slice | Manual is V1; each needs its own integration spike | Deferred |

## 5. Re-sequencing log

| Date | Change | Trigger | Effect on the plan |
| ---- | ------ | ------- | ------------------ |
| 2026-06-13 | History migration (→ #17/#18) and bank integration (→ #20) **deferred to post-V1** | Discovery forks A & B: **fresh start + manual** | Retired the two heaviest risks; V1 starts at the value/UX bet instead of a data-extraction slog |
| 2026-06-13 | **Templates** elevated to the primary accelerator (#4); **partial allocation** folded into the core loop (#3); **transfers/refunds/recurring/edit-split/archive** added as slices (#5–#9, mapped to A1.7/A1.5/A5.3 of the sheet) | [SPIKE-01](spikes/01-split-allocation-ux.md) confirmed the model + surfaced these | Shaped the domain-slice backlog; flagged transfers (double-entry) & edit-split (invariant) as the careful ones |
| 2026-06-13 | Added **SPIKE-02** (feasibility) ahead of the foundation | Stack still unchosen; SPIKE-01 was a *paper* test, didn't probe the build | `ADR-0001`/`0002` now gate the foundation slice |
| 2026-06-13 | **SPIKE-02 Done**; stack chosen (TS · React+Vite · Fastify · Postgres); `ADR-0001` `Validated`, `ADR-0002` `Proposed`, `ADR-0003` `Accepted` | [SPIKE-02](spikes/02-stack-feasibility.md) confirmed integer-money + split invariant exact (8/8 tests) | **Foundation slice → `Ready`**; no further pre-build spike needed |
| 2026-06-13 | **Foundation slice Done** (gate-green); `ADR-0002` → `Validated`; domain/data model → `Accepted`; FEAT-001/002 → `Implemented`; `06_API_CONTRACT` written; `ADR-0001` → `Accepted` | Built data→API→UI with 31 passing tests + HTTP smoke | **Slice 1** (core enter→allocate loop) is now unblocked & next |
| 2026-06-13 | **Slice 1 Done** (gate-green); FEAT-003 `Implemented`; UX `Accepted`; transaction/allocation endpoints added to `06_API_CONTRACT` | Built enter→split-allocate across domain→API→UI (44 tests + HTTP smoke); SPIKE-01 felt-friction caveat exercised in-app | **Next: Slice 2** (accelerators — templates) |
| 2026-06-13 | **Slice 2 Done** (gate-green); FEAT-004 `Implemented`; templates tables in `05_DATA_MODEL`; template endpoints in `06_API_CONTRACT`; template lines = **fixed amounts** (PRD open Q resolved) | Built templates + distribute/keyboard accelerators (55 tests + HTTP smoke); smoke caught & fixed a content-type/error-status bug | **Next: edit-a-past-split (#5), archive (#6)** |
| 2026-06-13 | **Slice #5 Done** (gate-green); FEAT-005 `Implemented`; **no new API** (reused `PUT /transactions/:id/allocations`) | Made register transactions editable via the AllocationEditor (57 tests + HTTP smoke) | **Next: archive an envelope (#6)** |
| 2026-06-13 | **`#6` Done** (gate-green); FEAT-006 `Implemented`; archive/unarchive endpoints in `06_API_CONTRACT` (**no schema change** — `archived_at` existed since the Foundation) | Archive/unarchive + picker filtering + Dashboard Archived section (62 tests + HTTP smoke) | **Next: transfers `#7`** (consider a modeling spike), then analysis |
| 2026-06-14 | **SPIKE-04 Done** (modeling); confirmed an **additive two-primitive** transfer model (account-transfer legs + `envelope_transfers` table); owner confirmed transfers must cover **envelope↔envelope** too | [SPIKE-04](spikes/04-transfer-modeling.md): both movements proven exact + orthogonal, split model untouched (8/8 tests) | **`#7` Ready** → recommend split into **`#7a` account-transfer** then **`#7b` envelope-reallocation**; produces `ADR-0004`. *Pending owner nod on the split + negative-envelope-balance Q before building.* |
| 2026-06-14 | Owner confirmed: **split `#7` into `#7a`/`#7b`**; **negative envelope balances allowed**. **`#7a` Done** (gate-green); `ADR-0004` `Accepted`; FEAT-007 `Implemented`; `transfers` table + `transactions.transfer_id`/kind-check in `05_DATA_MODEL`; `POST /transfers` in `06_API_CONTRACT` | Built account-transfer across domain→API→UI (73 tests + HTTP smoke) | **Next: `#7b`** (envelope reallocation — the second ADR-0004 primitive), then refunds `#8` |
| 2026-06-14 | **`#7b` Done** (gate-green) — completes `#7`/ADR-0004; `envelope_transfers` table + **two-source** `v_envelope_balances` in `05_DATA_MODEL`; `POST /envelope-transfers` in `06_API_CONTRACT`; EnvelopeTransfer entity in `04_DOMAIN_MODEL` | Built envelope-reallocation across domain→API→UI (82 tests + HTTP smoke); orthogonal to accounts, split model untouched | **Next: refunds `#8`**, recurring `#9`, reconcile `#10`, then analysis (`#11`–`#14`) |
| 2026-06-14 | Owner confirmed #8 scope: **mixed-sign rows within a split** via a per-row **Refund** toggle. **`#8` Done** (gate-green); FEAT-008 `Implemented`; **no schema change** (split invariant already governs the signed total); allocation inputs gain `refund` in `06_API_CONTRACT`; PRD §9 negative-rows Q **resolved** | Found the heart invariant already admits mixed signs — only the boundary blocked it; opened it + added the editor toggle (93 tests + HTTP smoke) | **Next: recurring `#9`**, reconcile `#10`, then analysis (`#11`–`#14`) |
| 2026-06-14 | Owner confirmed #9 scope: **weekly/biweekly/monthly**, explicit **Post due**, rule **carries its own split**. **`#9` Done** (gate-green); FEAT-009 `Implemented`; `recurring_transactions` + `recurring_lines` tables + `transactions.recurring_id` in `05_DATA_MODEL`; recurring endpoints in `06_API_CONTRACT`; RecurringTransaction entity in `04_DOMAIN_MODEL` | Built rule + idempotent generator across domain→API→UI (106 tests + HTTP smoke); pure schedule core with monthly anchor-day clamp | **Next: reconcile `#10`**, then analysis (`#11`–`#14`) |
| 2026-06-15 | Owner confirmed #10 scope: **plain compare + recorded history** (not a cleared/statement workflow); on mismatch **show the difference** (no auto-adjustment). **`#10` Done** (gate-green); FEAT-010 `Implemented`; `reconciliations` table in `05_DATA_MODEL`; reconcile endpoints in `06_API_CONTRACT`; Reconciliation entity in `04_DOMAIN_MODEL`; PRD §9 reconcile Q **resolved** | Built record+compare across domain→API→UI (120 tests + HTTP smoke); a recorded compare, no ledger/balance effect | **Next: the analysis area (`#11`–`#14`)** |
| 2026-06-15 | **CORS + .env fix** (not a roadmap slice): the browser app couldn't reach the API (no CORS headers → "Failed to fetch"); added `@fastify/cors` allowlist (`CORS_ORIGINS`) + repo-root `.env` auto-load (dotenv / Vite `envDir`) | Found via running the app at `localhost:5173`; the browser→API path was never exercised by curl/inject smokes | Hardening follow-up: add **browser e2e** (would have caught this) |
| 2026-06-15 | **Repo-wide review** captured ([reviews/2026-06-15-repo-review.md](reviews/2026-06-15-repo-review.md)); added **Engineering-health items `EH1`–`EH6`**, sequenced **before the analysis area** | No live bugs; themes = browser-path coverage gap + duplication drift | New order: **EH1→EH6 → analysis `#11`–`#14` → hardening `#15`–`#16`** (full e2e/a11y); no-auth folds into `#19` |
| 2026-06-15 | **`EH1` Done** (gate-green); web now imports money/date from `@budgeteer/domain`. Added `tryParseMoney` (single `DECIMAL_RE` home) + moved `splitEvenly` to domain; widened `formatMoney(value: number)`; `apps/web` declares the dep; `fakeApi` uses `dueOccurrences`/`anchorDayOf`. Refined the review's "delete format.ts": kept `formatCents` (the `$`/locale **display** formatter) as a presentation concern | The duplicated penny-exact parse regex (`MONEY_RE ≡ DECIMAL_RE`) + `splitEvenly` + the date mirror were a second source of truth; analysis (`#11`+) would build more atop it | **Next: `EH2`** (extract API service plumbing). 121 tests (+1 net) |
| 2026-06-15 | **`EH2` Done** (gate-green); API service plumbing extracted. New `util/dates.ts` (`todayStr`/`toISO`/`toDateStr`), `util/groupBy.ts` (generic, order-preserving), and top-level `constants.ts` for `DEFAULT_HOUSEHOLD_ID` (moved out of `db/migrate.ts`). Deduped the date converters (5→1, removed a dead `todayStr`), the bucket-by-parent loop (×4), and the household-constant↔migration coupling (9 importers) | Repeated service plumbing flagged by the review; analysis (`#11`+) adds more read services that would re-copy it | **Next: `EH3`** (map DB unique→409 + typed route params). 121 tests (no behavior change) |
| 2026-06-15 | **`EH3` Done** (gate-green); DB unique-violation now → 409 (was 500). New `services/dbErrors.ts` (`isUniqueViolation`/`asDuplicateName`) maps SQLSTATE `23505` → `DuplicateNameError` at the **service** layer (boundary rule: HTTP stays datastore-agnostic); wraps account/envelope/template create + rename/update. All 12 `req.params as {…}` casts replaced with Fastify route generics (`IdParams`/`AccountIdParams`) | The DB index is the real name guard (in-app check intercepts every *constructible* dup, so the index only fires under concurrency — and its failure was unhandled); casts were the last typed-assertion escape hatch | **Next: `EH4`** (ESLint in the gate). 125 tests (+4; backstop proven against real PGlite) |
| 2026-06-15 | **`EH4` Done** (gate-green); ESLint 9 flat config added (`@typescript-eslint` recommended + `react-hooks`) with `npm run lint` as a **zero-warning gate**; README gate list + commands updated. Caught & fixed a real rules-of-hooks error (non-hook helper `useRemaining` → `fillRemaining`); made the mount-load `exhaustive-deps` suppressions consistent across all 5 sites | No linter ran before, so the react-hooks rules were unenforced and the existing `eslint-disable` comments were inert | **Next: `EH5`** (minimal browser e2e — Playwright). 125 tests (no behavior change; one rename) |
| 2026-06-15 | **`EH5` Done** (gate-green); first **real browser→API** test layer. Added Playwright (Chromium-only): `playwright.config.ts` boots the real API + web (web on `:5173` to match the CORS allowlist) and Chromium drives one spec — dashboard-loads-vs-real-API + the account→envelope→deposit-allocated→derived-balance journey. New `npm run test:e2e` step (kept out of `npm test`); e2e TS folded into `typecheck`; ESLint/`.gitignore`/`.prettierignore` updated for the new surface | The CORS bug shipped because the three existing layers (domain unit · API `inject` · jsdom fake-API) never exercise a real browser→API round-trip ([2026-06-15 review](reviews/2026-06-15-repo-review.md) EH5 · [KIT_FEEDBACK](KIT_FEEDBACK.md) K3) | **Next: `EH6`** (repo hygiene — discard the absorbed transfer-modeling spike; record accepted notes). **125 Vitest + 1 e2e** |
| 2026-06-15 | **`#11` Done** (gate-green) — **first analysis slice**; FEAT-011 `Implemented`. Owner settled the core modeling Qs: **net signed allocation flow** per cell, **exclude** envelope↔envelope reallocations, **monthly grid + annual rollup**; archived envelopes **included**. Built data→API→UI: read-only `makeAnalysisService` aggregate (**no schema change**) + `GET /analysis/envelope-spend?grain=month\|year` + thin `AnalysisView` grid; `06_API_CONTRACT` + `05_DATA_MODEL` (read-path note) updated | No spike needed (derives from existing `allocations`/`transactions`, low risk); the spend definition was a *definition* decision, captured in the feature spec (not an ADR) | **Next: `#12`** budget-vs-actual (needs per-envelope monthly targets — open Q). **135 Vitest + 1 e2e** (+10) |
| 2026-06-16 | **`#12` Done** (gate-green) — second analysis slice; FEAT-012 `Implemented`. Owner settled the targets model: **one recurring monthly target per envelope** (a definition, **no ADR** — not effective-dated) and **actual = outflow spend** (funding excluded, refunds netted). Added `envelope_targets` table (idempotent migration) in `05_DATA_MODEL` + EnvelopeTarget in `04_DOMAIN_MODEL`; `targetService` (set/clear) + sibling `analysisService.budgetVsActual(month)`; `GET /analysis/budget-vs-actual` + `PUT`/`DELETE /envelopes/:id/target` in `06_API_CONTRACT`; resolved PRD §9 target Q. **Fixed a latent CORS bug** (preflight `Access-Control-Allow-Methods` defaulted to `GET,HEAD,POST`, blocking all browser `PUT`/`PATCH`/`DELETE`) — explicit methods allowlist + an e2e write-verb guard | The "actual" data already existed (FEAT-011); the gap was the **budget** store + the outflow definition — both settled with the owner (no spike: derives from existing data + a tiny store). The CORS gap surfaced because targets are the **first** browser write verbs the POST-only e2e exercised | **Next: `#13`** cash-flow forecast, then `#14` debt/credit, then hardening `#15`–`#16`. **151 Vitest + 1 e2e** (+16) |
| 2026-06-16 | **SPIKE-05 Done** then **`#13` Done** (gate-green) — third analysis slice, the area's only forward-looking one; FEAT-013 `Implemented`. Owner chose the **richer** model (per-account cash; **scheduled + expected-spend toggle**, default on; event-stepped 90-day default), which reintroduced the fuzzy fork → a spike was warranted. [SPIKE-05](spikes/05-cashflow-forecast.md) (8/8) proved the recurring engine projects the future **unchanged** and that netting expected spend against scheduled/actuals **kills the double-count** (naive raw-targets over-counted $1,620/mo vs. the correct $550 discretionary on the fixture). Built data → API → UI: **pure domain** `cashFlowForecast` + `analysisService.cashFlowForecast` read + `GET /analysis/cash-flow-forecast` + thin `ForecastView`; `06_API_CONTRACT` + `04_DOMAIN_MODEL` updated; **no schema change** (a pure projection). | The forecast *projects* (unlike #11/#12 which aggregate), so it carried the area's one genuine modeling unknown; the spike retired it, and "+ targets" was settled by the netting/spreading definition (no ADR) | **Next: `#14`** debt & credit trends (the analysis area's last slice), then hardening `#15`–`#16`. **168 Vitest + 1 e2e** (+16) |
| 2026-06-16 | **`#14b` Done** (gate-green) — **completes the split `#14` and the analysis area (`#11`–`#14`)**; FEAT-014b `Implemented`. Owner settled: **add a `kind='loan'` account type** (vs. a kind-agnostic store — symmetric with `#14a`: credit→limit→utilization, loan→original→payoff); **single original principal** (a definition, **no ADR**). Evolved the accounts kind check idempotently (+`'loan'` in schema/domain/web); added `loan_principals` (idempotent migration) in `05_DATA_MODEL` + LoanPrincipal in `04_DOMAIN_MODEL`; pure-domain `debtPayoff` (owed = −balance; payoff = 1 − owed/original in bps, truthful/unclamped; cumulative monthly trend; roll-up); `loanPrincipalService` (set/clear, loan-kind only) + `analysisService.debtPayoff`; `GET /analysis/debt-payoff` + `PUT`/`DELETE /accounts/:id/original-principal` in `06_API_CONTRACT` | No spike: aggregates existing balances + one stored number (like `#11`/`#12`/`#14a`); the cumulative-trend technique was already proven by `#14a`. The forks (loan modeling + effective-dating) were settled with the owner | **Analysis area complete. Next: hardening — `#15` backup/export, then `#16`. 214 Vitest + 1 e2e** (+22) |
| 2026-06-16 | **`#14` split → `#14a` + `#14b`; `#14a` Done** (gate-green) — fourth analysis slice; FEAT-014a `Implemented`. Owner split the roadmap's "debt & credit trends" (as `#7` was split): **credit utilization now (`#14a`)**, installment-debt **payoff-% later (`#14b`)**. Owner settled: **utilization, not payoff**; **per-account + portfolio roll-up**; a **separate `credit_limits` config table** (mirrors `envelope_targets`; a reversible definition, **no ADR**). Added `credit_limits` (idempotent migration) in `05_DATA_MODEL` + CreditLimit in `04_DOMAIN_MODEL`; pure-domain `creditUtilization` (owed = −balance; owed/limit in bps; cumulative monthly trend; roll-up); `creditLimitService` (set/clear, credit-kind only) + `analysisService.creditUtilization`; `GET /analysis/credit-utilization` + `PUT`/`DELETE /accounts/:id/credit-limit` in `06_API_CONTRACT`; resolved the `04_DOMAIN_MODEL` credit-balance **sign** open Q (owed = −balance at the analysis layer, ledger sign unchanged) | No spike: aggregates existing balances + one stored number (like `#11`/`#12`); the only new logic (cumulative balance per period vs. `#11`'s per-period net flow) is unit-proven, not a spike-level unknown. The gap was the **limit** store + the owed/utilization definitions — all settled with the owner | **Next: `#14b`** debt payoff %, then hardening `#15`–`#16`. **192 Vitest + 1 e2e** (+24) |
| 2026-06-15 | **`EH6` Done** (gate-green) — **completes the engineering-health track `EH1`–`EH6`**. Discarded the absorbed throwaway spike code `spikes/04-transfer-modeling/` (verified inert: no importer, not a workspace, `vitest.workspace.ts`/`eslint` already exclude `spikes/`); ticked the spike report's own discard follow-up. Recorded two accepted notes (no behavior change): `bigint`→`Number()` exact to ~`2^53` cents (≈$90T) on the `Cents` type + `05_DATA_MODEL`; `normalizeName` ⊃ DB `lower(btrim())` in `naming.ts` + `05_DATA_MODEL` | Kit rule: spike code is throwaway once its findings are absorbed (here into `ADR-0004` + `#7a`/`#7b` + the kept report); the two notes were "accepted as-is" in the [2026-06-15 review](reviews/2026-06-15-repo-review.md) (EH6) and needed a durable home | **Next: the analysis area (`#11`–`#14`)**, starting with `#11` spend-by-envelope-over-time. **125 Vitest + 1 e2e** (no test-count change) |
| 2026-06-17 | **e2e strategy expanded.** R14 promoted from P3 DX/"fold into `#16`" to a standalone P1 hardening item sequenced **before `#15`**: scope expanded to (1) split `journey.spec.ts` into per-area files, (2) write full journey coverage for all shipped features (#3–#14b), and (3) establish the per-slice e2e convention (each R6–R9 slice lands its own spec). `#16` updated: removed R14 reference, added **e2e-in-CI** (automated `test:e2e` trigger in `gate.yml`) as the CI gate companion to ESLint-in-CI. EH5 note updated to point to R14 instead of `#16` for full journeys. R6–R9 rows gated on R14 and each note their target spec file. | Owner decision: incorporate more e2e tests throughout | New order: R14 → #15 → R6–R9 (each with own spec) → #16 (CI gates) |
| 2026-06-17 | **Improvement review captured; `R1`–`R14` added to this roadmap.** Post-`#14b` review surfaced: 3 zero-API-work quick wins (R1–R3), 2 UX improvements using existing data (R4–R5), 4 new feature slices (R6–R9), and 5 DX items (R10–R14). `docs/03b_IMPROVEMENTS.md` (the working draft) deleted — this roadmap is the single plan of record. *(R14 scope and sequencing subsequently revised — see next entry.)* | Post-analysis improvement review | Added "UX polish" (R1–R5) and "Developer experience" (R10–R14) sections; R6–R9 appended to domain-slices table; current focus updated |
| 2026-06-17 | **R14 Done** (gate-green). `e2e/journey.spec.ts` deleted; replaced by 8 per-area spec files (`accounts`, `envelopes`, `transactions`, `templates`, `transfers`, `recurring`, `reconcile`, `analysis`) + shared `e2e/setup.ts` helper (`createAccount`, `createEnvelope`, `goToDashboard`). 20 Playwright tests (was 1), covering all features #3–#14b: single/split/partial allocation · edit-split · refunds · archive/unarchive · templates (create, save-as, apply, rename) · account transfer · envelope reallocation · recurring + post-due · reconcile (match + mismatch) · all 5 analysis views. Convention established: every new slice lands with its own `e2e/*.spec.ts`. | R14 scoped before `#15` so the per-area harness exists first | **Next: `#15` backup/export** (see [R14 status report](status-reports/2026-06-17-r14.md) kickoff prompt) |
| 2026-06-17 | **`#15b` deferred.** Owner decided to continue forward rather than tackle restore/import now. `#15b` remains in the hardening table for later but is not next. **New order: `#16` (a11y + CI gates) is the next hardening item; `R1`/`R10` are quick wins alongside; `R6` (transaction delete) is the first new feature slice.** | Owner preference | Removed `#15b` from "next"; `#16` is now the hardening focus |
| 2026-06-21 | **`R15` added — envelope ledger.** Surfaced by the question "can I view individual transactions in an envelope?". No schema change (data already in `allocations ⋈ transactions ⋈ accounts`). Gated on #3 · R14. Scope: `GET /envelopes/:id/ledger` (newest-first allocation list) + `EnvelopeLedger.tsx` panel; envelope-transfer reallocations out of scope for V1 (balance is still explained via the balance header). | Owner discovery during dev session | Added to domain-slices table as `R15`; FEAT-015 + UX spec written at `Proposed` |
| 2026-06-21 | **`R15` Done** (gate-green) — envelope ledger. `GET /envelopes/:id/ledger` returns all allocations for an envelope, newest-first. Envelope names on the Dashboard are now buttons that open `EnvelopeLedger.tsx`. No schema change. | Low risk, clean spec, no spike needed | **Next: `#16` (a11y + CI gates), `R1` (account rename), `R10` (API watch mode), `R6` (transaction delete). 227 Vitest + 23 e2e** (+9/+1) |
| 2026-06-21 | **`R1` Done** (gate-green) — account rename in UI. `renameAccount` wired in `api.ts` + `fakeApi.ts`; `AccountList` gains inline Rename button + input + Save; Dashboard `refreshAccounts` handler. R10 (API watch mode) was already in `apps/api/package.json`. | Zero API work; UI-only | **`exact: true` convention established** for `getByRole("button", { name: entityName })` where entityName appears in a sibling button's aria-label (applied across all 8 e2e specs). +1 e2e. **Next: `#16`, `R6`. 227 Vitest + 24 e2e.** |
| 2026-06-21 | **`#16` Done** (gate-green) — a11y pass + perf/NFR budgets + CI wiring. **A11y:** installed `@axe-core/playwright`; wrote `e2e/a11y.spec.ts` (12 tests, one per view); discovered 2 serious violations and fixed them: (1) `target-size` (WCAG 2.5.8 AA) — browser-default button/input/select height < 24 px → added `apps/web/src/index.css` with `min-height: 24px` on those elements; (2) `dlitem` (WCAG 1.3.1 A) — `<dl role="status">` in CreditView, PayoffView, ForecastView broke `<dt>`/`<dd>` containment → removed the role override. **Perf:** `apps/api/test/perf.test.ts` seeds synthetic data and measures key endpoints; all 3 budgets met: `GET /accounts` 0.9 ms p95, `GET /analysis/envelope-spend` 3.4 ms p95, `GET /export` 11.5 ms p95. **CI gate:** `gate.yml` wired with all 7 steps (typecheck · lint · format · unit+integration · e2e incl. a11y · build · SCA `npm audit --omit=dev --audit-level=critical`); `workflow_dispatch` kept intentionally. `07_NFR.md` updated to Active; 4 operational-readiness items ticked. | Challenge-first: axe scan before assuming clean; fixed 2 violations; measured before writing numbers. | **Next: `R6` (transaction delete). 230 Vitest + 36 e2e.** |
| 2026-06-21 | **`R6` Done** (gate-green) — transaction delete. `ConflictError` (→ 409) added; `transactionService.remove` + `transferService.remove`; `DELETE /transactions/:id` + `DELETE /transfers/:id` routes; `deleteTransaction`/`deleteTransfer` in `api.ts` + `fakeApi.ts`; Delete buttons on every register row. Allocations cascade via DB FK; transfer pair teardown cascades both legs. 5 new integration tests + 2 new e2e tests. | Hard delete; DB cascades handle allocations automatically — no application-layer cleanup needed | **Next: `R7` (account archive/close). 235 Vitest + 38 e2e.** |
| 2026-06-21 | **`R7` Done** (gate-green) — account archive/close. `accountService.setArchived(id, archived)` + `POST /accounts/:id/archive` / `/unarchive` (mirrors envelope `#6`); `archiveAccount`/`unarchiveAccount` in `api.ts` + `fakeApi.ts`; `AccountList` splits active/archived with a "Show archived" toggle and per-account `aria-label`'d Archive/Unarchive buttons. Challenge resolved: archived accounts stay **fully viewable** (register, transactions, delete all reachable via the archived list) — only **new** transfers reject them, via the **pre-existing** transfer-create guard (no new block needed). No schema change (`archived_at` existed since Foundation). 1 integration test + 1 e2e test. | Pure mirror of the envelope-archive pattern; the transfer-create archived-account guard already covered the only money-affecting path, so no domain/guard change was needed | **Next: `R8` (transaction search/filter). 236 Vitest + 39 e2e.** |
| 2026-06-22 | **`R8` Done** (gate-green) — transaction search/filter. `GET /accounts/:id/transactions` takes an inclusive `?from&to` window (defaults to the current calendar month via `currentMonthRange()`); `transactionService.listByAccount(accountId, { from, to })` filters by `occurred_on` but **always includes `opening` rows**. `listTransactions(accountId, range?)` threads the window through `api.ts` + `fakeApi.ts`; `AccountRegister` gained From/To date inputs (re-fetch on change) + a client-side payee/memo search box. Aligned `fakeApi`'s default txn date to today (the server already defaults to `todayStr()`), removing a latent date-fixture time-bomb. 3 integration tests + 1 web test + 1 e2e. | **Challenge resolved:** defaulting to the current month *would* hide the opening row (dated account-creation day) for older accounts → opening rows are special-cased to always show (the register's balance anchor); needs-allocation surfacing is a separate household-wide endpoint, so it is unaffected by the window. No schema change | **Next: `R9` (net worth over time). 240 Vitest + 40 e2e.** |
| 2026-06-22 | **`R9` Done** (gate-green) — net worth over time. Pure-domain `netWorthOverTime` + `isLiabilityKind` (new in `account.ts`); `analysisService.netWorth(grain)` gathers per-(period, kind) net flows over **all** transactions; `GET /analysis/net-worth?grain=month\|year` (reuses the `SpendQuery` generic); `getNetWorth` in `api.ts` + `fakeApi.ts`; thin `NetWorthView` (current totals + a cumulative Assets / Liabilities / Net trend) wired into `App`/`Dashboard` ("Net worth" header button). 7 domain + 7 integration + 4 web tests; `e2e/analysis.spec.ts` +1, `e2e/a11y.spec.ts` +1. | **Challenge resolved:** net worth = `Σ` of all **signed** account balances — liability kinds (`credit`/`loan`) carry debt negative (the FEAT-014a/b `owed = −balance` convention), so `net = assets + liabilities` falls out and transfer legs net to zero. Chose a **by-kind** split (stable over time) over a sign-based one (jagged: accounts flip columns as the balance crosses 0). The household-wide aggregate has no per-entity row, so the e2e asserts the **invariant** (net = assets + liabilities), immune to the shared DB. No schema change. | **Analysis area extended; next: the remaining UX-polish (`R2`–`R5`) / DX (`R11`–`R13`) backlog. 258 Vitest + 42 e2e.** |
| 2026-06-22 | **`R4` Done** (gate-green) — Dashboard net worth summary. A `NetWorthSummary` table below the account list (Total assets / Total liabilities / Net worth), computed **client-side** in `Dashboard.tsx` from the already-loaded account list — reuses the shared `isLiabilityKind`, sums **all** accounts (active + archived). `Dashboard.test.tsx` +2 (exact kind-based sums vs. a clean fake; hidden until an account exists); `e2e/accounts.spec.ts` +1 (invariant). No new endpoint, no schema/API change. | **Challenge resolved (rejected the kickoff's "call the endpoint" recommendation):** checked the SQL — `v_account_balances` and `R9`'s net-worth query sum the **same** transaction set, so a client-side `Σ` grouped by `isLiabilityKind` is **provably equal** to `R9`'s endpoint totals (no drift). Computing from the loaded list avoids a 3rd dashboard fetch and keeps the snapshot a faithful `Σ` of the rows shown right above it. The only real decision client-side forces — archived accounts — is settled by matching `R9` (include them). | **Next: the remaining UX-polish (`R2`, `R3`, `R5`) / DX (`R11`–`R13`) backlog. 260 Vitest + 43 e2e.** |
| 2026-06-22 | **`R2` Done** (gate-green) — needs-allocation count badge. The Dashboard fetches `api.listNeedsAllocation()` on mount (independently of the core account/envelope `Promise.all`, so a badge fetch failure leaves the badge absent rather than blanking the page) and renders the count as a red pill (`#991b1b`/white) on the "Needs allocation" header button, with the count in the button's `aria-label`. `Dashboard.tsx` only (state + independent fetch + `BADGE` style + button); `Dashboard.test.tsx` +2; `e2e/transactions.spec.ts` +1. No new endpoint, no schema/API/domain change. | **Challenge resolved (confirmed the kickoff's lean):** front-end fetch of the existing endpoint over a new count aggregate — zero API work, and it's the *same* single round-trip the `NeedsAllocation` view already makes (no N+1). Freshness needs no cache wiring: `App` conditionally renders the Dashboard keyed on `view.name`, so it remounts (re-fetches) on back-navigation after an allocation is completed. Accessibility is name-based, not color-based; the pill's ~8.3:1 contrast is validated by the existing Dashboard axe scan. | **Next: the remaining UX-polish (`R3`, `R5`) / DX (`R11`–`R13`) backlog. 262 Vitest + 44 e2e.** |
| 2026-06-22 | **`R3` Done** (gate-green) — grouped navigation, unified Analysis section. New thin `AnalysisSection` container groups all **6** analysis views (Spend, Budget, Forecast, Credit, Payoff, Net worth) behind one Dashboard "Analysis" entry: a `<nav aria-label="Analysis views">` sub-nav of buttons (`aria-current="page"` active) that **mounts only the active view** — so each view keeps its own `<h1>` + data fetch (no eager fetch-all-six) + its own `← Dashboard` exit. `App.tsx` collapses six `view` states + six `onOpen*` props into one `analysis`/`onOpenAnalysis`; `Dashboard.tsx` drops five header buttons. The six view components are untouched. `AnalysisSection.test.tsx` +3; `e2e/setup.ts` gains `openAnalysis(page, tab)`; `analysis.spec.ts`/`a11y.spec.ts` re-pointed to navigate via Analysis → tab (forecast test switches tab-to-tab in-section, exercising the new capability). | **Challenge resolved:** semantically-correct nav buttons over a `role="tablist"` widget (each view is a full `<main>`/`<h1>` page, not one swappable `tabpanel`); grouped **Net worth** too (R9 added it after the R3 line said "5"); no transition → `prefers-reduced-motion` N/A. Pure React/navigation refactor, no schema/API/domain change | **Next: the remaining UX-polish (`R5`) / DX (`R11`–`R13`) backlog. 265 Vitest + 44 e2e.** |
| 2026-06-22 | **`R5` Done** (gate-green) — **completes the UX-polish track (`R1`–`R5`)**; envelope targets visible inline. The Dashboard fetches `getBudgetVsActual(currentMonth)` on mount **independently** of the core account/envelope `Promise.all` (a `Map<envelopeId, BudgetVsActualRow>`), and each **active** envelope row renders an `EnvelopeBudgetInline` showing **target · spent · remaining** when a target is set. `Dashboard.tsx` only (state + independent fetch + `currentMonth` helper + `EnvelopeBudgetInline` + a threaded `budgetByEnvelope` prop on `EnvelopeList`); `Dashboard.test.tsx` +2 (triplet vs. a clean fake; un-budgeted row shows nothing extra); `e2e/envelopes.spec.ts` +1 (set a target in the Budget view → shows inline on the Dashboard row). No new endpoint, no schema/API/domain change. | **Challenge resolved (confirmed the kickoff's lean):** independent fetch of the existing budget endpoint mirrors the `R2` badge — a target-fetch failure leaves the inline figures absent, never blanks the Dashboard. Rendered **only** when a target is set (no faked `$0`); archived list + the R15 envelope-name click target + existing row controls all untouched; money via `formatCents`; figures are plain labelled text (not colour-only). Freshness needs no cache wiring — `App` remounts the Dashboard on back-navigation, so a target set in the Budget view shows on return. | **Next: the DX (`R11`–`R13`) backlog. 267 Vitest + 45 e2e.** |
| 2026-06-22 | **`R11` Done** (gate-green) — route modularization. The 682-line `http/server.ts` (43 routes inline) split into **9 per-domain Fastify plugin modules** (`http/routes/{accounts,envelopes,transactions,reconcile,transfers,recurring,analysis,templates,backup}.ts`) + `routes/shared.ts` (error envelope `fail`, `parsePositiveMagnitude`/`parseTemplateLines`, `DATE_RE`/`MONTH_RE`, `IdParams`/`AccountIdParams`, a `Services` container + `RoutePlugin` alias). `buildServer` → **104 lines**: keeps CORS + content-type parser + the single error handler + `/health`, builds the one `Services` object, and `app.register`s each plugin with `{ services }`. | **Challenge resolved:** the riskiest assumption — that encapsulated child plugins preserve the global error envelope / CORS / JSON body-parser — held, because all three are set on the **root** (inherited by children) and the route plugins add no hooks/decorators of their own. **No Fastify `prefix`** (domains share crossing URL roots — credit-limit/target setters live under `/accounts`/`/envelopes` but belong to analysis), so **every literal path is unchanged**; services constructed once and shared by reference. Pure DX refactor; the 125 API integration tests (incl. the `cors.test.ts` preflight + the 400/404/409 envelope) + 45 e2e are the regression net — green, unchanged. No schema/API/domain change. | **Next: `R12` (React error boundaries), then `R13` (structured API logging). 267 Vitest + 45 e2e** (unchanged). |
| 2026-06-23 | **`R12` Done** (gate-green) — React error boundaries. One reusable `ErrorBoundary` (`apps/web/src/ErrorBoundary.tsx`) placed twice: top-level wrapping `<App>` in `main.tsx` (recovery = reload) and per active analysis view in `AnalysisSection` (`key={tab}` → fresh boundary per tab; recovery = `← Dashboard`). Fallback = `<main>` + `<h1>` + `role="alert"` + a real button, a high-contrast card (`#1a1a1a`/`#fff`, dark-red border, `colorScheme: light`) readable in any color scheme. e2e-reachable via a dev-only `?boom` hook in `main.tsx` (`import.meta.env.DEV`-gated → tree-shaken from the prod bundle, verified 0 occurrences). `ErrorBoundary.test.tsx` +3, `e2e/error-boundary.spec.ts` +1, `e2e/a11y.spec.ts` +1. Zero API/schema/domain change. | **Challenge resolved:** error boundaries catch **render/lifecycle** throws only — *not* event handlers or async/fetch rejections (those already degrade gracefully via each view's `loadError` state). So the boundary's real job is the one currently-unprotected failure mode: a render-time throw, which `main.tsx`'s direct `<App/>` render would otherwise unmount to a blank white screen. Chose top-level **+** per-analysis-view (the analysis views do the most derived-data render work and sit behind a persistent sub-nav) over per-route-everywhere (over-engineering). Live dark-mode preview caught that the default-color fallback was black-on-dark (unreadable) — fixed by pinning the card's own high-contrast colors. | **Next: `R13` (structured API logging) — the last DX backlog item. 270 Vitest + 47 e2e.** |
| 2026-06-23 | **`R13` Done** (gate-green) — **completes the DX backlog (`R10`–`R13`)**; structured API logging. `buildServer`'s `logger` option widened `boolean`→`FastifyServerOptions["logger"]`; `index.ts` passes `{ logger: { level: config.LOG_LEVEL } }` → Fastify bundled-pino request/response/error lines at the configured level (a `reqId` correlation id per line). `LOG_LEVEL` (already in `config.ts`) tightened from `z.string()` to a **closed enum** of pino levels — validated at the boundary so a typo fails loudly at startup (spine §8). Tests omit the option → `logger: false` → quiet + deterministic (unchanged). `logging.test.ts` +2, `config.test.ts` +3. | **Challenge resolved:** logging is a cross-cutting effect, not a route; the risk was leaking financial request bodies/headers. Verified Fastify's **default** serializers emit only `method`/`url`/`statusCode`/`responseTime` (+ error type/message/stack) — **never** bodies or headers — so we keep them and add **no** body serializer (transaction memo/payee/amount + the `/export` snapshot never reach the logs; SECURITY.md §1/§5). Regression net asserts **both** halves via a capture stream: the access line is present *and* a money-route body + `authorization` header are absent; a real-startup smoke confirmed structured JSON to stdout, no leak. No API-contract/schema/domain change. | **DX + UX backlog clear; remaining work is the Hardening track + new feature slices. 275 Vitest + 47 e2e.** [status report](status-reports/2026-06-23-r13.md) |
| 2026-06-17 | **`#15` split → `#15a` + `#15b`; `#15a` Done** (gate-green) — first hardening slice. Owner confirmed: **export-only now** (`#15a`); restore/import deferred to `#15b` (riskier half — FK order, ID collisions, partial failure, validation — warrants a spike). Owner confirmed: **JSON snapshot** (engine-neutral, integer cents, round-trippable) over CSV (lossy) or SQL dump (Postgres-specific). Owner confirmed: **server endpoint** (`GET /export`) over CLI script, so the web app delivers a "Download backup" button (usable vertical slice). No spike for export — serializes existing rows, no unknown assumptions. `07_NFR.md` created (hardening track home; perf/a11y budgets are `#16` placeholders). Built: `backupService` (15-table parallel read, cents as `Number()`) + `GET /export` route (Content-Disposition: attachment, body never logged) + "Download backup" `<a>` in Dashboard header + `exportUrl` exported from `api.ts`. `e2e/backup.spec.ts` added (2 tests: link visible + API shape). | Owner settled all three forks (format / scope / delivery) before building; no spike warranted for export | **`#15a` done. Next: `#15b` (restore — spike first) or `#16` (a11y + CI). 218 Vitest + 22 e2e** (+4/+2) |
| 2026-06-25 | **UX Uplift initiative scoped; `UX1`–`UX15` added.** An owner design session set the framing — **foundation-first** (design system + client routing/app-shell, then migrate screens as vertical slices) and **desktop-first** — and three priorities: a **router-based** page framework, a **budget + future-planning landing cockpit**, and **new ways to visualize spending**. Captured thoroughly in [reviews/2026-06-25-ux-uplift-initiative.md](reviews/2026-06-25-ux-uplift-initiative.md); tracked here as the new **"UX Uplift"** section. The initiative **reverses the repo's deliberate "no design system" stance**, so it is **ADR-gated + spike-first** (`UX1`→`ADR-0005`/`ADR-0006`; `UX2`→`ADR-0007`), mirroring how `SPIKE-02` produced the stack ADRs. | Owner design session 2026-06-25 | New "UX Uplift" track added after the DX section; **next is `UX1`** (design-system + routing spike on one real screen). No code/spec/ADR written yet — items are `Planned`, not `Ready`. |
| 2026-06-25 | **`UX1` scoped as [SPIKE-06](spikes/06-design-system-routing.md).** Owner chose the **Account Register** as the probe screen and a **bake-off** (compare alternatives head-to-head) over single-hypothesis validation. Spike plan written: one falsifiable question, three decision axes — routing (React Router vs TanStack) × styling (CSS tokens+Modules vs Tailwind) × a11y primitives (React Aria vs Radix) — with a11y as a hard disqualifying gate, a measured bundle budget, and explicit pass/kill criteria. Blocks `ADR-0005` (styling) + `ADR-0006` (routing) → on green, `UX3`/`UX4` become `Ready`. | Owner scoping decisions (probe screen + bake-off) | `UX1` → `Ready`; **next is to execute SPIKE-06** (throwaway harness at `spikes/06-design-system-routing/`). |
| 2026-06-25 | **`UX1` Done — [SPIKE-06](spikes/06-design-system-routing.md) executed.** A throwaway Vite harness rebuilt the Account Register and baked off routing × styling × a11y primitives on **real rendered output**: a real-Chromium axe scan (WCAG 2.2 AA) and isolated per-library gzip bundle measurements. **Result: a viable axe-green stack exists** (lead screen + both dialogs = **0 violations**); chose **React Router + design tokens/CSS-Modules + Radix Primitives**. **The bake-off flipped the pre-spike lean** — both React Aria and Radix pass a11y, but Radix's dialog is **13.9 KB gz vs React Aria's 29.7 KB**, so the primitive choice moved React Aria → **Radix**; TanStack Router was found *slightly smaller* than React Router yet React Router wins on conventionality. `ADR-0005` (design system) + `ADR-0006` (routing) written → **`Validated`**. | Owner: execute SPIKE-06 | `UX1` → Done; **`UX3` (app shell) + `UX4` (component library) → `Ready`** on the chosen stack. Follow-ups: set the web bundle budget in `07_NFR` from the `UX3` shell baseline; discard the spike harness once absorbed. |
| 2026-06-25 | **`UX4` scoped → [FEAT-UX4](features/design-system.md).** Owner chose **seed-and-grow** (token sheet + only the starter primitives the first screen needs — Button/Field/Input/Select/Table/Badge/Dialog/Alert/EmptyState/Skeleton — then grow per slice) over a full-library-upfront build, and to **prove them by restyling the Account Register in place** (current nav unchanged). **Flagged + resolved a horizontal-layer risk:** a whole component library no screen uses violates *vertical, not horizontal* — so UX4 is now a vertical slice (tokens → primitives → one real styled screen, gate-green incl. a dark-mode axe pass). **Re-sequenced UX4 before UX3** (in-place restyle needs no routing; UX3 swaps in React Router after). No data/API/domain change. | Owner scoping decisions (breadth + proof screen) | `UX4` → `Ready (next)`; `UX3` follows. FEAT-UX4 spec at `Proposed`. |
| 2026-06-25 | **`UX4` Done (gate-green)** — design-system foundation, first UX-Uplift build slice. `apps/web/src/ui/`: token sheet (`tokens.css`, dark mode via `prefers-color-scheme`) + base layer (`base.css`, element styling; **absorbs + deletes `index.css`**) + seed primitives (Button/Badge/Card/Alert/EmptyState/Skeleton/Field/Input/Select). The **Account Register** restyled **in place** onto them (no routing/flow change; `<li>` rows + all aria/roles/text preserved), axe-clean (WCAG 2.2 AA) in **light AND dark** (a dark-scheme axe pass added to `e2e/a11y.spec.ts`). **`Dialog` + `Table` deferred** (seed-and-grow — the editor is a shared inline expand, not a modal; modal-izing it is a flow change). No data/API/domain change. | Owner: build UX4 | `UX4` → Done; **`UX3` (routing/app shell) is next**. FEAT-UX4 → `Implemented`. Follow-up: set the web bundle budget in `07_NFR` at the `UX3` shell build; discard the SPIKE-06 harness. **283 Vitest + 49 e2e.** |
| 2026-06-27 | **`UX3` Done (gate-green)** — routing + persistent app shell. `App.tsx`'s hand-rolled `view` state machine retired for **React Router 7 (data router)** behind a persistent `AppShell` (banner + primary `NavLink` nav + keyed route `<Outlet>`); new `api-context.tsx` (provide the `Api` above `RouterProvider`) + `routes.tsx` (the route map + thin adapters) + `EnvelopeLedgerRoute.tsx` (resolves `/envelopes/:id` against the list → refresh-safe). Every screen is URL-addressable / deep-linkable / refresh-safe with working back/forward; the per-screen "← Dashboard" + register "Needs allocation" buttons are gone (nav + the R2 count badge moved to the shell); `AnalysisSection` is now route-driven at `/insights/:view`. A reduced-motion-aware route transition (a **transform** slide, not an opacity fade). No data/API/domain change. `ADR-0006` → **`Accepted`**; FEAT-UX3 spec added; web bundle budget set in `07_NFR` (91.4 KB gz, < 120 KB budget); the **SPIKE-06 throwaway harness discarded** (findings persist in the spike report + ADR). | Owner: build UX3 | **Challenge resolved / flag:** `/manage` + the `/accounts`·`/envelopes` **list** routes were **deferred to `UX6`** (and `/transactions/new` to `UX7`) per the brief — building them empty now is the *Build-Without-Use* anti-pattern; UX3 routes only the screens that exist today. Caught a real a11y regression in review: an opacity route-fade dropped muted-text contrast below 4.5:1 during the axe scan → switched to a transform slide. **Next: `UX5` (the cockpit).** **283 Vitest + 52 e2e.** [status report](status-reports/2026-06-27-ux3.md) |
| 2026-06-27 | **`UX5` Done (gate-green)** — home budget + future-planning cockpit. New `Cockpit.tsx` (+`.module.css`, +`Cockpit.test.tsx`) rendered at the top of the home's `<main>` (`Dashboard.tsx`): five panels (this-month budget · needs-allocation · upcoming recurring · cash-flow forecast · net worth) that **compose existing reads** — `getBudgetVsActual` · `listNeedsAllocation` · `listRecurring` · `listAccounts` (one read feeds both the net-worth snapshot and the forecast account pick) · `getCashFlowForecast` — each panel loading/erroring independently (R2/R5). Each panel deep-links to its detail route; figures reconcile to the ledger (asserted). On the UX4 design system (`Card`/`Badge`/`EmptyState`/`Skeleton`) + UX3 router `<Link>`s. New `e2e/cockpit.spec.ts` (+2) + a dark-mode populated-home axe scan (+1). Fixed two stale `2026-06-25-ux4.md` → `2026-06-26-ux4.md` links (the UX4 report was renamed) in this roadmap + `features/design-system.md`. No new endpoint, no schema/API/domain change. | Owner: build UX5; the brief's open Q (home-summary endpoint vs. fan-out) | **Open Q resolved → fan-out** (no aggregate; R4/R5 precedent). **Challenge resolved / flag:** account/envelope management **stays on the home below the cockpit** — demoting it to `/manage` is `UX6`'s explicit acceptance, and removing the only setup path now would break *usable-at-every-step* (Download-backup already moved to the shell in UX3). **Visual check caught a real bug:** the budget panel first showed non-reconciling totals (the report's `totalSpent` counts untargeted spend while `totalRemaining` is targeted-only) → now sums over targeted envelopes so budgeted − spent = remaining; strengthened the unit test to cover untargeted spend. **Next: `UX6` (demote management to `/manage`).** **296 Vitest + 55 e2e.** [status report](status-reports/2026-06-27-ux5.md) |
| 2026-06-28 | **`UX6` Done (gate-green)** — demote management off the home. New route components `Home.tsx` (cockpit-only home), `AccountsList.tsx`/`EnvelopesList.tsx` (the `/accounts`·`/envelopes` **LIST routes** deferred from UX3 — progressive "Add" affordance, each name now a `<Link>` to its detail, inline rename/archive + R5 budget inline), and `ManageView.tsx` (the `/manage` **hub**: the R4 net-worth summary + Move-money + links to the two list pages); `Dashboard.tsx` retired. `routes.tsx` gains `/accounts`·`/envelopes`·`/manage`; the shell nav gains **Accounts · Envelopes · Manage**. Figures reconcile to the ledger; fan-out over existing reads (no new endpoint, no schema/API/domain change). The **bulk was the e2e re-point** (like UX3): `setup.ts` helpers (`createAccount`/`createEnvelope` drive `/accounts`·`/envelopes` + the progressive Add; new `goToAccounts`/`goToEnvelopes`/`goToManage`/`openAccount`/`openEnvelope`) + 9 specs moved off the home; `Dashboard.test.tsx` split into `Home`/`AccountsList`/`EnvelopesList`/`ManageView` tests; a11y suite scans the three new surfaces light + dark. | Owner: build UX6; **surface-split fork** (everything-on-`/manage` vs. list-routes-own-CRUD) | **Fork resolved → list routes own CRUD; `/manage` is the cross-cutting hub** (net-worth spans all accounts, Move-money spans envelopes) — reads the brief's "and" as a clean, non-duplicated distribution. **Caught in the axe sweep:** the `/manage` hub's links-in-prose tripped WCAG 1.4.1 `link-in-text-block` (link not distinguished from surrounding text) → moved the prose to a standalone intro `<p>` so each hub link stands alone. Home `/` is now the **cockpit only**; this also retires the UX5-flagged redundant net-worth (home + R4) and the within-mount freshness carry. **Next: `UX7` (global quick-add transaction).** **304 Vitest + 61 e2e.** [status report](status-reports/2026-06-28-ux6.md) |
| 2026-06-28 | **`UX7` Done (gate-green)** — global quick-add transaction. New `QuickAddTransaction.tsx` mounted as a **modal route** (`/transactions/new`, a child of the shell); an always-available **Add transaction** action in the shell nav opens it. It **reuses the register's editor** — `AddTransactionForm` gains an optional `accounts` picker mode, `AllocationEditor` gains an optional `disabled` gate (both default to the unchanged register behavior) — and **fans out to the existing `createTransaction`** (no new endpoint, no schema/API/domain change); a **partial** allocation's remainder still surfaces in needs-allocation. The modal is the new **`Dialog` primitive** (`apps/web/src/ui/Dialog.tsx`, `@radix-ui/react-dialog`) — the first genuine modal, so the UX4 seed-and-grow `Dialog` deferral lands here. History-safe close (`navigate(-1)`; deep-link → `/`). | Owner: build UX7; **Dialog mechanism fork** (Radix vs hand-rolled; child-route vs background-overlay) | **Forks resolved → Radix `Dialog` + child-route overlay.** ADR-0005 (Validated) reserves **Radix for the hard a11y widgets (Dialog named)** and SPIKE-06 validated the Radix dialog axe-clean, so Radix is the doc-aligned, lowest-a11y-risk path (focus trap / ESC / overlay-close / focus-restore / `role="dialog"` for free); +~12 KB gz → 105.4 KB (< 120 KB). With a data router a true background-location overlay is awkward, so a **child route + portal'd Dialog + history-safe back** gives the modal contract cleanly. **Entry = shell nav only** (the brief's "and/or") — return-focus-solid (the trigger stays mounted) and it avoids breaking the cockpit NeedsPanel's "all-clear → no link" test. Axe-green **light AND dark** on the open modal. **Next: `UX8` (Analysis → Insights), gated by the `UX2` charting spike.** **311 Vitest + 66 e2e.** [status report](status-reports/2026-06-28-ux7.md) |
| 2026-06-28 | **`UX2` Done — [SPIKE-07](spikes/07-accessible-charting.md) executed (the charting gate before the Insights phase).** A throwaway Vite harness built **one real chart** (net-worth-over-time, the `getNetWorth` shape) **hand-rolled in SVG** with the production design tokens, then ran a **real-Chromium axe scan** (same WCAG 2.2 AA tag set + serious/critical filter as `e2e/a11y.spec.ts`) and **React-externalized gzip bundle measurements** (the SPIKE-06 technique). **Result: an accessible chart is achievable hand-rolled** — **0 axe violations** across **light/dark × table-shown/hidden**, keyboard- and SR-reachable via a real `<table>` fallback, **color never the sole signal** (line color + dash + marker shape + direct label). **The decider was bundle:** hand-rolled adds **1.94 KB gz** (≈13% of the ~14.6 KB headroom) vs **Recharts 129 KB gz** — *larger than the entire 120 KB budget* — so a charting library is **disqualified**. `ADR-0007` (hand-rolled SVG + img-summary/table-fallback contract + new `--chart-*` tokens + a shared `Chart` primitive) written → **`Validated`**. App gate untouched (spike-only). | Owner: execute the UX2 charting spike; **fork** (charting library vs hand-rolled SVG) | **Fork resolved → hand-rolled SVG** (the budget forces it *and* it gives total a11y control). **Surprise:** Recharts v2's tree-shaken cost (~129 KB gz) far exceeds its "modular" framing; even the lightest credible lib (uPlot ~40 KB, visx modular) would eat most/all of the headroom for *one* of six charts. The fallback table doubles as the existing analysis content, so **UX8 is a low-risk additive migration** ("keep the table, add an SVG above it"). **Residual (low):** validated on a line/time-series; bars (spend/budget) + gauges (credit/payoff) reuse the shape-agnostic contract and re-run the axe gate per shape — no follow-up spike. **`UX8` → `Ready`.** Follow-up: discard the spike harness once `UX8` absorbs it. **App gate still 311 Vitest + 66 e2e, 105.4 KB gz.** [status report](status-reports/2026-06-28-ux2.md) |
| 2026-06-28 | **`UX8` Done (gate-green)** — Analysis → Insights migration, the first build slice of the Insights phase. The six analysis views are **renamed** ("Insights — …"), **restyled** onto the UX4 design system (`Insights.module.css` + `Field`/`Input`/`Button`), and each gains a **hand-rolled accessible chart** above its existing table on the ADR-0007 contract. New shared **`apps/web/src/ui/Chart.tsx`** (`ChartFigure` scaffold owning the a11y contract + `LineChart`/`BarChart`/`Gauge`) + new **`--chart-1/2/3`/`--chart-grid`** tokens (light+dark, ≥ 3:1). Net worth + cash-flow = **line**, spend + budget (grouped target-vs-spent, 2nd series hatched) = **bar**, credit + payoff (overall ratio + threshold marker) = **gauge**. Every chart `role="img"` + one-line summary, `aria-hidden` innards, the **existing table as the data-table fallback** (toggle default-shown so inline editors stay reachable), **colour never the sole signal**. `e2e/a11y.spec.ts` now **seeds data per shape** and axe-scans every chart **LIGHT AND DARK** (the previous insights a11y tests scanned empty states only). **Presentation-only — no new endpoint/schema/API/domain change.** | Owner: build UX8; chart-shape choices (gauge vs. extra line for credit/payoff) | **Shape calls:** credit/payoff get **one aggregate gauge** (the per-account + month trends stay in their tables as detail/fallback), keeping the slice tight; spend charts **per-envelope net totals** as diverging bars (direction encodes sign). **Integration call:** the `Chart` primitive takes the view's **existing table as a slot** (one table, editors preserved) rather than generating a second — faithful to the ADR's "add an SVG above the table". `ADR-0007` → **`Accepted`**; SPIKE-07 harness discarded (findings persist in spike report + ADR). Bundle **108.33 KB gz** (+2.9 KB for six charts + primitive; < 120 KB). **Verification note:** charts are rendered-and-axe-scanned in real Chromium by the e2e suite (interactive preview was blocked by a port clash with another running dev server). **Next: `UX9` (new viz: spending breakdown).** **315 Vitest + 72 e2e.** [status report](status-reports/2026-06-28-ux8.md) |
| 2026-07-01 | **`UX10` Done (gate-green)** — new viz: spending trends over time, the third build slice of the Insights phase. New `SpendingTrendsView.tsx` at `/insights/trends` ("Trends" tab, next to Breakdown): a multi-series **line chart** — household **total outflow** + **top-2 envelopes by outflow** — over a selectable **3/6/12-month trailing window** ending at a chosen month, **reusing the existing `LineChart` shape** (no new chart shape). **Read decision:** composes the **existing `getBudgetVsActual(month)` read, once per month in the window** (`Promise.all`) — **pure outflow**, not `getEnvelopeSpend`'s net flow, for the same reason UX9 chose it (funding-then-spend in one month would otherwise understate the trend); no new endpoint/schema/API/domain change, extending the R4/R5/UX9 fan-out precedent to several calls. Chart design: 3 series max, **one `--chart-*` token each** (no colour reuse) — Total solid/circle, the two ranked envelopes dashed/square and dashed/triangle — each also carrying a direct end label, so colour is never the sole signal; envelopes with zero outflow across the whole window are excluded from ranking, a ranked envelope's zero-spend month is **0-filled** (not omitted) to keep the axis aligned. `e2e/a11y.spec.ts` seeds outflow across two real backdated months and is **axe-green LIGHT AND DARK**. | Owner: build UX10; the read decision (net flow vs. pure outflow; one read vs. several) and how a multi-envelope trend stays legible without colour | **Both resolved as anticipated in the UX9 kickoff:** pure outflow via several `getBudgetVsActual` calls (not `getEnvelopeSpend`'s single-call net flow) — semantically correct over cheap, matching UX9's own precedent; legibility via **Total + top-2** (not all envelopes, not per-envelope small multiples) — one combined chart answers both "is spending trending up?" and "what's driving it?" without a new shape. **Next: `UX11` (budget burn-down).** **323 Vitest + 78 e2e.** Bundle **109.90 KB gz** (< 120 KB). [status report](status-reports/2026-07-01-ux10.md) |
| 2026-07-01 | **`UX11` Done (gate-green)** — new viz: budget burn-down, the **fourth and final** new-visualisation slice of the Insights phase. New `BudgetBurndownView.tsx` at `/insights/burndown` ("Burn-down" tab, next to Budget): a **`Gauge`** of **within-month budget-consumed** (spent ÷ target) with the gauge's **threshold marker repurposed as the elapsed-time pace** ("pace today"), for a chosen month + scope (all budgeted envelopes, or one) — so a user sees "am I on pace?" **before** month-end (budget-vs-actual only shows it after). **Read decision:** composes the **existing `getBudgetVsActual(month)` read — ONE call, no fan-out, no new endpoint/schema/API change** (the scope picker selects the household aggregate or one budgeted envelope from that single read); the only new code is **one pure, unit-tested domain function** (`assessBurndown` in `packages/domain/src/burndown.ts` — elapsed-day-fraction linear-burn pace, `today` passed in per the pure-core/impure-shell convention, reusing `daysInMonth`). **Chart-shape decision:** reuse `Gauge` **as-is** (no new shape/variant) — the burn-down is one ratio + one reference marker, exactly the gauge's job. `status ∈ over-budget | over-pace | on-track`, carried by a **verdict sentence + marker position + a table Pace column** — never colour alone; `role="img"` + one-line summary, `aria-hidden` innards, an exact-figures table (target · spent · % · pace + aggregate footer). `e2e/a11y.spec.ts` seeds a budgeted envelope with mid-month outflow and is **axe-green LIGHT AND DARK**. | Owner: build UX11; the pace/read decision (elapsed-time proxy vs. something else; one read vs. several; pure fn or not) and the chart-shape decision (reuse Gauge vs. new shape) | **Both resolved as the kickoff anticipated:** elapsed-time pace as a documented linear-burn **proxy** in a **pure domain function** (non-trivial date math ⇒ pure-core, mirroring `forecast.ts`); **ONE read** (lighter than UX10's fan-out — a burn-down is a single month, and that call already returns every row + the totals, so the scope picker needs no extra requests); **reuse `Gauge` as-is** (the threshold marker fits "pace today" with no variant). A day-by-day burn-down **curve** + an end-of-month **projection** are deferred (both need per-day spend, i.e. a new read). **Next: `UX12` (UX Uplift Polish track).** **335 Vitest + 81 e2e.** Bundle **111.06 KB gz** (< 120 KB). [status report](status-reports/2026-07-01-ux11.md) |
| 2026-07-02 | **`UX12` in progress — thread 1 of 4 Done (gate-green)** — the first slice of the Phase-4 **Polish** track: **confirmation on destructive actions**. UX12 bundles four independent polish threads (skeletons · toasts · destructive-action confirms · inline validation); per *vertical, not horizontal* (§5) + right-size/compress (§11), this slice ships **one** and leaves the rest tracked. New **`ConfirmDialog`** primitive (`apps/web/src/ui/ConfirmDialog.tsx`) — a **controlled/transient** modal on the **same SPIKE-06-validated Radix `Dialog`** the quick-add uses (reuses `Dialog.module.css` + a `.actions` row; **no new dependency**) — now gates the three previously one-click destructive actions: **Archive envelope, Archive account, Delete template**. State is carried by the **action-word text + dialog title, never colour** (the `danger` button variant is emphasis only); ESC/overlay = cancel, the mutation runs only on the explicit confirm; Cancel is first in DOM so it's the safe default focus. `e2e/a11y.spec.ts` scans the confirm dialog **open, LIGHT AND DARK**. New `FEAT-UX12` note (this note is the UX spec — §11 compressed). No ADR/spike (reuses a validated primitive, no dep, no data/API/domain/chart change). | Owner: which of the four threads to ship first (chose **destructive confirms**); whether reversible **Archive** gets a blocking confirm too (chose **all three, consistent**) | Both owner-decided up front. **No ADR/spike** — additive UI on a validated primitive, compressed ceremony (§11). Confirmed the two pre-existing carries still reproduce on clean `main` (recurring post-due; flaky forecast selector — the latter didn't trip this run). **Deferred, tracked:** skeletons (`UX12b`) · success toasts (`UX12c`) · inline validation (`UX12d`). **Next: finish the `UX12` threads, then `UX13`–`UX15`.** **340 Vitest + 83 e2e.** Bundle **111.48 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12.md) |
| 2026-07-02 | **`UX12` thread 2 of 4 Done (gate-green)** — the second Polish-track slice: **skeleton loaders**. The **16** bare `<p>Loading…</p>` / `<p role="status">Loading…</p>` strings across the lists (`EnvelopesList`, `AccountsList`, `ManageView`, `TemplatesView`, `NeedsAllocation`, `EnvelopeLedger`, `EnvelopeLedgerRoute`, `RecurringView`) and the **eight Insights views** now render the existing UX4 **`Skeleton`** primitive (as the home `Cockpit` already does — default 3 rows, no per-site tuning). **Net a11y win:** the ~9 sites that used a bare `<p>` (no live region) announced **nothing** to AT; they now announce politely via the primitive's sr-only `role="status"` "Loading…". Reduced-motion-safe (the primitive has no entrance animation); light/dark from the token sheet. **No ADR/spike, no new dependency, no new code** — a pure presentation swap reusing a validated primitive. New [FEAT-UX12b](features/skeleton-loaders.md) note. New unit tests assert the accessible loading state at 3 representative sites (early-return list + Insights `role="status"`); skeleton a11y otherwise covered by the primitive's existing tests + the `Cockpit` axe scan (the state is transient → a live Playwright scan would be racy — §11). | Owner: which thread next (chose **skeletons** — lowest risk) | **No ADR/spike** — additive presentation on a UX4 primitive, compressed ceremony (§11). Re-confirmed the two pre-existing carries still reproduce on clean `main` via `git stash` + isolated re-run (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Deferred, tracked:** success toasts (`UX12c` — needs a new Radix Toast primitive) · inline validation (`UX12d`). **Next: `UX12c`/`UX12d`, then `UX13`–`UX15`.** **343 Vitest + 83 e2e.** Bundle **111.44 KB gz** (net −0.04 KB; < 120 KB). [status report](status-reports/2026-07-02-ux12b.md) |
| 2026-07-02 | **`UX12` thread 3 of 4 Done (gate-green)** — the third Polish-track slice: **inline validation**. Money-amount inputs that failed *silently* (the quick-add / register amount just greys out `AllocationEditor`'s Save when `magnitudeCents` drops to 0) or only at the **server** round-trip (transfer · move-money · add-account starting-balance) now surface a **field-level** message the instant an amount can't parse. New pure **`amountFieldError`** (`apps/web/src/validation.ts`, reusing the single penny-exact parser `tryParseMoney` so the inline check can't drift from the boundary one) drives a new **`FieldError`** primitive (`apps/web/src/ui/Feedback.tsx`) — a `<span>` the input points `aria-describedby` at (a **description, not a live region**) paired with `aria-invalid`; the message **text** carries the state (`--color-danger` ~6.5:1 light+dark, **never colour alone**). Wired at 4 call sites (transaction · transfer · move-money · add-account); the three submit-driven forms also **guard the mutation** client-side (no round-trip on a known-bad amount), shown on blur (touched) then live so it clears as the user corrects. Every existing `aria-label`/`<label>` association preserved (the suites select by label text). `e2e/a11y.spec.ts` scans the add-account form with an amount error **visible** LIGHT AND DARK. New [FEAT-UX12d](features/inline-validation.md) note. | Owner: which thread next (chose **inline validation** over success toasts — lower-risk, no new dep) | **No ADR/spike** — additive UI reusing an existing pure parser + design-system primitives; no dependency, no data/API/domain/chart change (§11). **Scope call:** narrowed to the one *silent* failure (un-parseable money); required-field inline messaging + a `Field`/`Input` form migration are tracked follow-ons; semantic rules (positive amount, funds, distinct envelopes) stay **server-authoritative**. Re-confirmed the two pre-existing carries on clean `main` (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Deferred, tracked:** success toasts (`UX12c` — needs a new Radix Toast primitive). **Next: `UX12c`, then `UX13`–`UX15`.** **349 Vitest + 86 e2e.** Bundle **111.68 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12d.md) |
| 2026-07-02 | **`UX12` thread 4 of 4 Done — `UX12` COMPLETE (gate-green)** — the final Polish-track slice: **success toasts**, and the only UX12 thread needing a **new dependency**. Every successful mutation was **silent** (the global quick-add even navigates away, confirming nothing); a transient, auto-dismissing toast now confirms it. New **`ToastProvider`/`useToast()`** (`apps/web/src/ui/Toast.tsx`) on **`@radix-ui/react-toast`** — the last of the "hard interactive a11y widgets" ADR-0005 reserved for Radix — mounted at the **app root** (`App.tsx`, wrapping `RouterProvider` so a toast **survives** the quick-add's navigate-away). Wired to the successful-mutation set: create **account/envelope/transaction** (register + quick-add) · **transfer** · **move money** · **archive** account/envelope · **delete** template — each fires from the existing success path (**no data/API/domain/chart change**). A11y: Radix mirrors each toast into a **`role="status"` + `aria-live="polite"`** live region (`type="background"`, **never** `role="alert"`); entrance is **transform-only** (no text-opacity fade — the UX3 route-fade trap); reduced-motion-safe (global reset); an explicit **Dismiss** button; state carried by **text**, not the decorative `--color-success` accent. `useToast()` **no-ops without a provider** (a toast is an auxiliary affordance — mirrors the needs-badge degrade pattern), so direct-render unit tests need no churn. Proven **axe-clean light AND dark with the toast visible** before any wiring. New [FEAT-UX12c](features/success-toasts.md) note; new `Toast.test.tsx` (announce contract + dismiss + no-op) + `e2e/toasts.spec.ts` + a11y scans (light+dark). | Owner: which thread last (**toasts** — the one needing a dep); ADR-or-not (chose **no ADR** — ADR-0005 covers Radix Toast) | **No ADR** — ADR-0005 already reserves Radix for toast; the a11y risk (auto-dismissing animated live region) was retired **before wiring** by an axe-clean-light+dark scan with the toast visible. **Bundle:** **116.31 KB gz** (+4.63 KB — the Radix Toast is the real add; headroom now ~3.7 KB, was ~8.3 — `UX13`–`UX15` must watch it). **Consequence handled in-change:** the new app-wide `role="status"` live region made 3 pre-existing **bare** `getByRole("status")` e2e selectors (reconcile ×2, recurring) ambiguous → scoped to their view's notice. Added a jsdom pointer-capture polyfill (Radix swipe) to the web test setup. Re-confirmed the two pre-existing carries on clean `main` (recurring post-due 1-vs-4; flaky forecast selector didn't trip). **Next: `UX13` (money & budget-health visual encoding), then `UX14`–`UX15`.** **354 Vitest + 89 e2e.** Bundle **116.31 KB gz** (< 120 KB). [status report](status-reports/2026-07-02-ux12c.md) |
| 2026-07-02 | **`UX13` Done (gate-green)** — the Phase-4 Polish slice **money & budget-health visual encoding**. Budget health was **text-only** (target/spent/remaining as bare numbers; over-budget/negative money signalled by a **minus sign alone** — the V1 encoding). New **`ProgressBar`** primitive (`apps/web/src/ui/ProgressBar.tsx` + `.module.css`) — the last UX4 seed-and-grow primitive, **hand-rolled on the tokens + CSS with no charting dependency** (the SVG `Gauge`/`BarChart` from UX8 stay for the Insights charts; utilization/payoff already have a `Gauge`, so the new bar surface is budget **target-vs-spent**). Wired at two budget-health surfaces: a per-row **Spent-of-target** column in `BudgetVsActualView`'s table + a **summary bar** in the cockpit `BudgetPanel`; and negative money (over-budget remaining · liabilities · negative net worth) now gets **weight + `--color-danger` tone** in the budget table + cockpit figures, with the **minus sign kept as the non-colour signal** (supersedes "minus sign only"). A11y: the bar is **decorative** (`aria-hidden`, no `progressbar` role — no new AT surface, the figure is always adjacent text); the `over` tone lays a diagonal **hatch SHAPE** over the danger fill (a non-colour "past 100%" signal, mirroring the `Gauge` over-cap); the track carries a `border-strong` outline and each fill clears **3:1** vs the track (1.4.11) light+dark; **no animation** (reduced-motion-safe by construction). New `ProgressBar.test.tsx` (decorative + clamp + over-shape + tone) + wiring assertions in `BudgetVsActualView.test.tsx`/`Cockpit.test.tsx`; a new **over-budget** `e2e/a11y.spec.ts` scan (light AND dark) with the danger encoding **visible** (the existing `scanBudget` already covers the under-budget state). New [FEAT-UX13](features/budget-health-encoding.md) note. | Scope: right-sized (§11) to budget-health + a minimal net-worth money treatment — no app-wide money sweep, no `Amount` primitive (a possible later consolidation) | **No ADR/spike/dep** — additive presentation on the UX4 tokens; no data/API/domain/chart change (§11 fast-path). One **same-change** test consequence: the new Progress column added a second em-dash to an untargeted Budget row → updated that existing assertion. Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (91/91 e2e). **Next: `UX14` (empty states & first-run onboarding), then `UX15` (responsive pass).** **360 Vitest + 91 e2e.** Bundle **116.87 KB gz** (+0.56 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux13.md) |
| 2026-07-02 | **`UX14` Done (gate-green)** — the Phase-4 Polish slice **empty states & first-run onboarding**. A brand-new user opening a **truly empty** app met the cockpit's **five disconnected per-panel empty states** ("No monthly targets yet", "Everything is allocated", "No forecast yet"…) — a fragmented, un-guided first impression. The home now **DERIVES** whether the app is completely empty (**no accounts AND no envelopes**, from `listAccounts`+`listEnvelopes` — **never a stored "has onboarded" flag**; derive-don't-store) and, when it is, renders one guided next step: a new **`FirstRunOnboarding`** component (`apps/web/src/FirstRunOnboarding.tsx` + `.module.css`) that **composes the existing UX4 `EmptyState`** with a welcome + two CTA `<Link>`s ("Add an account" → `/accounts`, "Add envelopes" → `/envelopes`), tied to the PRD's week-one success metric. The moment **anything** exists, the cockpit takes over (its per-panel empty states then guide the remaining setup — progressive disclosure). New `FirstRunOnboarding.test.tsx` (region + two ordered steps + CTA hrefs) + updated `Home.test.tsx` (empty → onboarding; seeded → cockpit); `e2e/a11y.spec.ts` scans the onboarding surface **visible LIGHT AND DARK** — the empty-store scans are placed **first**, before any test seeds the run-scoped (never-reset) store. New [FEAT-UX14](features/first-run-onboarding.md) note. | ADR-or-not / spike-or-not (chose **neither** — additive presentation on a validated primitive); replace-vs-prepend the cockpit (chose **replace** on the truly-empty app — a guided screen, not a dashboard of empty panels) | **No ADR/spike/dep** — composes the UX4 `EmptyState` + routing; no data/API/domain change (§11 fast-path). Derive-don't-store honoured (no new server "onboarded" state). One **same-change** test consequence: `Home.test.tsx`'s empty-app case previously expected the Overview region → now expects onboarding (updated + a seeded case added). Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (92/92 e2e). **Next: `UX15` (responsive pass) — the last Phase-4 Polish item.** **364 Vitest + 92 e2e.** Bundle **117.20 KB gz** (+0.33 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux14.md) |
| 2026-07-02 | **`UX15` Done — the LAST UX-Uplift item; the initiative is COMPLETE (gate-green)** — the Phase-4 Polish slice **responsive pass**: desktop-first layouts that degrade gracefully to phone width. Much was **already responsive** by design (cockpit grid + Insights figure lists are `auto-fit minmax(...)` → stack; shell nav + register rows `flex-wrap`; ADR-0007 chart SVGs `width:100%` → scale), so this is a small **CSS/token-only** pass. (1) **Reflow (WCAG 1.4.10):** wide data tables previously overflowed the page at ~320px → now scroll within their own **keyboard-focusable** region via a global **`.table-scroll`** utility (`ui/base.css`, beside `.sr-only`) applied once in **`ChartFigure`** (every Insights table flows through it as the chart's data-table fallback — one edit covers all nine) + the standalone envelope-ledger table; each wrapper is `tabindex="0"` + named `role="group"` (axe `scrollable-region-focusable`). `display:block` on `<table>` was rejected (strips AT table semantics). (2) **Shell header** (`AppShell.module.css`, ≤ 640px) stacks brand over the wrapping nav with comfortable tap targets; the **Insights sub-nav** (`Insights.module.css` `.subnav`, wired in `AnalysisSection`) — previously raw adjacent `<a>`s that touch when wrapped — becomes a wrapping tab row with a ≥ 24px tap target per tab. (3) **Page padding** tightens ≤ 640px. New `.table-scroll` unit assertions (`Chart.test.tsx`, `EnvelopeLedger.test.tsx`) + a new **"responsive reflow at phone width"** e2e describe that seeds the widest table at 320px and asserts **no horizontal page scroll** + the focusable region, axe-clean **LIGHT AND DARK**. New [FEAT-UX15](features/responsive-pass.md) note. | Scope: "nav collapses" — hamburger vs. graceful degradation (chose **graceful degradation / no hamburger** — a hamburger is a new interactive a11y surface, against the slice's low-risk + desktop-first-not-mobile-first appetite; flagged as an available follow-on) | **No ADR/spike/dep** — media queries + one global utility on the existing UX3/UX4/UX8 modules (§11 fast-path). The narrow axe scan **caught** the Insights sub-nav's target-size gap at phone width (styled + fixed in-change) — reality before paper. Added the `DOM` lib to `tsconfig.e2e.json` (scoped to e2e) so the `page.evaluate` reflow probe type-checks. Re-confirmed the recurring post-due carry still fails on clean `main` (expects 4, gets 1); the flaky forecast selector didn't trip (94/94 e2e). **Next: the UX Uplift (`UX1`–`UX15`) is complete — no remaining Uplift items; see the status report for post-Uplift candidates.** **366 Vitest + 94 e2e.** Bundle **117.27 KB gz** (+0.07 KB, no dep; < 120 KB). [status report](status-reports/2026-07-02-ux15.md) |
| 2026-07-02 | **`EH7` Done (gate FULLY green)** — the injected clock. `buildServer` accepts `clock?: () => Date` (default = real clock), threaded to `accountService`/`recurringService`/`analysisService` + every route plugin (`RouteOptions.clock`) for the 5 route-level date defaults; `todayStr`/`currentMonthRange` now **require** the clock so new call sites can't regress. **Root cause of the ~6-week recurring carry finally diagnosed:** post-due always posted correctly — the *register endpoint's* server-derived current-month default window (R8) clipped the −21/−14/−7 relative-date fixtures out of view on any calendar day ≤ the 21st (the test could only pass on days 22–31). Fixed properly: `createTestApp({ today })` pins the clock; absolute fixture dates; `isoOffset` deleted. The flaky forecast-e2e `getByLabel("Account")` collision also fixed (`exact: true` — the chart's aria-label can contain the substring; R1/K16 convention). No API-contract/schema/domain/web-app change. | Owner: proceed with the next work; EH7 was the only item urgent on the project's own rules (a failing test blocks completion) | **The gate is fully green for the first time since ~June 22: 367/367 Vitest + 94/94 e2e**, build 117.29 KB gz. Scope note: `archived_at`/`updated_at` event timestamps deliberately not threaded (instants, not calendar derivations). EH8's timezone decision now has its seam ready (one conversion, one place). **Next: EH8–EH14 / S7/S9 — owner's call** (review suggests EH11, the one-line HOST default). [status report](status-reports/2026-07-02-eh7.md) |
| 2026-07-02 | **`S7`/`S9` added to the plan** — SPIKE-08's two candidates promoted from the report's §8 into a new §4 **"Sheet-parity features"** section (`S7` pay-period planning · `S9` still-owed-this-month), both `Proposed` | Owner decision: add them to the roadmap | S7 = the last capability keeping the owner's spreadsheet alive; recommended **after `EH7`** (injected clock → deterministic pay-date tests) and **derived-only first** (an assignment store is a §11 scale-up, decided at spec time). S9 is a §11-compressed warm-up. Relative order vs. `EH7`–`EH14` still the owner's call |
| 2026-07-02 | **SPIKE-08 captured** ([spikes/08-budgethome-sheet-analysis.md](spikes/08-budgethome-sheet-analysis.md)) — feature/formula/format analysis of the owner's working `BudgetHome.xlsx` planner (gitignored; amounts + creditors redacted in the report), mapped feature-by-feature to Budgeteer | Owner requested the sheet be analyzed and mapped to the existing system | Most of the sheet is already covered (recurring rules, biweekly income, cash-flow forecast, targets, budget health, debt payoff). **One genuine gap: S7 pay-period planning** (bills pre-assigned to specific paychecks ~a month ahead, with a committed-obligations headroom line) + a small **S9 "still owed this month"** derived read — both candidate items, **owner's call, not scheduled**. The 12-yr history work (`#17`/`#18`, SPIKE-03) is unchanged — this is the *current* planner, not the history file |
| 2026-07-02 | **Architecture review captured** ([reviews/2026-07-02-architecture-review.md](reviews/2026-07-02-architecture-review.md)); added **Engineering-health items `EH7`–`EH14`** to §4 (all `Proposed` — sequencing is the owner's call) | Owner requested a repo-wide architect's review at UX-Uplift completion | No live bugs; themes = clock-as-I/O (EH7 explains the carried red recurring test) · ops readiness (restore `EH10`, versioned migrations `EH9`, localhost binding `EH11`) · convention-only boundaries (`EH12`/`EH13`). Suggested order in the review: **EH7 → EH11 → EH8 → EH10 → EH9/EH14 → EH12/EH13**; EH7 is the only item urgent on the project's own rules (a failing test blocks completion) |

## 6. Done / shipped

| # | Item | Shipped | Notes |
| - | ---- | ---- | ----- |
| R12 | **R12** — React error boundaries | 2026-06-23 | One reusable `ErrorBoundary` (`apps/web/src/ErrorBoundary.tsx`) placed twice: top-level wrapping `<App>` in `main.tsx` (recovery = reload) **and** per active analysis view in `AnalysisSection` (`key={tab}`; recovery = `← Dashboard`) · accessible fallback — `<main>` + `<h1>` + `role="alert"` + a real button, high-contrast card (`#1a1a1a`/`#fff`, dark-red border, `colorScheme: light`) readable in dark mode · catches **render/lifecycle** throws only (async/fetch already degrade via `loadError`) · e2e-reachable via a dev-only `?boom` hook (`import.meta.env.DEV`-gated → tree-shaken from prod, verified) · zero API/schema/domain change · `ErrorBoundary.test.tsx` +3, `e2e/error-boundary.spec.ts` +1, `e2e/a11y.spec.ts` +1; [status report](status-reports/2026-06-23-r12.md); **270 Vitest + 47 e2e** |
| R11 | **R11** — route modularization | 2026-06-22 | `http/server.ts` 682→**104 lines** · 43 inline routes split into **9 per-domain Fastify plugin modules** (`http/routes/{accounts,envelopes,transactions,reconcile,transfers,recurring,analysis,templates,backup}.ts`) + `routes/shared.ts` (error envelope `fail`, money/date helpers, `IdParams`/`AccountIdParams` generics, `Services` container + `RoutePlugin` alias) · `buildServer` keeps CORS + body parser + the one error handler + `/health`, builds one `Services` object, `app.register`s each plugin · **no Fastify `prefix`** → every literal path unchanged · global error/CORS/parser are root-level (inherited by encapsulated children) · services constructed once · pure DX refactor, **no behavior/schema/API change**; [status report](status-reports/2026-06-22-r11.md); **267 Vitest + 45 e2e** (unchanged) |
| R5 | **R5** — envelope targets visible inline | 2026-06-22 | Each **active** envelope's **target · spent · remaining** shown inline in its Dashboard envelope-list row · Dashboard fetches `getBudgetVsActual(currentMonth)` on mount **independently** of the core load (graceful degrade), joins to active rows by `envelopeId` · `EnvelopeBudgetInline` renders **only** when a target is set (no faked `$0`) · archived list + R15 click target + existing row controls untouched · `formatCents`, plain labelled text (not colour-only) · no new endpoint, no schema/API/domain change · `Dashboard.test.tsx` +2, `e2e/envelopes.spec.ts` +1; [status report](status-reports/2026-06-22-r5.md); **267 Vitest + 45 e2e** |
| R3 | **R3** — grouped navigation, unified Analysis section | 2026-06-22 | Six analysis views (Spend, Budget, Forecast, Credit, Payoff, Net worth) grouped behind one Dashboard "Analysis" entry · thin `AnalysisSection` container · `<nav aria-label="Analysis views">` sub-nav (`aria-current="page"`) that **mounts only the active view** (own `<h1>` + own fetch, no eager fetch-all-six) · views untouched; only `App.tsx`/`Dashboard.tsx` changed · pure React/navigation refactor, no schema/API/domain change · `AnalysisSection.test.tsx` +3, `e2e/analysis.spec.ts` + `e2e/a11y.spec.ts` re-pointed (+`openAnalysis` helper); [status report](status-reports/2026-06-22-r3.md); **265 Vitest + 44 e2e** |
| R2 | **R2** — needs-allocation count badge | 2026-06-22 | Red count pill on the Dashboard "Needs allocation" button · `Dashboard` fetches `listNeedsAllocation()` on mount, **independently** of the core load (graceful degrade) · count in the button's `aria-label` (white-on-`#991b1b`, ~8.3:1) · refreshes on Dashboard remount (back-navigation) · **front-end fetch of the existing endpoint, no new aggregate** · no schema/API/domain change · `Dashboard.test.tsx` +2, `e2e/transactions.spec.ts` +1; [status report](status-reports/2026-06-22-r2.md); **262 Vitest + 44 e2e** |
| R4 | **R4** — Dashboard net worth summary | 2026-06-22 | `NetWorthSummary` table below the account list (Total assets / Total liabilities / Net worth) · computed **client-side** from the already-loaded account list, reusing the shared `isLiabilityKind` (kind-based split, agrees with `NetWorthView`) · sums **all** accounts (active + archived), provably equal to `R9`'s endpoint totals · no new endpoint, no schema/API change · `Dashboard.test.tsx` +2, `e2e/accounts.spec.ts` +1; [status report](status-reports/2026-06-22-r4.md); **260 Vitest + 43 e2e** |
| R9 | **R9** — net worth over time analysis | 2026-06-22 | `GET /analysis/net-worth?grain=month\|year` → derived Assets / Liabilities / Net trend (`Σ` signed account balances; liabilities `credit`/`loan` negative, so `net = assets + liabilities`; transfer legs cancel) · by-kind split (stable over time) · pure-domain `netWorthOverTime` + `isLiabilityKind` · thin `NetWorthView` ("Net worth" header button) · **no schema change** (read-only aggregate) · `e2e/analysis.spec.ts` +1 (invariant) · `e2e/a11y.spec.ts` +1; [status report](status-reports/2026-06-22-r9.md); **258 Vitest + 42 e2e** |
| R8 | **R8** — transaction search / filter | 2026-06-22 | `GET /accounts/:id/transactions?from&to` (inclusive window, defaults to current month) · `opening` rows always shown (balance anchor) · From/To date inputs + client-side payee/memo search box on `AccountRegister` · needs-allocation unaffected (separate endpoint) · no schema change · `e2e/transactions.spec.ts` +1; [status report](status-reports/2026-06-22-r8.md); **240 Vitest + 40 e2e** |
| R7 | **R7** — account archive / close | 2026-06-21 | `accountService.setArchived` + `POST /accounts/:id/archive` / `/unarchive` (mirrors envelope `#6`) · `AccountList` active/archived split with "Show archived" toggle · archived accounts stay viewable (register + delete) — only new transfers reject them (existing guard) · no schema change · `e2e/accounts.spec.ts` +1; [status report](status-reports/2026-06-21-r7.md); **236 Vitest + 39 e2e** |
| R6 | **R6** — transaction delete | 2026-06-21 | `DELETE /transactions/:id` (normal/opening; 409 for transfer legs) + `DELETE /transfers/:id` (cascades both legs + allocations via DB FK) · Delete buttons on every `AccountRegister` row; [status report](status-reports/2026-06-21-r6.md); **235 Vitest + 38 e2e** |
| 16 | **`#16`** — a11y pass + perf budgets + CI wiring | 2026-06-21 | 12 axe-core e2e tests (1/view) · 2 violations fixed (target-size via `index.css`; dlitem via `<dl role>` removal) · perf budgets measured (< 1 ms, 3.4 ms, 11.5 ms p95) · `gate.yml` wired (7 steps + SCA) · `07_NFR.md` Active; [status report](status-reports/2026-06-21-16.md); **230 Vitest + 36 e2e** |
| R1 | **R1** — account rename in UI | 2026-06-21 | Inline Rename control in `AccountList` · `renameAccount` in `api.ts` + `fakeApi.ts` · `exact: true` convention applied across all 8 e2e specs; [status report](status-reports/2026-06-21-r1.md); **227 Vitest + 24 e2e** |
| R10 | **R10** — API watch mode | 2026-06-21 | Already present (`"dev": "tsx watch src/index.ts"`) — no new work |
| R15 | **R15** — envelope ledger | 2026-06-21 | `GET /envelopes/:id/ledger` → newest-first allocation list · envelope names on Dashboard are now clickable buttons opening `EnvelopeLedger.tsx` · no schema change · `e2e/envelopes.spec.ts` extended; [feature](features/envelope-ledger.md)·[status report](status-reports/2026-06-21-r15.md); **227 Vitest + 23 e2e** |
| 15a | **`#15a`** — backup / export | 2026-06-17 | `GET /export` → `budgeteer-backup-YYYY-MM-DD.json` · 15-table JSON snapshot · cents as numbers · "Download backup" link in Dashboard · `backupService` + `e2e/backup.spec.ts`; [status report](status-reports/2026-06-17-slice-15a.md); **218 Vitest + 22 e2e** |
| R14 | **R14** — e2e expansion | 2026-06-17 | `journey.spec.ts` → 8 per-area spec files + `e2e/setup.ts` · 20 Playwright tests (was 1) · full journey coverage for all features #3–#14b · per-slice convention established; [status report](status-reports/2026-06-17-r14.md); 214 Vitest + **20 e2e** |
| 14b | **`#14b`** — analysis: debt payoff | 2026-06-16 | Per-loan **paid-down vs. original** — payoff (`1 − owed/original`, bps, truthful) · monthly payoff **trend** · portfolio **roll-up** · new **`kind='loan'`** account type + `loan_principals` store (set/cleared inline) · **pure domain** `debtPayoff` · completes the split `#14` and the analysis area; [FEAT-014b](features/debt-payoff.md); 214 tests + 1 e2e |
| 14a | **`#14a`** — analysis: credit utilization | 2026-06-16 | Per-credit-account **owed vs. limit** — current utilization (`owed ÷ limit`, basis points) · monthly utilization **trend** (cumulative owed) · portfolio **roll-up** · `owed = −balance` (analysis-layer sign) · new `credit_limits` store (set/cleared inline, credit-kind only) · **pure domain** `creditUtilization` · first half of the split `#14` (payoff-% → `#14b`); [FEAT-014a](features/credit-utilization.md); 192 tests + 1 e2e |
| 13 | **`#13`** — analysis: cash-flow forecast | 2026-06-16 | Per-account event-stepped cash projection over a 90-day default horizon · derived balance + scheduled recurring + netted **expected-spend** toggle · ending / min+date / first-negative · **pure domain** `cashFlowForecast`, **no schema change** · de-risked by [SPIKE-05](spikes/05-cashflow-forecast.md); [FEAT-013](features/cash-flow-forecast.md); 168 tests + 1 e2e |
| 12 | **`#12`** — analysis: budget vs. actual | 2026-06-16 | Per-envelope recurring **monthly target** (`envelope_targets`, set/cleared inline) vs. **actual outflow spend** per month (funding excluded, refunds netted) · `remaining = target − spent` · + latent CORS allow-methods fix; [FEAT-012](features/budget-vs-actual.md); 151 tests + 1 e2e |
| 11 | **`#11`** — analysis: spend by envelope over time | 2026-06-15 | Generated net-flow grid (monthly + annual) · reallocations excluded · archived included · **no schema change** (read-only aggregate); [FEAT-011](features/analysis-envelope-spend.md); 135 tests + 1 e2e |
| 10 | **`#10`** — reconcile to bank (manual compare) | 2026-06-15 | Plain compare + recorded history · `reconciliations` table · no balance effect; [FEAT-010](features/reconcile.md); 120 tests + HTTP smoke |
| 9 | **`#9`** — recurring transactions | 2026-06-14 | Rule + split + schedule (weekly/biweekly/monthly) · idempotent **Post due** · `recurring_id` on generated txns; [FEAT-009](features/recurring.md); 106 tests + HTTP smoke |
| 8 | **`#8`** — refunds (refund rows within a split) | 2026-06-14 | Per-row **Refund** toggle · signed-total invariant · no schema change; [FEAT-008](features/refunds.md); 93 tests + HTTP smoke |
| 7b | **`#7b`** — envelope↔envelope reallocation | 2026-06-14 | `envelope_transfers` table · two-source `v_envelope_balances` · into-archived blocked/drain+negative allowed; [ADR-0004](adr/ADR-0004-transfer-modeling.md); 82 tests + HTTP smoke |
| 7a | **`#7a`** — account↔account transfer (double-entry) | 2026-06-14 | `transfers` parent + `kind:'transfer'` legs · balances conserved · exempt from needs-allocation; [ADR-0004](adr/ADR-0004-transfer-modeling.md); 73 tests + HTTP smoke |
| 6 | **`#6`** — archive an envelope (soft-delete) | 2026-06-13 | Archive/unarchive · pickers hide archived · history preserved; 62 tests + HTTP smoke |
| 5 | **Slice #5** — edit a past split (from the register) | 2026-06-13 | Reuses the editor + `PUT allocations`; 57 tests + HTTP smoke |
| 4 | **Slice 2** — allocation templates + accelerators | 2026-06-13 | Apply/save templates · distribute-remaining · keyboard-first; 55 tests + HTTP smoke |
| 3 | **Slice 1** — transactions & split allocation, data→API→UI | 2026-06-13 | Single/Split/partial · needs-allocation · allocate-later; 44 tests + HTTP smoke |
| 1 | **Foundation slice** — accounts + envelopes, data→API→UI | 2026-06-13 | Gate-green: 31 tests (domain/API/web), web build, HTTP smoke; usable app shell |
| 2 | SPIKE-02 — stack feasibility | 2026-06-13 | Proved integer-money + split invariant exact in TS (8/8); produced `ADR-0001`/`0002`/`0003` |
| 0 | SPIKE-01 — split-allocation UX | 2026-06-13 | De-risked the core bet (paper); reshaped the plan (see §5) |
