---
type: roadmap
status: Living
---
<!--
ROADMAP v2 — restructured under the BUD-E/S/T id scheme (Epic → Story → Task).
Transitional: lives beside 03_ROADMAP.md until cutover. Objective + id convention:
docs/reviews/2026-07-12-roadmap-restructure-initiative.md. Ids are STABLE HANDLES,
never positions — re-sequencing never renames. Order lives in layout + status, not
the id. Spikes keep SPIKE-##. Legacy ids preserved in every `Was` column and §2.
-->

# Roadmap — Budgeteer (v2, restructured)

| Field         | Value          |
| ------------- | -------------- |
| Status        | Living (transitional — parallel to `03_ROADMAP.md`) |
| Owner         | Wesley Cutting |
| Last updated  | 2026-07-12     |
| Scheme        | `BUD-E##` epic · `BUD-S##` story · `BUD-T##` task · `SPIKE-##` spike |
| Supersedes    | (at cutover) [`03_ROADMAP.md`](03_ROADMAP.md) |
| Restructure   | [2026-07-12 initiative](reviews/2026-07-12-roadmap-restructure-initiative.md) |

## 0. Current state

V1 is in Alpha (review + UAT). Every build track is `Done` and the gate is green —
**434 Vitest + 121 e2e**, build **125.53 KB gz** against the **140 KB** budget. Done: the
core domain (BUD-E1–E3), engineering health (BUD-E4), security posture (BUD-E5), the UX
polish/DX backlogs (BUD-E6/E7), the whole UX Uplift (BUD-E8) and UX Redesign (BUD-E10),
sheet parity (BUD-E9), hardening (BUD-E11), and the real data & history import (BUD-E12).

**Open decisions (no new build without an owner pick):**
- Ratify or veto FEAT-S7 §5's residual divergence (`BUD-S61`) — balanced latest-fit vs. the
  sheet's month-boundary float ([FEAT-S7 §5](features/pay-periods.md) · [SPIKE-10 §3.6](spikes/10-payperiod-policy-validation.md)); a veto scopes the §8 assignment store (a §11 scale-up).
- Apply the reviewed merchant→envelope rules for the statement era (`BUD-S80`; opt-in, no app code).
- `BUD-E13` multi-user / household scoping — deferred epic, full ceremony.

> **This is a transitional restructure.** Content is faithful to `03_ROADMAP.md`; only the
> id scheme and grouping changed. `FEAT-*` spec names, `features/*` and `status-reports/*`
> paths, and `ADR-*` keep their legacy names until the deferred spec-back-referencing pass.

## 1. The id convention

- **Type-tagged, project-prefixed, one counter per type that never restarts:** `BUD-E##`
  (epic), `BUD-S##` (story), `BUD-T##` (task). The number is a **stable handle, not a
  position** — re-sequencing never renames. Order lives in the document layout + the status
  column, never in the id.
- **Hierarchy two ways:** the type tag says *what* an item is; the epic grouping + a `Parent`
  reading (each story sits under its epic heading) says *where it rolls up*.
- **Spikes keep `SPIKE-##`** (already consistent) and are listed under the epic they de-risked.
- Legacy ids are preserved in every table's **`Was`** column and in §2.

## 2. Crosswalk (old id → new id)

The authoritative translation while both roadmaps coexist. `SPIKE-08/09/10` had no plan row
in either doc (they appear only in the log/spike reports) and are omitted here.


| New ID | Type | Title | Was | Epic |
| --- | --- | --- | --- | --- |
| `BUD-E13` | epic | Multi-user / household scoping | `#19` | Multi-user / household scoping |
| `BUD-S1` | story | Foundation | `#1` | Foundation & stack |
| `BUD-S2` | story | core enter→allocate loop — enter deposit/withdrawal → allocate in Sing… | `#3` | Core budgeting domain |
| `BUD-S3` | story | accelerators — templates/presets | `#4` | Core budgeting domain |
| `BUD-S4` | story | Edit a past split | `#5` | Core budgeting domain |
| `BUD-S5` | story | Archive an envelope | `#6` | Core budgeting domain |
| `BUD-S6` | story | Transfer | `#7a` | Core budgeting domain |
| `BUD-S7` | story | Reallocation | `#7b` | Core budgeting domain |
| `BUD-S8` | story | Refunds | `#8` | Core budgeting domain |
| `BUD-S9` | story | Recurring transactions | `#9` | Core budgeting domain |
| `BUD-S10` | story | Reconcile to bank | `#10` | Core budgeting domain |
| `BUD-S11` | story | Transaction delete | `R6` | Core budgeting domain |
| `BUD-S12` | story | Account archive / close | `R7` | Core budgeting domain |
| `BUD-S13` | story | Transaction search / filter | `R8` | Core budgeting domain |
| `BUD-S14` | story | Envelope ledger | `R15` | Core budgeting domain |
| `BUD-S15` | story | spend by envelope over time | `#11` | Analysis & Insights data |
| `BUD-S16` | story | budget vs. actual | `#12` | Analysis & Insights data |
| `BUD-S17` | story | cash-flow forecast | `#13` | Analysis & Insights data |
| `BUD-S18` | story | credit utilization | `#14a` | Analysis & Insights data |
| `BUD-S19` | story | debt payoff % | `#14b` | Analysis & Insights data |
| `BUD-S20` | story | Net worth over time analysis | `R9` | Analysis & Insights data |
| `BUD-S21` | story | Dashboard net worth summary | `R4` | Analysis & Insights data |
| `BUD-S22` | story | Share the domain in the web | `EH1` | Engineering health |
| `BUD-S23` | story | Extract API service plumbing | `EH2` | Engineering health |
| `BUD-S24` | story | Map DB unique-violation → 409 | `EH3` | Engineering health |
| `BUD-S25` | story | Add ESLint to the gate | `EH4` | Engineering health |
| `BUD-S26` | story | Browser e2e | `EH5` | Engineering health |
| `BUD-S27` | story | Repo hygiene | `EH6` | Engineering health |
| `BUD-S28` | story | Inject the clock into services | `EH7` | Engineering health |
| `BUD-S29` | story | Decide + document the timezone policy | `EH8` | Engineering health |
| `BUD-S30` | story | Versioned migrator | `EH9` | Engineering health |
| `BUD-S31` | story | Prove restore | `EH10` | Engineering health |
| `BUD-S32` | story | Bind localhost by default | `EH11` | Engineering health |
| `BUD-S33` | story | Share the DTO types; decide the client-boundary stance | `EH12` | Engineering health |
| `BUD-S34` | story | Lint the layer boundaries | `EH13` | Engineering health |
| `BUD-S35` | story | Make post-due idempotency structural | `EH14` | Engineering health |
| `BUD-S36` | story | Retire vulnerable kysely + fix the SCA justification | `SEC1` | Security hardening |
| `BUD-S37` | story | Dev-tool advisories | `SEC2` | Security hardening |
| `BUD-S38` | story | Unauthenticated API, incl. one-request full-data /export | `SEC3` | Security hardening |
| `BUD-S39` | story | Account rename in UI | `R1` | UX polish |
| `BUD-S40` | story | Needs-allocation count badge | `R2` | UX polish |
| `BUD-S41` | story | Grouped navigation — unified Analysis section | `R3` | UX polish |
| `BUD-S42` | story | Envelope targets visible inline | `R5` | UX polish |
| `BUD-S43` | story | API watch mode | `R10` | Developer experience |
| `BUD-S44` | story | Route modularization | `R11` | Developer experience |
| `BUD-S45` | story | React error boundaries | `R12` | Developer experience |
| `BUD-S46` | story | Structured API logging | `R13` | Developer experience |
| `BUD-S47` | story | e2e expansion | `R14` | Developer experience |
| `BUD-S48` | story | Design-system foundation | `UX4` | UX Uplift |
| `BUD-S49` | story | Routing + persistent app shell | `UX3` | UX Uplift |
| `BUD-S50` | story | Home: budget + future-planning cockpit | `UX5` | UX Uplift |
| `BUD-S51` | story | Demote management to a dedicated surface | `UX6` | UX Uplift |
| `BUD-S52` | story | Global quick-add transaction | `UX7` | UX Uplift |
| `BUD-S53` | story | rename + restyle the 6 views, replace number grids with accessible cha… | `UX8` | UX Uplift |
| `BUD-S54` | story | New viz: spending breakdown | `UX9` | UX Uplift |
| `BUD-S55` | story | New viz: spending trends over time | `UX10` | UX Uplift |
| `BUD-S56` | story | New viz: budget burn-down | `UX11` | UX Uplift |
| `BUD-S57` | story | Feedback & states | `UX12` | UX Uplift |
| `BUD-S58` | story | Money & budget-health visual encoding | `UX13` | UX Uplift |
| `BUD-S59` | story | Empty states & first-run onboarding | `UX14` | UX Uplift |
| `BUD-S60` | story | Responsive pass | `UX15` | UX Uplift |
| `BUD-S61` | story | Pay-period planning | `S7` | Sheet parity |
| `BUD-S62` | story | "Still owed this month" | `S9` | Sheet parity |
| `BUD-S63` | story | Sidebar app shell | `UXR1` | UX Redesign |
| `BUD-S64` | story | Pay-period planner | `UXR2` | UX Redesign |
| `BUD-S65` | story | Ledgers tables | `UXR3` | UX Redesign |
| `BUD-S66` | story | Templates page | `UXR4` | UX Redesign |
| `BUD-S67` | story | Recurring page | `UXR5` | UX Redesign |
| `BUD-S68` | story | Insights IA | `UXR6` | UX Redesign |
| `BUD-S69` | story | Manage page | `UXR7` | UX Redesign |
| `BUD-S70` | story | Demo-grade synthetic seed | `UXR8` | UX Redesign |
| `BUD-S71` | story | Dashboard IA | `UXR9` | UX Redesign |
| `BUD-S72` | story | Insights chart X-axis readability | `UXR10` | UX Redesign |
| `BUD-S73` | story | Add-transaction cleanup | `UXR11` | UX Redesign |
| `BUD-S74` | story | Manage page formatting | `UXR12` | UX Redesign |
| `BUD-S75` | story | Allocate form on the pattern | `UXR13` | UX Redesign |
| `BUD-S76` | story | Backup / export | `#15a` | Hardening |
| `BUD-S77` | story | Restore / import | `#15b` | Hardening |
| `BUD-S78` | story | a11y pass | `#16` | Hardening |
| `BUD-S79` | story | Historical import | `#18` | Data & history import |
| `BUD-S80` | story | Statement import | `#20` | Data & history import |
| `BUD-T1` | task | Confirm on destructive actions | `UX12a` | UX Uplift |
| `BUD-T2` | task | Skeleton loaders | `UX12b` | UX Uplift |
| `BUD-T3` | task | Success toasts | `UX12c` | UX Uplift |
| `BUD-T4` | task | Inline field validation | `UX12d` | UX Uplift |
| `SPIKE-01` | spike | Split-allocation UX | `#0` | (cross-cutting) |
| `SPIKE-02` | spike | Technical feasibility / stack | `#2` | (cross-cutting) |
| `SPIKE-03` | spike | SPIKE-03 — data-profiling: | `#17` | Data & history import |
| `SPIKE-04` | spike | Transfer modeling | `#4` | (cross-cutting) |
| `SPIKE-05` | spike | Cash-flow forecast model | `#5` | (cross-cutting) |
| `SPIKE-06` | spike | Spike: design-system + routing foundation | `UX1` | UX Uplift |
| `SPIKE-07` | spike | Spike: accessible charting / viz a11y | `UX2` | UX Uplift |
| `SPIKE-11` | spike | SPIKE-11 — data-profiling: | `#21` | Data & history import |


## 3. The plan

Grouped by epic. Within each epic, rows keep their **source-section columns verbatim**
(only the id column is replaced by `ID` + `Was`); notes are unchanged except for the
id re-keying. `✅ Done` unless the Status cell says otherwise.


### BUD-E1 — Foundation & stack

The app skeleton and the two bets that had to clear before any build: the split-allocation value test and the TypeScript integer-money/stack-feasibility proof. De-risked by SPIKE-01 and SPIKE-02 (see the risk-retirement spikes below).

| ID | Was | Item | Kind | Value | Risk | Gated by | Status | Links (will produce) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S1** | #1 | **Foundation** — app shell + Postgres store + domain core + **account CRUD** (open with a starting balance, held *unallocated*) + **envelope CRUD** (create/rename). *Usable: set up your real accounts & envelopes and see balances.* Sets the **day-zero `.gitignore` + synthetic fixtures** guardrail. | slice | High | Med | ✅ SPIKE-02 · ADR-0001/0002/0003 | **✅ Done** | Built & gate-green (31 tests + HTTP smoke); [status report](status-reports/2026-06-13-foundation-slice.md) |

### BUD-E2 — Core budgeting domain

The enter→allocate loop and every ledger primitive built on it — transfers, refunds, recurring, reconcile — plus the later domain additions (delete, account archive, search, envelope ledger). Transfer modeling was de-risked by SPIKE-04.

**Core loop & ledger slices**

| ID | Was | Item | Kind | Value | Risk | Gated by | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S2** | #3 | **Slice 1 — core enter→allocate loop** — enter deposit/withdrawal → allocate in **Single** (one-tap) or **Split** (multi-row, live `Allocated/Remaining`, use-remaining) + **partial allocation** (save now, "needs allocation" surface) → balances reconcile, invariant holds | slice | High | Med | BUD-S1 | **✅ Done** | Built & gate-green (44 tests + HTTP smoke); [feature](features/transactions.md)·[UX](ux/transactions.md); felt-friction caveat now exercised in-app |
| **BUD-S3** | #4 | **Slice 2 — accelerators** — **templates/presets** (primary, **fixed-amount** lines) · keyboard-first row entry · distribute-remainder | slice | High | Med | BUD-S2 | **✅ Done** | Built & gate-green (55 tests + HTTP smoke); [feature](features/templates.md)·[UX](ux/templates.md) |
| **BUD-S4** | #5 | **Edit a past split** (preserve the sum invariant) | slice | High | Med | BUD-S2 | **✅ Done** | Built & gate-green (57 tests + HTTP smoke); reuses editor + `PUT allocations`; [feature](features/edit-split.md)·[UX](ux/edit-split.md) |
| **BUD-S5** | #6 | **Archive an envelope** (soft-delete; history preserved) | slice | Med | Low | BUD-S1 | **✅ Done** | Built & gate-green (62 tests + HTTP smoke); archive/unarchive + picker filtering; [feature](features/archive-envelope.md)·[UX](ux/archive-envelope.md) |
| **BUD-S6** | #7a | **Transfer** (account↔account, **double-entry**) | slice | Med | High | BUD-S2 · ✅ SPIKE-04 | **✅ Done** | Built & gate-green (73 tests + HTTP smoke); `transfers` parent + `kind:'transfer'` legs, exempt from needs-allocation; [ADR-0004](adr/ADR-0004-transfer-modeling.md)·[feature](features/transfers.md)·[UX](ux/transfers.md) |
| **BUD-S7** | #7b | **Reallocation** (envelope↔envelope) | slice | Med | Med | BUD-S2 · ✅ SPIKE-04 · BUD-S6 | **✅ Done** | Built & gate-green (82 tests + HTTP smoke); `envelope_transfers` table + two-source `v_envelope_balances`; into-archived blocked, drain/negative allowed; [ADR-0004](adr/ADR-0004-transfer-modeling.md)·[feature](features/transfers.md)·[UX](ux/transfers.md) |
| **BUD-S8** | #8 | **Refunds** (negative allocation within a split) | slice | Med | Med | BUD-S2 | **✅ Done** | Built & gate-green (93 tests + HTTP smoke); per-row **Refund** toggle, signed-total invariant (no schema change); [feature](features/refunds.md)·[UX](ux/refunds.md). Resolved the "negative rows?" open Q |
| **BUD-S9** | #9 | **Recurring transactions** | slice | Med | Low | BUD-S2 | **✅ Done** | Built & gate-green (106 tests + HTTP smoke); rule + split + schedule, idempotent **Post due** generator; [feature](features/recurring.md)·[UX](ux/recurring.md) |
| **BUD-S10** | #10 | **Reconcile to bank** (manual balance compare) | slice | Med | Low | BUD-S1 | **✅ Done** | Built & gate-green (120 tests + HTTP smoke); plain compare + recorded history; resolved the cleared-vs-compare open Q (plain compare); [feature](features/reconcile.md)·[UX](ux/reconcile.md) |

**Later domain additions**

| ID | Was | Item | Kind | Value | Risk | Gated by | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S11** | R6 | **Transaction delete** — no way to remove a mis-entered `normal` transaction or a transfer pair; edit-split (`BUD-S4`) handles corrections-in-place but a duplicate or completely wrong entry has no corrective path | slice | High | Med | BUD-S2 · BUD-S47 | **✅ Done** | Hard delete. `DELETE /transactions/:id` (normal/opening only; 409 for transfer legs) + `DELETE /transfers/:id` (cascades both legs + allocations via DB FK). Delete buttons on every row in `AccountRegister`. **235 Vitest + 38 e2e.** [status report](status-reports/2026-06-21-r6.md) |
| **BUD-S12** | R7 | **Account archive / close** — `archived_at` exists on accounts in the schema and `AccountView.archivedAt` is already in `api.ts`, but no service method or UI exposes it | slice | Med | Low | BUD-S1 · BUD-S47 | **✅ Done** | Mirrors the envelope-archive pattern (`BUD-S5`): `accountService.setArchived` + `POST /accounts/:id/archive` / `/unarchive`; Dashboard `AccountList` splits active/archived with a "Show archived" toggle; archived accounts stay viewable (register + transaction delete work) — only **new** transfers reject them (existing guard). No schema change. **236 Vitest + 39 e2e.** [status report](status-reports/2026-06-21-r7.md) |
| **BUD-S13** | R8 | **Transaction search / filter** — the account register loads all transactions with no server-side filter; as data grows the list becomes unbounded | slice | Med | Low | BUD-S2 · BUD-S47 | **✅ Done** | Date-range query params (`?from=YYYY-MM-DD&to=YYYY-MM-DD`, default = current month) on `GET /accounts/:id/transactions` + From/To inputs and a client-side payee/memo search box on the register. **Challenge resolved:** `opening` rows are always included (the register's balance anchor), and needs-allocation surfacing is a separate household-wide endpoint so it is unaffected by the window. No schema change; `occurredOn` already stored. **240 Vitest + 40 e2e.** [status report](status-reports/2026-06-22-r8.md) |
| **BUD-S14** | R15 | **Envelope ledger** — the Dashboard shows each envelope's derived balance but gives no way to drill into what's in it; you can't answer "what transactions touched Groceries this month?" without going through the Spend-by-Envelope analysis view | slice | Med | Low | BUD-S2 · BUD-S47 | **✅ Done** | Read-only per-envelope allocation list: `GET /envelopes/:id/ledger` (allocations ⋈ transactions ⋈ accounts, newest-first, household-scoped) + `EnvelopeLedger.tsx` panel opened by clicking an envelope name on the Dashboard. No schema change. Opening- and transfer-kind rows included; envelope-transfer reallocations out of scope for V1. **e2e/envelopes.spec.ts extended (+1 test).** [feature](features/envelope-ledger.md)·[UX](ux/envelope-ledger.md)·[status report](status-reports/2026-06-21-r15.md) |

### BUD-E3 — Analysis & Insights data

The read-only analysis area — spend, budget-vs-actual, forecast, credit/debt, net worth — plus the cockpit net-worth snapshot. Forecast was de-risked by SPIKE-05. (The UX Uplift later rebuilt these views as charts; see BUD-E8.)

**Analysis slices**

| ID | Was | Item | Kind | Value | Risk | Gated by | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S15** | #11 | **Analysis — spend by envelope over time** (monthly/annual rollups) | slice | Med | Low | real data from BUD-S2 | **✅ Done** | Built & gate-green (135 tests + 1 e2e); generated net-flow grid, reallocations excluded, archived included; **no schema change** (read-only aggregate query); [feature](features/analysis-envelope-spend.md)·[UX](ux/analysis-envelope-spend.md) |
| **BUD-S16** | #12 | **Analysis — budget vs. actual** | slice | Med | Med | envelope **monthly targets** capability | **✅ Done** | Built & gate-green (151 tests + 1 e2e); new `envelope_targets` store (one recurring monthly target, set/cleared inline) + sibling `budgetVsActual(month)` read (actual = **outflow** spend, funding excluded, refunds netted); resolved PRD §9 target Q (single recurring, no ADR); shipped a latent CORS allow-methods fix; [feature](features/budget-vs-actual.md)·[UX](ux/budget-vs-actual.md) |
| **BUD-S17** | #13 | **Analysis — cash-flow forecast** (pay-period projection) | slice | Med | Med | BUD-S2 · BUD-S9 · ✅ SPIKE-05 | **✅ Done** | Built & gate-green (168 tests + 1 e2e); per-account event-stepped cash projection from `v_account_balances` + scheduled recurring + netted expected-spend toggle; pure domain `cashFlowForecast`, **no schema change**; de-risked by [SPIKE-05](spikes/05-cashflow-forecast.md); [feature](features/cash-flow-forecast.md)·[UX](ux/cash-flow-forecast.md) |
| **BUD-S18** | #14a | **Analysis — credit utilization** (owed vs. limit · trend · roll-up) | slice | Med | Med | `kind='credit'` accounts + a credit-limit store | **✅ Done** | Built & gate-green (192 tests + 1 e2e); new `credit_limits` store (one limit per credit account, set/cleared inline) + pure-domain `creditUtilization` (owed = −balance; owed/limit in bps; cumulative monthly trend; portfolio roll-up); resolved the credit-balance **sign** open Q (owed = −balance at the analysis layer); [feature](features/credit-utilization.md)·[UX](ux/credit-utilization.md) |
| **BUD-S19** | #14b | **Analysis — debt payoff %** (installment loans: 1 − owed/original) | slice | Med | Med | a `kind='loan'` account + an original-principal store | **✅ Done** | Built & gate-green (214 tests + 1 e2e); new **`kind='loan'`** account type (idempotent kind-check evolution) + `loan_principals` store (one original per loan, set/cleared inline) + pure-domain `debtPayoff` (owed = −balance; payoff = 1 − owed/original in bps, truthful; cumulative monthly trend; portfolio roll-up); completes the analysis area; [feature](features/debt-payoff.md)·[UX](ux/debt-payoff.md) |
| **BUD-S20** | R9 | **Net worth over time analysis** — analysis covers envelopes and credit/loan trends but has no account-level trend; the "how am I doing overall?" picture is missing | slice | Med | Low | real data from BUD-S2 · BUD-S47 | **✅ Done** | Monthly/annual `Σ account balances` derived from existing transaction data (same aggregate-query pattern as `BUD-S15`). New `GET /analysis/net-worth?grain=month\|year` + thin `NetWorthView` (Assets / Liabilities / Net trend). **Challenge resolved:** net worth = `Σ` of all **signed** balances — liabilities (`kind ∈ {credit, loan}`) sum in negative (the FEAT-014a/b `owed = −balance` convention), so `net = assets + liabilities` and transfer legs cancel; decomposed **by kind** (stable over time), not by sign. Pure-domain `netWorthOverTime`; **no schema change** (read-only aggregate). Extended `e2e/analysis.spec.ts` (+1) + `e2e/a11y.spec.ts` (+1). **258 Vitest + 42 e2e.** [status report](status-reports/2026-06-22-r9.md) |

**Dashboard net-worth (re-homed from the UX-polish batch)**

| ID | Was | Item | Kind | Pri | Gated by | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S21** | R4 | **Dashboard net worth summary** — accounts are listed individually but never summed; no aggregate gives the "how am I doing?" snapshot | polish | P2 | — | **✅ Done** | The current-snapshot sibling of `BUD-S20`. A `NetWorthSummary` table below the account list shows Total assets / Total liabilities / Net worth, computed **client-side** from the already-loaded account list — no new endpoint. **Challenge resolved:** the sketched **sign-based** split (`Σ balance>0`/`<0`) was rejected for `BUD-S20`'s **kind-based** split — it reuses the shared `isLiabilityKind` (credit/loan = liabilities, carried negative) so the snapshot agrees with `NetWorthView`. Sums **all** accounts (active + archived) to match `BUD-S20`'s endpoint, and is equal to it by construction (same transactions via `v_account_balances`, same classifier). Extended `e2e/accounts.spec.ts` (+1) + `Dashboard.test.tsx` (+2). **260 Vitest + 43 e2e.** [status report](status-reports/2026-06-22-r4.md) |

### BUD-E4 — Engineering health

Cross-cutting cleanups and operational-readiness work from two reviews. No live money/invariant bugs — coverage, drift-prevention, and readiness (restore, migrations, network exposure, boundary lint). Restore round-trip was de-risked by SPIKE-09.

**Wave 1 — 2026-06-15 repo review**

| ID | Was | Item | Kind | Pri | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| **BUD-S22** | EH1 | **Share the domain in the web** — import money/format/date from `@budgeteer/domain`; delete the `format.ts` reimpl + `fakeApi` date mirror | refactor | P1 | **✅ Done** | Added `tryParseMoney` (single `DECIMAL_RE` home) + moved `splitEvenly` to domain; widened `formatMoney`; web declares the dep + imports it; kept only the `$`-display `formatCents` (presentation). 121 tests; Vite dev+build consume the workspace TS. [status report](status-reports/2026-06-15-eh1.md) |
| **BUD-S23** | EH2 | **Extract API service plumbing** — shared `dates` util + generic `groupBy`; move `DEFAULT_HOUSEHOLD_ID` out of `db/migrate.ts` into a constants/config module | refactor | P2 | **✅ Done** | New `util/dates.ts` + `util/groupBy.ts` + top-level `constants.ts`; date converters deduped (5→1, dropped a dead `todayStr`), Map-by-parent loop replaced (×4 incl. transfer legs), household const decoupled from the migrator (9 importers). No behavior change; 121 tests. [status report](status-reports/2026-06-15-eh2.md) |
| **BUD-S24** | EH3 | **Map DB unique-violation → 409** + typed route params | fix | P2 | **✅ Done** | New `services/dbErrors.ts` (`isUniqueViolation`/`asDuplicateName`) maps SQLSTATE `23505` → `DuplicateNameError` (→409) at the service layer; wraps the 6 name-writing methods. All 12 `req.params as {…}` casts → Fastify route generics. 125 tests (+4, incl. real-PGlite violation). [status report](status-reports/2026-06-15-eh3.md) |
| **BUD-S25** | EH4 | **Add ESLint to the gate** (`@typescript-eslint` + `react-hooks`); remove now-dead `eslint-disable` comments | tooling | P3 | **✅ Done** | ESLint 9 flat config + `npm run lint` (zero-warning gate); caught & fixed a real rules-of-hooks error (`useRemaining`→`fillRemaining`); disables now meaningful + made consistent. 125 tests. [status report](status-reports/2026-06-15-eh4.md) |
| **BUD-S26** | EH5 | **Browser e2e (Playwright)** — minimal now (dashboard loads vs. real API + 1 journey); **full journeys and per-area split land in `BUD-S47`** | hardening | P1 | **✅ Done** | Playwright (Chromium-only) drives the real web app vs. the real API: dashboard loads (the CORS-class check) + one journey (account → envelope → deposit allocated → derived envelope balance). New `test:e2e` gate step, kept out of `npm test`. 125 Vitest + **1 e2e**. [status report](status-reports/2026-06-15-eh5.md) |
| **BUD-S27** | EH6 | **Repo hygiene** — discard the absorbed `spikes/04-transfer-modeling/`; record accepted notes (bigint→Number; normalize-vs-btrim) | chore | P3 | **✅ Done** | Removed the throwaway spike code (no importer; not a workspace; tooling already excluded `spikes/`); findings persist in `ADR-0004` + `BUD-S6`/`BUD-S7` + the [spike report](spikes/04-transfer-modeling.md). Two notes recorded as accepted (no behavior change): bigint→`Number()` exact to ~$90T in `money.ts`/`05_DATA_MODEL`; `normalizeName` ⊃ DB `lower(btrim())` in `naming.ts`/`05_DATA_MODEL`. No test-count change (125 + 1 e2e). [status report](status-reports/2026-06-15-eh6.md) |

**Wave 2 — 2026-07-02 architecture review**

| ID | Was | Item | Kind | Pri | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| **BUD-S28** | EH7 | **Inject the clock into services** — `todayStr()`/`currentMonthRange()` reach for `new Date()` inside `recurringService` + the register's default window; the carried **failing** `recurring.test.ts` post-due case is the symptom. Thread `now?: () => Date` through `buildServer`/service factories; make the recurring fixtures absolute; fold in the flaky forecast-e2e selector fix. Retires both carries properly and restores a fully green gate. | fix | P1 | **✅ Done** | `buildServer({ clock })` → 3 services + every route plugin; `todayStr`/`currentMonthRange` **require** the clock; `createTestApp({ today })` pins it; both carries retired (root cause: the register's current-month default window clipped relative-date fixtures on days ≤ 21; the e2e flake: non-exact `getByLabel` matched the chart aria-label → `exact: true`). **Gate fully green: 367/367 Vitest + 94/94 e2e.** [status report](status-reports/2026-07-02-eh7.md) |
| **BUD-S29** | EH8 | **Decide + document the timezone policy** — "today"/"this month" are UTC-derived on server (`util/dates.ts`) and client (8 components); every default shifts a day/month early for users west of UTC from ~evening on. Client derives local calendar dates; server stops deriving "today" for user-facing defaults (rides BUD-S28's seam). Record in `04_DOMAIN_MODEL`. | fix | P2 | **✅ Done** | Policy in `04_DOMAIN_MODEL` §6: calendar dates are **user-local**; client derives them (`apps/web/src/dates.ts`), server requires every date/month param at the boundary (`06_API_CONTRACT` §1) — `currentMonthRange` deleted, `todayStr` survives only for the backup filename. [status report](status-reports/2026-07-03-eh8.md) |
| **BUD-S30** | EH9 | **Versioned migrator** — `migrate.ts`'s single idempotent migration has outgrown its own "replace me when the schema evolves" premise (drop/re-add kind checks, defensive `add column if not exists`). Adopt Kysely `Migrator`; freeze today's file as `0001-baseline`. **Must land before real-Postgres data or `BUD-S77`.** | refactor | P2 | **✅ Done** | Kysely `Migrator` + in-code registry (`db/migrations/`); `0001-baseline` = the old function frozen verbatim (idempotent → pre-migrator stores adopt cleanly); forward-only, fail-loud, household seed stays a startup ensure (the `db:reset` truncate contract). `05_DATA_MODEL` §4 rewritten in the same change. **Unblocks BUD-S31/`BUD-S77`.** [status report](status-reports/2026-07-03-eh9-eh14.md) |
| **BUD-S31** | EH10 | **Prove restore (`BUD-S77` re-raise)** — the export has never round-tripped; `07_NFR` names it the only recovery path while "Restore proven" is unchecked and real data accumulates in `PGLITE_DIR`. Spike (FK order, ID collisions, versioning) → minimal CLI restore + a gate test proving `export → restore → export` equivalence. UI can wait; the proof can't. | hardening | P2 | **✅ Done** | [SPIKE-09](spikes/09-restore-roundtrip.md) proved exact round-trip equivalence; `npm run db:restore -- <file>` (non-destructive: empty-store-only, one transaction, FK-safe order, schema check via the export's new `schema.migrations` stamp — pre-stamping files restore with a warning). `07_NFR` "Restore proven" ✅; `06_API_CONTRACT` restore section. [status report](status-reports/2026-07-03-eh10.md) |
| **BUD-S32** | EH11 | **Bind localhost by default** — the API hardcodes `host: "0.0.0.0"` with no auth: the household ledger is writable by any device on the LAN (CORS doesn't stop `curl`). Add validated `HOST` config, default `127.0.0.1`; opting into `0.0.0.0` is the documented trigger to pull `BUD-E13` forward. | fix | P2 | **✅ Done** | Validated `HOST` config (trimmed non-empty string, default `127.0.0.1`) → `app.listen`; `.env.example`/README/SECURITY.md §3 document the loopback default and the `0.0.0.0`-pulls-`BUD-E13`-forward trigger. [status report](status-reports/2026-07-02-eh11.md) |
| **BUD-S33** | EH12 | **Share the DTO types; decide the client-boundary stance** — web `api.ts` hand-duplicates the service view interfaces (`AccountView` et al.) and trusts responses via one `as T` cast. Export view types from the api workspace (types-only) and import them in web; then either add a light runtime check on reads (measure vs. the ~2.7 KB bundle headroom) or record "client trusts the typed contract" in `06_API_CONTRACT`. | refactor | P3 | **✅ Done** | `@budgeteer/api/contract` (types-only module; the server's own view types + the domain-defined report types) replaces every hand-copy in web `api.ts` — zero drift found; the bundle is byte-identical (117.32 KB gz). Stance decided: **client trusts the typed contract on reads**, recorded in `06_API_CONTRACT` §4 (single first-party client; a client-side schema mirror = BUD-S22's two-sources-of-truth again). [status report](status-reports/2026-07-03-eh12-eh13.md) |
| **BUD-S34** | EH13 | **Lint the layer boundaries** — ARCHITECTURE §2 prefers tooling-enforced boundaries; ESLint (BUD-S25) is syntactic only. Per-zone `no-restricted-imports`: domain bans fastify/kysely/pg/react/node:*; web bans kysely/pg; `http/routes/**` bans direct `db/*`. ~20 lines of config; erosion fails the gate instead of the next review. | tooling | P3 | **✅ Done** | Three zones in `eslint.config.js` as specced, plus a fourth ban the review didn't have: web may import `@budgeteer/api` **types-only** (`allowTypeImports`) — the lint that keeps BUD-S33's sharing honest. Zero pre-existing violations; every ban verified to fire. [status report](status-reports/2026-07-03-eh12-eh13.md) |
| **BUD-S35** | EH14 | **Make post-due idempotency structural** — concurrent `postDue` calls can double-post (both read the cursor pre-commit). Add `unique (recurring_id, occurred_on) where recurring_id is not null`; treat the violation as already-posted via the BUD-S24 `dbErrors` shim. Bundle with BUD-S30's first migration file. | hardening | P3 | **✅ Done** | Migration `0002`: partial unique `(recurring_id, occurred_on)`; `postDue` treats the violation as already-posted (0 posted, no error — the concurrent winner advanced the cursor). Bundled with BUD-S30 as planned. [status report](status-reports/2026-07-03-eh9-eh14.md) |

### BUD-E5 — Security hardening

The 2026-07-06 whole-repo security review. No live bugs; all three findings are posture/supply-chain. BUD-S38 (unauthenticated API) folds into the deferred multi-user epic BUD-E13.

| ID | Was | Item | Kind | Pri | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| **BUD-S36** | SEC1 | **Retire vulnerable `kysely` + fix the SCA justification** — `kysely@0.27.6` is a **direct runtime** dep (3 high advisories), not the "transitive type-gen tool, not used at runtime" the gate comment claims. Not currently exploitable (Postgres dialect → the `sql.lit` backslash advisory is MySQL-only · the two `sql.lit` sites are enum-fed · no JSON-path/`Kysely<any>`), but the comment would wave off a future runtime-Postgres advisory. Correct `gate.yml`'s note to the real reasons; bump `kysely` (or `overrides`-pin) once `kysely-pglite` ships `kysely-codegen ≥ 0.19`; re-run `npm audit --omit=dev`. | tooling / dep | P2 | **Proposed** | `apps/api/package.json:24` · `.github/workflows/gate.yml:60-68` · `analysisService.ts:219,585` |
| **BUD-S37** | SEC2 | **Dev-tool advisories (vitest `critical`, vite/esbuild dev-server)** — correctly excluded from the shipped gate by `--omit=dev`; none ship. Informational, one operational caveat: keep the Vite dev server + Vitest UI **loopback-only**. Fold a `vite@5→8`/`vitest@2→3` bump (both breaking) into the next tooling refresh. | tooling | P3 | **Info (no action)** | dev tree only — not in the web build output |
| **BUD-S38** | SEC3 | **Unauthenticated API, incl. one-request full-data `/export`** — reaffirms (does not reopen) the V1 no-auth stance. The loopback bind (BUD-S32) is the only control; CORS doesn't gate `curl`; `/export` is total exfiltration in one call. Binding `0.0.0.0` exposes read+write of the whole ledger with zero auth. **Folds into `BUD-E13`** (already its documented home); until then, add no HTTP restore/import endpoint (CLI-only restore is correct). | epic (folds into BUD-E13) | P2 | **Tracked in BUD-E13** | `backup.ts:10` · `server.ts` · `config.ts:10` |

### BUD-E6 — UX polish

Quick user-facing wins from the 2026-06-17 improvement review that needed no new API. (The net-worth summary R4 moved to BUD-E3; the feature slices R6–R9/R15 moved to BUD-E2/BUD-E3.)

| ID | Was | Item | Kind | Pri | Gated by | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S39** | R1 | **Account rename in UI** — `PATCH /accounts/:id` is implemented and tested; `api.ts` has no `renameAccount` method and the Dashboard has no rename control. Envelopes already expose inline rename. | polish | P1 | — (API done) | **✅ Done** | `renameAccount` in `api.ts` + `fakeApi.ts`; `AccountList` inline rename (Rename button + input + Save); `refreshAccounts` in Dashboard. `exact: true` convention applied across all e2e specs. +1 e2e. **227 Vitest + 24 e2e.** [status report](status-reports/2026-06-21-r1.md) |
| **BUD-S40** | R2 | **Needs-allocation count badge** — the "Needs allocation" header button has no count, so its urgency is invisible until clicked | polish | P1 | — (API done) | **✅ Done** | The Dashboard fetches `api.listNeedsAllocation()` once on mount (**independently** of the core account/envelope load, so a badge failure degrades to "no badge" rather than blanking the page) and renders the count as a red pill on the "Needs allocation" button. **Challenge resolved:** front-end fetch of the existing endpoint over a new count aggregate (zero API work; same single round-trip the `NeedsAllocation` view already makes; no N+1). Freshness is automatic — `App` remounts the Dashboard on back-navigation, so completing an allocation and returning refreshes the count. Accessible: the count is in the button's `aria-label` ("Needs allocation (3)"), and the pill is white-on-`#991b1b` (~8.3:1, passes AA — validated by the existing Dashboard axe scan). `Dashboard.test.tsx` +2 (exact count vs. a clean fake; hidden when caught up); `e2e/transactions.spec.ts` +1 (count renders accessibly + links to the right list). **262 Vitest + 44 e2e.** [status report](status-reports/2026-06-22-r2.md) |
| **BUD-S41** | R3 | **Grouped navigation — unified Analysis section** — the header had grown a long flat row of analysis-view buttons (Spend, Budget, Forecast, Credit, Payoff, **Net worth**) that required full back-navigation between them | polish | P2 | — | **✅ Done** | New thin `AnalysisSection` container groups all **6** analysis views behind one Dashboard "Analysis" entry: a `<nav aria-label="Analysis views">` sub-nav of buttons (`aria-current="page"` on the active one) that **mounts only the active view**, so each view keeps its own `<h1>` + its own data fetch (no eager fetch-all-six) and its own `← Dashboard` exit. **Challenge resolved:** chose semantically-correct nav buttons over a `role="tablist"` widget (each view is a full `<main>`/`<h1>` page, not one swappable panel); grouped Net worth too (BUD-S20 added it after BUD-S41 was written). The 6 views are untouched — only `App.tsx`/`Dashboard.tsx` changed. No transition added → nothing for `prefers-reduced-motion` to gate. `AnalysisSection.test.tsx` +3; `e2e/analysis.spec.ts` + `e2e/a11y.spec.ts` re-pointed to navigate via Analysis → tab (forecast test now switches tab-to-tab in-section). No schema/API/domain change. **265 Vitest + 44 e2e.** [status report](status-reports/2026-06-22-r3.md) |
| **BUD-S42** | R5 | **Envelope targets visible inline** — monthly targets exist (`BUD-S16`) but are only visible in the Budget view; reaching them from the daily-use Dashboard requires a navigation hop | polish | P3 | BUD-S16 (done) | **✅ Done** | Each **active** envelope's **target · spent · remaining** shows inline in its Dashboard envelope-list row. The Dashboard fetches `getBudgetVsActual(currentMonth)` on mount **independently** of the core account/envelope load (mirrors the `BUD-S40` badge — a target-fetch failure degrades to "no inline figures", never blanks the page), then joins to active rows by `envelopeId`. **Challenge resolved:** rendered **only** when a target is set (no faked `$0`); archived list + BUD-S14 envelope-name click target + existing row controls untouched; money via `formatCents`; figures are plain labelled text (not colour-only). Display-layer addition only — no schema/API/domain change. `Dashboard.test.tsx` +2, `e2e/envelopes.spec.ts` +1. **267 Vitest + 45 e2e.** [status report](status-reports/2026-06-22-r5.md) |

### BUD-E7 — Developer experience

Cross-cutting developer-loop improvements from the 2026-06-17 review — watch mode, route modularization, error boundaries, structured logging, and the e2e foundation every later slice builds into.

| ID | Was | Item | Kind | Pri | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| **BUD-S43** | R10 | **API watch mode** — the API dev server (`npm run start`) has no file-watching; every API change requires a manual restart during development | DX | P1 | **✅ Done** | `"dev": "tsx watch src/index.ts"` already in `apps/api/package.json` from a prior session — no work needed. |
| **BUD-S44** | R11 | **Route modularization** — `apps/api/src/http/server.ts` is 682 lines with all 40+ routes inline; adding a new route to any area requires reading the whole file | DX | P2 | **✅ Done** | Split into **9 per-domain Fastify plugin modules** (`http/routes/{accounts,envelopes,transactions,reconcile,transfers,recurring,analysis,templates,backup}.ts`) + a `routes/shared.ts` (error envelope `fail`, money/date helpers, `IdParams`/`AccountIdParams` generics, `Services` container type), each `app.register`'d from `buildServer` with `{ services }`. `buildServer` 682→**104 lines** (keeps CORS + body parser + the one error handler + `/health`). **Challenge resolved:** no Fastify `prefix` (domains share crossing URL roots) → **every literal path unchanged**; the global error/CORS/parser are root-level (inherited by encapsulated children); services constructed once. Pure DX refactor, no behavior/schema/API change. **267 Vitest + 45 e2e** (unchanged — the regression net held). [status report](status-reports/2026-06-22-r11.md) |
| **BUD-S45** | R12 | **React error boundaries** — no `ErrorBoundary` exists; a render error in any view crashes the entire app to a blank screen | DX | P2 | **✅ Done** | One reusable `ErrorBoundary` (`apps/web/src/ErrorBoundary.tsx`) placed twice: top-level wrapping `<App>` in `main.tsx` (recovery = reload) **and** per active analysis view in `AnalysisSection` (`key={tab}`; recovery = `← Dashboard`). Accessible fallback — `<main>` + `<h1>` + `role="alert"` + a real button, a high-contrast card (`#1a1a1a`/`#fff`, `colorScheme: light`) readable in dark mode. **Challenge resolved:** catches **render/lifecycle** throws only (async/fetch already degrade via `loadError`), so it protects the one unprotected failure — a render crash that `main.tsx`'s direct `<App/>` render would blank. e2e-reachable via a **dev-only** `?boom` hook (`import.meta.env.DEV`-gated → tree-shaken from the prod build; verified). `ErrorBoundary.test.tsx` +3, `e2e/error-boundary.spec.ts` +1, `e2e/a11y.spec.ts` +1. Zero API/schema/domain change. [status report](status-reports/2026-06-23-r12.md). **270 Vitest + 47 e2e.** |
| **BUD-S46** | R13 | **Structured API logging** — `buildServer` defaulted to `logger: false`; the API emitted no request/response logging. Fastify bundles `pino` at zero cost. | DX | P3 | **✅ Done** | `buildServer`'s `logger` widened `boolean`→`FastifyServerOptions["logger"]`; `index.ts` passes `{ logger: { level: config.LOG_LEVEL } }` → Fastify bundled-pino request/response/error lines at the configured level. `LOG_LEVEL` tightened to a **closed enum** of pino levels, validated at the config boundary (fail-loud, spine §8). Tests omit the option → `logger: false` → quiet + deterministic. **Challenge resolved:** keep Fastify's **default** serializers (log `method`/`url`/`statusCode`/`responseTime` + error type/message/stack — never bodies/headers) and add **no** body serializer, so financial bodies (transaction memo/payee/amount, the `/export` snapshot) never reach the logs (SECURITY.md §1/§5). Regression net = a capture-stream test asserting the access line is present *and* a money-route body/`authorization` header never leak, + a `config.test.ts` fail-loud check; real-startup smoke confirmed structured JSON to stdout with a `reqId` correlation id. No API-contract/schema/domain change. `logging.test.ts` +2, `config.test.ts` +3. **275 Vitest + 47 e2e.** [status report](status-reports/2026-06-23-r13.md) |
| **BUD-S47** | R14 | **e2e expansion** — `e2e/journey.spec.ts` is a single growing file chaining 8+ steps; no full journey coverage exists for features shipped before `BUD-S15` (transactions, templates, transfers, refunds, recurring, reconcile); failures are slow to isolate; the monolith grows with every new slice | hardening | P1 | **✅ Done** | `journey.spec.ts` deleted; replaced by 8 per-area files + `e2e/setup.ts` helper. 20 Playwright tests (was 1): accounts (2), envelopes (1), transactions (5), templates (2), transfers (2), recurring (1), reconcile (2), analysis (5). Full coverage for all features BUD-S2–BUD-S19. Convention established: every new slice lands with its own spec. [status report](status-reports/2026-06-17-r14.md) |

### BUD-E8 — UX Uplift

A foundation-first, desktop-first overhaul — design system + client routing/app-shell, a budget/future-planning cockpit, and accessible spending visualizations. Spike-first / ADR-gated: the two foundational spikes below validated the approach on one real screen before any build slice.

**Foundational spikes**

| ID | Was | Item | Phase | Kind | Value | Risk | Gated by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **SPIKE-06** | UX1 | **Spike: design-system + routing foundation** — bake-off on the Account Register → **chose React Router + design tokens/CSS-Modules + Radix Primitives** (both primitive libs axe-clean; Radix < half the bundle, so the lean flipped from React Aria). `ADR-0005` + `ADR-0006` **`Validated`** | 0 Foundation | spike | High | High | — | **✅ Done** — [SPIKE-06](spikes/06-design-system-routing.md) · [ADR-0005](adr/ADR-0005-frontend-design-system.md) · [ADR-0006](adr/ADR-0006-client-routing.md) |
| **SPIKE-07** | UX2 | **Spike: accessible charting / viz a11y** — built one real chart (net-worth-over-time) → **0 axe violations** (WCAG 2.2 AA, light+dark × table-shown/hidden), keyboard + SR via a data-table fallback; **hand-rolled SVG 1.94 KB gz vs Recharts 129 KB gz** → a library blows the budget. `ADR-0007` **`Validated`** | 0 Foundation | spike | High | High | — | **✅ Done** — [SPIKE-07](spikes/07-accessible-charting.md) · [ADR-0007](adr/ADR-0007-accessible-charting.md) |

**Build slices**

| ID | Was | Item | Phase | Kind | Value | Risk | Gated by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S48** | UX4 | **Design-system foundation** (tokens + base layer + primitives · ADR-0005) — **seed-and-grow**: token sheet + dark mode + primitives (Button/Field/Input/Select/Badge/Card/Alert/EmptyState/Skeleton), **proven by restyling the Account Register in place**; Dialog/Table deferred to the first modal/table slice. [FEAT-UX4](features/design-system.md) | 1 Shell | slice | High | Med | SPIKE-06 ✅ | **✅ Done** — [status](status-reports/2026-06-26-ux4.md) |
| **BUD-S49** | UX3 | **Routing + persistent app shell** (React Router · ADR-0006) — retire `App.tsx`'s `view` state machine; URL-addressable, back/forward, refresh-safe, deep-linkable; reduced-motion-aware ([FEAT-UX3](features/app-shell.md)) | 1 Shell | slice | High | Med | SPIKE-06 ✅ · after BUD-S48 | **✅ Done** — [status](status-reports/2026-06-27-ux3.md) |
| **BUD-S50** | UX5 | **Home: budget + future-planning cockpit** — this-month envelope health + upcoming recurring + cash-flow snapshot + needs-allocation + net-worth, composed from existing reads ([FEAT-UX5](features/cockpit.md)·[UX](ux/cockpit.md)) | 2 Cockpit | slice | High | Med | BUD-S49 · BUD-S48 | **✅ Done** — [status](status-reports/2026-06-27-ux5.md) |
| **BUD-S51** | UX6 | **Demote management to a dedicated surface** — account/envelope CRUD onto the `/accounts`·`/envelopes` LIST routes (progressive add, name-as-Link) + the cross-cutting `/manage` hub (net-worth summary + Move-money); home becomes cockpit-only ([FEAT-UX6](features/manage.md)·[UX](ux/manage.md)) | 2 Cockpit | slice | Med | Low | BUD-S49 · BUD-S48 · BUD-S50 | **✅ Done** — [status](status-reports/2026-06-28-ux6.md) |
| **BUD-S52** | UX7 | **Global quick-add transaction** — always-available entry (modal route) for PRD journey BUD-S1 (the buried common case); reuses the register editor + an account picker, fans out to existing endpoints; new `Dialog` primitive (Radix) ([FEAT-UX7](features/quick-add-transaction.md)·[UX](ux/quick-add-transaction.md)) | 2 Cockpit | slice | High | Med | BUD-S49 · BUD-S48 · BUD-S50 | **✅ Done** — [status](status-reports/2026-06-28-ux7.md) |
| **BUD-S53** | UX8 | **Analysis → Insights migration** — rename + restyle the 6 views, replace number grids with accessible charts (chart + table fallback, per ADR-0007: hand-rolled SVG + a shared `Chart` primitive) | 3 Insights | slice | High | Med | SPIKE-07 ✅ · BUD-S49 ✅ · BUD-S48 ✅ | **✅ Done** — [FEAT-UX8](features/insights-charts.md)·[UX](ux/insights-charts.md)·[status](status-reports/2026-06-28-ux8.md); shared `ui/Chart` (line/bar/gauge) on the ADR-0007 contract; axe-green light+dark with each chart rendered; **315 Vitest + 72 e2e**, 108.33 KB gz; ADR-0007 → `Accepted` |
| **BUD-S54** | UX9 | **New viz: spending breakdown** — "where did the money go" composition for a period (per-envelope share of outflow) | 3 Insights | slice | High | Med | SPIKE-07 · BUD-S53 | **✅ Done** — [FEAT-UX9](features/spending-breakdown.md)·[UX](ux/spending-breakdown.md)·[status](status-reports/2026-06-28-ux9.md); new `/insights/breakdown` ranked-bars view on the `ui/Chart` primitive (new `BreakdownBars` shape); **composes the existing `getBudgetVsActual` read** (no new endpoint); colour carries no categorical meaning (one bar colour + direct labels + rank); axe-green light+dark; **320 Vitest + 75 e2e**, 108.93 KB gz |
| **BUD-S55** | UX10 | **New viz: spending trends over time** — per-envelope + total trend lines, month-over-month comparison | 3 Insights | slice | High | Med | SPIKE-07 · BUD-S53 | **✅ Done** — [FEAT-UX10](features/spending-trends.md)·[UX](ux/spending-trends.md)·[status](status-reports/2026-07-01-ux10.md); new `/insights/trends` line-chart view (reuses `LineChart`, no new shape) — total + top-2 envelopes by outflow over a 3/6/12-month window; **composes the existing `getBudgetVsActual` read, several times** (no new endpoint); 3 series, one chart token each, colour never sole signal; axe-green light+dark; **323 Vitest + 78 e2e**, 109.90 KB gz |
| **BUD-S56** | UX11 | **New viz: budget burn-down** — within-month pace vs. target (on-track projection) | 3 Insights | slice | Med | Med | SPIKE-07 · BUD-S50 | **✅ Done** — [FEAT-UX11](features/budget-burndown.md)·[UX](ux/budget-burndown.md)·[status](status-reports/2026-07-01-ux11.md); new `/insights/burndown` **gauge** view (reuses `Gauge`, no new shape) — within-month budget-consumed (spent ÷ target) vs. the **elapsed-time pace** (threshold marker = "pace today"); **composes the existing `getBudgetVsActual` read, ONE call** (scope picker: household aggregate or one envelope) + one new **pure domain fn** (`assessBurndown`); `over-budget`/`over-pace`/`on-track` via text + position, never colour; axe-green light+dark; **335 Vitest + 81 e2e**, 111.06 KB gz |
| **BUD-S57** | UX12 | **Feedback & states** — skeletons, success toasts, **confirm on destructive actions** (Archive/Delete), inline validation | 4 Polish | slice | Med | Low | BUD-S48 | **✅ Done** — all 4 threads shipped. **BUD-T1** [FEAT-UX12a](features/destructive-confirms.md)·[status](status-reports/2026-07-02-ux12.md): new **`ConfirmDialog`** primitive (controlled/transient, on the validated Radix `Dialog` — no new dep) gates the three one-click destructive actions (**Archive envelope · Archive account · Delete template**); consistent blocking confirm, state via action-word text + title (never colour), axe-green light+dark with the dialog open. **BUD-T2** [FEAT-UX12b](features/skeleton-loaders.md)·[status](status-reports/2026-07-02-ux12b.md): the 16 bare `Loading…` strings → the existing BUD-S48 **`Skeleton`** primitive (net a11y win — ~9 sites now announce via `role="status"`; no new dep/code, net −0.04 KB gz). **BUD-T4** [FEAT-UX12d](features/inline-validation.md)·[status](status-reports/2026-07-02-ux12d.md): money-amount inputs that failed silently/at-submit now surface a **field-level** error (new pure `amountFieldError` reusing `tryParseMoney` → new **`FieldError`** primitive: `aria-invalid` + `aria-describedby`, text carries the state never colour) across the transaction/transfer/move/add-account forms; submit guarded client-side, semantic rules stay server-authoritative; no new dep; axe-green light+dark with the error visible. **BUD-T3** [FEAT-UX12c](features/success-toasts.md)·[status](status-reports/2026-07-02-ux12c.md): new **`ToastProvider`/`useToast()`** on **`@radix-ui/react-toast`** (the last hard a11y widget ADR-0005 reserved for Radix — the only thread needing a new dep) fires a transient success toast on the successful-mutation set (create/transfer/move/archive/delete); announces **`role="status"` polite (never alert)**, transform-only entrance, reduced-motion-safe, text carries the state; proven axe-green light+dark with the toast visible. **354 Vitest + 89 e2e**, 116.31 KB gz (+4.63 KB Radix Toast). |
| **BUD-S58** | UX13 | **Money & budget-health visual encoding** — progress bars + weight/color (never color-only) for over-budget / utilization / payoff / net worth | 4 Polish | slice | Med | Low | BUD-S48 | **✅ Done** — [FEAT-UX13](features/budget-health-encoding.md)·[status](status-reports/2026-07-02-ux13.md); new **`ProgressBar`** primitive (the last BUD-S48 seed-and-grow primitive — hand-rolled on tokens + CSS, **no charting dep**; the SVG `Gauge`/`BarChart` stay for the Insights charts) drives a per-row **spent-of-target** bar in the Budget table + a **summary bar** in the cockpit budget panel; over-budget/negative money (remaining · liabilities · net worth) gets **weight + colour** with the minus sign kept as the non-colour signal (supersedes the V1 "minus sign only" encoding). Bar is **decorative** (`aria-hidden` — no new AT surface), the `over` tone adds a diagonal **hatch SHAPE**, track+fills clear 3:1, **no animation**; axe-green light+dark with both the under- and **over**-budget encoding visible. No ADR/spike/dep, no data/API/domain/chart change. **360 Vitest + 91 e2e**, 116.87 KB gz (+0.56 KB, no dep). |
| **BUD-S59** | UX14 | **Empty states & first-run onboarding** — guided "add an account, add envelopes" (ties to the PRD week-one metric) | 4 Polish | slice | Med | Low | BUD-S49 · BUD-S48 | **✅ Done** — [FEAT-UX14](features/first-run-onboarding.md)·[status](status-reports/2026-07-02-ux14.md); new **`FirstRunOnboarding`** component (**composes the existing BUD-S48 `EmptyState`** — no new dep) shown by the home when the app is completely empty, a state **DERIVED** from `listAccounts`+`listEnvelopes` (no stored "has onboarded" flag; derive-don't-store); replaces the cockpit's five per-panel empty states with one guided next step (two CTA `<Link>`s → `/accounts` · `/envelopes`), tied to the PRD week-one metric; the moment anything exists the cockpit takes over. Axe-green light+dark with the onboarding surface visible (empty-store scan runs first). No ADR/spike/dep, no data/API/domain change. **364 Vitest + 92 e2e**, 117.20 KB gz (+0.33 KB, no dep). |
| **BUD-S60** | UX15 | **Responsive pass** — desktop-first layouts that degrade gracefully to phone width | 4 Polish | slice | Med | Low | BUD-S49 · BUD-S48 | **✅ Done** — [FEAT-UX15](features/responsive-pass.md)·[status](status-reports/2026-07-02-ux15.md); **CSS/token-only** (no dep). Much was already responsive (cockpit + figure grids `auto-fit`, nav + rows `flex-wrap`, chart SVGs `width:100%`); this slice closes the phone-width gaps — **reflow (WCAG 1.4.10)**: wide data tables scroll within their own **keyboard-focusable** region (one global `.table-scroll` wrapper in **`ChartFigure`** covers all nine Insights tables, + the envelope ledger), never the page; the **shell header** degrades to a narrow column + the **Insights sub-nav** becomes a wrapping tab row with valid **target-size** (WCAG 2.5.8); page padding tightens ≤ 640px. **Scope call:** "nav collapses" = graceful degradation (already wraps), **no hamburger** (follow-on). Axe-clean **at 320px light+dark** (new reflow e2e describe asserts no horizontal page scroll). No ADR/spike/dep, no data/API/domain/chart change. **366 Vitest + 94 e2e**, 117.27 KB gz (+0.07 KB, no dep). **Completes the UX Uplift (`SPIKE-06`–`BUD-S60`).** |

### BUD-E9 — Sheet parity

The two capabilities of the owner's working BudgetHome.xlsx planner Budgeteer did not yet cover (SPIKE-08 mapped the rest as already-shipped). Pay-period policy was validated by SPIKE-10.

| ID | Was | Item | Kind | Value | Risk | Gated by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S61** | S7 | **Pay-period planning ("which paycheck covers what")** — assign recurring rules + per-period envelope funding to **expected income occurrences**; show each paycheck's bucket total and a **committed-obligations headroom** line (SPIKE-08 S8 folds in); carry the sheet's ~month-ahead reservation buffer. The sheet's colour-only blue/red bucket join is expressed as **text + structure, never colour alone** (ADR-0007/BUD-S58 conventions). Start **derived-only** (buckets computed from recurring rules + targets, no schema change); an explicit bill↔paycheck **assignment store** is a §11 ceremony scale-up (spec + data-model change, possibly an ADR) — decide at spec time. | slice | High | Med | — | **✅ Done** — [FEAT-S7](features/pay-periods.md) `Implemented` · [UX](ux/pay-periods.md) `Validated` · [SPIKE-10](spikes/10-payperiod-policy-validation.md) · [status](status-reports/2026-07-03-s7-slice.md). The §9 validation **invalidated the date-only policy** on the owner's real bills; §5 revised to **balanced latest-fit** (capacity-aware) and the derived-only V1 built the same day: pure `payPeriodPlan` → `GET /analysis/pay-periods` → Insights **Pay periods** tab. The assignment store stays the recorded §8 scale-up (trigger sharpened: pinning a specific bill to a specific check) |
| **BUD-S62** | S9 | **"Still owed this month"** — a small derived read (unposted recurring occurrences through month-end, summed) surfaced on the cockpit's upcoming-recurring panel: the sheet's D-column countdown, computed instead of hand-chained. No schema change; §11 compressed (FEAT note, no spike). | polish | Med | Low | — | **✅ Done** — [FEAT-S9](features/still-owed.md)·[status](status-reports/2026-07-03-s9-s7-spec.md); pure `stillOwedCents` in `@budgeteer/domain` (withdrawals only; past-due unposted count; posting clears) + a labelled `<dl>` figure on the Upcoming panel, client-derived per the cockpit's BUD-S21/BUD-S42 compose-existing-reads precedent (month-end via `dates.ts`, BUD-S29). No API/schema/contract change |

### BUD-E10 — UX Redesign

Owner-directed redesign toward a grouped-sidebar dashboard layout, then a post-track polish batch surfaced from living with the result. Presentation/IA only — no data/API/domain change.

**Redesign track (was UXR1–UXR8)**

| ID | Was | Item | Kind | Value | Risk | Gated by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S63** | UXR1 | **Sidebar app shell (the layout shift)** — replace the top-banner nav with a grouped left sidebar (**Budget · Ledgers · Planning · Administration** — resolved 2026-07-06, §11 Q1; footer = the global **Add transaction**, Q2), a top bar (collapse toggle · page title; compact **+ Add** at ≤ 640px where the sidebar is off-canvas), and the route content as the canvas; desktop collapse-to-icon-rail (persisted, client-side) + off-canvas drawer ≤ 640px (**deliberately supersedes BUD-S60's "no hamburger" scope call** — the recorded follow-on; drawer rides the `Dialog` focus machinery). Same routes/data/API behind new chrome. Tensions (brief §3): the budget conversation **resolved 2026-07-06** (re-baselined **120 → 140 KB gz**, `07_NFR` §1³) · the `<h1>` call **resolved 2026-07-06** (**shell owns it** — route-handle titles + a title context for dynamic names, `document.title` synced, views drop theirs; UX spec §11 Q3); **every §11 design Q resolved 2026-07-06** (groups → Budget · Ledgers · Planning · Administration · Add transaction → sidebar footer + compact top-bar entry ≤ 640px, plus an Accounts-page button riding `BUD-S65`+ · icons → **lucide (ISC) paths copied into repo-owned components, zero dependency** · slice ships **whole**, no UXR1a/1b split); remaining `Ready` work: FEAT-UXR1 + the e2e re-point (mechanical). | slice | High | Med | — | **Done** 2026-07-07 — [FEAT-UXR1](features/app-shell-sidebar.md) `Implemented`; shell owns the `<h1>`, rail persisted, Radix-Dialog drawer, repo-owned lucide icons (+3.84 KB gz → 122.51). [Status report](status-reports/2026-07-07-uxr1-sidebar-shell.md) |
| **BUD-S64** | UXR2 | **Pay-period planner (rework the BUD-S61 surface)** — the shipped view's data in the sheet's shape: promote `/insights/pay-periods` → **`/pay-periods`** (first-class **Pay periods** item in BUD-S63's Planning group; old URL redirects; cockpit gains a "Next paycheck" deep-link line) and re-lay the stacked bucket sections as **two side-by-side ledgers** (bills: due · amount · **left-to-pay countdown** · covered-by ‖ paychecks: income · committed · headroom · **projected balance** · **reserve** · status badge), whole horizon on one screen, selection-highlight additive over the permanent "Covered by" text join (colour-only join stays banned). Owner-confirmed 2026-07-06: the problem is presentation/placement/missing figures — **NOT the assignment policy** (the FEAT-S7 §5 ratify/veto stays its own open decision). Balance/Reserve **owner-confirmed 2026-07-06** (*Funds* = leftovers → **Reserve** = running Σ of per-check headroom; Balance = projected payday balance): **additive fields on `GET /analysis/pay-periods`** (same gather as the forecast; contract doc in the same change); the left-to-pay countdown shows **both scopes** (month-scoped column + subtotal rows · 90-day pane figure); the cockpit Next-paycheck line ships **in-slice**. | slice | High | Med | BUD-S63 | **Done** 2026-07-07 — [FEAT-UXR2](features/pay-periods-planner.md) `Implemented`; two-ledger `/pay-periods` (redirect from Insights), additive `projectedBalanceCents`/`reserveCents` (reserve ≡ headroomAfter, surfaced per owner), both countdown scopes client-derived, cockpit Next-paycheck line (+1.29 KB gz → 123.80). [Status report](status-reports/2026-07-07-uxr2-pay-period-planner.md) |
| **BUD-S65** | UXR3 | **Ledgers tables** — Accounts · Envelopes · Needs-allocation lists → real tables (design-system table treatment; **presentation only** — BUD-S51 progressive Add, inline rename/archive, BUD-S42 inline budget, the allocate flow, and the count badge all carry) **+ the Accounts-page Add-transaction button** (BUD-S63 §11 Q2's additive half). Bar: `<th>` semantics · per-row accessible action names · BUD-S60 reflow (`.table-scroll`, 320 px) · axe light+dark. | slice | High | Low | BUD-S63 (sequence) | **Done** 2026-07-07 — [UX spec](ux/ledgers-tables.md) `Implemented` (+§8 as-built); one shared [`Ledgers.module.css`](../apps/web/src/Ledgers.module.css); Needs gains a Date column (existing read) + keeps "needs $X"; Accounts Add-transaction `<Link>` → BUD-S52 route (+0.29 KB gz → 124.09). [Status report](status-reports/2026-07-07-uxr3-ledgers-tables.md) |
| **BUD-S66** | UXR4 | **Templates page** — saved-templates list → table; the template editor (name + multi-row envelope/amount lines) re-organized, **establishing the form-layout pattern** BUD-S67/BUD-S69 reuse. | slice | Med | Low | BUD-S63 · BUD-S70 (demo data to design against) | **Done** 2026-07-07 — [UX spec](ux/templates-page.md) `Implemented` (+§8); saved list → table on the BUD-S65 `Ledgers.module.css` treatment (reused verbatim); the **form-layout pattern built in `FormLayout.module.css`** (fieldset/legend · `Field` primitives · envelope/amount line mini-grid · action row) BUD-S67/BUD-S69 import; presentation-only (+0.38 KB gz → 124.47); also fixed a latent `.numeric` right-align specificity bug across all four ledger/Templates tables. [Status report](status-reports/2026-07-07-uxr4-templates-page.md) |
| **BUD-S67** | UXR5 | **Recurring page** — rule forms onto the BUD-S66 pattern; the rules list re-formatted for readability (table with the carried split as an expandable per-row disclosure). Post-due flow unchanged. | slice | Med | Low | BUD-S66 | **Done** 2026-07-07 — [UX spec](ux/recurring-page.md) `Implemented` (+§9); rule form imports `FormLayout.module.css` (**proving the pattern reuses** — the one pattern-completing addition was `.fieldRow`, realizing §3.1's gridded pairs for its first consumer); rules list → table on the BUD-S65 `Ledgers.module.css` treatment (verbatim) with **Payee surfaced** + the split behind a per-row disclosure; **Delete gains the BUD-S57 `ConfirmDialog`** (both owner-ratified); presentation-only (+0.49 KB gz → 124.96). Hardened a pre-existing pay-periods a11y selection race in passing (test helper only). [Status report](status-reports/2026-07-07-uxr5-recurring-page.md) |
| **BUD-S68** | UXR6 | **Insights IA** — the wrapping sub-nav → a page-level **top tab bar**; **owner-endorsed 2026-07-07**: **five category tabs** (Spending: by-envelope/breakdown/trends · Budget: vs-actual/burn-down · Cash flow: forecast · Debt: credit/payoff · Net worth) with segmented sub-views; **every `/insights/:view` URL preserved** (the flat-tab alternative passed over). | slice | High | Med | BUD-S63 · BUD-S64 (tab set final once Pay periods leaves) | **Done** 2026-07-07 — [UX spec](ux/insights-ia.md) `Implemented` (+§8); flat nine-link sub-nav → a two-row category IA in `AnalysisSection.tsx` (a `CATEGORIES` map → five category `<Link>`s marked current by section + a segmented sub-view row only when > 1 view); **links not ARIA tabs** (routes — ADR-0006); **all nine `/insights/:view` URLs preserved** (a `routing.spec` sweep asserts each + its category tab); labels **By envelope**/**vs Actual** are nav-only (view `<h2>`s untouched); presentation-only (+0.19 KB gz → 125.15). [Status report](status-reports/2026-07-07-uxr6-insights-ia.md) |
| **BUD-S69** | UXR7 | **Manage page** — the Move-money form onto the BUD-S66 pattern. §11-compressed (FEAT note). | polish | Low | Low | BUD-S66 | **Done** 2026-07-07 — [FEAT note](features/manage-move-money.md) `Implemented`; `MoveMoneyForm` re-laid by **importing `FormLayout.module.css`** (fieldset/legend · `Field`/`Input`/`Select` primitives · From/To + Amount/Memo gridded as `.fieldRow` pairs stacking ≤ 640px · right-aligned action row); **second proof the pattern reuses — no new pattern code**. Presentation-only, flow byte-for-byte (inline amount validation, both/different-envelope guards, reset-on-success, hide-until-two-envelopes, API call all carried); tests passed **unchanged** (label/role/button selectors preserved by the primitives — no re-point needed). +0.06 KB gz → 125.21; CSS unchanged (import only). **Closes the BUD-S63–BUD-S70 track.** [Status report](status-reports/2026-07-07-uxr7-manage-form.md) |
| **BUD-S70** | UXR8 | **Demo-grade synthetic seed** — a **standalone** `npm run seed:demo` with a rich **strictly synthetic** dataset (~6 months of transactions · targets · recurring incl. biweekly paycheck + a month-boundary bill cluster · credit/loan limits/principals · 3 templates) so Insights/planner/Templates read meaningfully in design/dev (the owner's "masked behind limited data" callout). Deterministic (fixed-seed PRNG); refuses an occupied store (BUD-S31); baseline `seed` byte-identical; synthetic only (SECURITY.md). The durable unlock stays the deferred history import `SPIKE-03`/`BUD-S79`. | tooling | Med | Low | — (can precede BUD-S63) | **Done** 2026-07-07 — [FEAT note](features/demo-seed.md) `Implemented` ([`seedDemo.ts`](../apps/api/src/db/seedDemo.ts)) |

**Post-track polish (was UXR9–UXR13)**

| ID | Was | Item | Kind | Value | Risk | Gated by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S71** | UXR9 | **Dashboard IA** — rename sidebar "Home" → **Dashboard**; make the home two sub-tabs, **Overview** (the cockpit) + **Pay periods** (the FEAT-UXR2 planner, absorbed from the standalone route); center the site-wide sub-tab navs (Dashboard **and** Insights); widen the planner. `/pay-periods` URL preserved (deep links + cockpit link + `/insights/pay-periods` redirect unchanged). | slice | High | Med | BUD-S63 · BUD-S64 · BUD-S68 (shares the sub-tab-nav treatment) | **Done** 2026-07-07 — [UX spec](ux/dashboard-ia.md) `Implemented` (+§8); new pathless `DashboardLayout` wraps Overview + Pay periods with a centered `.categoryNav` sub-tab nav; sidebar "Home"→"Dashboard" (Pay periods leaves Planning); `PayPeriodsView` `main` widened to 1200px; `.categoryNav`/`.segmentNav` centered + `.categoryNav` active matches NavLink `aria-current`. Presentation-only, cockpit + planner byte-for-byte (+0.06 KB gz → 125.27, CSS +0.02 → 5.23). [Status report](status-reports/2026-07-07-uxr9-dashboard-ia.md) |
| **BUD-S72** | UXR10 | **Insights chart X-axis readability** — the categorical/line chart X-axis labels (long envelope names) overlap and thin out to unreadable; **slant them** (~-35°) and show more, so they read without collision. Touches the shared `ui/Chart.tsx` (all Insights charts). | polish | Med | Low | — | **Done** 2026-07-07 — in the shared `ui/Chart.tsx`, both `LineChart` and `BarChart` x-axis labels are now **slanted -35°** (end-anchored, `rotate(-35 …)`) and **de-thinned** (keep every label up to 24, then thin — was ~8 max), with the viewBox grown to 640×360 (`PAD.bottom` 40→84) so long names don't clip; also removed the `.svg` `max-width: 720px` cap so each chart fills its figure = as wide as the data table below it. Verified live: spend-by-envelope shows all 22 envelope names without collision, and the chart widens to match the table (720→912px at 1440px). Presentation-only, no data/API/domain change (+0.05 KB gz → 125.32; CSS unchanged 5.23). +2 Vitest (slant + density). [Status report](status-reports/2026-07-07-uxr10-chart-xaxis.md) |
| **BUD-S73** | UXR11 | **Add-transaction cleanup** — remove the page-local **Add transaction** button on `/accounts` (the sidebar footer action suffices); re-lay the quick-add **modal form** (`AddTransactionForm`) on the `FormLayout.module.css` pattern the other forms use (it still renders raw `<label>`/`<select>`/`<input>`). | polish | Med | Low | BUD-S66 (the form pattern) | **Done** 2026-07-08 — removed the redundant `/accounts` Add-transaction `<Link>` (the persistent `AppShell` footer action is the single entry to `/transactions/new`); re-laid `AddTransactionForm` (the quick-add modal **and** the account-register embed) on the shared `FormLayout.module.css` pattern — a grouped `New transaction` fieldset+legend, every control via the BUD-S48 `Field`/`Input`/`Select` primitives, Amount+Date gridded (collapses ≤ 640px), full-width Payee. The carried Deposit/Withdrawal radiogroup and the embedded `AllocationEditor` (still raw — restyled next in `BUD-S75`) are unchanged. **Behaviour byte-for-byte**: the amount field keeps `aria-label="Transaction amount"` (distinct from its visible "Amount" label) so every test/e2e selector resolves; picker gate, BUD-T4 inline validation, reset-on-success, and `createTransaction` untouched. Verified live: modal renders the pattern, all accessible names preserved, no console errors, fieldRow collapses at 375px. Presentation-only, no data/API/domain change (+0.01 KB gz → 125.33; CSS unchanged 5.23). Tests: −1/+1 (the `/accounts` Add-transaction link assertion replaced by a "no page-local control" assertion), net 433 Vitest. [Status report](status-reports/2026-07-08-uxr11-add-transaction.md) |
| **BUD-S74** | UXR12 | **Manage page formatting** — re-lay `ManageView` (the net-worth table + management links) on the design-system table/section treatment for consistency with the rest of the app. | polish | Low | Low | BUD-S65 (`Ledgers.module.css`) | **Done** 2026-07-08 — the net-worth summary adopts the shared BUD-S65 table treatment (`Ledgers.module.css` — `.table` bordered rows, `.numeric` right-aligned tabular money, caption now `sr-only` since the `<h2>` labels the section, wrapped in the global `.table-scroll` group) in place of the raw `<table>` with inline `text-align` styles; the two management links (`Accounts` · `Envelopes`) become a row of design-system button-like `<Link>`s (mirroring `ui/Button`'s default variant via a new `ManageView.module.css`) in place of a bare bullet list. **Behaviour byte-for-byte** — the `Management` nav's link names/hrefs and the net-worth table's accessible name (`Net worth summary`) + figures are unchanged, so every unit selector resolves; Move-money (already BUD-S69-patterned) untouched. Verified live: `/manage` renders the bordered summary table + pill links, no console errors. Presentation-only, no data/API/domain change (+0.04 KB gz → 125.52; CSS +0.04 → 5.41). Tests unchanged (433 Vitest + 121 e2e). [Status report](status-reports/2026-07-08-uxr12-manage-formatting.md) |
| **BUD-S75** | UXR13 | **Allocate form on the pattern** — the shared **`AllocationEditor`** (its `<fieldset><legend>Allocate</legend>` — Single/Split modes, template apply, per-row envelope/amount/refund, remainder helpers) still renders **raw `<label>`/`<select>`/`<input>`/`<button>`**, so on the **Recurring** rule form (and everywhere it's embedded) the Allocate section visually clashes with the `FormLayout`-patterned fields around it. Re-lay it on the `Field`/`Input`/`Select` primitives + the split line mini-grid (the BUD-S66 pattern). **Shared component** — also inside the add-transaction modal (BUD-S73), the account register, and needs-allocation, so restyling it aligns all four; **behaviour byte-for-byte** (modes, template apply, refund, distribute/use-remaining, save guard). | polish | Med | Med | BUD-S66 (form pattern) · coordinate with BUD-S73 (shares the add-transaction modal) | **Done** 2026-07-08 — re-laid the shared `AllocationEditor` on the pattern: the grouped fieldset shell + `Field`/`Input`/`Select`/`Button` primitives from `FormLayout.module.css`, with the allocation-specific rows (mode radiogroup, single/split lines with their extra refund + use-remaining controls, live summary) in a new sibling `AllocationEditor.module.css` (the split row is richer than TemplatesView's 3-col `.lineRow`, so its 5-col grid lives there — FormLayout untouched, so TemplatesView is unaffected). The `Apply template`/`Save as new template` accelerators become `Field`s in a `.fieldRow`; the split rows reflow to one column ≤ 640px. **Behaviour byte-for-byte** — every accessible name (`Allocate`, `Allocation mode`, `Apply template`, `New template name`, `Envelope`, `Envelope/Amount/Refund/Remove for row N`, `use remaining`, `Add row`, `distribute remaining`), handler, and the save guard are unchanged, so all four embeds (quick-add modal, register, needs-allocation, Recurring rule form) and every unit/e2e selector resolve. Verified live: the Recurring Allocate section + the quick-add modal now match the fieldsets around them (clash BUD-S73 left is gone), split-row grid aligns, mobile reflows at 375px, no console errors. Presentation-only, no data/API/domain change (+0.15 KB gz → 125.48; CSS +0.14 → 5.37). Tests unchanged (433 Vitest + 121 e2e, all passing). [Status report](status-reports/2026-07-08-uxr13-allocate-form.md) |

### BUD-E11 — Hardening

Backup/export, restore/import, and the a11y + perf-budget + CI gate. (Restore was later proven under BUD-S31/EH10.)

| ID | Was | Item | Kind | Value | Risk | Trigger (when) | Status | Links |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **BUD-S76** | #15a | **Backup / export** — `GET /export` → `budgeteer-backup-YYYY-MM-DD.json` (JSON snapshot of all 15 tables); "Download backup" link on the Dashboard. Cents as numbers, timestamps as ISO strings, no views included. | hardening | Med | Med | real daily data exists | **✅ Done** | [`07_NFR`](07_NFR.md) · [`status-reports/2026-06-17-slice-15a.md`](status-reports/2026-06-17-slice-15a.md) |
| **BUD-S77** | #15b | **Restore / import** — round-trip a JSON backup file back into the local store. Gated on a spike: FK insert order, ID collision handling, partial failure, schema evolution. | hardening | Med | High | `BUD-S76` done | Deferred | Owner tabled restore for now — continuing forward with `BUD-S78` + new feature slices. Spike question (when revisited): does a JSON snapshot round-trip cleanly through the schema, IDs and all? |
| **BUD-S78** | #16 | **a11y pass** (WCAG 2.2 AA) + perf/NFR budgets + **ESLint in CI** ([BUD-S25](reviews/2026-06-15-repo-review.md)) + **e2e in CI** (automated `test:e2e` trigger in `gate.yml`, companion to ESLint-in-CI) | hardening | Med | Low | UI surfaces exist · BUD-S47 done | **✅ Done** | [`07_NFR`](07_NFR.md) · [status report](status-reports/2026-06-21-16.md) — axe-core scan (12 tests, 2 violations fixed); perf budgets measured + verified; gate.yml wired; SCA added |

### BUD-E12 — Data & history import

The owner's real financial history — the 12-year workbook and the post-reset statement era — imported into the gitignored ledger store with zero app code. De-risked by SPIKE-03 (workbook) and SPIKE-11 (statements).

| ID | Was | Item | Kind | Why deferred | Status |
| --- | --- | --- | --- | --- | --- |
| **SPIKE-03** | #17 | **SPIKE-03 — data-profiling:** does the 12-yr `Budget.xlsx` reconcile/extract cleanly from its in-cell formula strings? | spike | History migration is out of V1; this is the proper spike for the extraction that "got too messy" | **Done** 2026-07-10 — answered **yes, empirically**: the owner's out-of-repo ETL (2026-06-23) proved the formula-term split lossless (every tab nets to $0; whole-book totals tie out); findings captured **redacted** in [SPIKE-03](spikes/03-history-extraction.md) |
| **BUD-S79** | #18 | **Historical import** | slice | Gated by SPIKE-03 | **Done** 2026-07-10 — **26,640 rows** (1 synthetic account · 27 envelopes · 12,080 transactions · 14,497 allocations · 34 envelope transfers; 2014-07-18 → 2025-10-03) restored via the BUD-S31 `db:restore` CLI into the dedicated gitignored `data/budgeteer-ledger` store (envelope wrapper: `budget-extraction/wrap_backup.py`); independently re-validated (Σ = 0 · 0 split-invariant violations · 0 FK dangles) and verified live in the app. The post-reset gap (2025-10 → now) is the statement ETL → `BUD-S80`. [Status report](status-reports/2026-07-10-history-import.md) |
| **BUD-S80** | #20 | **Statement import** (file half; the **live bank API** half stays deferred and will need its own spike) | slice | Was "needs its own integration spike" — **run 2026-07-10 as SPIKE-11 (`SPIKE-11`)**. The decision points are **decided** (D1–D4 owner-confirmed, [SPIKE-11 §4](spikes/11-statement-extraction.md)): era cut at 2025-10-03 (import the 844 post-cut rows, no row dedup) · three real accounts + computed opening anchors (synthetic `Budget` unchanged) · merchant→envelope rules for the ~69% head, rest unallocated into the needs-allocation flow · intra-institution pairs → ADR-0004 transfers, external-source deposits stay plain. Load path per [SPIKE-11 §5](spikes/11-statement-extraction.md): extract → merge with the workbook backup → rebuild `data/budgeteer-ledger` (`db:reset` + `db:restore`; BUD-S31 restore is empty-store-only); zero app code expected. **Built 2026-07-10** — committed deterministic tooling (`extract_statements.py` + `build_merchant_rules.py` + `merge_statements.py`) parsed the 20 PDFs into 845 statement-era rows, merged with the workbook backup into one **27,523-row** BudgeteerBackup, and rebuilt `data/budgeteer-ledger`; **verified live** — per-account balances = statement closings (BofA $0.00 · CO Checking $819.53 · CO Savings $21.27), 70 transfer legs Σ=0 (excluded from spend), needs-allocation 775, net worth $840.80 spanning into 2026-06; **zero app code changed**. Two reality corrections found & applied (D1–D4 unchanged): (a) intra-CO transfers run **both** directions → **35 pairs** (23 savings→checking + 12 checking→savings), not F5's one-directional 23 — modelling only 23 would misreport internal money as spend/income; (b) Capital One **wraps long merchant descriptions across lines**, which the spike parser dropped (259 blank payees) — the extractor reassembles them (0 blank). Merchant rules import **unallocated by default** (owner reviews the gitignored draft, then opts in via `--rules`). [Status report](status-reports/2026-07-10-statement-import.md) | **Done** 2026-07-10 |
| **SPIKE-11** | #21 | **SPIKE-11 — data-profiling:** do the 20 statement PDFs extract deterministically and reconcile — and what do they say about SPIKE-03 §6's decision points? | spike | The integration spike `BUD-S80` requires ("reality before paper") | **Done** 2026-07-10 — answered **yes, empirically**: 30/30 account-statements (three real accounts across two institutions; the Capital One PDFs each carry checking **and** savings) parse to **1,009 rows**, reconcile to the penny, and chain unbroken 2025-08-15 → 2026-06-30; row-level overlap dedup **invalidated** (era cut instead — reality mirrors the reset: all three accounts drain to exactly $0 in early Oct 2025); findings **redacted** in [SPIKE-11](spikes/11-statement-extraction.md) |

### BUD-E13 — Multi-user / household scoping

Deferred epic (no stories yet). Auth + owner/household isolation — a §11 scale-up trigger requiring full ceremony (tenancy ADR + threat model + property tests). Absorbs the reviews' "no authentication" finding and BUD-S38 (SEC3).

| ID | Was | Item | Kind | Why deferred | Status |
| --- | --- | --- | --- | --- | --- |
| **BUD-E13** | #19 | **Multi-user / household scoping** (auth · owner/household isolation) | epic | Future direction; a §11 scale-up trigger → full ceremony (tenancy ADR + threat model + property tests). **Absorbs the review's "no authentication" finding** ([2026-06-15 review](reviews/2026-06-15-repo-review.md)) | Deferred |

### Tasks under BUD-S57 (UX Uplift — Feedback & states)

`BUD-S57` shipped as four independent threads; under the new scheme each is a Task.

| ID | Was | Task | Status |
| --- | --- | --- | --- |
| **BUD-T1** | UX12a | Confirm on destructive actions (ConfirmDialog) | ✅ Done |
| **BUD-T2** | UX12b | Skeleton loaders | ✅ Done |
| **BUD-T3** | UX12c | Success toasts (Radix Toast) | ✅ Done |
| **BUD-T4** | UX12d | Inline field validation | ✅ Done |



### Risk-retirement spikes

Kept under the stable `SPIKE-##` scheme. Each de-risked the epic noted in the crosswalk; the two UX-Uplift spikes live as rows in BUD-E8 (was UX1/UX2) and the two import spikes in BUD-E12 (was #17/#21).

| ID | Was | Item | Kind | Value | Risk | Answers (the question) | Status | Spike report |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **SPIKE-01** | #0 | Split-allocation UX | spike | High | High | Is enter-once-then-split lower-friction than the sheet, esp. the many-way splits? | **Done** | [SPIKE-01](spikes/01-split-allocation-ux.md) — *Confirmed* |
| **SPIKE-02** | #2 | **Technical feasibility / stack** | spike | High | High | Can the TS stack deliver **integer-minor-unit money** + the **exact split invariant** (the prior float failure)? | **Done** | [SPIKE-02](spikes/02-stack-feasibility.md) — *Confirmed* (strict typecheck + 8/8 tests); produced `ADR-0001`/`ADR-0002` |
| **SPIKE-04** | #4 | **Transfer modeling** (gates `BUD-S6/S7`) | spike | High | High | How to model **account↔account** *and* **envelope↔envelope** moves without breaking the split invariant? | **Done** | [SPIKE-04](spikes/04-transfer-modeling.md) — *Confirmed* (strict typecheck + 8/8 tests); recommends additive two-primitive model → `ADR-0004` |
| **SPIKE-05** | #5 | **Cash-flow forecast model** (gates `BUD-S17`) | spike | Med | Med | Can we project per-account cash, and fold monthly targets in as expected spend **without double-counting**? | **Done** | [SPIKE-05](spikes/05-cashflow-forecast.md) — *Confirmed* (strict typecheck + 8/8 tests); engine projects the future unchanged; netting kills the double-count → FEAT-013 |

## 4. History

The re-sequencing log and the shipped ledger — the append-only narrative of how this plan
reached its current state — live in a sibling file so the plan of record stays lean (K31):
**[`03_ROADMAP-HISTORY-v2.md`](03_ROADMAP-HISTORY-v2.md)**.
