/*
 * Bundle-cost probe for the RECOMMENDED approach: the hand-rolled SVG chart, React-externalized
 * (the app already bundles React). The emitted gzip is the chart's OWN marginal cost — directly
 * comparable to the Recharts probe measured the same way.
 */
export { AccessibleChart } from "../AccessibleChart";
