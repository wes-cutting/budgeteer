{
  "name": "spike-02-stack-feasibility",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "THROWAWAY spike — proves integer-money + split invariant in TypeScript. Not the V1 app.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/feasibility.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
